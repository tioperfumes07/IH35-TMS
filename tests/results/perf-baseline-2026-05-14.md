# Performance baseline — 2026-05-14

This file is generated by `npm run smoke:perf`.

Run locally against a running API:

```bash
PERF_OPERATING_COMPANY_ID="<uuid>" npm run smoke:perf
```

Optional:

- `PERF_BASE_URL` (default `http://127.0.0.1:3000`)
- `PERF_COOKIE` for prod/session auth (overrides `PERF_TEST_AUTH`)
