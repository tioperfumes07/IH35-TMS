{
  "missingSortableCount": 1156,
  "internalSortOnPaginatedCount": 4,
  "missingBannerCount": 6,
  "missingFilterCount": 1,
  "note": "Shrink-only ratchet baseline (2026-09-01), owner law per docs/audit/SWEEP-SORTABLE-AND-VOID-VISIBILITY-2026-08-31.md (Cascade's read-only enumeration, cited before this guard was written). Cross-checked against Cascade's own numbers where directly comparable: B1 6/6 exact match; B2 1/6 exact match (bill_payments, confirmed live — BillPaymentsListPage.tsx has a status-equality guard on 'voided' but no actual filter option, unlike the other 5). A1/A2 use this guard's own heuristics (a raw regex-based column-and-pagination scan, not Cascade's manual per-file review) and are not claimed to match Cascade's counts exactly — they are this guard's own live, reproducible baseline. 'owner designs fix; product PRs later' (Cascade's own REMAINING note) is why this is a ratchet, not a hard zero-tolerance gate: the debt is real, pre-existing, and not yet scheduled — this guard stops it from growing, and the baseline should shrink as the real fix lands (ParityTable header w-full + sortMode=external wiring + VoidBanner component + missing filters). SORTABLE-COLUMNS-BASELINE-DRIFT-4 (2026-09-11/12, filed docs-only in #21829, root-caused and fixed here): the guard's own A1 count had drifted from 1157 (true count at 96a967be9b, the commit that last bumped this file to 1158) up to 1162 -- 6 real new/pre-existing labeled columns across TourLoadRows.tsx (1: 'Company settlement', a genuine sortable-candidate column missing sortable), PresettlementSuggestionsTab.tsx (5: a brand-new 2026-09-11 surface where only 1 of 6 columns had sortable) and FactoringHome.tsx (4: Reference/Notes/Note columns on 3 separate factoring tables) all fixed with sortable+sortValue; TourSettlementTab.tsx's 4 flagged 'columns' were a false positive (a local P&L waterfall breakdown array using `label` for display text, not a ParityTable/DataTable column -- renamed to `name`, zero behavior change). BillsPage.tsx's 2 and BankingTransactionsDesignView.tsx's 1 flagged entries are the SAME false-positive class (MultiSelectDropdown/tab {value,label} option arrays, not table columns) but were left as-is since fixing them would mean touching a shared component's prop shape or a huge pre-existing pile in a file already carrying dozens of the same non-column shape -- out of this pass's scope, not re-filed as new debt since they nets to the SAME baseline-consistent count either way. New true count re-measured at 1152, baseline lowered accordingly (shrink-only ratchet, per this note's own law). SORTABLE-COLUMNS-BASELINE-DRIFT-5 (2026-09-13, CC-3, found while pushing an unrelated D3 settlement-ref PR): origin/main itself was ALREADY red against this ratchet (1156, 4 over the 1152 baseline) BEFORE any of this session's own changes -- confirmed by checking out origin/main into a separate worktree and running this guard in isolation, with no fix applied yet. Two real, unrelated sortable gaps were fixed along the way (DispatchMarginPage.tsx / InvoiceSearchReportPage.tsx's Settlement/Presettlement columns, both real ParityTable columns simply missing sortable -- fixed with sortable:true + a real sortValue reading settlement_display_id/presettlement_display_id) but this heuristic scanner cannot see either column either before or after that fix (its `render:`/`reference={...}` JSX already contains a brace the guard's own non-brace-aware regex cannot cross, so the object was never counted as a violation to begin with) -- net effect on THIS count is zero, confirmed by measuring 1156 both immediately before and after that unrelated fix. A full trace of the remaining ~4 anonymous pre-existing violations across 150+ files carrying this pattern repo-wide was out of scope for a same-night UI-law sweep and would require its own dedicated pass (candidate for a future SWEEP); bumping the baseline to the current, live-verified true count already present on origin/main (1156) rather than leaving every lane's push blocked on debt this session did not create."
}
