## Owner ruling (2026-07-24) — completes the fleet-factory conversion
> "per entity, but we use the same catalog for all entities."

**Stacked on #3397** (base = `feat/lst-fleet-catalogs-per-entity`) — needs its config-driven fleet factory. Retarget to `main` once #3397 lands.

`equipment_types` was the one fleet catalog held back from #3397: it has a **dual write-surface** (fleet factory *and* the legacy `/catalogs/equipment-types` route) **plus a child table** (`equipment_line_item_templates`). Both are handled here.

## Migration `202607880000` (HELD — owner Neon-applies)
- **PARENT** `equipment_types` (7 rows, 3 inbound FKs): add `operating_company_id` (backfill → primary company resolved dynamically from `org.companies`), COPY to every other active company (fresh PKs), `UNIQUE(code)` → `UNIQUE(operating_company_id, code)`, FORCE RLS, REVOKE DELETE.
- **CHILD** `equipment_line_item_templates` (14 rows, FK `equipment_type_id` ON DELETE CASCADE): add `operating_company_id` (backfill from the **parent's** opco); seed copies **remapped** onto each entity's own equipment_type (JOIN by code) so a template always shares its equipment type's entity; `(equipment_type_id, code)` unique stays valid; FORCE RLS, REVOKE DELETE.

## Backend
- `fleet/factory.ts` config: `equipment_types` → `companyScoped:true`.
- **legacy `equipment-types.routes.ts`** (the second write-surface): all 5 handlers scoped via `resolveOperatingCompanyId` + GUC; both INSERTs write `operating_company_id`; **add-line-item verifies the parent equipment_type is the caller's entity** (RLS-scoped SELECT) so a template can't attach cross-entity (the FK check itself bypasses RLS).
- count-spec + scoping test flip `equipment_types` → `companyScoped:true`; fleet guard relaxed to "at least 8"; entity-isolation backlog: both tables now compliant (removed).

## Verification
- Fresh local from-0001 migrate: **21 parent + 42 child rows** (7×3, 14×3), **remap-integrity 0 violations** (every template same-entity as its equipment_type), **apply-twice idempotent**.
- Backend `tsc`: 0 errors. Guard `verify:equipment-types-per-entity` (step 1353), 5-defect `--selftest` incl. the remap.

## ⛔ HELD — catalogs.* = financial cluster, owner-gated
Do not self-merge. Merge order: #3397 → this. Owner applies `202607880000` on a Neon branch, ledger-backfills, then merges.
