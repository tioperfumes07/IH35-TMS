Converts `catalogs.customer_quality_event_reasons` (24 rows, 1 inbound FK) to per-entity — companion to #3408. Migration `202607920000` (HELD): add opco, copy to every entity, `UNIQUE(opco,code)`, FORCE RLS, REVOKE DELETE. **72 rows = 24×3** verified fresh from-0001, apply-twice idempotent. Backend: all 3 handlers set the GUC; GET catalog scopes to caller company; the quality-event handlers resolve the customer's company (POST now scoped too — closes the id-only XE-IDOR the GET already fixed). Guard step 1355 (5-defect selftest, verify-steps only). tsc clean. ⛔ HELD — catalogs.* owner-gated; apply on Neon → ledger-backfill → merge.
