## Owner ruling (2026-07-24)
> "lists and catalogs should be per entity, but we use the same catalog for all entities."

Companion to **#3397** (the fleet 8). Second LST per-entity conversion.

## Migration `202607870000` (HELD — owner Neon-applies)
`catalogs.driver_load_statuses` (13 rows, **0 inbound FKs**, verified Neon 2026-07-24) → per-entity:
- add `operating_company_id`, backfill existing → primary company **resolved dynamically from `org.companies`** (never a hardcoded UUID — the fresh-DB FK lesson from #3397);
- **COPY** the primary's rows to every other active company, fresh PKs, same values, carrying the NOT-NULL `phase` column (`ON CONFLICT` no-op);
- `UNIQUE(code)` → `UNIQUE(operating_company_id, code)`; `ENABLE`+`FORCE` RLS with the standard GUC `company_scope` policy; `REVOKE DELETE`.

## Backend
- `driver-load-statuses.routes.ts` — every handler scopes via `resolveOperatingCompanyId` (defaults to the caller's company when the param is omitted, same grammar as the cancellation-reason catalogs) + sets the GUC; list/insert/update are entity-bound.
- `catalog-registry.routes.ts` — now sets the entity GUC before its counts/previews, so the **per-entity catalogs it already read as a false 0** under FORCE RLS (`accounts`/`classes`/`items`) *and* `driver_load_statuses` return the caller's real rows. (Read-scoping only — no financial logic.)
- entity-isolation backlog regenerated (`driver_load_statuses` now compliant, 137→136).

## Verification
- Fresh local from-0001 migrate: **39 rows = 13 × 3 entities**, RLS forced, composite unique, DELETE revoked, phase carried, **apply-twice idempotent**.
- Backend `tsc --noEmit`: 0 errors. Guard `verify:driver-load-statuses-per-entity` (step 1352) with a 5-defect `--selftest`.

## ⛔ HELD — catalogs.* = financial cluster, owner-gated
Do not self-merge. Owner applies `202607870000` on a Neon branch, ledger-backfills, then merges.
