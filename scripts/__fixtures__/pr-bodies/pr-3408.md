## Owner ruling (2026-07-24) — "per entity, same catalog for all entities"

**Corrects my earlier "blocked" assessment:** `mdata.drivers` carries `operating_company_id`, so the referencing handler resolves the **driver's** entity — no mdata schema change needed.

## Migration `202607890000` (HELD — owner Neon-applies)
`catalogs.driver_termination_reasons` (16 rows, 1 inbound FK from `mdata.driver_safety_events`) → per-entity: add `operating_company_id` (backfill → primary company resolved dynamically from `org.companies`), COPY to every other active company (fresh PKs, same values), `UNIQUE(code)` → `UNIQUE(operating_company_id, code)`, FORCE RLS, REVOKE DELETE.

## Backend — the key difference from the fleet catalogs: a **referencing write path**
- **Catalog CRUD** (4 handlers in `driver-safety-events.routes.ts`): scope via `resolveOperatingCompanyId` + GUC; INSERT writes `operating_company_id`; RLS bounds update/deactivate.
- **Referencing handlers** (list events + POST safety-event): resolve the **driver's** `operating_company_id` (`mdata.drivers`) and set the GUC **before** the per-entity reason lookup/JOIN — so the reason validation resolves in the driver's entity, not the caller's default.
- **Cross-entity returning-driver detection** (`driver-returning-detection.routes.ts`): a driver terminated at one entity may apply at another, so this sets the caller's-company GUC — the reason label resolves for same-entity matches and **degrades to `null` (never wrong)** for cross-entity ones; the safety signal (terminated + severity) lives on the event row and is preserved regardless.

## Verification
- Fresh local from-0001 migrate: **48 rows = 16 × 3 entities**, RLS forced, DELETE revoked, **apply-twice idempotent**.
- Backend `tsc`: 0 errors. Guard `verify:driver-termination-reasons-per-entity` (step 1354), 5-defect `--selftest`. entity-isolation backlog: compliant (removed).

## ⛔ HELD — catalogs.* = owner-gated
Do not self-merge. Owner applies `202607890000` on a Neon branch, ledger-backfills, then merges.
