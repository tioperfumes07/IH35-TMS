{
  "half_released_rows": 1858,
  "measured_at": "2026-09-23",
  "note": "Pre-existing, discovered live by this guard's first run — NOT caused by BANK-UNDO-01. Each row has status='pending_categorization'/categorized_at IS NULL but a categorization_* column (e.g. categorization_unit_id) already populated, most plausibly a frontend categorization-draft auto-save writing a partial selection before the row is actually submitted. Ratchet: shrink-only, never allowed to grow. Regenerate ONLY after a real fix reduces the live count — never to silence a new regression."
}
