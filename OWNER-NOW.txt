# OWNER — Relay + OB (Allow UI broken; do these in Render dashboard)

## Relay is NOT working yet
Neon proof (just now):
| Entity | relay_fuel_transactions | fuel_transactions | bridged |
|--------|------------------------:|------------------:|--------:|
| TRANSP | 0 | 0 | 0 |
| USMCA  | 0 | 0 | 0 |
| TRK    | 0 | 0 | 0 |

- TRANSP Relay flag: **ON** (since Jul 12) but **0 RELAY audits** for TRANSP
- USMCA: 128 audit windows all `pulled=0` (API reached, empty or wrong scope)
- 2026 transactions: **none in TMS**

## What Mike confirmed (today)
- Prod URL: `https://app.relaypayments.com/api/fuel/transactions/`
- Auth: header **Authorization** = raw API key (**no** `Bearer `)
- Our code already matches that (`relay-client.ts`)
- Earlier 403s were Relay-side; Mike now gets success with your keys

## Render env (YOU — agent has no Render CLI/token; Allow blocks live-key probe)
On **ih35-tms** backend service → Environment → set/confirm then **Manual Restart**:

1. `RELAY_API_BASE` = `https://app.relaypayments.com/api/fuel/transactions/`
2. `RELAY_API_KEY_TRANSP` = IH35 Transportation key from Mike (`iak_…`)
3. `RELAY_API_KEY_USMCA` = USMCA key from Mike (`iak_…`)
4. Do **NOT** rely on bare `RELAY_API_KEY` for entity pulls (no fallback when entity code is set)

Then: `/fuel` → Import Relay history **or**  
`POST /api/integrations/relay/fuel/backfill` body `{"months":24}`

Prove: Neon `integrations.relay_fuel_transactions` COUNT > 0 for TRANSP.

## Security
Keys were pasted in email/chat — after Relay works, ask Mike to rotate if needed.

## OPENING_BALANCE_IMPORT_ENABLED (TRANSP)
Enabling now in Neon for TRANSP (you asked). Preview still **read-only** (no JE post).
