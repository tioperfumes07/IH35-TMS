{
  "block_id": "HEALTHZ-PG-BUDGET-503-FLAP-FIX",
  "branch": "fix/healthz-pg-timeout-budgets",
  "phase": "CLOSURE",
  "task": "Relax healthz postgres.select1 (50ms->1000ms) and migrations.ledger (50ms+80ms->1000ms) inner query timeout budgets to stop residual production /healthz 503 flap; keep both checks tier=critical; no other check/tier/query/pool/file touched",
  "agent": "A",
  "allowed_files": [
    "apps/backend/src/health/health.routes.ts",
    ".block-ready.json"
  ],
  "extra_gates": [],
  "runtime_path": "src",
  "db_required": false,
  "guard_required": false
}
