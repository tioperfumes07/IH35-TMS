{
  "block_id": "UI-01_CALENDARS-AND-BOXES_DISPATCH",
  "status": "NEEDS-VERIFY",
  "classification": "FINANCIAL",
  "phase": "design-parity",
  "source": "MASTER-6 authored dispatch (07-UI-DESIGN)",
  "source_file": "UI-01_CALENDARS-AND-BOXES_DISPATCH.txt",
  "allowed_files": [
    "apps/frontend/src/** (presentation only) + scripts/** + ci guard list. NO backend, NO db/migrations, NO posting/GL/amount logic. Additive-safe: remove only redundant inner frames + raw date inputs; never a field/tab/route. Ships on green (non-financial)."
  ],
  "acceptance": [
    {
      "check": "0 raw type=\\\"date\\\" remain in apps/frontend/src (guard proves it); every date field opens the shared QuickBooks month-grid picker."
    },
    {
      "check": "Manual JE date fields (banking + accounting) now use the shared DatePicker (render proof)."
    },
    {
      "check": "No new boxes-within-boxes; baseline of existing offenders recorded, and the coder flattens as many as safely possible in this PR (list what was flattened)."
    },
    {
      "check": "Both guards wired (verify-guard-wired G3 passes), --selftests pass, tsc clean, mobile-responsive new_vs_baseline=0."
    }
  ],
  "note": "Registered by faithful extraction from the authored MASTER-6 dispatch spec (real acceptance/lane/status). Status reflects the dispatch's own marker; NEEDS-VERIFY = still needs a live code check."
}
