{
  "block_id": "pre-push-env-isolation",
  "branch": "cursor/pre-push-verification-safeguards-955a",
  "status": "BUILD",
  "classification": "NON-FINANCIAL (CI tooling / pre-push env isolation)",
  "phase": "PIPELINE-TRUTH",
  "task": "Corrective follow-up to #2720: rename verify-step 912 -> globally-free 926; replace the bare TCP capability probe with a real authenticated pg identity connection proving current_database()='ih35_verify' on a loopback server (raw TCP / wrong database / wrong credentials / non-local prod URL all false); wire the branch:precheck-push behavioral suite UNCONDITIONALLY into verify-steps/926; close the BRANCH_PRECHECK_STEPS_JSON=[] + IH35_BRANCH_TOOLING_SKIP_FETCH=1 all-gates bypass in the production CLI. Preserve the stale-.env fix and server-required CI equivalent fail-closed skip. No HUSKY/--no-verify/env bypass; no package/CI hotfiles (Rule 17 auto-discovery).",
  "priority": "P0",
  "module": "ci-tooling",
  "db_required": false,
  "guard_required": true,
  "runtime_path": "src",
  "extra_gates": [],
  "allowed_files": [
    ".husky/pre-push",
    "scripts/install-git-hooks.mjs",
    "scripts/branch-precheck-push.mjs",
    "scripts/__tests__/branch-precheck-push.test.mjs",
    "scripts/verify-pre-push-env-isolation.mjs",
    "scripts/verify-steps/912-verify-pre-push-env-isolation.mjs",
    "scripts/verify-steps/926-verify-pre-push-env-isolation.mjs",
    "docs/specs/BRANCH-TOOLING.md",
    ".block-ready/pre-push-env-isolation.json"
  ],
  "acceptance": [
    {
      "check": "verify-step renamed 912 -> globally-free 926; verify-pre-push-env-isolation.mjs FILES.verifyStep points at 926"
    },
    {
      "check": "detectLocalCapabilities uses probeVerifyDatabaseIdentity: an authenticated pg connection asserting current_database()='ih35_verify'; a raw TCP listener, wrong database, wrong credentials, or a non-local/prod URL is false"
    },
    {
      "check": ".husky/pre-push does not source repository .env; install-git-hooks regenerates the same (preserved)"
    },
    {
      "check": "stale Neon env skips block-ready only via named server-required ci / build-typecheck (preserved)"
    },
    {
      "check": "production CLI ignores BRANCH_PRECHECK_STEPS_JSON and IH35_BRANCH_TOOLING_SKIP_FETCH; resolvePrecheckSteps takes steps only from a caller option or the built-in chain; git fetch always runs"
    },
    {
      "check": "branch-precheck-push.test.mjs is run UNCONDITIONALLY by verify-steps/926 (executed by verify:pre-commit / CI build-typecheck), with planted fake-TCP / wrong-current_database / wrong-credentials / env-bypass tests"
    },
    {
      "check": "verify-pre-push-env-isolation + verify-steps/926 (Rule 17; no package/CI hotfiles)"
    }
  ],
  "note": "Follow-up to #2720. #2720 stopped husky pre-push from shell-sourcing .env and stopped a bare DATABASE_URL string counting as capability, but (a) landed the verify-step at globally-colliding number 912, (b) proved DB capability with only a bare TCP connect (a raw listener or a wrong/prod database could satisfy it), (c) left the behavioral suite behind an env flag CI never set, and (d) still allowed BRANCH_PRECHECK_STEPS_JSON=[] + IH35_BRANCH_TOOLING_SKIP_FETCH=1 to report READY TO PUSH with zero substantive checks. This block fixes all four at the root."
}
