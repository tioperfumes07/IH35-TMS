{
  "block_id": "P1-BILLPAY-GL-bill-payment-auto-gl-post",
  "status": "BUILD",
  "classification": "FINANCIAL",
  "phase": "PHASE-1-POSTING-INTEGRITY",
  "task": "P1-BILLPAY-GL: payBill() inserted accounting.bill_payments and decremented banking.bank_accounts.current_balance_cents directly (updateBankBalance) but NEVER posted a JE — cash moved with no offsetting DR ap_control / CR bank entry, so bank and GL diverged and A/P was never relieved in double-entry. GUARD ruled (2026-07-11, live prod) the bank cache and GL cash account are SEPARATE stores (no trigger writes current_balance_cents) → recording -amount in BOTH is correct, not double-counting, and REQUIRES atomicity. Extracted postSourceTransaction's body into postSourceTransactionInClientTx (client-accepting, behavior-preserving — existing callers unchanged) so payBill posts the bill_payment JE ATOMICALLY in the same transaction as updateBankBalance (both commit or both roll back — never diverge). OPTION B (per GUARD Neon-proof — BILL_PAYMENT_GL_POSTING_ENABLED is OFF for ALL entities in prod, so 'block entirely' would 409 every bill payment company-wide on deploy): the payment + bank decrement ALWAYS happen (no regression to OFF entities), the balanced JE posts ONLY when the per-entity flag is ON, and the result carries gl_posting:'posted'|'blocked_flag_off' (honest, no silent success). Idempotent, relieves A/P via ap_control. Static guard verify-bill-payment-posts-gl also fails an UNCONDITIONAL post (outage guard).",
  "branch": "fix/bill-payment-auto-gl-post",
  "lane": "PHASE-1-POSTING-INTEGRITY",
  "allowed_files": [
    "apps/backend/src/accounting/posting-engine.service.ts",
    "apps/backend/src/accounting/bills.service.ts",
    "apps/backend/src/accounting/bills.routes.ts",
    "apps/backend/src/payroll/driver-settlement.service.ts",
    "scripts/verify-bill-payment-posts-gl.mjs",
    "scripts/verify-steps/98-verify-bill-payment-posts-gl.mjs",
    ".block-ready/P1-BILLPAY-GL-bill-payment-auto-gl-post.json",
    "package.json",
    ".github/workflows/locked-guards.yml"
  ],
  "acceptance": [
    { "kind": "guard", "script": "scripts/verify-bill-payment-posts-gl.mjs", "wired_into": "verify-pre-commit (verify-steps/98) + locked-guards.yml + package.json verify:bill-payment-posts-gl" },
    { "kind": "route", "value": "apps/backend/src/accounting/bills.service.ts", "note": "payBill posts atomic bill_payment JE via postSourceTransactionInClientTx (same txn as updateBankBalance); gated OFF->blocked_flag_off" },
    { "kind": "live", "assert": "static guard PASS + --selftest PASS; postSourceTransactionInClientTx extracted (postSourceTransaction behavior unchanged — proven by the poster db.tests in CI). BEHAVIORAL Neon-branch proof OWED TO GUARD: pay a bill flag ON -> balanced DR ap_control / CR bank + bank cache Δ == JE cash-credit Δ == -amount (no double-reduction) + ap_control debited + idempotent; flag OFF -> no payment/bank/JE + 409 blocked.", "verified_by": "GUARD Neon branch-proof" },
    { "kind": "design", "spec": "docs/specs/repairs/REPAIR-C-BOUNDARY-AUTOPOSTS-DESIGN.md", "sections": ["postBillPaymentGlIfEnabled dead"] }
  ],
  "linkage": "bill_payment (accounting.bill_payments) -> bill (accounting.bills) -> vendor (mdata.vendors) | bank (banking.bank_accounts cache + catalogs.accounts GL cash via ledger_account_id) | GL balanced JE (accounting.journal_entries) behind BILL_PAYMENT_GL_POSTING_ENABLED | operating_company_id + FORCED RLS. Closes the bank<->GL divergence on the A/P cash-out path.",
  "db_required": false,
  "guard_required": true,
  "extra_gates": [],
  "deferred": [
    { "item": "vendor A/P batch-pay path (apps/backend/src/accounting/vendor-bill-payments.routes.ts)", "reason": "DISTINCT code path with its own bill_payments insert + updateBankBalance (does NOT call payBill); needs the same postSourceTransactionInClientTx + flag-gate wiring + its own Neon proof. Explicit follow-up within P1-BILLPAY-GL (per LINKAGE-LAW explicit-deferred), NOT silently dropped. GUARD acceptance covers both UI paths." }
  ],
  "superseded_hold": "OWNER LAW 2026-08-03: no holds, no approval label; coders have full Neon access and merge on green. Proof, not approval, is the gate."
}
