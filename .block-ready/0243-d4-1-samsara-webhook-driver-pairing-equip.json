{
  "block_id": "0243-d4-1-samsara-webhook-driver-pairing-equip",
  "status": "BUILD",
  "classification": "NON-FINANCIAL",
  "phase": "tier2",
  "source": "MASTER-6 authored dispatch (05-TIER2-BLOCKS)",
  "source_file": "0243-d4-1-samsara-webhook-driver-pairing-equip.txt",
  "task": "The Samsara vehicle-driver-pairing WEBHOOK path (telematics/vehicle-driver-lookup.service.ts resolveLocalIds) resolved the local unit ONLY via mdata.equipment.samsara_vehicle_id -> current_unit_id, with no mdata.units.samsara_vehicle_id PRIMARY lookup. Because mdata.equipment.samsara_vehicle_id is usually unpopulated, webhook-delivered driver logins dropped until the 5-min reconcile poll papered over them. FIX: mirror the proven units-primary -> equipment-fallback pattern already used by integrations/samsara/vehicle-driver-pairing/pairing.service.ts resolveLocalUnitAndDriver. Reads mdata.units/mdata.equipment; writes telematics.vehicle_driver_assignments (non-financial).",
  "branch": "fix/0243-nonfin-batch",
  "allowed_files": [
    "apps/backend/src/telematics/vehicle-driver-lookup.service.ts",
    "scripts/verify-samsara-webhook-unit-fallback.mjs",
    "scripts/verify-steps/119-verify-samsara-webhook-unit-fallback.mjs",
    "package.json",
    ".github/workflows/locked-guards.yml"
  ],
  "acceptance": [
    {
      "kind": "guard",
      "script": "scripts/verify-samsara-webhook-unit-fallback.mjs",
      "wired_into": "verify-steps/119 + locked-guards.yml + package.json verify:samsara-webhook-unit-fallback"
    },
    {
      "kind": "live",
      "assert": "backend tsc 0 errors; guard --selftest passes (units-primary clears, equipment-only fails); scan green; pattern copied verbatim from pairing.service.ts resolveLocalUnitAndDriver."
    }
  ],
  "linkage": "mdata.units (PRIMARY read) / mdata.equipment (fallback read) -> telematics.vehicle_driver_assignments; entity-scoped COALESCE(currently_leased_to_company_id, owner_company_id); driver<->unit pairing continuity.",
  "db_required": false,
  "guard_required": true,
  "note": "Verified against live code: resolveLocalIds was equipment-only while pairing.service.ts already had units-primary. Non-financial read-path resilience; no accounting/catalogs/mdata write, no migration."
}
