{
  "block_id": "shared-catalog-creator-profile-debox",
  "status": "BUILD",
  "classification": "NON_FINANCIAL",
  "phase": "tier3",
  "task": "Accounting Core Block 5/67 — add a shared read-only accounting catalog profile drawer. Row clicks open profiles; writable catalogs expose an explicit Edit transition to the existing modal; read-only catalogs, including Journal Entry Types, remain GET-only with no Edit or Create. Pure frontend presentation and tests only: no backend, schema, route, tab, or financial mutation.",
  "branch": "fix/accounting-catalog-profile-debox",
  "lane": "accounting",
  "allowed_files": [
    ".block-ready/shared-catalog-creator-profile-debox.json",
    "apps/frontend/src/pages/lists/accounting/AccountingCatalogListPage.tsx",
    "apps/frontend/src/pages/lists/accounting/AccountingCatalogProfileDrawer.tsx",
    "apps/frontend/src/pages/lists/accounting/__tests__/AccountingCatalogListPage.test.tsx",
    "scripts/verify-accounting-catalog-profile.mjs",
    "scripts/verify-steps/906-verify-accounting-catalog-profile.mjs"
  ],
  "runtime_path": "src",
  "db_required": false,
  "guard_required": true,
  "acceptance": [
    {
      "kind": "mounted",
      "check": "AccountingCatalogListPage mounts AccountingCatalogProfileDrawer and row clicks open the read-only profile instead of edit mode."
    },
    {
      "kind": "design",
      "check": "Profiles render code, name, description, metadata, status, sort order, and safe created/updated timestamps using the existing ParityDrawer."
    },
    {
      "kind": "effective",
      "check": "Writable profile Edit closes the profile and opens the existing AccountingCatalogModal in edit mode; + Create remains unchanged."
    },
    {
      "kind": "effective",
      "check": "readOnly catalogs expose neither Edit nor + Create. Journal Entry Types remains canonical GET-only by existing readOnly configuration; this is completed behavior, not deferred work."
    },
    {
      "kind": "guard",
      "check": "verify-accounting-catalog-profile is auto-discovered through its verify-step and passes planted regression selftests."
    }
  ],
  "extra_gates": [
    "focused AccountingCatalogListPage vitest",
    "frontend tsc",
    "diff-check"
  ]
}
