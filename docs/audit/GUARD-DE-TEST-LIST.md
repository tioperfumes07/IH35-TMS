# GUARD D/E Layer — Live Exercise Test List

> Generated by CASCADE code trace (2026-08-02). Each row tells GUARD:
> - **WHERE** to navigate
> - **WHAT** to click / exercise
> - **HOW** to confirm PASS (persist proof / design-bar proof)
>
> CODE-VERIFIED ≠ PASS. Only GUARD flips to VERIFIED after live exercise.

---

## D-Layer Pickers (7 rows) — Inline +Create Exercise

| Row | Module | Surface (URL) | Picker to Exercise | +Create Action | Canonical Table | PASS Criteria |
|-----|--------|---------------|-------------------|---------------|-----------------|---------------|
| 19 | maintenance | `/maintenance` → Create Work Order | **Vendor picker** (ReferenceSelect) | Click "+ Add new vendor" in dropdown → InlineCreateDrawer opens | `mdata.vendors` | 1. New vendor appears selected after save. 2. Reload page → vendor still in dropdown. 3. Check `mdata.vendors` for new row. |
| 19 | maintenance | `/maintenance` → Create Work Order | **Driver picker** (DriverPickerWithCreate) | Click "+ Create driver" in dropdown → CreateDriverModal opens | `mdata.drivers` | 1. New driver appears selected. 2. Reload → driver still selectable. |
| 19 | maintenance | `/maintenance` → Create Work Order (Roadside type) | **Roadside Provider Vendor** (ReferenceSelect) | Click "+ Add new vendor" → same InlineCreateDrawer | `mdata.vendors` | Same as vendor above. Roadside type must show this picker. |
| 20 | safety | `/safety` → Fines & Discipline → +Create Fine | **Driver picker** (DriverPickerWithCreate) | Click "+ Create driver" in dropdown | `mdata.drivers` | New driver selected, survives reload. |
| 20 | safety | `/safety` → Fines & Discipline → +Create Fine | **Civil Fine Type** (ReferenceSelect catalog) | Click "+ Add new" → CatalogQuickCreateDrawer | `catalogs.civil_fine_types` | New type selected, survives reload. |
| 21 | fleet | `/fleet` → any unit detail | **QBO Class picker** (QboCombobox) | NO +Create (synced from QBO) — verify dropdown loads real class names (not UUIDs) | `mdata.qbo_classes` | Dropdown shows human-readable class names from 172 TRANSP rows. Selection persists on save. |
| 22 | legal | `/legal` → +Create Contract | **Driver picker** (DriverPickerWithCreate) | Click "+ Create driver" | `mdata.drivers` | New driver selected as signer, survives reload. |
| 23 | compliance | `/compliance` → HOS History | **Driver picker** (DriverPickerWithCreate) | Click "+ Create driver" | `mdata.drivers` | New driver selected, HOS data queries for that driver. |
| 26 | bank | `/banking` → account selector | **Bank Account** selector (Combobox) | NO +Create (Plaid-imported) — verify dropdown loads all 6 TRANSP accounts with real names | `banking.bank_accounts` | All 6 accounts shown with human-readable names. Selection switches the transaction view. |
| 55 | drivers · profile | `/drivers` → Disputes tab or Cash Advance | **Driver picker** (DriverPickerWithCreate) | Click "+ Create driver" | `mdata.drivers` | New driver selected, persists. |

### D-Layer Universal Checks (apply to ALL rows above):
1. **QB-STD-1**: "+ Add new" row is ALWAYS the first row of the open dropdown (visible before typing)
2. **QB-STD-2**: No external button needed — create trigger is IN the dropdown
3. **QB-STD-3**: On Save, the record is returned already selected (no navigation away)
4. **QB-STD-4**: Entered data in the parent form is NOT lost when the inline create fires
5. **QB-STD-5**: Created records survive full page reload (written to the same canonical table the list reads from)

---

## E-Layer Design-Bar (28 rows) — Resize / Filters / Proportions Exercise

All modules use `ParityTable` (shared grammar, 275 pages). Exercise each module's PRIMARY table:

| Row | Module | Surface (URL) | Table to Exercise | Resize Test | Filter Test | Proportion/Vocab Test |
|-----|--------|---------------|-------------------|-------------|-------------|----------------------|
| 27 | home · tasks | `/home` | Home KPI tiles + Task Board | KPI tiles responsive at ≤768px? | Range selector (Today/7d/30d/MTD/YTD) changes data? | Typography tokens consistent (12px body, 11px compact)? |
| 28 | dispatch | `/dispatch` → List view | Load board table | Drag column border → width persists on reload | Tab filters (Load board/At-Risk/Detention) reduce rows? | Kanban/List/Overview toggle changes layout? |
| 29 | maintenance | `/maintenance` → Active WOs | Work orders table | Drag WO# column wider → reloads at same width | Status filter (Open/Closed) reduces set? | KPI bar numbers match filtered table count? |
| 30 | safety | `/safety` → Fines tab | Fines table | Drag "Amount" column → persists | Driver filter scopes to one driver? | Dual-KPI row sums match table total? |
| 31 | fleet | `/fleet` | Fleet roster table | Drag "Unit ID" column → persists | Type tabs (Trucks/Trailers) filter correctly? | KPI bar "Total" matches visible row count? |
| 32 | legal | `/legal` → Contracts | Contracts table | Drag "Status" column → persists | Tab filters (Active/Expired/Pending) work? | KPI "Active Templates" matches filtered count? |
| 33 | compliance | `/compliance` → Filings | Filings table | Drag "Due Date" column → persists | Status tab (Overdue/Due Soon/Upcoming) filters? | Red count badge matches Overdue tab rows? |
| 34 | settlements | `/settlements` → Drivers | Drivers settlement table | Drag "Balance" column → persists | Payment Pipeline filter (Unpaid/Queued/Sent) works? | KPI "Total Unpaid" matches sum of filtered rows? |
| 35 | fuel | `/fuel` → History & savings | Fuel history table | Drag "Amount" column → persists | Date range filter changes row set? | KPI "MTD Spend" ≈ sum of current-month rows? |
| 36 | accounting | `/accounting` → Bills | Bills table | Drag "Vendor" column → persists | Status filter (Open/Paid/Overdue) works? | KPI "Open Bills" matches Open filter count? |
| 37 | factoring | `/factoring` → Reserve Tracker | Reserve table | Drag any column → persists | Date filter changes visible rows? | KPI "Reserve Balance" consistent with table? |
| 38 | vendors | `/vendors` | Vendor list (left panel) | Drag "Name" column → persists | Search filter finds vendor by name? | Master-detail: clicking vendor shows detail on right? |
| 39 | customers | `/customers` | Customer list (left panel) | Drag "Name" column → persists | Search filter finds customer? | Financial Summary updates for selected customer? |
| 40 | drivers · profile | `/drivers` | Drivers roster table | Drag "Name" column → persists | Status filter (Active/Inactive/On Leave) works? | KPI "Active" matches filtered Active count? |
| 41 | cash-flow | `/cash-flow` | Projected cash flow table | Day navigation arrows change date? | Tab (Projected/Actual/Manual) switches view? | Opening cash → Predicted closing arithmetic correct? |
| 42 | inventory | `/inventory` → Parts & Stock | Parts inventory table | Drag "Part Name" column → persists | Category filter works (if categories exist)? | Low Stock indicator matches threshold comparison? |
| 43 | reports | `/reports` | Report hub grid | Category tabs (Operations/Financial/etc.) filter cards? | Search finds report by name? | "Available Reports=15" matches visible card count? |
| 44 | docs | `/docs` | Documents table | Drag "File" column → persists | Entity tab (Drivers/Customers/Vendors) filters? | KPI "Total Docs=13" matches total table rows? |
| 45 | users | `/users` | Users table | Drag "Name" column → persists | Tab filter (Active/Pending/Deactivated) works? | KPI "Total Users=10" matches All tab count? |
| 46 | eld | `/eld` | HOS daily roster table | Drag "Driver" column → persists | Status filter (On Duty/Driving) works? | KPI "On Duty" matches filtered count? |
| 47 | help | `/help` | Help center (card layout) | N/A (no table) | Search bar finds articles? | Category sections render with correct headings? |
| 48 | program | `/program` | Program block table | Drag "Block" column → persists | Status tab (Pending/In Progress/Completed) filters? | KPI "Pending=487" matches Pending tab count? |
| 49 | finance | `/finance-hub` | Finance hub (gated) | N/A (gated — honest "not enabled" message) | N/A | Gate message displays correctly with back link? |
| 50 | form_425 | `/form-425c` | Form 425C (form layout) | N/A (form, not table) | Entity tabs switch between companies? | Form fields populate with saved data on reload? |
| 51 | driver-hub | `/driver-hub` | Inbox list | Category tabs filter inbox items? | Tab counts match visible items? | Empty state renders cleanly? |
| 52 | system | `/system` | System dashboard (panels) | N/A (dashboard panels, not table) | Tab navigation switches panels? | KPI numbers match Program Tracker live data? |
| 53 | insurance | `/insurance` (under Safety) | Policies table | Drag any column → persists | Sub-tab (Policies/Claims/Lawsuits) switches? | Coverage Gap count badge matches table? |
| 54 | tasks | `/tasks` | Task board (calendar) | N/A (calendar grid) | Calendar arrows change week? | "+Create Task" opens correctly? |

### E-Layer Universal Checks (apply to ALL rows above):
1. **Resize**: Drag the column-header border (4-6px hit target) — column widens/narrows, width persists after reload (localStorage key `paritytable:{storageKey}`)
2. **Density**: Click gear icon → toggle Regular/Compact/Ultra → row height changes immediately, persists
3. **Column Toggle**: Gear popover → uncheck a column → it disappears → re-check → it returns
4. **Sort**: Click a sortable column header → asc chevron → click again → desc → click again → unsorted
5. **Pagination**: If >15 rows → pager renders (First/Prev/numbered/Next/Last + per-page selector)
6. **Export**: If exportFilename is set → ⤓ Export button downloads CSV of visible columns
7. **Sticky Header**: Scroll down in a long table → header stays pinned (default `stickyHeader={true}`)

---

## Sequencing Notes

- **Seed Battery (SEED-BATTERY-SPEC.md)** is BLOCKED until money-gate ships (bills/invoices don't post to GL yet). Sequence seed AFTER Claude's Finalize/Post build; GUARD verifies each JE.
- **Row 606** (maintenance WO): C1 validator fix landed (#4071) but WO POST still fails "Failed to fetch" live. Retest after POST fix deploys.
- **Row 57** (resize hit-target): Known UX polish issue — 4-6px target with no visible grip icon. Functionally works but not discoverable. Flag if user cannot find it within 5 seconds.
