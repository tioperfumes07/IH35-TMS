# Archive — 2026-09-02 (GO-25)

Not a queue. Audit-phase documents, **moved** (`git mv`), never deleted.

**Stayed at `docs/audit/`** because law or CI reads them: `GUARD-WORKORDERS.md`, `AUDIT-COVERAGE-LIVE.md`, plus every `docs/audit/*` path referenced from `scripts/**`, `.github/workflows/**`, `apps/**`, or `docs/law/LAW.json`.

Current queue is GO-23 + `docs/bus/INBOX-<SEAT>.md`. Do not dispatch from archived sweep cards.
