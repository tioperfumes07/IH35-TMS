
| **FINDING+FIXED CODE THIS PR (Codex 2026-08-28):** `DSP-F7127B-LOAD-DETAIL-DIRECT-CALLERS-DROP-COMPANY-SCOPE` — the class census after #17169 found the three remaining direct `getLoad` consumers in Cash Advance create, Invoice detail, and Invoice create. Each already owned the active company but omitted it from both request and cache identity, leaving Owner sessions able to resolve a known cross-company load UUID. **SOURCE-OF-TRUTH:** `mdata.loads` through its optional-but-authoritative `operating_company_id` by-id predicate. **I QUERIED:** repository-wide `getLoad` call census after #17169 and each caller's existing company context. **NOT CHECKED:** load PATCH routes; money calculations/posters; Live Chrome; TRANSP/TRK. `BLOCKS=DSP-F7127-class-completion`. | cash-advance and invoice load-link consumers; existing claimed step 3724 | B | **Codex — load connectivity class sweep** | Add company to all three cache keys, pass it into every direct GET, fail closed until scope exists, and extend the existing guard/selftest from eight to sixteen planted scope regressions so the complete caller census cannot silently shrink. | guard normal+selftest (sixteen mutations); frontend typecheck; OWNER-GATED=no | **FIXED CODE THIS PR · Live=post-deploy UNVERIFIED** |
