# Module completion — Safety (Module 3)

**PROGRESS: 15 of 38** · complete: `false` · as_of: 2026-07-25T15:30:00.000Z · live_sha: `7875de6`

| Status | Count |
|---|---:|
| PASS | 15 |
| HOLD | 0 |
| OPEN | 4 |
| FAIL | 12 |
| UNVERIFIED | 7 |

| ID | Status | Title | Evidence | PR |
|---|---|---|---|---|
| `SAF-B01` | **UNVERIFIED** | Escrow forfeiture endpoint + economics | CODE-VERIFIED, BLOCKED ON A FLAG THAT CANNOT BE TURNED ON. Route escrow-forfeit.routes.ts:41 registered at index.ts:887; service :88-97,110-170 has the over-draw guard, flag gate, JE via createJournalEntryOnClient and escrow_postings source_type='forfeit'; prereq migration 202607800000 applied_on_prod:true. Guard 1328 (4 planted defects). BLOCKER: DRIVER_ESCROW_FORFEIT_GL_POSTING_ENABLED has NO seed migration anywhere, and feature-flags/service.ts:277-279 resolves an unknown key false — so every forfeit 409s escrow_forfeit_gl_posting_flag_off. There is no enable path in existence, which is worse than OFF. SETTLES IT: SELECT flag_key,default_enabled FROM lib.feature_flags WHERE flag_key='DRIVER_ESCROW_FORFEIT_GL_POSTING_ENABLED' plus the overrides table, on prod. Flag stays OFF; no agent flips it. PREMISE REFUTED: the finding names safety.driver_escrow / driver_escrow_transactions, which do not exist — the real surface is accounting.escrow_accounts / escrow_postings + driver_finance.driver_liabilities. FLAG SEED REASSIGNED 2026-07-25: GUARD confirmed live that lib.feature_flags has ZERO rows for DRIVER_ESCROW_FORFEIT_GL_POSTING_ENABLED (columns {flag_key, description, default_enabled, rollout_pct, created_at}), so there is no enable path in existence. Seeding it belongs with the escrow money-direction fix in CURSOR's #3533 stack, not here — it is meaningless until that lands. Seed default_enabled=false; the OWNER enables afterwards. No agent flips it. The ROUTE itself is code-verified registered (escrow-forfeit.routes.ts:41, index.ts:887) and stays this coder's surface. HANDOVER: findings recorded in docs/trackers/ESCROW-FINDINGS-HANDOVER-TO-CURSOR-2026-07-25.md — the ELSE->positive forfeit sign, the absent flag row, the 0 signed driver contracts, and the split-brain that a trigger fix alone will not close. Standdown also covers accounting/escrow/service.ts. GUARD SAF-DOM PACKET 2026-07-26: SAF-DOM-03 supersedes the flag framing. Escrow is a LIABILITY and cannot post because the escrow_liability_default ROLE IS UNBOUND on prod (0 rows), so accounting/escrow/service.ts:65 throws 409. That is an OWNER NEON SEED, not a code build — no agent builds it and no agent seeds it. This is a SECOND, independent gate from the feature-flag one already recorded: even with the flag seeded and enabled, an unbound role still 409s. Both must clear. *** SAF-DOM-03 PREMISE REFUTED — DO NOT SEED. Re-verified on prod 2026-07-26 under lucia in a real transaction: accounting.chart_of_accounts_roles has escrow_liability_default BOUND AND ACTIVE on ALL THREE entities — USMCA 5c854333 -> account 2100 'Driver Escrow - Held in Trust'; TRANSP 91e0bf0a -> QBO-250 '2025-Damage Claim Escrow'; TRK b49a737b -> QBO-1150040187 'Damage Claim Escrow'. The role is NOT unbound and there is nothing to seed. Seeding it would create a DUPLICATE binding on a table whose UNIQUE(operating_company_id, role_key) constraint only landed with migration 202607990000. HOW THE FALSE READING HAPPENED, because it will happen again: the '0 rows' came from a query with set_config('app.bypass_rls','lucia') in the SELECT LIST rather than as its own statement — the FROM is evaluated before the bypass takes effect. I reproduced it exactly: the same predicate returned 4 as a scalar subquery and 0 with a JOIN. That is the Rule 10 'a 0 is not absence' trap. Always run set_config as a SEPARATE statement in a transaction before counting. ONE REAL RESIDUAL: USMCA carries TWO escrow_liability_default rows for the same account (one is_active=false, one true) — a duplicate role binding, harmless today, wants a deactivate-dupes cleanup, NOT a seed. THE REMAINING ESCROW GATE IS THE FEATURE FLAG ONLY (lib.feature_flags, 1 row, default_enabled=false). Flipping a GL posting flag is the owner's sole decision — no agent flips it, even under blanket authorization. *** | 3348 |
| `SAF-B02` | **PASS** | Fine->deduction chain held migrations applied | NEON lucia 2026-07-25 br-fancy-credit-akjnd07a: safety.civil_fines.driver_settlement_deduction_id EXISTS (uuid); driver_finance.driver_settlement_deductions.load_id + source_bank_transaction_id EXIST. Ledgered in _system._schema_migrations + ih35_migrations.applied_migrations (202607080000, 202606272300, 202607070100). .held-migrations.json stamped applied_on_prod:true (was stale-false — honesty defect, not absent columns). Economics F+R click-through still UNVERIFIED separately (no live convert smoke this PR). | — |
| `SAF-B03` | **PASS** | Claim reverse graph held migrations applied | NEON lucia 2026-07-25: safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all EXIST (uuid). Migrations 202607250000 + 202607240000 ledgered 2026-07-11. applied_on_prod stamped true. Claim-graph browser F+R still UNVERIFIED (data-blocked — insurance.claim row density / click-through). | — |
| `SAF-B04` | **FAIL** | Accident wizard persists every rendered field | VERIFIED FAIL 2026-07-25. AccidentReportDrawer.tsx:230-231 (Record Type) and :241-242 (Service Type) are still hardcoded no-op comboboxes — value fixed, onChange empty — and neither appears in linkPayload (:165-183). Cost lines have state (:56), an editor (:501) and a total (:504) but are NEVER sent by linkPayload/saveAccident (:184-198). Guard 1329 covers only the 7 TEXT fields in its FIELDS array (verify-accident-wizard-fields-persisted.mjs:29-37), so CI is green while two controls and every cost line are discarded — exactly the shape-vs-substance defect F30 exists to kill. | 3353 |
| `SAF-B05` | **FAIL** | Base-less fetch sweep + guard | VERIFIED FAIL 2026-07-25 on the F32 half. F06 IS satisfied: zero base-less fetch sites remain in apps/frontend/src, Permits.tsx:11 uses resolveApiUrl and :29,37 removed the fabricated fallback (guard 1323). F32 is NOT: Form2290Filings.tsx:108 still renders `due {deadline?.deadline ?? "Aug 31"}` — a fabricated literal — and form-2290-generator.ts:90-101 hardcodes Date.UTC(deadlineYear,7,31) with no per-unit first-use month and no weekend/holiday shift. CRITICAL: scripts/verify-form-2290-deadlines-current.mjs is NOT under scripts/verify-steps/, so it NEVER EXECUTES (only verify-steps files run), and it asserts only ISO shape and 0<=days<=366 anyway. | 3336 |
| `SAF-B06` | **UNVERIFIED** | D&A / RTD block in driver-qualification gate | CODE-VERIFIED, needs an authenticated action. driver-qualification.service.ts:148-192 reads drug_test + da_test_records with RTD-negative-after-violation clearance and excludes voided rows; :214-239 reads safety.clearinghouse_query and prohibits on record_found; reasons pushed :250-252; consumed by book-load :896, quicksave :283, planner :294, quick-assign :47. Guard 1340. SETTLES IT: in TRANSP and USMCA, assign a driver holding a record_found clearinghouse query (or a positive/refusal drug_test with no later RTD-negative) — the assign must be refused with the named reason. | 3375 |
| `SAF-B07` | **PASS** | Held-column consumer guard | SAF-F08 root fix: scripts/verify-schema-parity-from-prod.mjs + verify-step 1437. Prod truth = docs/schema-parity-prod-evidence.json (information_schema lucia snapshot br-fancy-credit-akjnd07a 2026-07-25), NOT migration-parsed schema-parity-baseline.json. --selftest plants held/unapplied phantom column (FALSE-PASS + PHANTOM CONSUMER) and must FAIL; prod-present held column does not fail. Prior verify-held-column-consumers (PR 3384) capability-skipped without DB — insufficient for the false-PASS class. | 3457 |
| `SAF-B08` | **FAIL** | Escrow KPI + signed-clause source of truth | VERIFIED FAIL 2026-07-25 — the blocker is now measured, not open. The target source is correct and constant-free (escrow-target.service.ts:49-95 reads driver_pay_settings.escrow_target_cents then escrow_settings then null; ESCROW_TARGET_DEFAULT is gone from the frontend client; migration 202607830000 applied on prod; guard 1344:112-123). GUARD live-read prod: driver_finance.escrow_settings has 2 rows (2 entities) — that half is fine. But the signed-clause gate at :74-82 requires EXISTS(legal.contract_instances WHERE signer_type='driver' AND status='signed_electronically' AND voided_at IS NULL), and that count is ZERO on prod. So has_signed_clause is false for every driver and EVERY forfeit is permanently clause-blocked, independent of the feature flag. This is not a code defect in the target resolver — it is an unmet data precondition, and the KPI is therefore reporting against a gate no driver can pass. Also carries a declared deviation (:31-38): the predicate matches ANY signed driver contract, not an escrow-specific clause, because prod has no escrow-clause column. Findings handed to Cursor in docs/trackers/ESCROW-FINDINGS-HANDOVER-TO-CURSOR-2026-07-25.md. | 3380 |
| `SAF-B09` | **UNVERIFIED** | Escrow forfeit drawer + pickers + validation | CODE-VERIFIED, needs a click-through. EscrowForfeitModal.tsx:91 ParityDrawer, :104-109 MoneyInput dollars seam, :137-143 liability picker over GET /api/v1/liabilities/by-driver/:id, :83-88 submitDisabled = clauseBlocked // amount<=0 // overBalance // reasonTooShort; server mirrors it (escrow-forfeit.routes.ts:15-23 zod positive + reason.min(3); service :89-91 over-draw). Mounted EscrowRecordTab.tsx:7, tab live SAFETY_TABS_CONFIG.ts:70. Guard 1345. SETTLES IT: open Forfeit in TRANSP and USMCA, confirm the debt picker lists only that driver's liabilities and submit is blocked at $0, above balance, and reason<3. PREMISE REFUTED: F10 claims a raw-UUID CoA ACCOUNT box; there was never one — the UUID was linked_liability_id (a driver_liabilities FK) and both GL accounts resolve server-side (service :110,:113). | 3381 |
| `SAF-B10` | **UNVERIFIED** | Void-reason governance (DOT inspections, complaints) | CODE-VERIFIED, needs a click-through. dot-inspections.ts:34-36 void_reason z.string().trim().min(3).max(500), :364 parses it, :371 SET voided_at=now(), voided_by=$2, void_reason=$4 with the COALESCE placeholder gone; complaints.ts:28,255,262 identical; both registered index.ts:939,941; UI sends the reason (DOTInspectionsTab.tsx:69, ComplaintsTab.tsx:117). Guard 1325. SETTLES IT: void one DOT inspection and one complaint per entity and confirm the row carries the typed reason + voided_by/voided_at and appears in history. | 3339 |
| `SAF-B11` | **UNVERIFIED** | Internal-fine lifecycle routes + row actions | CODE-VERIFIED with a SCOPE GAP. safety-v5.routes.ts:368 dispute (PATCH), :452 void (POST, Owner/Administrator, refuses an already-converted fine, writes voided_at + voided_reason, void-not-delete), :289 convert -> driver_liabilities + settlement deduction; registered index.ts:928; row actions InternalFinesPage.tsx:176,186 gated by isVoided/isConverted at :167-168. Guard 1338. GAP vs F12's 'full lifecycle': there is no post-hoc approve/convert/waive/write-off transition — status='approved' (0050:269) is reachable only at CREATE time. SETTLES IT: create -> dispute -> void in both entities, confirming each status + actor + timestamp persists. PREMISE REFUTED: F12 says the actions were rendered but no-op; pre-fix there were NO row actions at all and the backend exposed only GET and POST — the defect was absence, not dead wiring. | 3371 |
| `SAF-B12` | **PASS** | External-fine actions wired | SHIPPED + MERGED 2026-07-25 as #3547 (merge commit 0f72be2805, on origin/main). Four external-fine lifecycle routes on safety.civil_fines were fully built and audited with ZERO frontend callers — an operator could create a fine and only Convert to Driver Liability, never contest or dismiss a wrongly-issued authority fine. Now wired: apps/frontend/src/api/safety.ts gains contestSafetyFine, dismissSafetyFine, reduceSafetyFine, linkSafetyFinePayment (append-only, +68 lines), consumed by the new FineLifecycleActions.tsx mounted inside FineDetailDrawer. Every signature transcribed from the route's own zod schema. UI gates MIRROR the server and never replace it: contest/dismiss disabled on voided_at; reduce additionally disabled on converted_to_liability_id, rendering the server's own remedy text from the OWNER RULING 2026-07-23 Option B; link-payment carries NO invented client gate because fines.routes.ts:616 holds no state predicate; role gate equals canMutate() exactly. GUARD: verify-step 1502 — walks relative imports out of FineDetailDrawer so a dead export fails, resolves each disabled expression transitively to the server's gate columns, re-reads the handler to confirm the server still predicates on them, and diffs the frontend role list against canMutate(). Its selftest rejects TWO fixtures — the real pre-fix shape AND a half-fix where all four callers exist but Reduce is gated on voided_at only — so it cannot decay into a grep that blesses a UI which 409s the operator. Pre-fix FAIL and post-fix PASS both captured. tsc exit 0 with a negative control proving coverage; vitest and eslint counts identical to pristine origin/main (zero net-new). PROD-EXPOSURE CONCERN RAISED AND REFUTED: the reduce handler reads safety.civil_fines.voided_at, added by HELD migration 202607110200_civil_fines_voidable.sql. That migration IS applied on prod — confirmed twice, applied_on_prod=true in .held-migrations.json AND present in the pinned prod ledger snapshot — so the column exists and /reduce does not 500. REMAINING (separate, financial): issued_by_authority is free text with no mdata.vendors FK, so a fine still has no A/P path to the issuing authority — schema change, build-and-HOLD. Row-level buttons in the FinesPage grid deliberately NOT added: that page's idiom is a single Open column handing off to the drawer, and adding a second pattern would be a redesign, not an addition. UNVERIFIED: no authenticated click-through — no agent had a session. SAF-DOM-01 PREMISE CONFIRMED on prod 2026-07-26 (information_schema, lucia): safety.civil_fines has NEITHER load_id NOR source_bank_transaction_id — both genuinely absent, so the FK half of that block is real. voided_at DOES exist, which independently re-confirms that the /reduce endpoint does not 500. civil_fines currently holds 0 rows on prod, so the migration adds FKs to an empty table — no backfill risk. | — |
| `SAF-B13` | **UNVERIFIED** | DOT/HOS creator pickers (no raw uuids) | CODE-VERIFIED, needs a click-through. DOTInspectionsTab.tsx:139 uses DriverPickerWithCreate plus a unit Combobox (testids dot-inspection-driver-picker / dot-inspection-unit-picker); HOSViolationsTab.tsx:120-129 uses DriverPickerWithCreate (hos-violation-driver-picker). No placeholder="driver_id" survives anywhere in apps/frontend/src. Guard 1326. SETTLES IT: in TRANSP and USMCA open the DOT-inspection and HOS-violation creators — only in-entity drivers listed, inline +Add opens the driver wizard, and the selection survives a reload. | 3340 |
| `SAF-B14` | **PASS** | Bind the 3 orphan safety catalogs to creators | VERIFIED PASS 2026-07-28 — 3 of 3 bound. Guard verify-safety-catalog-bound-to-creator.mjs discovers the catalog set from api/catalogs-safety.ts (6 of 6 consumed by a creator, denominator printed) and is proven RED on the real defect: reverting both bindings in the working tree exits 1 naming listCivilFineTypes, listDotViolationTypes; restore returns green. Selftest re-plants the defect into the REAL creator files and requires the catch. NEW BINDINGS: catalogs.civil_fine_types (57 live rows) -> FineCreateModal violation-type Combobox with inline +Add new writing the same canonical table it reads, now sending violation_code (accepted by fines.routes.ts:31,204,218 since it was written and NULL on every row because nothing sent it); catalogs.dot_violation_types (213 live rows, 33 hours_of_service) -> HOSViolationsTab Combobox storing the FMCSA code and rendering the name. SECOND DEFECT FOUND AND FIXED: csa_points was never sent, so every HOS violation stored 0 severity though the route has always accepted it — the catalog's severity_weight is that number. Server-side basic_category filter added because the list caps at limit=200 against 213 rows (SAF-F31 cap landmine) — client-side filtering would have hidden 13 rows silently. Live: safety.civil_fines and safety.hos_violations both n_live_tup=0 under lucia bypass, so this corrects the write path before any data accumulates. | 3690 |
| `SAF-B15` | **PASS** | Driver profile reverse: fines/complaints/D&A | VERIFIED 2026-07-25. DriverSafetyReverseSection.tsx:103,109,115,121 — all four reads driver-scoped; MOUNTED at DriverDetail.tsx:41,1440; route manifest.tsx:4091; backend complaints.ts scopes both complainant and respondent. Guard 1334 with a 7-planted-defect selftest incl. not-mounted and a cents/dollars cross. | 3363 |
| `SAF-B16` | **PASS** | Unit + trailer profile safety reverse section | VERIFIED 2026-07-25. AssetSafetyReverseSection.tsx reads accidents/DOT/DVIR/incidents; MOUNTED at VehicleProfilePage.tsx:40,322 (assetKind=unit) and TrailerProfilePage.tsx:21,144 (assetKind=trailer); routes manifest.tsx:4163,4179. Guard 1335 pins accidents unit-only via enabled && isUnit. | 3365 |
| `SAF-B17` | **PASS** | External fines driver column + detail link | VERIFIED 2026-07-25. fines.routes.ts:135,142,176,178 JOIN on BOTH list and detail returning subject_driver_name; FinesPage.tsx:84,88 renders the Driver column as EntityLink kind=driver; FineDetailDrawer.tsx:79. Guard 1330. | 3354 |
| `SAF-B18` | **PASS** | External fine load / unit / document capture | VERIFIED PASS 2026-07-28. source_doc_id (FK -> docs.files, confirmed on prod) was accepted by the create route since it was written (fines.routes.ts:37,205,224) and collected by NO UI — zero frontend references — so every fine was filed with no citation image. Not cosmetic: the same column is carried onto the LIABILITY the fine spawns (fines.routes.ts:391), so the payable an auditor, insurer or attorney reads had no supporting document behind it. FIX: the creator gains a citation/ticket upload using the established requestUploadUrl -> PUT -> confirmUpload flow, run BEFORE the fine is created so a fine is never written pointing at a file that failed to land, and a failed upload surfaces as an error instead of a silently document-less fine. docs.file_links has no 'fine' entity type, so the file is filed under the driver/unit/load it concerns (the entity types that DO exist) giving reverse reachability from those document lists; the fine->document direction is the source_doc_id FK. GUARD 1670 locks the whole defect CLASS rather than this one field: it parses the route's own zod schema and requires the creator to send every field it accepts (12 of 12). Proven RED against real pre-fix origin/main, where it names exactly 'source_doc_id, 11 of 12'. The same class previously produced SAF-F19 (related_load_id/related_unit_id) and SAF-B14 (violation_code), each found by hand months apart after the data was already wrong. NOT DONE HERE (schema change, owner gate): issued_by_authority is free text and is not an FK to mdata.vendors, so 'authority as EntityLink' plus an AP path to the authority needs a migration — flagged, not silently skipped. | 3693 |
| `SAF-B19` | **PASS** | Incidents cluster edit + lifecycle + void | VERIFIED PASS 2026-07-28. The manifest premise was correct but INCOMPLETE — the void was not only unreachable, its contract was never enforced. REACHABILITY: added voidSafetyIncident client + an Owner/Administrator-gated 'Void incident' control with required reason on SafetyIncidentsClusterSurface, kept deliberately separate from the close/reopen buttons (closing is an outcome, voiding is a retraction) and surfacing the error instead of failing silently. ENFORCEMENT (second defect, found while verifying): incidents.routes.ts promised in its own doc comment that a voided incident 'is immutable and drops out of the operational list' and implemented NEITHER — the list never filtered voided_at and the edit, status and photo-upload paths never checked it, so a retracted incident stayed in the operational list and could still be edited, re-opened and have evidence attached. All four now enforce it; the list gained include_voided=true so voided rows stay auditable rather than hidden (void-not-delete). Guard 1668 discovers void routes from backend source (5 of 5 reachable from a component) and asserts all 4 enforcement points; its selftest plants each of the 5 defects INDIVIDUALLY into the real sources and requires each to be caught. Prod under lucia: safety.incidents voided_at/voided_by_user_id/voided_reason all present, migration 202607820000 applied, n_live_tup=0. Backend tests 16 passed; IncidentsClusterPages 3 passed; verify:static exit 0. | 3691 |
| `SAF-B20` | **PASS** | Cargo claims ParityTable + links | VERIFIED 2026-07-25 for its built scope. CargoClaimIntakeSurface.tsx:485 ParityTable, :176 claimant EntityLink kind=customer, :190 kind=load; mounted CargoClaimsPage.tsx:17 -> CargoClaimsTab -> manifest.tsx:1577. Guard 1343. The economics clause (amount -> poster) is NOT built and is deferred ON RECORD by owner ruling — recovery_rail defaults to 'ask', nothing posts on ask, no dollar threshold (verify-safety-gated-batch.mjs:15-18, migration 202607820000). Tracked as SAF-F34, not silently dropped. | 3379 |
| `SAF-B21` | **PASS** | Orphan-route nav entry points | BROWSER-VERIFIED 2026-07-24 19:59 CT, app.ih35dispatch.com/safety as tioperfumes07@gmail.com, company 'IH 35 Transportation' (TRANSP), live_sha 2088757. Safety module renders with all top nav groups. Full route audit of SAFETY_TABS_CONFIG.ts vs routes/manifest.tsx:1541-1616: all 34 config tabs (28 SAFETY_GROUPS + 6 SAFETY_ALIAS_TABS) are route-registered AND component-mounted; ZERO orphans — aliases are merged into each group's dropdown as real NavLinks by SafetyGroupNav.tsx:16. No ComingSoonPage anywhere under pages/safety. This boundary's entire scope (orphan routes / dead drill-throughs) is closed. | 3370 |
| `SAF-B22` | **FAIL** | Escrow tab drill-through (driver/settlement/GL/bank) | VERIFIED FAIL 2026-07-25 — 1 of 4 drill-throughs. EscrowRecordTab.tsx:62-63 has ONLY EntityLink kind=driver; there is no settlement, no journal_entry and no bank_transaction link on the tab, and EscrowHistoryView.tsx has zero EntityLinks. Guard 1337 asserts only kind=driver on that file, so it is green while three quarters of the requirement is absent. F23's 'links to the driver's settlement and to a bank transaction, drills both ways' is unmet. | 3370 |
| `SAF-B23` | **FAIL** | Internal-fine inline + Add new reason | VERIFIED FAIL 2026-07-25. The NAMED scope is genuinely done: InternalFinesPage.tsx:102,214,227 puts the create sentinel as the FIRST ROW INSIDE the dropdown with createInternalFineReason and auto-select (guard 1333). But F24's acceptance is EVERY Safety picker, and ComplaintsTab.tsx:213-222 renders the complaint-type catalog as a bare SelectCombobox with no '+ Add new' row. No guard covers the picker SET, only single files. | 3360 |
| `SAF-B24` | **FAIL** | ParityDrawer migration (3 surfaces) | VERIFIED FAIL 2026-07-25. The 3 NAMED surfaces are migrated and guarded (EscrowForfeitModal, AccidentReportDrawer.tsx:205, FineDetailDrawer.tsx:67 all render ParityDrawer; guard 1345). But F25's acceptance is 'no bespoke drawer remains' and THREE survive, all carrying mutations and inputs: CompanyViolationDetailDrawer.tsx:98, IntegrityAlertDetailDrawer.tsx:73, AnomalyDetailDrawer.tsx:104 — bare <aside> elements. Guard 1345 pins only the 3 named files, so the 3 survivors are invisible to CI. | 3381 |
| `SAF-B25` | **PASS** | Accidents list name resolution + pickers | VERIFIED PASS 2026-07-28. The manifest named the LOAD leg; the vendor leg had the identical defect and is fixed too. safety.accident_reports.load_id (FK -> mdata.loads) and vendor_id (FK -> mdata.vendors) have both been captured by the office creator (AccidentReportDrawer sends all four links) and persisted by the INSERT since the table existed, while the LIST joined only driver and unit — so an accident could be filed against a specific trip and a specific tow/repair vendor and neither could be read back: no column, no join, no drill-through. Added both joins (entity-scoped by operating_company_id; mdata.loads was verified on prod to HAVE operating_company_id, unlike mdata.units) and both EntityLink columns, inserted after Unit so existing column order is otherwise preserved verbatim. Guard 1669 discovers the captured link set from the creator payload (4 of 4 resolve) and asserts join + entity scoping + drill-through; selftest plants 4 defects individually including a dropped scoping clause. TESTS: AccidentsPage + AccidentReportDrawer were 11 FAILING on clean origin/main (verified by stashing) for two pre-existing reasons unrelated to this block — no Router in the harness though the components render EntityLink, and a factory vi.mock of api/mdata missing createDriver, which threw at import so all 8 asserted nothing. Both root-caused and fixed, plus a stale 'Cazares, Alfredo' expectation corrected to the app-wide 'First Last' convention and two assertions rewritten to drive the real Combobox the drawer migrated to. Now 11 of 11 pass. | 3692 |
| `SAF-B26` | **PASS** | De-duplicate 7 double-registered routes + guard | PREMISE REFUTED then acceptance met by evidence, 2026-07-25. The finding claimed 7 double-registered Safety routes. There are ZERO: apps/frontend/src/routes/manifest.tsx on origin/main has 28 absolute path="/safety..." registrations and 28 unique — verified three times independently (my scan, a separate reviewing agent, and a re-scan on current main). The acceptance criterion "exactly one registration each" therefore already holds. What the sweep DID find is a real duplicate-registration defect elsewhere: /compliance and /notifications were each registered twice as byte-identical <Route> blocks ~2700 lines apart, so the second copy of each was unreachable — removed in PR #3522, unique route surface unchanged at 546. The guard half of this item is satisfied by scripts/verify-steps/1484-verify-no-duplicate-route-registrations.mjs (#3522), which asserts uniqueness of ABSOLUTE route paths and is proven to FAIL on the pre-fix manifest naming both paths. It deliberately does NOT flag the three relative children that legitimately repeat under different parents (settings, hos, loads/:id — office portal vs driver app, different URLs and components); scoping it to leading-slash paths is what lets the assertion be true without an allowlist. | — |
| `SAF-B27` | **PASS** | Docs drift: docs/CLAUDE.md section 7 -> 28/9 | REPO-VERIFIED 2026-07-25: docs/CLAUDE.md section 7 now states 28 tabs / 9 groups and points at SAFETY_TABS_CONFIG.ts as source of truth, matching the config array (28 SAFETY_GROUPS entries confirmed by full enumeration) and both count guards. The stale 21/8 claim is gone. | 3384 |
| `SAF-B28` | **FAIL** | company_violations JSONB -> real FKs (migration) | VERIFIED FAIL 2026-07-25 — application code, NOT an unapplied migration. Junctions exist with FORCE RLS and a 0-orphan backfill abort (202607820000), the GET reads junctions (company-violations.routes.ts:118-123) and CREATE writes them (:200-202). But PATCH still writes the RETIRED jsonb columns and never touches the junctions (:258-266, sets.push(`${key} = $${idx}::jsonb`), schema :50-52) — so editing a violation's related drivers/units/fines SILENTLY NO-OPS, because the read side uses junctions. Guard 1344's cutover check matches JSON.stringify(body.data.related_...) while the PATCH uses JSON.stringify(value ?? null), so the guard cannot see this write path. PREMISE CORRECTED: 202607820000 IS applied on prod (in the pinned ledger snapshot, checksum 578a6d7d, applied_on_prod:true); the genuinely unapplied sibling is 202607840000. GUARD LANE 2026-07-25 — SWEEP-C2 (RETIRE-schema live writes). The PATCH at company-violations.routes.ts:258-266 writes the retired jsonb columns and never touches the junctions, so editing a violation's related drivers/units/fines SILENTLY NO-OPS because the GET reads junctions. Repoint the writer to canonical. NOT this coder's to build. GUARD SAF-DOM PACKET 2026-07-26: SAF-DOM-02 confirms on prod that company_violations.related_drivers / related_units are jsonb id-ARRAYS, not FKs. This seeds class C13 (jsonb id-array where an enforced FK is required). An id ARRAY is strictly worse than a scalar: a partial delete leaves silent dangling members that no constraint can catch. C1 is inventorying C13 while it walks these surfaces; the FIX stays Cursor's C2. SAF-DOM-02 PREMISE CONFIRMED on prod 2026-07-26: safety.company_violations.related_drivers = jsonb and related_units = jsonb; a fine_ids column is ABSENT entirely. Both are id-ARRAYS, the worse C13 shape. | 3380 |
| `SAF-B29` | **FAIL** | Server-side type-ahead for 200-cap pickers | VERIFIED FAIL 2026-07-25 — 1 of ~14 pickers. The engine is correct (Combobox.tsx:29,124-126,133 optional onSearch that skips the double-filter) but only AccidentReportDrawer.tsx:50-52,122-133 is wired. Still hardcoded limit:200 with NO search: FineCreateModal.tsx:41,47,54; SafetyIncidentsClusterSurface.tsx:156,161,166; ComplaintsTab.tsx:60,69; DOTInspectionsTab.tsx:144; DrugAlcoholTab.tsx:96; CompanyViolationCreateModal.tsx:59; InternalFinesPage.tsx:62,73. F31's Rule-21 clause is 'server search everywhere it is reused'. Guard 1343 asserts the 3 AccidentReportDrawer pickers only. | 3379 |
| `SAF-B30` | **FAIL** | EntityLink safety kinds + safety detail routes | VERIFIED FAIL 2026-07-25. There is NO `incident` EntityLink kind — EntityLink.tsx:50-55 is accident/safety_fine/complaint/dot_inspection/escrow_record/permit, and dot_inspection occupies the slot F33 named incident. The code admits the consequence: AssetSafetyReverseSection.tsx:127 'No EntityLink kind: safety.incidents has no per-record detail surface to open' — damage reports, trailer interchanges and cargo claims stay undrillable. Only 2 of 6 kinds actually drill (AccidentsPage.tsx:63, FinesPage.tsx:63 read their param); complaints/DOT/escrow/permits honour no param. F35: safety.routes.ts:690-714 spawn-liability returns spawned_liability_id:null and creates NOTHING (audit row only); spawn-wo (:717) has no idempotency check and persists no back-link column — the WO id lives only in ephemeral React state. Guard 1332 pins the six kinds WITH dot_inspection in the incident slot, so it ratifies the substitution. | 3357 |
| `SAF-B31` | **UNVERIFIED** | No Safety surface is a static placeholder | CODE FIXED 2026-07-25 in PR #3537, branch fix/saf-b31-safety-placeholder-surfaces (branched off origin/main dfff68e954). Both 11-line static placeholders replaced with real, entity-scoped query surfaces. SafetyReportsPage.tsx (283 lines) now reads GET /api/v1/safety/csa-scores/current (routes/safety/csa-scores.ts:163), GET /api/v1/safety/csa-scores (csa-scores.ts:148), GET /api/v1/safety/dot-inspections/clean-rate (routes/safety/dot-inspections.ts:78) and GET /api/v1/safety/reports/:report_id (safety/reports/safety-reports.routes.ts:39). Audit425cPage.tsx (309 lines) now reads GET /api/v1/audit/events-list (audit/audit-events-list.routes.ts:178) filtered to event_class ILIKE '%form_425c%', with a real section filter driven by payload.resource_type (compliance.form_425c_reports / _exhibit_a_entries / _exhibit_b_entries, form-425c.routes.ts:575/:1065/:1103) and per-row drill-through to /425c?tab=history and /425c/exhibits. Guard scripts/verify-saf-b31-safety-placeholder-surfaces.mjs (wired scripts/verify-steps/1491-*): produces 24 failures against the verbatim pre-fix files and exits 0 after the fix; --selftest asserts both directions. apps/frontend npx tsc -b --force = exit 0; verify-mobile-responsive-audit new_vs_baseline=0; verify-safety-reports-coverage / verify-safety-route-coverage / verify-safety-orphan-routes-and-drillthrough / verify-xlsx-cve-closeout all exit 0. STATUS UNVERIFIED, NOT PASS: no live browser verification is possible from this branch (unmerged, undeployed) and the agent has no prod DB access. PASS requires post-deploy browser check of /safety/reports and /safety/audit-425c as TRANSP. TWO BACKEND DEFECTS FOUND, NOT FIXED HERE (frontend-only block): (1) GET /api/v1/safety/audit-425c (safety/audit-425c.routes.ts:36) SELECTs id and emitted_at from audit.audit_events, but 0001_audit_init.sql defines uuid and created_at and grep over db/migrations/ finds zero 'emitted_at' hits, so that route raises undefined_column - the page uses the working events-list reader instead. (2) GET /api/v1/safety/reports/:report_id/export.xlsx returns renderSafetyReportXlsx() (safety-reports.routes.ts:29-36), a hardcoded ["safety.sample", 0] workbook that ignores company and report - no XLSX control is wired to it; the guard forbids wiring it. | 3537 |
| `SAF-B32` | **PASS** | Safety KPI tiles read real sources (not permanent zeros) | BROWSER-VERIFIED 2026-07-24 19:59 CT, app.ih35dispatch.com/safety as tioperfumes07@gmail.com, company 'IH 35 Transportation' (TRANSP), live_sha 2088757. ACTIVE DRIVERS tile reads 82. Verified honest, not a stub: Neon lucia shows mdata.drivers active for TRANSP = 83, and safety.routes.ts:170 applies EXCLUDE_PSEUDO_DRIVERS_SQL, so 83 minus 1 pseudo-driver = 82 exactly. The remaining tiles read 0 and that is ALSO honest — safety.* has 64 tables and only 7 carry any rows (driver_safety_scores 161, fuel_gps_matches 143, document_alert_rules 21, anomaly_alert_rules 18, integrity_alert_rules 17, permit_renewal_reminders 3, safety_settings 3). There are no safety events, accidents, fines or violations on prod to display. | 3334 |
| `SAF-ORPH-01` | **FAIL** | Escrow forfeiture moves the balance in the correct direction | P0 MONEY DIRECTION, prod-verified verbatim. accounting.apply_escrow_posting_delta(): deposit -> +amount, release -> -amount, ELSE -> +amount. The forfeit posts posting_type='adjustment' with a POSITIVE amount, so a forfeiture INCREASES accounting.escrow_accounts.balance_cents while the JE DEBITS (reduces) the escrow liability - GL and subledger diverge by 2x the forfeit. LATENT: all escrow subledgers are 0 rows on prod and DRIVER_ESCROW_FORFEIT_GL_POSTING_ENABLED has NO row in lib.feature_flags at all (unknown key resolves false), so the route returns 409 forever and there is no enable path. FINANCIAL - build-and-HOLD, CPA VETO, owner Neon-applies; no agent flips that flag. SCOPE: distinct from SAF-B01 (endpoint reachability) and SAF-ORPH-02 (which subledger is written) — this item is ONLY the trigger sign. LANE REASSIGNED 2026-07-25 — OWNER STANDDOWN: this is CURSOR's lane, stacking on #3533 (ACCT-R-01 escrow canonical write-path sync, MERGED). The Lists+Safety coder must NOT touch accounting.apply_escrow_posting_delta or any accounting.escrow_* object. Do not rebuild this here. HANDOVER: findings recorded in docs/trackers/ESCROW-FINDINGS-HANDOVER-TO-CURSOR-2026-07-25.md — the ELSE->positive forfeit sign, the absent flag row, the 0 signed driver contracts, and the split-brain that a trigger fix alone will not close. Standdown also covers accounting/escrow/service.ts. | — |
| `SAF-ORPH-02` | **FAIL** | Escrow forfeiture writes the canonical driver subledger | SPLIT-BRAIN subledger. The forfeit writes accounting.escrow_postings only, while EVERY driver-facing balance reads driver_finance.escrow_balances / escrow_ledger (banking escrow visualizer + register, driver-detail Escrow History, the over-draw guard in escrow-forfeit.service.ts). So the balance the over-draw check reads NEVER decrements - the same escrow could be forfeited repeatedly - and no driver-facing surface shows a forfeit. driver_finance.escrow_ledger already carries an UNUSED 'forfeit' value in its transaction_type CHECK. LATENT: both subledgers are 0 rows on prod today. Fixing SAF-ORPH-01 alone leaves this open, so they are ONE CPA review. SCOPE: distinct from SAF-B01 and SAF-ORPH-01; overlaps SAF-B22 (escrow drill-through) which stays the UI-side boundary. LANE REASSIGNED 2026-07-25 — OWNER STANDDOWN: this is CURSOR's lane, stacking on #3533 (ACCT-R-01 escrow canonical write-path sync, MERGED). The Lists+Safety coder must NOT touch accounting.apply_escrow_posting_delta or any accounting.escrow_* object. Do not rebuild this here. HANDOVER: findings recorded in docs/trackers/ESCROW-FINDINGS-HANDOVER-TO-CURSOR-2026-07-25.md — the ELSE->positive forfeit sign, the absent flag row, the 0 signed driver contracts, and the split-brain that a trigger fix alone will not close. Standdown also covers accounting/escrow/service.ts. | — |
| `SAF-ORPH-03` | **OPEN** | Forfeiture Audit panel can render a row | PHANTOM COLUMN, prod-verified. driver_finance.escrow_ledger has 11 columns and NO posted_at, yet deductions.routes.ts:147 does ORDER BY posted_at -> 42703, swallowed by getEscrowDriverTimeline(...).catch(()=>({timeline:[]})). forfeiture_history_count is therefore always 0 and the Safety Forfeiture Audit panel is STRUCTURALLY incapable of ever showing a row. Non-financial, builder's lane: order by created_at (what sibling readers use) + a guard failing on ORDER BY of a column absent from the table. | — |
| `SAF-ORPH-04` | **OPEN** | Form 2290 deadline is per-unit and business-day correct | The hardcoded 'Aug 31' STRING was removed, but the server computation it now trusts is still wrong twice. upcomingForm2290Deadline() always returns Aug 31 for the WHOLE company, ignoring per-unit first use - per IRS Form 2290 a vehicle first used after July is due the last day of the month following first use (a truck placed in service in October is due Nov 30) and this banner would never surface it; the per-unit proration that exists affects the TAX AMOUNT, not the DUE DATE. And there is NO weekend/holiday shift: Aug 31 2026 is a Monday so it is dormant, but 2024 (Saturday) and 2025 (Sunday) would have mis-rendered. The existing guard asserts only ISO shape and a 0-366 bound, so it would catch neither. | — |
| `SAF-ORPH-05` | **OPEN** | Every Safety tab passes a DoD click-through | COVERAGE GAP, not a defect. SAFETY_TABS_CONFIG.ts declares SAFETY_CANONICAL_TAB_COUNT=28 / GROUP_COUNT=9, but the 32 SAF-B## items are BOUNDARIES, not tabs - there is no per-tab item and no per-tab sweep has run. 23 of the 32 boundaries are UNVERIFIED with the identical evidence 'Code merged; NO live proof'. Until a per-tab click-through exists on TRANSP+USMCA, Safety tab coverage is UNKNOWN rather than complete. This is the single largest blocker in the module. | — |
| `SAF-DOM-02` | **OPEN** | company_violations retired jsonb: re-backfill sweep, auto-fine trigger repoint, DB-enforced archive (held migration) | NEW FAIL FOUND 2026-07-25 while scoping SAF-DOM-02, not previously on the board: safety.auto_create_internal_fine_from_violation() (SECURITY DEFINER, 0124:971-1101, fired by trg_auto_fine_on_violation_resolve) resolves its driver ONLY from NEW.related_drivers, the RETIRED jsonb. The create route stopped populating that column when 202607820000 (applied on prod) landed, so for every violation created through the UI since, resolve-as-monetary_fine hits 'IF v_driver_id IS NULL THEN RETURN NEW' and issues NO FINE while the endpoint returns 200 with autoCreatedInternalFineUuid:null. Proven on a throwaway postgres:16-alpine built from 0001: pre-fix function -> UPDATE 1, outcome=monetary_fine, auto_created_internal_fine_uuid NULL, 0 rows in safety.internal_fines; post-fix function -> fine created, linked, 0.05 approved, driver from the junction. Migration 202609130000 is HELD and UNAPPLIED anywhere; status stays OPEN until the owner Neon-applies it and GUARD re-proves the ledger row. Its stop-write section must not be applied before SAF-B28's PATCH repoint (C2 lane) is live. | — |

Desktop audit: ~/Desktop/IH35-CURSOR-AUDIT/modules/safety.md
