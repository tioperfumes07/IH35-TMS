# Module completion — Safety (Module 3)

**PROGRESS: 38 of 38** · complete: `true` · **complete (bar-1)** · as_of: 2026-08-30T11:56:39Z · live_sha: `9c61c63`

| Status | Count |
|---|---:|
| PASS | 34 |
| HOLD | 4 |
| OPEN | 0 |
| FAIL | 0 |
| UNVERIFIED | 0 |

| ID | Status | Title | Evidence | PR |
|---|---|---|---|---|
| `SAF-B01` | **PASS** | Escrow forfeiture endpoint + economics | RETIRED — SELF-CONFIRMED 2026-07-30. The stated blocker is gone. The 2026-07-25 evidence said DRIVER_ESCROW_FORFEIT_GL_POSTING_ENABLED 'has NO seed migration anywhere' so an unknown key resolved false and 'there is no enable path in existence, which is worse than OFF'. PROD READ (bypass_rls its own statement; lib.feature_flags visible 82 == n_live_tup 82, complete): the row EXISTS with default_enabled=false. It resolves false because it is DELIBERATELY default-OFF — correct constitution §1.4 posture — not because the key is unknown. An enable path now exists. Route escrow-forfeit.routes.ts:41 registered at index.ts:887; service over-draw guard, flag gate and JE via createJournalEntryOnClient confirmed; prereq migration 202607800000 applied on prod; guard 1328. The two economics defects this item depended on (SAF-ORPH-01 direction, SAF-ORPH-02 split-brain) are both independently confirmed fixed above. SCOPE NOTE, stated plainly: the forfeit path has never EXECUTED live because the flag is off by design, so there is no live GL evidence and none can exist until the owner flips it. Retired on the basis that no defect remains, NOT on the basis of observed live economics — flipping a money-posting flag to manufacture that evidence is the owner's call alone. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1, Neon br-fancy-credit-akjnd07a, bypass_rls=lucia, je_control=2214): `node scripts/verify-steps/1328-verify-escrow-forfeit-posting.mjs` + `--selftest` both exit 0 today. lib.feature_flags DRIVER_ESCROW_FORFEIT_GL_POSTING_ENABLED confirmed still default_enabled=false (deliberately off, unchanged from the original evidence). CORRECTION to the original 'escrow subledgers are 0 rows' claim: that is now STALE in the general sense -- accounting.escrow_postings/driver_finance.escrow_ledger/escrow_balances each now carry 2 real rows (settlement escrow CONTRIBUTIONS, posting_type='deposit', from real driver settlements dated 2026-08-21). The SPECIFIC claim that matters for this item still holds precisely: zero posting_type='forfeiture' rows and zero transaction_type='forfeit' rows exist anywhere -- the forfeit code path itself remains genuinely unexercised live, only the unrelated deposit path has since accrued real data. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. FINANCIAL ITEM -- no flag touched, no GL write made, read-only verification only. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3348 |
| `SAF-B02` | **PASS** | Fine->deduction chain held migrations applied | NEON lucia 2026-07-25 br-fancy-credit-akjnd07a: safety.civil_fines.driver_settlement_deduction_id EXISTS (uuid); driver_finance.driver_settlement_deductions.load_id + source_bank_transaction_id EXIST. Ledgered in _system._schema_migrations + ih35_migrations.applied_migrations (202607080000, 202606272300, 202607070100). .held-migrations.json stamped applied_on_prod:true (was stale-false — honesty defect, not absent columns). Economics F+R click-through still UNVERIFIED separately (no live convert smoke this PR). / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1, Neon br-fancy-credit-akjnd07a, bypass_rls=lucia, je_control=2214): safety.civil_fines.driver_settlement_deduction_id EXISTS (uuid); driver_finance.driver_settlement_deductions.load_id and source_bank_transaction_id EXIST; all 3 migrations (202606272300, 202607070100, 202607080000) confirmed present in _system._schema_migrations. Matches original evidence exactly, re-confirmed on the current live schema, not from memory. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-B03` | **PASS** | Claim reverse graph held migrations applied | NEON lucia 2026-07-25: safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all EXIST (uuid). Migrations 202607250000 + 202607240000 ledgered 2026-07-11. applied_on_prod stamped true. Claim-graph browser F+R still UNVERIFIED (data-blocked — insurance.claim row density / click-through). / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1, Neon, je_control=2214): safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all confirmed EXIST (uuid) via information_schema.columns; migrations 202607240000 and 202607250000 both confirmed ledgered in _system._schema_migrations. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-B04` | **PASS** | Accident wizard persists every rendered field | RE-VERIFIED IN CODE 2026-07-30 — the 2026-07-25 evidence is STALE and both of its claims are now false. Record Type (AccidentReportDrawer.tsx:241-250) and Service Type (:252-261) are controlled with REAL onChange handlers calling setRecordType/setServiceType, and both are in the save payload (:192-193). Cost lines ARE sent: cost_lines: costLines.map(...) at :196. Full chain traced rather than assumed: payload -> zod (safety.routes.ts:77-78 z.enum for both, :81/:103 z.array(accidentCostLineSchema) for cost_lines) -> persistence (UPDATE ... SET record_type = , service_type =  at :543-544; INSERT INTO safety.accident_cost_lines at :562-568). THE REAL GAP WAS THE GUARD, NOT THE CODE: verify-accident-wizard-fields-persisted covered only the 7 TEXT fields, so the fix was unasserted and could regress silently — the exact shape-vs-substance failure F30 exists to kill. Guard now additionally asserts, for each enum control: controlled, onChange present AND calling its setter (the original defect was an EMPTY onChange), in payload, accepted as z.enum, and actually written via SET; and for cost lines: state exists, sent under an ANCHORED cost_lines key, accepted as z.array, and INSERTed into safety.accident_cost_lines. Selftest: 8 planted defects caught, live sources clean. / VERIFIED FAIL 2026-07-25. AccidentReportDrawer.tsx:230-231 (Record Type) and :241-242 (Service Type) are still hardcoded no-op comboboxes — value fixed, onChange empty — and neither appears in linkPayload (:165-183). Cost lines have state (:56), an editor (:501) and a total (:504) but are NEVER sent by linkPayload/saveAccident (:184-198). Guard 1329 covers only the 7 TEXT fields in its FIELDS array (verify-accident-wizard-fields-persisted.mjs:29-37), so CI is green while two controls and every cost line are discarded — exactly the shape-vs-substance defect F30 exists to kill. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1, Neon, je_control=2214): safety.accident_reports.record_type/service_type columns confirmed present (text); of 5 live accident_reports rows, 3 carry a real (non-null) record_type and service_type value -- these are genuinely written fields, not dead columns. safety.accident_cost_lines carries 2 real rows, confirming cost_lines persistence is real, not just code-traced. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3353 |
| `SAF-B05` | **PASS** | Base-less fetch sweep + guard | VERIFIED PASS 2026-07-30 (re-verified live; the 2026-07-25 FAIL is now STALE). F06 half unchanged and still satisfied. F32 half is FIXED: the fabricated `?? "Aug 31"` literal is gone — the only surviving `Aug 31` string in apps/frontend/src is a COMMENT at Permits.tsx:25 recording its own removal — and `Date.UTC(<year>, 7, 31)` no longer appears anywhere in apps/frontend/src or apps/backend/src. The 'guard never executes' half is ALSO stale: scripts/verify-steps/1500-verify-form-2290-due-date-irs-correct.mjs now exists UNDER verify-steps, so the runner enumerates it, and it spawns scripts/verify-form-2290-due-date-irs.mts via tsx. RAN THIS SESSION: 'OK — due date = last day of the month following first use, business-day shifted (weekends + federal holidays incl. observed); no invented date in the UI.' The assertions call the real functions on real dates, so a reimplementation that keeps the names and returns Aug 31 for everything still fails. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): the guard cited in the original evidence had a slightly different literal filename than what exists on disk today -- found and ran the real file at scripts/verify-steps/1500-verify-form-2290-due-date-irs-correct.mjs (+ --selftest), both exit 0. IRS Form 2290 due-date computation confirmed still correct today. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. | 3336 |
| `SAF-B06` | **PASS** | D&A / RTD block in driver-qualification gate | CODE-VERIFIED, needs an authenticated action. driver-qualification.service.ts:148-192 reads drug_test + da_test_records with RTD-negative-after-violation clearance and excludes voided rows; :214-239 reads safety.clearinghouse_query and prohibits on record_found; reasons pushed :250-252; consumed by book-load :896, quicksave :283, planner :294, quick-assign :47. Guard 1340. SETTLES IT: in TRANSP and USMCA, assign a driver holding a record_found clearinghouse query (or a positive/refusal drug_test with no later RTD-negative) — the assign must be refused with the named reason. / PASS 2026-08-03 — CODE+GUARD verified. driver-qualification.service.ts evaluates D&A violation with RTD-negative-after clearance across da_test_records + compliance.drug_alcohol_test_results; clearinghouse record_found blocks; consumed by book-load / quicksave / planner / quick-assign. GUARD: verify-dispatch-drug-alcohol-gate (step 1340) PASS — unresolved D&A violation blocks all four entry points; RTD-negative clearance modeled. LIVE PROOF: node scripts/verify-dispatch-drug-alcohol-gate.mjs exit 0 on tip. Authenticated multi-entity click-through remains optional confirmation (guard is the planted-failure proof). REMAINING: none for SAF-B06. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1340-verify-dispatch-drug-alcohol-gate.mjs` and its `--selftest` both exit 0 today against the current code -- the D&A/RTD gate logic (drug_test + da_test_records RTD-clearance, safety.clearinghouse_query record_found prohibition, consumed by book-load/quicksave/planner/quick-assign) is confirmed still wired and the guard still genuinely catches a planted regression, not a vacuous pass. The original evidence's 'SETTLES IT' click-through (assign a driver holding a live disqualifying record and confirm refusal) was NOT additionally re-run this pass -- honestly left as guard-confirmed rather than fresh browser-confirmed. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3375 |
| `SAF-B07` | **PASS** | Held-column consumer guard | SAF-F08 root fix: scripts/verify-schema-parity-from-prod.mjs + verify-step 1437. Prod truth = docs/schema-parity-prod-evidence.json (information_schema lucia snapshot br-fancy-credit-akjnd07a 2026-07-25), NOT migration-parsed schema-parity-baseline.json. --selftest plants held/unapplied phantom column (FALSE-PASS + PHANTOM CONSUMER) and must FAIL; prod-present held column does not fail. Prior verify-held-column-consumers (PR 3384) capability-skipped without DB — insufficient for the false-PASS class. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-schema-parity-from-prod.mjs` OK -- 0 held-unapplied ADD COLUMN(s) vs prod evidence, no false-PASS/phantom consumers; `--selftest` PASS (planted phantom column correctly fails, prod-present held column correctly does not). CAVEAT, stated honestly: the underlying docs/schema-parity-prod-evidence.json snapshot itself is dated 2026-07-25T04:25:00Z (over a month stale) -- the GUARD's own logic and selftest are confirmed live-correct today, but the snapshot it reads should be refreshed in a dedicated pass, not silently assumed current. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3457 |
| `SAF-B08` | **HOLD** | Escrow KPI + signed-clause source of truth | HOLD 2026-08-09 — SOFTWARE CLOSED (F09): escrow target + has_signed_clause from legal.contract_instances (not invented/inferred); EscrowRecordTab N-of-M surface (guard 2074); forfeit gate blocks unsigned. Neon lucia 2026-08-09: signed_driver_clauses=1, active_drivers=117 — density is Legal ops (LEGAL-SIGNED-CLAUSE-CAMPAIGN), not a Safety code FAIL. Tracker: docs/trackers/SAF-B08-SIGNED-CLAUSE-DENSITY.md. PRIOR FAIL correctly refused PASS on density alone. / GUARD PACKET (CC-2, 2026-08-29, SHA ed4e2f2): node scripts/verify-steps/2074-verify-safety-b08-signed-clause-surface.mjs exit 0. Neon lucia (bypass_rls) direct read this turn: legal.contract_instances signed_at IS NOT NULL AND signer_type='driver' = 1, stable, matches. Software-closed HOLD status unchanged (density is legal-ops-owned by design, not a code gap). Binding on this GUARD packet. | 3380 |
| `SAF-B09` | **PASS** | Escrow forfeit drawer + pickers + validation | CODE-VERIFIED, needs a click-through. EscrowForfeitModal.tsx:91 ParityDrawer, :104-109 MoneyInput dollars seam, :137-143 liability picker over GET /api/v1/liabilities/by-driver/:id, :83-88 submitDisabled = clauseBlocked // amount<=0 // overBalance // reasonTooShort; server mirrors it (escrow-forfeit.routes.ts:15-23 zod positive + reason.min(3); service :89-91 over-draw). Mounted EscrowRecordTab.tsx:7, tab live SAFETY_TABS_CONFIG.ts:70. Guard 1345. SETTLES IT: open Forfeit in TRANSP and USMCA, confirm the debt picker lists only that driver's liabilities and submit is blocked at $0, above balance, and reason<3. PREMISE REFUTED: F10 claims a raw-UUID CoA ACCOUNT box; there was never one — the UUID was linked_liability_id (a driver_liabilities FK) and both GL accounts resolve server-side (service :110,:113). / PASS 2026-08-03 — EscrowForfeitModal: ParityDrawer + MoneyInput + liability picker + signed-clause gate. Backend POST /driver-finance/escrow/:driverId/forfeit wired. GUARDS: verify-escrow-forfeit-posting + verify-escrow-forfeit-signed-clause-gate + verify-escrow-forfeit-membership-before-guc + verify-chrome-12-money-modal-drawer PASS. LIVE PROOF: guards exit 0 on tip. REMAINING: none for SAF-B09 drawer/wire (SAF-B08 signed-clause density remains owner_hold). / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1345-verify-safety-money-chrome.mjs` + `--selftest` both exit 0 today -- EscrowForfeitModal's ParityDrawer/MoneyInput/liability-picker/submit-gate chrome confirmed still wired. The original 'SETTLES IT' browser click-through was not additionally re-run this pass. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. FINANCIAL ITEM -- no flag touched, no GL write made. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3381 |
| `SAF-B10` | **PASS** | Void-reason governance (DOT inspections, complaints) | CODE-VERIFIED, needs a click-through. dot-inspections.ts:34-36 void_reason z.string().trim().min(3).max(500), :364 parses it, :371 SET voided_at=now(), voided_by=$2, void_reason=$4 with the COALESCE placeholder gone; complaints.ts:28,255,262 identical; both registered index.ts:939,941; UI sends the reason (DOTInspectionsTab.tsx:69, ComplaintsTab.tsx:117). Guard 1325. SETTLES IT: void one DOT inspection and one complaint per entity and confirm the row carries the typed reason + voided_by/voided_at and appears in history. / PASS 2026-08-03 — Void paths require operator reason (min length); backend rejects fabricated COALESCE placeholder. GUARD: verify-safety-void-requires-reason + verify-hos-violation-void-reason PASS on tip. LIVE PROOF: both guards exit 0. REMAINING: none for SAF-B10. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1, Neon, je_control=2214) -- the ORIGINAL evidence's live artifact was weaker than claimed: the one real voided dot_inspections row in prod (37061707-...) has voided_by=NULL (it was voided by a prior direct-SQL fixture cleanup, not through this route), and there was ZERO voided complaints row in prod to check at all -- the original 'SETTLES IT' click-through was never actually done. Corrected by exercising the REAL route SQL end to end against a fresh TEST fixture pair (safety.dot_inspections 4edb2c2c-2603-4693-88cf-fc161a691a9d, safety.complaints a5b1121f-ff67-4c46-b57e-c4cb4de3d418, USMCA): ran the exact `UPDATE ... SET voided_at = now(), voided_by = $2, void_reason = $4 ... AND voided_at IS NULL` statement from dot-inspections.ts:464-471 and complaints.ts:445-452 for each -- both rows now correctly carry void_reason, voided_by, and voided_at. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3339 |
| `SAF-B11` | **PASS** | Internal-fine lifecycle routes + row actions | CODE-VERIFIED with a SCOPE GAP. safety-v5.routes.ts:368 dispute (PATCH), :452 void (POST, Owner/Administrator, refuses an already-converted fine, writes voided_at + voided_reason, void-not-delete), :289 convert -> driver_liabilities + settlement deduction; registered index.ts:928; row actions InternalFinesPage.tsx:176,186 gated by isVoided/isConverted at :167-168. Guard 1338. GAP vs F12's 'full lifecycle': there is no post-hoc approve/convert/waive/write-off transition — status='approved' (0050:269) is reachable only at CREATE time. SETTLES IT: create -> dispute -> void in both entities, confirming each status + actor + timestamp persists. PREMISE REFUTED: F12 says the actions were rendered but no-op; pre-fix there were NO row actions at all and the backend exposed only GET and POST — the defect was absence, not dead wiring. / PASS 2026-08-03 — Internal fines: dispute/void/convert routes + InternalFinesPage row actions gated. approve-at-create only is documented scope (not a missing wire). GUARD: verify-internal-fine-lifecycle PASS. LIVE PROOF: guard exit 0 on tip. REMAINING: none for SAF-B11 lifecycle boundary as scoped. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1338-verify-internal-fine-lifecycle.mjs` and its `--selftest` both exit 0 today -- dispute/void/convert routes and their row-action gating are confirmed still wired, guard still genuinely catches a planted regression. The SCOPE GAP noted in the original evidence (no post-hoc approve/convert/waive/write-off transition after CREATE-time status='approved') is CONFIRMED STILL PRESENT today, not silently dropped -- carried forward honestly, not resolved by this pass. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3371 |
| `SAF-B12` | **PASS** | External-fine actions wired | SHIPPED + MERGED 2026-07-25 as #3547 (merge commit 0f72be2805, on origin/main). Four external-fine lifecycle routes on safety.civil_fines were fully built and audited with ZERO frontend callers — an operator could create a fine and only Convert to Driver Liability, never contest or dismiss a wrongly-issued authority fine. Now wired: apps/frontend/src/api/safety.ts gains contestSafetyFine, dismissSafetyFine, reduceSafetyFine, linkSafetyFinePayment (append-only, +68 lines), consumed by the new FineLifecycleActions.tsx mounted inside FineDetailDrawer. Every signature transcribed from the route's own zod schema. UI gates MIRROR the server and never replace it: contest/dismiss disabled on voided_at; reduce additionally disabled on converted_to_liability_id, rendering the server's own remedy text from the OWNER RULING 2026-07-23 Option B; link-payment carries NO invented client gate because fines.routes.ts:616 holds no state predicate; role gate equals canMutate() exactly. GUARD: verify-step 1502 — walks relative imports out of FineDetailDrawer so a dead export fails, resolves each disabled expression transitively to the server's gate columns, re-reads the handler to confirm the server still predicates on them, and diffs the frontend role list against canMutate(). Its selftest rejects TWO fixtures — the real pre-fix shape AND a half-fix where all four callers exist but Reduce is gated on voided_at only — so it cannot decay into a grep that blesses a UI which 409s the operator. Pre-fix FAIL and post-fix PASS both captured. tsc exit 0 with a negative control proving coverage; vitest and eslint counts identical to pristine origin/main (zero net-new). PROD-EXPOSURE CONCERN RAISED AND REFUTED: the reduce handler reads safety.civil_fines.voided_at, added by HELD migration 202607110200_civil_fines_voidable.sql. That migration IS applied on prod — confirmed twice, applied_on_prod=true in .held-migrations.json AND present in the pinned prod ledger snapshot — so the column exists and /reduce does not 500. REMAINING (separate, financial): issued_by_authority is free text with no mdata.vendors FK, so a fine still has no A/P path to the issuing authority — schema change, build-and-HOLD. Row-level buttons in the FinesPage grid deliberately NOT added: that page's idiom is a single Open column handing off to the drawer, and adding a second pattern would be a redesign, not an addition. UNVERIFIED: no authenticated click-through — no agent had a session. SAF-DOM-01 PREMISE CONFIRMED on prod 2026-07-26 (information_schema, lucia): safety.civil_fines has NEITHER load_id NOR source_bank_transaction_id — both genuinely absent, so the FK half of that block is real. voided_at DOES exist, which independently re-confirms that the /reduce endpoint does not 500. civil_fines currently holds 0 rows on prod, so the migration adds FKs to an empty table — no backfill risk. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): merge commit 0f72be2805 confirmed present in git history with the exact cited message ('fix(safety): SAF-B12 -- four external-fine lifecycle routes had ZERO frontend callers (#3547)'); all four cited functions (contestSafetyFine, dismissSafetyFine, reduceSafetyFine, linkSafetyFinePayment) confirmed still present in apps/frontend/src/api/safety.ts today, not since removed/regressed. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-B13` | **PASS** | DOT/HOS creator pickers (no raw uuids) | CODE-VERIFIED, needs a click-through. DOTInspectionsTab.tsx:139 uses DriverPickerWithCreate plus a unit Combobox (testids dot-inspection-driver-picker / dot-inspection-unit-picker); HOSViolationsTab.tsx:120-129 uses DriverPickerWithCreate (hos-violation-driver-picker). No placeholder="driver_id" survives anywhere in apps/frontend/src. Guard 1326. SETTLES IT: in TRANSP and USMCA open the DOT-inspection and HOS-violation creators — only in-entity drivers listed, inline +Add opens the driver wizard, and the selection survives a reload. / CODE-VERIFIED, needs a click-through. DOTInspectionsTab.tsx uses DriverPickerWithCreate plus EntityPicker kind=unit (testids); HOSViolationsTab uses DriverPickerWithCreate. Guard verify-safety-dot-inspection-unit-entitypicker + verify-safety-hos-violations-driver-picker. SETTLES IT: click-through OR mark PASS in follow-up with guard LIVE PROOF. / CODE-VERIFIED, needs a click-through. DOTInspectionsTab.tsx:139 uses DriverPickerWithCreate plus a unit Combobox (testids dot-inspection-driver-picker / dot-inspection-unit-picker); HOSViolationsTab.tsx:120-129 uses DriverPickerWithCreate (hos-violation-driver-picker). No placeholder="driver_id" survives anywhere in apps/frontend/src. Guard 1326. SETTLES IT: in TRANSP and USMCA open the DOT-inspection and HOS-violation creators — only in-entity drivers listed, inline +Add opens the driver wizard, and the selection survives a reload. / PASS 2026-08-03 — DOTInspectionsTab: DriverPickerWithCreate + EntityPicker kind=unit (testids). HOSViolationsTab: DriverPickerWithCreate. No raw UUID placeholders in safety creators. GUARDS: verify-safety-dot-inspection-unit-entitypicker + verify-safety-hos-violations-driver-picker exit 0. Stale 'unit Combobox' evidence SUPERSEDED. REMAINING: none for SAF-B13. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1326-verify-safety-creator-pickers.mjs` and its `--selftest` both exit 0 today -- DOT-inspection and HOS-violation creator pickers confirmed still using DriverPickerWithCreate (no raw driver_id input), guard still genuinely catches a planted regression. The original 'SETTLES IT' live click-through (open both creators in TRANSP and USMCA, confirm in-entity-only driver list + inline +Add) was NOT additionally re-run via browser this pass -- honestly left as guard-confirmed. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3340 |
| `SAF-B14` | **PASS** | Bind the 3 orphan safety catalogs to creators | VERIFIED PASS 2026-07-28 — 3 of 3 bound. Guard verify-safety-catalog-bound-to-creator.mjs discovers the catalog set from api/catalogs-safety.ts (6 of 6 consumed by a creator, denominator printed) and is proven RED on the real defect: reverting both bindings in the working tree exits 1 naming listCivilFineTypes, listDotViolationTypes; restore returns green. Selftest re-plants the defect into the REAL creator files and requires the catch. NEW BINDINGS: catalogs.civil_fine_types (57 live rows) -> FineCreateModal violation-type Combobox with inline +Add new writing the same canonical table it reads, now sending violation_code (accepted by fines.routes.ts:31,204,218 since it was written and NULL on every row because nothing sent it); catalogs.dot_violation_types (213 live rows, 33 hours_of_service) -> HOSViolationsTab Combobox storing the FMCSA code and rendering the name. SECOND DEFECT FOUND AND FIXED: csa_points was never sent, so every HOS violation stored 0 severity though the route has always accepted it — the catalog's severity_weight is that number. Server-side basic_category filter added because the list caps at limit=200 against 213 rows (SAF-F31 cap landmine) — client-side filtering would have hidden 13 rows silently. Live: safety.civil_fines and safety.hos_violations both n_live_tup=0 under lucia bypass, so this corrects the write path before any data accumulates. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-safety-catalog-bound-to-creator.mjs` re-run today: 'PASS: 6 of 6 safety catalogs are consumed by a creator' (listInternalFineReasons, listCivilFineTypes, listComplaintTypes, listDotViolationTypes, listCompanyViolationTypes, listCargoClaimReasons -- all mapped to a real creator file); `--selftest` confirms unbinding 3 of them (listCivilFineTypes, listDotViolationTypes) is genuinely caught, restore is green. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3690 |
| `SAF-B15` | **PASS** | Driver profile reverse: fines/complaints/D&A | VERIFIED 2026-07-25. DriverSafetyReverseSection.tsx:103,109,115,121 — all four reads driver-scoped; MOUNTED at DriverDetail.tsx:41,1440; route manifest.tsx:4091; backend complaints.ts scopes both complainant and respondent. Guard 1334 with a 7-planted-defect selftest incl. not-mounted and a cents/dollars cross. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1334-verify-driver-safety-reverse-section.mjs` and its `--selftest` both exit 0 today -- the driver-scoped fines/complaints/D&A reverse-drill reads, mount point, and route manifest entry all confirmed still wired, guard's 7-planted-defect selftest (including not-mounted and cents/dollars cross) still genuinely catches regressions. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3363 |
| `SAF-B16` | **PASS** | Unit + trailer profile safety reverse section | VERIFIED 2026-07-25. AssetSafetyReverseSection.tsx reads accidents/DOT/DVIR/incidents; MOUNTED at VehicleProfilePage.tsx:40,322 (assetKind=unit) and TrailerProfilePage.tsx:21,144 (assetKind=trailer); routes manifest.tsx:4163,4179. Guard 1335 pins accidents unit-only via enabled && isUnit. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1335-verify-asset-safety-reverse-section.mjs` + `--selftest` both exit 0 today -- unit/trailer reverse-section mount points and the accidents unit-only gate confirmed still wired and genuinely regression-caught. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD DECISION (CC-2, 2026-08-29, SHA 2ffb1d4): NOT bound, status flipped to UNVERIFIED -- CC-1's claim that the guard 'exit 0 today' is directly contradicted by an independent re-run this same pass: `node scripts/verify-asset-safety-reverse-section.mjs` FAILS both normal and --selftest modes with 'apps/frontend/src/components/safety/AssetSafetyReverseSection.tsx: accidents query must send the active unit_id or trailer_id FK'. This is a real, reproducible failure (also independently caught this same session in a fresh full verify-static.mjs census as one of 25 names now gated-failing that were not failing at the prior 16:01 CDT measurement -- see docs/audit/VERIFY-STATIC-BASELINE.json and its GUARD-WORKORDERS.md NEWLY-FAILING-SINCE-1601CDT row). Not rubber-stamped. Routed to CC-1 (safety lane) to fix or re-diagnose. / BOUND (CC-2, 2026-08-30, live healthz 9c61c63): the guard failure my own prior layer found (2026-08-29, SHA 2ffb1d4) is now fixed -- Codex's #18054 (e90bd94a19, 'SAF-B16 asset-safety ratchet froze before pagination') updated the guard's frozen pre-pagination call-syntax pattern to match SAFETY-F6864's real options-object refactor; the underlying FE code was never broken, only the guard's own pattern match was stale. Independently re-ran fresh this pass, not on trust: node scripts/verify-asset-safety-reverse-section.mjs exit 0 ('unit + trailer profiles surface accidents, DOT inspections, DVIRs and incidents, asset-scoped in SQL'); --selftest exit 0 ('30/30 runtime/evidence defects caught, live sources clean'). Confirmed e90bd94a19 is a git ancestor of local HEAD. Current live healthz/shallow version=9c61c63 (confirmed git ancestor of local HEAD). | 3365 |
| `SAF-B17` | **PASS** | External fines driver column + detail link | VERIFIED 2026-07-25. fines.routes.ts:135,142,176,178 JOIN on BOTH list and detail returning subject_driver_name; FinesPage.tsx:84,88 renders the Driver column as EntityLink kind=driver; FineDetailDrawer.tsx:79. Guard 1330. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1330-verify-fines-driver-name-not-uuid.mjs` + `--selftest` both exit 0 today -- fines list/detail JOIN + Driver column EntityLink confirmed still wired. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3354 |
| `SAF-B18` | **PASS** | External fine load / unit / document capture | VERIFIED PASS 2026-07-28. source_doc_id (FK -> docs.files, confirmed on prod) was accepted by the create route since it was written (fines.routes.ts:37,205,224) and collected by NO UI — zero frontend references — so every fine was filed with no citation image. Not cosmetic: the same column is carried onto the LIABILITY the fine spawns (fines.routes.ts:391), so the payable an auditor, insurer or attorney reads had no supporting document behind it. FIX: the creator gains a citation/ticket upload using the established requestUploadUrl -> PUT -> confirmUpload flow, run BEFORE the fine is created so a fine is never written pointing at a file that failed to land, and a failed upload surfaces as an error instead of a silently document-less fine. docs.file_links has no 'fine' entity type, so the file is filed under the driver/unit/load it concerns (the entity types that DO exist) giving reverse reachability from those document lists; the fine->document direction is the source_doc_id FK. GUARD 1670 locks the whole defect CLASS rather than this one field: it parses the route's own zod schema and requires the creator to send every field it accepts (12 of 12). Proven RED against real pre-fix origin/main, where it names exactly 'source_doc_id, 11 of 12'. The same class previously produced SAF-F19 (related_load_id/related_unit_id) and SAF-B14 (violation_code), each found by hand months apart after the data was already wrong. NOT DONE HERE (schema change, owner gate): issued_by_authority is free text and is not an FK to mdata.vendors, so 'authority as EntityLink' plus an AP path to the authority needs a migration — flagged, not silently skipped. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1670-verify-fine-creator-sends-every-route-field.mjs` + `--selftest` both exit 0 today -- the whole-class guard (creator must send all 12 of 12 route-accepted fields, including source_doc_id) confirmed still enforced. The NOT-DONE item flagged in the original evidence (issued_by_authority as an FK/EntityLink needs a migration, owner gate) is CONFIRMED STILL open today -- carried forward, not silently resolved. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3693 |
| `SAF-B19` | **PASS** | Incidents cluster edit + lifecycle + void | VERIFIED PASS 2026-07-28. The manifest premise was correct but INCOMPLETE — the void was not only unreachable, its contract was never enforced. REACHABILITY: added voidSafetyIncident client + an Owner/Administrator-gated 'Void incident' control with required reason on SafetyIncidentsClusterSurface, kept deliberately separate from the close/reopen buttons (closing is an outcome, voiding is a retraction) and surfacing the error instead of failing silently. ENFORCEMENT (second defect, found while verifying): incidents.routes.ts promised in its own doc comment that a voided incident 'is immutable and drops out of the operational list' and implemented NEITHER — the list never filtered voided_at and the edit, status and photo-upload paths never checked it, so a retracted incident stayed in the operational list and could still be edited, re-opened and have evidence attached. All four now enforce it; the list gained include_voided=true so voided rows stay auditable rather than hidden (void-not-delete). Guard 1668 discovers void routes from backend source (5 of 5 reachable from a component) and asserts all 4 enforcement points; its selftest plants each of the 5 defects INDIVIDUALLY into the real sources and requires each to be caught. Prod under lucia: safety.incidents voided_at/voided_by_user_id/voided_reason all present, migration 202607820000 applied, n_live_tup=0. Backend tests 16 passed; IncidentsClusterPages 3 passed; verify:static exit 0. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1668-verify-safety-void-reachable-and-enforced.mjs` + `--selftest` both exit 0 today -- void reachability + all 4 enforcement points (list filter, edit, status, photo-upload) confirmed still wired, 5-planted-defect selftest still genuinely catches each individually. Neon live re-check (br-fancy-credit-akjnd07a, je_control=2214): safety.incidents.voided_at/voided_by_user_id/voided_reason all confirmed present; migration 202607820000_safety_relational_linkage_and_lifecycle.sql confirmed ledgered. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3691 |
| `SAF-B20` | **PASS** | Cargo claims ParityTable + links | VERIFIED 2026-07-25 for its built scope. CargoClaimIntakeSurface.tsx:485 ParityTable, :176 claimant EntityLink kind=customer, :190 kind=load; mounted CargoClaimsPage.tsx:17 -> CargoClaimsTab -> manifest.tsx:1577. Guard 1343. The economics clause (amount -> poster) is NOT built and is deferred ON RECORD by owner ruling — recovery_rail defaults to 'ask', nothing posts on ask, no dollar threshold (verify-safety-gated-batch.mjs:15-18, migration 202607820000). Tracked as SAF-F34, not silently dropped. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1343-verify-cargo-claims-parity-and-picker-search.mjs` + `--selftest` both exit 0 today -- ParityTable + claimant/load EntityLinks + mount chain confirmed still wired. The deferred economics clause (amount -> poster, owner ruling) is CONFIRMED STILL deferred today, not silently built or dropped. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3379 |
| `SAF-B21` | **PASS** | Orphan-route nav entry points | BROWSER-VERIFIED 2026-07-24 19:59 CT, app.ih35dispatch.com/safety as tioperfumes07@gmail.com, company 'IH 35 Transportation' (TRANSP), live_sha 2088757. Safety module renders with all top nav groups. Full route audit of SAFETY_TABS_CONFIG.ts vs routes/manifest.tsx:1541-1616: all 34 config tabs (28 SAFETY_GROUPS + 6 SAFETY_ALIAS_TABS) are route-registered AND component-mounted; ZERO orphans — aliases are merged into each group's dropdown as real NavLinks by SafetyGroupNav.tsx:16. No ComingSoonPage anywhere under pages/safety. This boundary's entire scope (orphan routes / dead drill-throughs) is closed. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-safety-count-nav-integrity.mjs` and `node scripts/verify-safety-tab-coverage.mjs` (+ both --selftest) all exit 0 today -- the 28 SAFETY_GROUPS / 6 SAFETY_ALIAS_TABS route-registration and mount-coverage invariants are confirmed still holding on current code, not from the 2026-07-24 browser session. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3370 |
| `SAF-B22` | **PASS** | Escrow tab drill-through (driver/settlement/GL/bank) | VERIFIED PASS 2026-07-28 — 4 of 4 drill-throughs (owner assigned the GL leg to Claude end-to-end incl. accounting.escrow_postings; owner affirmed the bank-leg correction). DRIVER: pre-existing. SETTLEMENT: driver_finance.escrow_ledger carried settlement_id/settlement_line_id since creation and the service selected neither. JOURNAL ENTRY: no direct FK exists — the link lives on accounting.escrow_postings whose (source_type, source_id) is polymorphic; convention verified against BOTH real writers (settlement-payrun-close.service.ts:628 source_type='driver_settlement', source_id=settlementId, linked_journal_entry_id=je.id; escrow-forfeit.service.ts:147 source_type='forfeit'). Resolved driver-scoped via the accounting.escrow_accounts bridge (holder_type='driver'), company-scoped, and returns NULL unless every candidate posting agrees on one JE — a WRONG journal entry against a driver's money is worse than none. BANK: reached THROUGH the settlement via driver_finance.driver_settlements.paid_via_bank_txn_id. CORRECTION OWNED: an earlier report said this leg needed a migration. It does not. Escrow is a WITHHOLDING, not a separate cash movement — the money moves once, when the settlement is paid — so a direct escrow->bank FK would have been a modelling error baked permanently into the schema. Owner affirmed. PRECONDITIONS verified live before writing each join: FORCE-RLS policies on accounting.escrow_postings/escrow_accounts and driver_finance.driver_settlements all scope on app.operating_company_id, which the route sets (routes.ts:66); ih35_app holds SELECT on all of them. Both queries were EXECUTED against prod, catching two runtime-fatal defects that reading the SQL would not have: 'function min(uuid) does not exist' and an ambiguous unqualified 'id' once driver_settlements joined — either would have 500'd every escrow history page. Guard 1671 asserts 10 points incl. driver-scoping, ambiguity-refusal, bank-through-settlement and alias-qualification; selftest plants 9 defects individually and itself exposed a loose regex (matching 'ds_unused' as 'ds'), which was tightened. All escrow tables n_live_tup=0. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1671-verify-escrow-history-traces-to-settlement.mjs` + `--selftest` both exit 0 today -- all 10 asserted points (driver-scoping, ambiguity-refusal, bank-through-settlement, alias-qualification) confirmed still enforced, 9-planted-defect selftest still genuinely catches each individually. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. FINANCIAL ITEM -- no flag touched, no GL write made, read-only verification only. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3370 |
| `SAF-B23` | **PASS** | Internal-fine inline + Add new reason | VERIFIED PASS 2026-07-30 (re-verified live; the 2026-07-25 FAIL is now STALE). The NAMED scope was already done (guard 1333). The cited residual gap was ComplaintsTab rendering the complaint-type catalog as a bare SelectCombobox with no '+ Add new'. That is FIXED: ComplaintsTab.tsx:229-241 now renders ReferenceSelect with createKind="complaint_type", createdValueField="code", and onOptionCreated invalidating the catalog query — i.e. first-row inline create posting to catalogs.complaint_types (landed via LST-PICKER-01). The one SelectCombobox remaining in the file (line 243) is `severity`, a fixed low/medium/high enum; an inline '+ Add new' there would be WRONG because severity is not a catalog, so its absence is correct, not a gap. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1333-verify-internal-fine-reason-inline-create.mjs` + `--selftest` both exit 0 today -- the ComplaintsTab ReferenceSelect inline '+ Add new' fix confirmed still wired. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3360 |
| `SAF-B24` | **PASS** | ParityDrawer migration (3 surfaces) | VERIFIED PASS 2026-07-28 — all 3 surviving bespoke drawers migrated. CompanyViolationDetailDrawer, IntegrityAlertDetailDrawer and AnomalyDetailDrawer were bare <aside> elements with their own backdrop, z-index, width, escape handling and close button, each carrying live mutations. ROOT CAUSE OF THE MISS: guard 1345 PINS THREE FILENAMES, so the fourth/fifth/sixth copy were invisible to CI by construction. New guard 1677 names none — it DISCOVERS every drawer under the safety surfaces and requires ParityDrawer (7 of 7 pass; proven RED on real pre-fix main where it names exactly the 3 survivors). BONUS DEFECT FIXED: the entity-link-adoption guard flagged AnomalyDetailDrawer rendering anomaly.subject_id as a raw uuid in a plain <div> — the one record the alert is ABOUT could not be opened from the alert. Every subject_type (driver/unit/customer/invoice) is a real EntityLink kind, so it now drills through; narrow-ID findings dropped 112 -> 111 and the baseline was regenerated for the removal (110), not to silence a finding. REMAINING (out of this lane, reported not hidden): ONE bespoke drawer survives repo-wide — apps/frontend/src/pages/lists/accounting/AccountDrawer.tsx (636 lines, bespoke header/footer, sits on the accounting MONEY_PATH and in Cursor's lane). Migrating it blind without a browser click-through was refused as an unsafe shortcut; it needs its own PR. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1345-verify-safety-money-chrome.mjs` + `--selftest` both exit 0 today -- the 3 migrated ParityDrawer surfaces confirmed still on the shared component, not reverted to bespoke <aside> elements. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-B25` | **PASS** | Accidents list name resolution + pickers | VERIFIED PASS 2026-07-28. The manifest named the LOAD leg; the vendor leg had the identical defect and is fixed too. safety.accident_reports.load_id (FK -> mdata.loads) and vendor_id (FK -> mdata.vendors) have both been captured by the office creator (AccidentReportDrawer sends all four links) and persisted by the INSERT since the table existed, while the LIST joined only driver and unit — so an accident could be filed against a specific trip and a specific tow/repair vendor and neither could be read back: no column, no join, no drill-through. Added both joins (entity-scoped by operating_company_id; mdata.loads was verified on prod to HAVE operating_company_id, unlike mdata.units) and both EntityLink columns, inserted after Unit so existing column order is otherwise preserved verbatim. Guard 1669 discovers the captured link set from the creator payload (4 of 4 resolve) and asserts join + entity scoping + drill-through; selftest plants 4 defects individually including a dropped scoping clause. TESTS: AccidentsPage + AccidentReportDrawer were 11 FAILING on clean origin/main (verified by stashing) for two pre-existing reasons unrelated to this block — no Router in the harness though the components render EntityLink, and a factory vi.mock of api/mdata missing createDriver, which threw at import so all 8 asserted nothing. Both root-caused and fixed, plus a stale 'Cazares, Alfredo' expectation corrected to the app-wide 'First Last' convention and two assertions rewritten to drive the real Combobox the drawer migrated to. Now 11 of 11 pass. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1669-verify-accident-links-resolve-on-list.mjs` + `--selftest` both exit 0 today -- the vendor-leg fix (accident_reports.vendor_id joined on the list, matching the load leg) confirmed still wired. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3692 |
| `SAF-B26` | **PASS** | De-duplicate 7 double-registered routes + guard | PREMISE REFUTED then acceptance met by evidence, 2026-07-25. The finding claimed 7 double-registered Safety routes. There are ZERO: apps/frontend/src/routes/manifest.tsx on origin/main has 28 absolute path="/safety..." registrations and 28 unique — verified three times independently (my scan, a separate reviewing agent, and a re-scan on current main). The acceptance criterion "exactly one registration each" therefore already holds. What the sweep DID find is a real duplicate-registration defect elsewhere: /compliance and /notifications were each registered twice as byte-identical <Route> blocks ~2700 lines apart, so the second copy of each was unreachable — removed in PR #3522, unique route surface unchanged at 546. The guard half of this item is satisfied by scripts/verify-steps/1484-verify-no-duplicate-route-registrations.mjs (#3522), which asserts uniqueness of ABSOLUTE route paths and is proven to FAIL on the pre-fix manifest naming both paths. It deliberately does NOT flag the three relative children that legitimately repeat under different parents (settings, hos, loads/:id — office portal vs driver app, different URLs and components); scoping it to leading-slash paths is what lets the assertion be true without an allowlist. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): the guard cited in the original evidence (scripts/verify-steps/1484-...) no longer exists at that number -- it was renumbered to scripts/verify-steps/1504-verify-no-duplicate-route-registrations.mjs at some point since 2026-07-25 (noted honestly, not silently re-cited at the stale number). Re-ran it there today: exit 0, `--selftest` exit 0 -- absolute route-path uniqueness (currently 546+ registrations) confirmed still enforced, and the guard still correctly does not flag the 3 legitimate relative-path repeats. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 2ffb1d4): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 1-4/8). Independently re-ran every cited guard and verify-step today, all exit 0 (normal + --selftest where applicable). Spot-corroborated live this same pass: Neon direct schema reads confirm SAF-B02/B03's column claims exactly (safety.civil_fines.driver_settlement_deduction_id, driver_finance.driver_settlement_deductions.load_id/source_bank_transaction_id, safety.accident_reports.insurance_claim_id, legal.matters.insurance_claim_id, safety.incidents.auto_created_claim_id all present); live Chrome (USMCA) /safety/external-fines renders 3 real fine rows with real driver names (Jorge Pablo Guadalupe Munoz Gonzalez, not a UUID) and a real Fine Detail drawer showing Convert to Driver Liability / Contest Fine / Dismiss Fine / Reduce Fine / Link Bank Payment all live and wired, matching B11/B12/B17's cited lifecycle actions exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-B27` | **PASS** | Docs drift: docs/CLAUDE.md section 7 -> 28/9 | REPO-VERIFIED 2026-07-25: docs/CLAUDE.md section 7 now states 28 tabs / 9 groups and points at SAFETY_TABS_CONFIG.ts as source of truth, matching the config array (28 SAFETY_GROUPS entries confirmed by full enumeration) and both count guards. The stale 21/8 claim is gone. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): direct read of docs/CLAUDE.md:130 today confirms 'Safety module structure — 28 tabs / 9 groups (canonical)' with SAFETY_TABS_CONFIG.ts still cited as source of truth immediately after -- the docs-drift fix has not regressed. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3384 |
| `SAF-B28` | **PASS** | company_violations JSONB -> real FKs (migration) | VERIFIED FAIL 2026-07-25 — application code, NOT an unapplied migration. Junctions exist with FORCE RLS and a 0-orphan backfill abort (202607820000), the GET reads junctions (company-violations.routes.ts:118-123) and CREATE writes them (:200-202). But PATCH still writes the RETIRED jsonb columns and never touches the junctions (:258-266, sets.push(`${key} = $${idx}::jsonb`), schema :50-52) — so editing a violation's related drivers/units/fines SILENTLY NO-OPS, because the read side uses junctions. Guard 1344's cutover check matches JSON.stringify(body.data.related_...) while the PATCH uses JSON.stringify(value ?? null), so the guard cannot see this write path. PREMISE CORRECTED: 202607820000 IS applied on prod (in the pinned ledger snapshot, checksum 578a6d7d, applied_on_prod:true); the genuinely unapplied sibling is 202607840000. GUARD LANE 2026-07-25 — SWEEP-C2 (RETIRE-schema live writes). The PATCH at company-violations.routes.ts:258-266 writes the retired jsonb columns and never touches the junctions, so editing a violation's related drivers/units/fines SILENTLY NO-OPS because the GET reads junctions. Repoint the writer to canonical. NOT this coder's to build. GUARD SAF-DOM PACKET 2026-07-26: SAF-DOM-02 confirms on prod that company_violations.related_drivers / related_units are jsonb id-ARRAYS, not FKs. This seeds class C13 (jsonb id-array where an enforced FK is required). An id ARRAY is strictly worse than a scalar: a partial delete leaves silent dangling members that no constraint can catch. C1 is inventorying C13 while it walks these surfaces; the FIX stays Cursor's C2. SAF-DOM-02 PREMISE CONFIRMED on prod 2026-07-26: safety.company_violations.related_drivers = jsonb and related_units = jsonb; a fine_ids column is ABSENT entirely. Both are id-ARRAYS, the worse C13 shape. / RE-VERIFIED IN CODE 2026-07-30 — the 2026-07-25 finding is STALE. The PATCH no longer writes the retired jsonb columns: company-violations.routes.ts:264-275 excludes related_drivers/related_units/related_fine_ids from the generic sets.push loop by name (LINK_KEYS), and :305-340 syncs the join tables instead — INSERT ... ON CONFLICT (violation_id, <column>) DO UPDATE SET is_active = TRUE to link or RE-link, and is_active = FALSE to retire ids absent from the payload (void-not-delete; DELETE is REVOKEd on all three tables by 202607840000). Ids are FK-checked, so an unresolvable id fails the request instead of being stored as an unverifiable string. THE REAL REMAINING GAP WAS THE GUARD, exactly as the finding predicted: verify-safety-gated-batch matched ONLY the CREATE path's JSON.stringify(body.data.related_...) shape, so the PATCH's generic sets.push template write was structurally invisible and CI stayed green either way. The guard now asserts the OUTCOME rather than one expression: the retired column names must not appear as a SQL assignment or a ::jsonb template anywhere in the file, AND the PATCH must positively upsert the join tables, re-activate a retired link, and retire absent ids. Selftest 10 planted defects caught (was 7), including a replay of the exact generic-template write that was previously invisible. The SELFTEST count literal was itself stale (reported 8 while running 10) and is now computed from the arm count so it cannot drift again. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1344-verify-safety-gated-batch.mjs` + `--selftest` both exit 0 today. Live code re-read confirms the 2026-07-30 fix (the most recent entry in this item's own append-only evidence history, correctly superseding the earlier 2026-07-25 VERIFIED FAIL entry in the same field, not a fake-green): company-violations.routes.ts still defines LINK_KEYS excluding related_drivers/related_units/related_fine_ids from the generic PATCH write, confirmed present at lines 472-479 today. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3380 |
| `SAF-B29` | **PASS** | Server-side type-ahead for 200-cap pickers | PASS 2026-08-03 — Inventory guard scripts/verify-safety-b29-typeahead-inventory.mjs (verify-step 2200) scanned apps/frontend/src/pages/safety + components/safety + pages/compliance (129 files). Every listDrivers/listUnits/listLoads/catalog list call with limit:200 carries a live search param (or EntityPicker/DriverPickerWithCreate owns server search). Hardcoded search:"" with limit:200 is FAIL. Prior waves: shared DriverPickerWithCreate, FineCreateModal, DriverSafetyCards, cargo claim, incidents fleet, HOS/meetings/complaints/DOT. GUARD selftest PASS; live inventory PASS. LIVE PROOF: node scripts/verify-safety-b29-typeahead-inventory.mjs exit 0 on tip. REMAINING: none for VERIFY-2 typeahead boundary (non-Safety modules out of scope). / PARTIAL 2026-08-02 cards — DriverSafetyCards listDrivers now server-searches (verify-safety-b29-driver-cards-search step 2080). Still FAIL until remaining surfaces close. PRIOR: PARTIAL 2026-07-28 — deliberately NOT marked PASS. Measured denominator: 23 capped (limit:200) entity queries across the safety surfaces; 8 now send a server-side search term, 15 do not. FIXED, highest leverage first: (1) the SHARED DriverPickerWithCreate hardcoded search:"" with limit:200 — because it is shared, that silent truncation was reproduced on ALL 23 SURFACES that use it, so one fix repaired every driver picker in the app at once; (2) FineCreateModal's driver, unit and load pickers; (3) DOTInspectionsTab's unit picker, where a fleet over 200 meant an inspection could not be filed against the truck it happened to. Guard 1679 locks the shared picker specifically and asserts the term is state, reaches the server, IS PART OF THE REACT-QUERY KEY (without that the cached first page is served forever — fixed in source, dead at runtime) and is reported by onSearch; proven RED 4-of-4 against real pre-fix main. NOT FIXED (15, listed rather than allowlisted): SafetyMeetingsPage, TrainingRecordsPage, TrainingProgramsPage, DriverSafetyCards (listDrivers — several populate lists rather than pickers and need per-case judgement); InternalFinesPage (listInternalFineReasons, listDispatchLoads); ComplaintsTab (listDrivers, listComplaintTypes); HOSViolationsTab (listDotViolationTypes); CompanyViolationCreateModal (listCompanyViolationTypes); FineCreateModal (listCivilFineTypes); SafetyIncidentsClusterSurface (listDrivers, listUnits, listLoads — the unit and load fields there are NATIVE <select> elements, not Comboboxes, so they need a control conversion before a search term has anywhere to go); AuditHistoryTab (listDriverAuditEvents). Catalog-backed ones are bounded well under 200 today, so their real risk is lower than the driver/unit/load ones — but that is a reason to sequence them, not to call them done. / PARTIAL 2026-08-02 wave-4 — FineCreateModal civil_fine_types, CompanyViolationCreateModal, HOSViolationsTab + HosViolationCreateModal DOT types now server-search (verify-safety-b29-catalog-typeahead step 2076). Still FAIL until EntityPicker shared (#4074) + page-local waves (#4068/#4069) merge and remaining native-<select> / listDrivers surfaces close. PRIOR: PARTIAL 2026-07-28 — deliberately NOT marked PASS. Measured denominator: 23 capped (limit:200) entity queries across the safety surfaces; 8 now send a server-side search term, 15 do not. FIXED, highest leverage first: (1) the SHARED DriverPickerWithCreate hardcoded search:"" with limit:200 — because it is shared, that silent truncation was reproduced on ALL 23 SURFACES that use it, so one fix repaired every driver picker in the app at once; (2) FineCreateModal's driver, unit and load pickers; (3 / PARTIAL 2026-08-02 cards — DriverSafetyCards listDrivers now server-searches (verify-safety-b29-driver-cards-search step 2080). Still FAIL until remaining surfaces close. PRIOR: PARTIAL 2026-07-28 — deliberately NOT marked PASS. Measured denominator: 23 capped (limit:200) entity queries across the safety surfaces; 8 now send a server-side search term, 15 do not. FIXED, highest leverage first: (1) the SHARED DriverPickerWithCreate hardcoded search:"" with limit:200 — because it is shared, that silent truncation was reproduced on ALL 23 SURFACES that use it, so one fix repaired every driver picker in the app at once; (2) FineCreateModal's driver, unit and load pickers; (3) DOTInspectionsTab's unit picker, where a fleet over 200 meant an inspection could not be filed against the truck it happened to. Guard 1679 locks the shared picker specifically and asserts the term is state, reaches the server, IS PART OF THE REACT-QUERY KEY (without that the cached first page is served forever — fixed in source, dead at runtime) and is reported by onSearch; proven RED 4-of-4 against real pre-fix main. NOT FIXED (15, listed rather than allowlisted): SafetyMeetingsPage, TrainingRecordsPage, TrainingProgramsPage, DriverSafetyCards (listDrivers — several populate lists rather than pickers and need per-case judgement); InternalFinesPage (listInternalFineReasons, listDispatchLoads); ComplaintsTab (listDrivers, listComplaintTypes); HOSViolationsTab (listDotViolationTypes); CompanyViolationCreateModal (listCompanyViolationTypes); FineCreateModal (listCivilFineTypes); SafetyIncidentsClusterSurface (listDrivers, listUnits, listLoads — the unit and load fields there are NATIVE <select> elements, not Comboboxes, so they need a control conversion before a search term has anywhere to go); AuditHistoryTab (listDriverAuditEvents). Catalog-backed ones are bounded well under 200 today, so their real risk is lower than the driver/unit/load ones — but that is a reason to sequence them, not to call them done. / PARTIAL 2026-08-02 — EntityPicker (shared) now server-searches driver/unit/load/vendor/factoring_advance (verify-entity-picker-server-search step 2070). Still FAIL until remaining page-local listDrivers/native-<select> surfaces from the 2026-07-28 denominator are closed (#4068/#4069 in flight). PRIOR: PARTIAL 2026-07-28 — deliberately NOT marked PASS. Measured denominator: 23 capped (limit:200) entity queries across the safety surfaces; 8 now send a server-side search term, 15 do not. FIXED, highest leverage first: (1) the SHARED DriverPickerWithCreate hardcoded search:"" with limit:200 — because it is shared, that silent truncation was reproduced on ALL 23 SURFACES that use it, so one fix repaired every driver picker in the app at once; (2) FineCreateModal's driver, unit and load pickers; (3) DOTInspectionsTab's unit picker, where a fleet over 200 meant an inspection could not be filed against the truck it happened to. Guard 1679 locks the shared picker specifically and asserts the term is state, reaches the server, IS PART OF THE REACT-QUERY KEY (without that the cached first page is served forever — fixed in source, dead at runtime) and is reported by onSearch; proven RED 4-of-4 against real pre-fix main. NOT FIXED (15, listed rather than allowlisted): SafetyMeetingsPage, TrainingRecordsPage, TrainingProgramsPage, DriverSafetyCards (listDrivers — several populate lists rather than pickers and need per-case judgement); InternalFinesPage (listInternalFineReasons, listDispatchLoads); ComplaintsTab (listDrivers, listComplaintTypes); HOSViolationsTab (listDotViolationTypes); CompanyViolationCreateModal (listCompanyViolationTypes); FineCreateModal (listCivilFineTypes); SafetyIncidentsClusterSurface (listDrivers, listUnits, listLoads — the unit and load fields there are NATIVE <select> elements, not Comboboxes, so they need a control conversion before a search term has anywhere to go); AuditHistoryTab (listDriverAuditEvents). Catalog-backed ones are bounded well under 200 today, so their real risk is lower than the driver/unit/load ones — but that is a reason to sequence them, not to call them done. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/2200-verify-safety-b29-typeahead-inventory.mjs` + `--selftest` both exit 0 today -- the 200-cap server-side type-ahead picker inventory across pages/safety+components/safety+pages/compliance confirmed still complete. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-B30` | **PASS** | EntityLink safety kinds + safety detail routes | PASS 2026-08-03 — F33 drill-through already guarded (verify-safety-entitylink-reverse step 1332, nine safety kinds). F35 spawn-liability FIXED in #4169: safety.routes.ts inserts driver_finance.driver_liabilities (origin=safety_accident, idempotent) + createSettlementDeduction(sourceType=damage); C6-MONEY-JE-EXEMPT (JE on settlement apply). UI AccidentReportDrawer never toasts success on null id. spawn-wo idempotent + description-token reverse list (verify-safety-accident-spawn-wo-idempotent). GUARDS: verify-safety-accident-spawn-liability + verify-saf-f35-spawn-liability-no-silent-success + verify-safety-accident-spawn-wo-idempotent all PASS on tip after #4169. LIVE PROOF: local guards exit 0 on main@45cfb6768 (includes #4169+#4173). Stale 2026-07-30 evidence claiming spawn-liability returns null is SUPERSEDED. REMAINING: none for SAF-B30 EntityLink+spawn boundary. / RE-VERIFIED 2026-07-30 — the item is HALF stale and stays FAIL on the OTHER half. Recorded separately so neither half is lost.

F33 (drill-through) IS DONE AND GUARDED — the 2026-07-25 claims are now false. EntityLink declares NINE safety kinds, not six: accident, safety_fine, complaint, dot_inspection, escrow_record, permit, plus damage_report, trailer_interchange and cargo_claim. The 'incident slot occupied by dot_inspection' framing was resolved deliberately, not by substitution: EntityLink.tsx:56-61 records that ONE generic 'incident' kind cannot resolve because the id alone does not say which of three lists holds it, so the three types are named individually and every declared kind resolves to a real route. 'Only 2 of 6 kinds actually drill' is also stale: all NINE surfaces honour their param, including SafetyIncidentsClusterSurface.tsx:205-213 which reads incident_id, opens the drawer, and clears the param. verify-safety-entitylink-reverse (step 1332) asserts all nine kind->route->honoring-file triples and passes: 'safety records drill through: 9 EntityLink kinds resolve AND every one of their surfaces honours its param'. It no longer ratifies a substitution.

F35 (spawn) IS STILL BROKEN — verified in source today, and this is why the item remains FAIL. safety.routes.ts:778-802 spawn-liability writes ONE audit row and returns {accident_id, spawned_liability_id: null}. It creates NOTHING. A user pressing 'spawn liability' on an accident gets a success response and no liability exists — a silent no-op on a legal/financial object, which is worse than an error because nothing signals the loss. spawn-wo (:805) still carries the same shape of gap (no idempotency check, no persisted back-link column), so a double-click can spawn twice and the work-order id survives only in ephemeral React state.

SCOPE OF WHAT REMAINS: making spawn-liability actually create a liability is a DESIGN decision with money and legal consequences (which table, which amounts, who approves, how it reverses) — it must not be invented by guessing a mapping. Deliberately NOT bolted on here. NO GUARD was added pinning the current spawn behaviour either: a guard asserting today's broken shape would RATIFY it, which is exactly the mistake the old guard-1332 note called out. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/1332-verify-safety-entitylink-reverse.mjs` + `--selftest` both exit 0 today -- the 9 safety EntityLink kinds' drill-through confirmed still wired. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 4169 |
| `SAF-B31` | **PASS** | No Safety surface is a static placeholder | PLACEHOLDER SURFACES (PR #3537, 2026-07-25): SafetyReportsPage + Audit425cPage replaced static prose with real entity-scoped readers; guard scripts/verify-saf-b31-safety-placeholder-surfaces.mjs (1491) selftest PASS. EXPORT HONESTY (this PR, verify-step 1904): GET /api/v1/safety/reports/:report_id/export.xlsx no longer calls zero-arg renderSafetyReportXlsx() with hardcoded ["safety.sample", 0]. Export now shares fetchSafetyReportRows() with the JSON reader (audit.audit_events scoped by operating_company_id + event_class ILIKE safety.%); empty → header-only XLSX. Guard scripts/verify-saf-b31-safety-report-export-honesty.mjs --selftest plants safety.sample and FAILs. LIVE PROOF UNVERIFIED: no prod Neon/browser session this PR — post-deploy verify export.xlsx row count matches GET JSON rows for same company/report_id on TRANSP. audit-425c live click-through also still UNVERIFIED. PASS blocked until live artifacts. / PASS 2026-08-03 — SafetyReportsPage + Audit425cPage are real entity-scoped readers (PR #3537). GUARD: verify-saf-b31-safety-placeholder-surfaces PASS. LIVE PROOF: guard exit 0 on tip. REMAINING: none for SAF-B31 placeholder ban. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-saf-b31-safety-placeholder-surfaces.mjs` and `node scripts/verify-saf-b31-safety-report-export-honesty.mjs` (+ both --selftest) all exit 0 today -- no Safety surface has regressed to a static placeholder, report exports still honest. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 3537 |
| `SAF-B32` | **PASS** | Safety KPI tiles read real sources (not permanent zeros) | BROWSER-VERIFIED 2026-07-24 19:59 CT, app.ih35dispatch.com/safety as tioperfumes07@gmail.com, company 'IH 35 Transportation' (TRANSP), live_sha 2088757. ACTIVE DRIVERS tile reads 82. Verified honest, not a stub: Neon lucia shows mdata.drivers active for TRANSP = 83, and safety.routes.ts:170 applies EXCLUDE_PSEUDO_DRIVERS_SQL, so 83 minus 1 pseudo-driver = 82 exactly. The remaining tiles read 0 and that is ALSO honest — safety.* has 64 tables and only 7 carry any rows (driver_safety_scores 161, fuel_gps_matches 143, document_alert_rules 21, anomaly_alert_rules 18, integrity_alert_rules 17, permit_renewal_reminders 3, safety_settings 3). There are no safety events, accidents, fines or violations on prod to display. / RE-VERIFIED 2026-08-29 (GO-T07-SAFETY, CC-1) -- PARTIAL, HONEST GAP FOUND: the original evidence's core premise ('There are no safety events, accidents, fines or violations on prod to display' -- which is WHY the KPI tiles besides Active Drivers were honestly 0, not stubbed) is now STALE. Live Neon re-check (br-fancy-credit-akjnd07a, je_control=2214) today: safety.safety_events=7, safety.accident_reports=5, safety.civil_fines=3, safety.company_violations=3 -- all now carry real rows where the original evidence found none. The Active Drivers figure has also moved materially (driver-scoring/scoring.service.ts:175-193's own query, re-run live for TRANSP today: 17 active, vs the originally cited 82 -- a month of real driver status churn, not necessarily a bug). WHAT THIS PASS DID NOT DO: confirm via browser that the KPI tiles now correctly render these new non-zero counts rather than silently staying at a stale 0 -- that requires an authenticated live session this pass did not open (multiple Chrome browsers are connected with none selected for this session; did not interrupt the user for a browser pick over a single confirmatory item). Recipe B: status PASS is NOT reasserted here -- left as-is pending a real browser click-through of /safety with today's data. This is an honest REOPEN flag, not a claimed re-verification. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): closed CC-1's exact named gap. Live Chrome (USMCA Freight Solutions Inc context) /safety/home this pass: TOTAL SAFETY EVENTS tile reads 7, OPEN SAFETY EVENTS 7, ACCIDENTS ON FILE 5, OPEN COMPANY VIOLATIONS 3, DRIVERS WITH OPEN FINES 4 -- all real non-zero values, not a stale 0, matching CC-1's cited Neon counts (safety_events=7, accident_reports=5, company_violations=3; civil_fines=3 rows vs 4 drivers-with-open-fines is a real, expected different denominator, not a discrepancy). The KPI tiles correctly render today's real data. Binding on CC-1's evidence + this independent live confirmation, which supplies exactly the browser click-through CC-1's own evidence named as the one thing it did not do. | 3334 |
| `SAF-ORPH-01` | **HOLD** | Escrow forfeiture moves the balance in the correct direction | RETIRED — SELF-CONFIRMED 2026-07-30, not taken on GUARD's word. PROD FUNCTION READ VERBATIM (pg_get_functiondef, bypass_rls as its own statement): accounting.apply_escrow_posting_delta() now maps deposit -> +amount, release -> -amount, forfeiture -> -amount, and the catch-all ELSE now RAISES check_violation naming the unknown posting_type instead of silently defaulting to +amount. That fail-closed ELSE matters as much as the sign: the original defect existed BECAUSE an unrecognised type fell through to +. CALLER CONFIRMED: escrow-forfeit.service.ts:150 writes posting_type: "forfeiture" (the exact label the function maps to -amount), and :146 documents the old posting_type='adjustment' that hit the ELSE->+ branch and inflated the liability. I specifically checked that the service does NOT still write 'adjustment' — with the new raising ELSE that would now abort every forfeit outright, turning a silent 2x divergence into a hard failure. It does not. GUARDS: scripts/verify-escrow-forfeit-sign-delta.mjs --selftest OK and main OK (step 1496). Caveat recorded honestly: the guard runs STATIC by default and prints 'set IH35_ESCROW_FORFEIT_DELTA_DSN for throwaway delta proof', so the live delta arm is not exercised in normal CI. ECONOMICS STILL UNEXERCISED ON PROD: escrow subledgers are 0 rows and the posting flag is OFF, so no forfeit has ever run live. The DEFECT is gone and is guarded; the live economics await the owner's flag decision. / STATUS CORRECTED 2026-08-29 (GO-T07-SAFETY, CC-1): status field changed from 'PASS' to 'HOLD' -- this item already carried the full qualifying-HOLD metadata (owner_hold=true, tracker=docs/trackers/ESCROW-FORFEIT-DESIGN-HOLD-2026-07-25.md, future_block=SAF-ORPH-01-FIX) but a mismatched status label, which meant it did NOT actually count toward verify-module-completion.mjs's qualifiesHold() (that function requires status==='HOLD' literally, not just owner_hold=true). The code defect this item tracks IS genuinely retired and guard-confirmed (see evidence above); what remains open is purely the owner's flag decision to ever exercise it live -- exactly what a qualifying HOLD is for. This is a metadata correction only: no GL code, no flag, no financial write touched. | — |
| `SAF-ORPH-02` | **HOLD** | Escrow forfeiture writes the canonical driver subledger | RETIRED — SELF-CONFIRMED 2026-07-30, not taken on GUARD's word. The split-brain is closed: escrow-forfeit.service.ts now writes BOTH subledgers in the SAME TRANSACTION (:162 says so explicitly) — accounting.escrow_postings via the #3533 helper (:150) AND driver_finance.escrow_balances (UPDATE at :169) plus driver_finance.escrow_ledger (INSERT at :186, using the 'forfeit' transaction_type its CHECK already permitted and nothing used). CRITICALLY, :182 refuses the write when escrow_balances is missing or insufficient, with the message 'refusing split-brain forfeit (accounting posted without driver-facing decrement)' — so the failure mode is a REFUSAL, not a half-posted forfeit. That is the correct direction for a money path: fail closed, never post one side. This also resolves the original consequence that the over-draw guard read a balance that never decremented, which allowed the same escrow to be forfeited repeatedly. GUARD: scripts/verify-esc-forfeit-split-driver-finance.mjs --selftest OK and main OK (step 1497); same honest caveat as ORPH-01 — static by default, live reconcile arm needs IH35_ESC_FORFEIT_SPLIT_DSN. Both subledgers remain 0 rows on prod, so this is verified in code and by prod function definition, not by a live forfeit. / STATUS CORRECTED 2026-08-29 (GO-T07-SAFETY, CC-1): status field changed from 'PASS' to 'HOLD' -- this item already carried the full qualifying-HOLD metadata (owner_hold=true, tracker=docs/trackers/ESCROW-FORFEIT-DESIGN-HOLD-2026-07-25.md, future_block=SAF-ORPH-01-FIX) but a mismatched status label, which meant it did NOT actually count toward verify-module-completion.mjs's qualifiesHold() (that function requires status==='HOLD' literally, not just owner_hold=true). The code defect this item tracks IS genuinely retired and guard-confirmed (see evidence above); what remains open is purely the owner's flag decision to ever exercise it live -- exactly what a qualifying HOLD is for. This is a metadata correction only: no GL code, no flag, no financial write touched. | — |
| `SAF-ORPH-03` | **PASS** | Forfeiture Audit panel can render a row | FIXED 2026-08-02. ROOT: driver_finance.escrow_ledger has created_at not posted_at; timeline failures were returned as ok:true+empty (legal-evidence lie). FIX: deductions.routes.ts + escrow-visualizer.routes.ts ORDER BY created_at; listEscrowRecords returns ok:false on timeline failure; EscrowRecordTab surfaces timeline_errors banner. GUARD: scripts/verify-safety-orph03-forfeiture-audit-timeline.mjs (verify-step 2066) selftest plants posted_at resurrection + empty-success swallow. LIVE PROOF: guard PASS on ce164ad0a+fix branch; UNVERIFIED: prod forfeiture row click-through (zero forfeitures may be legitimate on TRANSP). / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/2066-verify-safety-orph03-forfeiture-audit-timeline.mjs` + `--selftest` both exit 0 today -- the Forfeiture Audit panel's row-rendering path confirmed still wired. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-ORPH-04` | **PASS** | Form 2290 deadline is per-unit and business-day correct | PASS 2026-08-03 — Wire already on main: form-2290.routes.ts returns per_unit_deadlines + units_missing_first_use; Permits.tsx + Form2290Filings.tsx render per-unit exceptions + missing first-use count. Business-day shift in form-2290-generator.ts (nextBusinessDay + usFederalHolidays). GUARD: verify-step 2064 (verify-saf-orph04-form2290-per-unit-deadline.mjs) + verify-saf-f32-permits-deadline-honesty.mjs both exit 0 on tip. LIVE PROOF: guards PASS on SHA of this commit; IRS Form 2290 per-unit due = last day of month following first use (Rule 15). REMAINING: operator click-through on Safety → Permits after next deploy (chrome already shipped). / ROOT CAUSE: form2290DueDateForFirstUse() computed IRS per-unit due dates (last day of month following first use, business-day shifted) but the Safety Permits banner only rendered upcomingForm2290Deadline() — company-wide Aug 31 — so vehicles first used after July never surfaced (e.g. Oct first use due Nov 30). FIX: upcoming-deadline route returns per_unit_deadlines + units_missing_first_use; Permits.tsx and Form2290Filings.tsx render per-unit exceptions. Business-day shift already in form-2290-generator.ts (nextBusinessDay + usFederalHolidays). GUARD: verify-step 2064 (verify-saf-orph04-form2290-per-unit-deadline.mjs) + enhanced verify-saf-f32-permits-deadline-honesty.mjs. LIVE PROOF: UNVERIFIED — deploy SHA pending merge; local guard selftest PASS. REMAINING: live click-through on Safety → Permits after deploy. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/2064-verify-saf-orph04-form2290-per-unit-deadline.mjs` + `--selftest` both exit 0 today -- the per-unit, business-day-correct Form 2290 deadline confirmed still correctly computed. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | — |
| `SAF-ORPH-05` | **HOLD** | Every Safety tab passes a DoD click-through | HOLD 2026-08-09 — LAYER A CLOSED for module wire certify: verify-saf-orph05-safety-tab-route-mount (2204) 28/28 routes mounted; prior live SPA smoke TRANSP+USMCA + authenticated 28/28 HTTP 200; Leave Balances list (2206); weekend Safety event create+FK prove. REMAINING (owner-hold future_block SAF-ORPH-05-DEPTH): full DoD-B wizard depth on every tab both entities — tracked docs/trackers/SAF-ORPH-05-TAB-WIZARD-DEPTH.md. Not UNVERIFIED forever: Layer A is the ORPH-05 mount bar. | 4198 |
| `SAF-DOM-02` | **PASS** | company_violations retired jsonb: re-backfill sweep, auto-fine trigger repoint, DB-enforced archive | NEW FAIL FOUND 2026-07-25 while scoping SAF-DOM-02, not previously on the board: safety.auto_create_internal_fine_from_violation() (SECURITY DEFINER, 0124:971-1101, fired by trg_auto_fine_on_violation_resolve) resolves its driver ONLY from NEW.related_drivers, the RETIRED jsonb. The create route stopped populating that column when 202607820000 (applied on prod) landed, so for every violation created through the UI since, resolve-as-monetary_fine hits 'IF v_driver_id IS NULL THEN RETURN NEW' and issues NO FINE while the endpoint returns 200 with autoCreatedInternalFineUuid:null. Proven on a throwaway postgres:16-alpine built from 0001: pre-fix function -> UPDATE 1, outcome=monetary_fine, auto_created_internal_fine_uuid NULL, 0 rows in safety.internal_fines; post-fix function -> fine created, linked, 0.05 approved, driver from the junction. Migration 202609130000 is HELD and UNAPPLIED anywhere; status stays OPEN until the owner Neon-applies it and GUARD re-proves the ledger row. Its stop-write section must not be applied before SAF-B28's PATCH repoint (C2 lane) is live. / PASS 2026-08-03 — Cursor Neon-applied 202609130000 on br-fancy-credit-akjnd07a as neondb_owner (direct). Function reads safety.company_violation_drivers (reads_join_table=t still_reads_jsonb=f). Triggers trg_auto_fine_on_violation_resolve + trg_reject_retired_cv_jsonb tgenabled=O. Dual ledger applied_at 2026-08-03T15:58:18Z checksum 896dbc341393b9806f1e524243d20f7a7efd2ee3d193f749839dd352198c3f54. Registry: applied_held[] (not held[]). Guard verify-saf-dom-02-autofine-reads-join-table step 2202. Owner standing: no HOLD / no JORGE-APPROVED gate — Cursor applies Neon. / RE-VERIFIED LIVE 2026-08-29 (GO-T07-SAFETY, CC-1): `node scripts/verify-steps/2202-verify-saf-dom-02-autofine-reads-join-table.mjs` + `--selftest` both exit 0 today -- the 2026-08-03 fix (auto-fine trigger reading safety.company_violation_drivers, not the retired jsonb) confirmed still holding on current code. Recipe B: status PASS, prod_verified left false for CC-2 to stamp. / GUARD PACKET (CC-2, 2026-08-29, SHA 0dd70bf): CC-1 handed bindable evidence via OUTBOX (Recipe B, Hard Rule 5, GO-T07-SAFETY batches 5-7/8-FINAL). Independently re-ran every cited guard/verify-step today, all exit 0 (normal + --selftest where applicable) -- no contradictions found this pass. Spot-corroborated live this same pass: Chrome (USMCA) /safety/escrow-record renders the real, live, mounted Escrow Record table (Driver/Current Balance/Pre-clause/Post-clause/Signed clause/Accumulation Rate/Forfeiture History columns) plus a real Forfeiture Audit panel ('Successful forfeitures: 0 / No forfeiture attempts yet' -- an honest empty, not a stub), matching B01/B09/B22/ORPH-03's cited escrow/forfeiture chrome exactly. Binding on CC-1's evidence + this independent guard re-run + spot corroboration. | 4178 |

Desktop audit: ~/Desktop/IH35-CURSOR-AUDIT/modules/safety.md
