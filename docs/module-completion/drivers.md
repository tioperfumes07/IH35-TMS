# Module completion — Drivers — acceptance checklist

**PROGRESS: 10 of 20** · complete: `false` · as_of: 2026-07-29 · live_sha: `—`

| Status | Count |
|---|---:|
| PASS | 10 |
| HOLD | 0 |
| OPEN | 10 |
| FAIL | 0 |
| UNVERIFIED | 0 |

| ID | Status | Title | Evidence | PR |
|---|---|---|---|---|
| `DRV-S01` | **PASS** | Surface /drivers renders real entity-scoped data with no dead end | Route /drivers wired to pages/Drivers.tsx via routes/manifest.tsx. listDrivers requires operating_company_id and is gated by selectedCompanyId. Roster renders via ParityTable with sortable columns and row click to /drivers/:id. Empty state now reads 'No drivers found.' (honest empty). Unit tests DriversPage.tabs.test.tsx + DriversPage.create.test.tsx pass (11/11). / NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S02` | **PASS** | Surface /drivers/:id renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/:id wired to pages/DriverDetail.tsx. getDriver(id, operatingCompanyId) requires company scope. Page uses ParityTable + ListErrorState, renders 12 operations sub-views via OperationsDepthNav, and links back to roster and HOS. verify-driver-detail-company-scope.mjs OK; verify-driver-detail-uses-paritytable.mjs OK; verify-driver-operations-depth.mjs OK (12 sub-views). | — |
| `DRV-S03` | **PASS** | Surface /drivers/:id/hos renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/:id/hos wired to pages/drivers/DriverHosDetailPage.tsx. Calls getDriver(id, operatingCompanyId) and getDriverHosDetail(id, operatingCompanyId), both gated by id + operatingCompanyId. Renders FMCSA clocks and HOS timeline, with Back to driver link. Honest empty state when no company selected. | — |
| `DRV-S04` | **PASS** | Surface /drivers/:id/profile renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/:id/profile wired to pages/drivers/DriverProfilePage.tsx. getDriver requires operating_company_id. Profile sections complete (verify-driver-profile-sections-complete.mjs OK; verify-driver-profile-part2-sections-complete.mjs OK). Navigation, assign-truck, and dead-action guards pass. DriverProfilePage.test.tsx + assign-truck test pass. | — |
| `DRV-S05` | **PASS** | Surface /drivers/alerts renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/alerts wired to pages/alerts/DocumentAlertsPage.tsx. getDocumentAlertsInbox / getDocumentAlertRules take operatingCompanyId and are gated by selectedCompanyId. Inbox links to /drivers/:driver_id/profile or /safety/permits; rules editor persists thresholds. DocumentAlertsPage.test.tsx passes (4/4). | — |
| `DRV-S06` | **PASS** | Surface /drivers/applicants renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/applicants wired to pages/drivers/ApplicantsPipelinePage.tsx. listDriverApplicants scoped to selectedCompanyId; convert-to-driver and onboarding links wired. Empty state + pipeline card render. ApplicantsPipelinePage.test.tsx passes (4/4). | — |
| `DRV-S07` | **PASS** | Surface /drivers/auto-deductions renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/auto-deductions redirects to /drivers/deductions and shares AutoDeductionPoliciesPanel. useAutoDeductionPolicies accepts operatingCompanyId + optional driver_id filter; policy list joins mdata.drivers for driver_name and renders EntityLink. verify-driver-default-expense-account-held.mjs OK; verify-driver-default-expense-account-wired.mjs OK. | — |
| `DRV-S08` | **PASS** | Surface /drivers/cash-advances renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/cash-advances renders debt-alert panel in pages/Drivers.tsx using selectedCompanyId-scoped cashAdvanceRequestsOfficeApi.list, liabilitiesQuery, and settlementsQuery. Links to canonical /driver-finance/cash-advance-requests. No dead end. | — |
| `DRV-S09` | **PASS** | Surface /drivers/deductions renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/deductions wired to AutoDeductionPoliciesPanel. Uses useDriverDeductionTypeCatalog (entity-scoped catalogs.driver_deduction_types) and ReferenceSelect with inline create. Driver name rendered as EntityLink. reverse-link from driver profile ?driver_id honored. | — |
| `DRV-S10` | **PASS** | Surface /drivers/disputes renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. / Route /drivers/disputes wired to pages/drivers/SettlementDisputeList.tsx. useSettlementDisputes reads selectedCompanyId and sends operating_company_id. Table renders driver and settlement as EntityLinks and supports create dispute modal. No dead end. | — |
| `DRV-S11` | **OPEN** | Surface /drivers/leave renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S12` | **OPEN** | Surface /drivers/messages renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S13` | **OPEN** | Surface /drivers/onboarding/:session_id renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S14` | **OPEN** | Surface /drivers/pay-rate-templates renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S15` | **OPEN** | Surface /drivers/permits renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S16` | **OPEN** | Surface /drivers/pre-settlements renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S17` | **OPEN** | Surface /drivers/profiles renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S18` | **OPEN** | Surface /drivers/retention renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S19` | **OPEN** | Surface /drivers/settlements renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |
| `DRV-S20` | **OPEN** | Surface /drivers/team-splits renders real entity-scoped data with no dead end | NOT YET VERIFIED. Surface enumerated from the route manifest on 2026-07-29. To reach PASS this route must be opened in the running app and shown to render real entity-scoped data (TRANSP and USMCA), every rendered field present in the submit payload where it writes, and forward/reverse linkage proven. No claim is made here beyond the route existing. | — |

Desktop audit: —
