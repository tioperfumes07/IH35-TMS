# Module completion — Dispatch — acceptance checklist

**PROGRESS: 6 of 37** · complete: `false` · as_of: 2026-08-29T18:40:00Z · live_sha: `965789a`

| Status | Count |
|---|---:|
| PASS | 37 |
| HOLD | 0 |
| OPEN | 0 |
| FAIL | 0 |
| UNVERIFIED | 0 |

| ID | Status | Title | Evidence | PR |
|---|---|---|---|---|
| `DISP-S01` | **PASS** | Surface /dispatch renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch: real Overview dashboard (Active Loads 23, At-Risk/Late 1 (0 at-risk / 1 late), Unassigned Units, Round-Trip Exposure), USMCA entity. healthz=965789a. | — |
| `DISP-S02` | **PASS** | Surface /dispatch/alerts renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-ALERTS (2 items, one live walk: hub then drill-down). /dispatch/alerts: real live KPI counts (Accidents Open 4, Cash Advance Requests 1, Late Arrivals 1, In-Transit Issues 0), each sourced live per its own subtitle. healthz=965789a. | — |
| `DISP-S03` | **PASS** | Surface /dispatch/alerts/late-arrivals renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-ALERTS (2 items, one live walk: hub then drill-down). /dispatch/alerts/late-arrivals: real 1-row drill-down (LUSMCAFREIGHT-20260808-0004, ZZ-SAMPLE Customer A, driver Jorge Pablo Guadalupe Muñoz Gonzalez, ETA signal 'late/No ETA'). healthz=965789a. | — |
| `DISP-S04` | **PASS** | Surface /dispatch/assignment-history renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-ASSIGN-HISTORY (2 items -- /dispatch/assignments deliberately embeds AssignmentHistoryPage as its 'Assignments' tab content, per source comment 'ARCHIVE B21-D12 Sunset 2026-06-04: assignments stub replaced by D2 AssignmentHistoryPage embed', not a bug). /dispatch/assignment-history: AssignmentHistoryPage, real 14-row audit trail (assigned_at/load/method/prev-new driver-unit/reason), incl. our own L-20260828-0022 full_form reassignment to Javier Vargas Solis + T150. healthz=965789a. | — |
| `DISP-S05` | **PASS** | Surface /dispatch/assignments renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-ASSIGN-HISTORY (2 items -- /dispatch/assignments deliberately embeds AssignmentHistoryPage as its 'Assignments' tab content, per source comment 'ARCHIVE B21-D12 Sunset 2026-06-04: assignments stub replaced by D2 AssignmentHistoryPage embed', not a bug). /dispatch/assignments renders DispatchSecondaryTabRoute subTab="assignments" -> DispatchPage embeds the SAME AssignmentHistoryPage as S04 (source-confirmed intentional at apps/frontend/src/pages/Dispatch.tsx:491-494, not a routing bug). healthz=965789a. | — |
| `DISP-S06` | **PASS** | Surface /dispatch/at-risk renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/at-risk: AtRiskQueuePage, honest-empty 'No at-risk loads right now' (0 rows) -- correctly nothing in-transit with a late/near-late ETA today. healthz=965789a. | — |
| `DISP-S07` | **PASS** | Surface /dispatch/book-load renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/book-load: real 'BOOK LOAD' entry panel with working +Book Load action inside the shared Dispatch shell/subnav. healthz=965789a. | — |
| `DISP-S08` | **PASS** | Surface /dispatch/border-crossing renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/border-crossing: 'Border Crossing Wizard' real step 1 (Load & direction, Unit/Driver/Direction) + live CBP wait times for 4 Laredo-region ports (each real, though currently '—' -- no live CBP feed configured, honestly shown as em-dash not a fake number). healthz=965789a. | — |
| `DISP-S09` | **PASS** | Surface /dispatch/border-crossing/history renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/border-crossing/history: honest-empty 'No completed crossings yet' (0 rows), real 'New crossing' action + detail-select placeholder. healthz=965789a. | — |
| `DISP-S10` | **PASS** | Surface /dispatch/borders/geofence-history renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/borders/geofence-history: 'GPS Border Crossing Events', honest-empty 'No border crossing events found for this period' (0 rows), real filter chip. healthz=965789a. | — |
| `DISP-S11` | **PASS** | Surface /dispatch/chat renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/chat: 'Dispatch Chat', honest explanation 'No chats for this company yet... Empty is expected until the first thread is created' -- correct honest-empty, not a stub. healthz=965789a. | — |
| `DISP-S12` | **PASS** | Surface /dispatch/detention renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/detention: 'Detention board', real 1-row live accrual (L-20260816-0168, CC2-BOOKLOAD-INLINE-TEST, TX pickup, TEST DriverTESTMTDP79YF, T-TESTMTDP79YF, accruing). healthz=965789a. | — |
| `DISP-S13` | **PASS** | Surface /dispatch/equipment-transfers renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/equipment-transfers: 'Equipment transfer requests', real 1-row dual-confirm handoff (unit 010101, pending_outbound, SAMPLE Cascade-2042 -> Jorge Pablo Guadalupe Muñoz Gonzalez). healthz=965789a. | — |
| `DISP-S14` | **PASS** | Surface /dispatch/factoring-packets renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/factoring-packets redirects to the canonical /accounting/factoring (not a bug -- a real, working alias): real 1-row batch FAC-2026-00001, Faro Factoring, $1,850.00 total / $1,794.50 advanced / $55.50 reserve, status ADVANCED. healthz=965789a. | — |
| `DISP-S15` | **PASS** | Surface /dispatch/factoring-queue renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/factoring-queue: real KPIs (Active Factor Faro Factoring, Reserve Balance $55.50, MTD Advances $1,794.50/1 batch, Packet Queue 1 pending) + real 1-row queue (L-20260802-0258, TIO PERFUMES, Not Factored, missing rate_conf/BOL/POD/invoice). healthz=965789a. | — |
| `DISP-S16` | **PASS** | Surface /dispatch/geofencing renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/geofencing: 'Geofences', real 1-row active geofence (TEST CODEX GO0040, custom, 4 vertices, Active) + working polygon-editor create form. healthz=965789a. | — |
| `DISP-S17` | **PASS** | Surface /dispatch/in-transit-issues renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/in-transit-issues: real 2-row queue (L-20260824-0007/Javier Vargas Solis/USMCA-001/mechanical/warning/open, L-20260810-0003/Rafael Rogelio Rivero Reynoso/T149/mechanical/warning/open), +Create Issue. healthz=965789a. | — |
| `DISP-S18` | **PASS** | Surface /dispatch/incidents renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/incidents redirects to the canonical /dispatch/alerts (same real KPIs as S02, not a bug -- a working alias). healthz=965789a. | — |
| `DISP-S19` | **PASS** | Surface /dispatch/layovers/driver/:driverId renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-09 — apps/frontend/src/pages/drivers/DriverLayoverHistoryPage.tsx renders the driver's name in the header, entity-scopes via operating_company_id (DriverLayoverHistory's fetch includes operating_company_id + driver in the query string), and honestly names 3 empty/blocked states: no company selected, no driver in the route, and (inside ParityTable) "No layovers detected in this period." for a real zero-row result — never a bare number or silent blank. Fetch failure shows ListErrorState with retry. Regression test: DriverLayoverHistoryPage.test.tsx (4 tests, all green) covers render+scope, honest-zero, no-driver, and honest-error. / Historical acceptance definition. Page component: apps/frontend/src/pages/drivers/DriverLayoverHistoryPage.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PROD-VERIFIED 2026-08-09 Neon br-fancy-credit-akjnd07a + lucia bypass + USMCA opco 5c854333-6ea5-4faa-af31-67cb272fef80: dispatch.driver_layovers usmca_n=1 (uuid=1fd3131e-fe0e-4b97-951a-2074434979ba driver_uuid=a32a35c8-7cd5-4368-83f0-35e185092433 Neftali Coronado Urbano; previous_load=495793b8… L-0074 class; next_load=5c2d7446… L-20260809-0001; duration_hours=22.19). Route /dispatch/layovers/driver/:driverId → DriverLayoverHistoryPage; API GET /api/v1/dispatch/layovers?operating_company_id&driver registered (routes.ts). USMCA active drivers=16. healthz/shallow version=49ff971. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/layovers/driver/a32a35c8-7cd5-4368-83f0-35e185092433 rendered Driver Layover History for Neftali Coronado Urbano. GET /api/v1/dispatch/layovers?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80&driver=a32a35c8-7cd5-4368-83f0-35e185092433 and the scoped driver detail GET returned 200. Live zero-row result showed the named honest empty state No layovers detected in this period, 0 rows, 0 of 0; no console errors or warnings. DriverLayoverHistoryPage tests cover scope, zero, missing-driver, and error paths. | — |
| `DISP-S20` | **PASS** | Surface /dispatch/loads renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/loads: real 42-load board (Awaiting Assignment 0, Booked 42), full unit/driver/HOS/status columns populated live. healthz=965789a. | — |
| `DISP-S21` | **PASS** | Surface /dispatch/loads/:id renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/loads/b347aeb6-0272-4d93-9c3c-5bd3dbc22f1c: renders as a [role="dialog"] portal OUTSIDE <main> (a plain document.querySelector('main') check misses it -- confirmed via document.querySelectorAll('[role="dialog"]').length===1 and body text containing the load number) -- real drawer 'Load L-20260828-0022', Overview/Stops/Driver Pay/Documents/Factoring/Cargo Sensors/Settlement/Geofence Timeline/Assignment History/Audit/Pre-Settlement tabs, real customer/status/miles/trip-type/unit fields matching Neon. healthz=965789a. | — |
| `DISP-S22` | **PASS** | Surface /dispatch/loads/:id/banking renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-09 — apps/frontend/src/pages/dispatch/LoadBankingLinkagePage.tsx renders header + LinkedBankTransactionsPanel; the linkage fetch (getBankTransactionsByLinkage) carries operating_company_id + load_id and is gated enabled: Boolean(companyId && linkage.id). Honest empty state is already named: "No bank transactions tagged to this load yet. Tagging happens on Banking → Transactions → Categorize (persisted links only)." shown only after a successful zero-row response (never on pending/failed fetch); fetch failure shows an error banner with Refresh. Regression test: LoadBankFeedLinkagePage.test.tsx (3 tests, all green) covers entity-scoped fetch, honest-zero, and honest-error. / PROD-VERIFIED 2026-08-09 Neon br-fancy-credit-akjnd07a + lucia + USMCA: mdata.loads usmca_n=26 (sample L-20260809-0001 id=5c2d7446-b54d-4b63-83e4-bc9cc65e924f). banking.bank_transactions USMCA n=189; matched_load_id IS NOT NULL =0; categorization_load_id IS NOT NULL =0 → reverse by-linkage returns honest zero for load drill (Empty-state DoD OK — density not required). Route /dispatch/loads/:id/banking → LoadBankingLinkagePage + GET /api/v1/banking/transactions/by-linkage (categorization.routes.ts rateLimit). healthz/shallow version=49ff971. | — |
| `DISP-S23` | **PASS** | Surface /dispatch/map renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/map: 'Active Load Map', honest explanation 'Map provider not configured... 3 active loads with GPS — positions are not shown here until map rendering is enabled (no fake map pins)' -- correct honesty, not a placeholder bug. healthz=965789a. | — |
| `DISP-S24` | **PASS** | Surface /dispatch/notify-preferences renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/notify-preferences: 'Customer ETA notify', real 1-row delivery log (L-20260816-0168, TIO PERFUMES, Departed, SMS, pending, TEST DATA keep). healthz=965789a. | — |
| `DISP-S25` | **PASS** | Surface /dispatch/ocr-queue renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/ocr-queue: 'OCR queue', real 1-row intake (TEST DATA / TEST DATA, ready_review, lane UNKNOWN,TX->UNKNOWN,TX, confidence 62%, Convert to load action). healthz=965789a. | — |
| `DISP-S26` | **PASS** | Surface /dispatch/planner renders, is entity-scoped, and shows an honest empty state | Historical acceptance definition. Page component: apps/frontend/src/pages/dispatch/PlannerCalendarPage.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PASS 2026-08-09 — PlannerCalendarPage company-gated getDispatchPlannerWeek; need-company + drivers honest empty + ListErrorBanner; guard scripts/verify-disp-s26-planner-surface.mjs --selftest exit 0. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/planner rendered the populated weekly planner (driver/load/HOS rows); GET /api/v1/dispatch/planner/week?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80&week_start=2026-08-24 returned 200. The empty branch was not invoked by populated live data; scripts/verify-disp-s26-planner-surface.mjs --selftest proves company gating, named honest empty, and ListErrorBanner. | — |
| `DISP-S27` | **PASS** | Surface /dispatch/planners renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-PLANNERS (5 items, one shared DispatchPlanners page, tab param switch, one live walk). /dispatch/planners redirects to the canonical /dispatch/planners/timeline (shared Timeline/Driver Planner/Truck Planner/Loads Planner subnav, date-range picker). healthz=965789a. | — |
| `DISP-S28` | **PASS** | Surface /dispatch/planners/driver renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-PLANNERS (5 items, one shared DispatchPlanners page, tab param switch, one live walk). /dispatch/planners/driver: real per-driver leave/availability grid (CODEX TEST 0034 Driver, PEDRO ABRAHAM LOPEZ COLLADO/T149, JORGE ALBERTO JUAREZ, etc., 30-day columns). healthz=965789a. | — |
| `DISP-S29` | **PASS** | Surface /dispatch/planners/loads renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-PLANNERS (5 items, one shared DispatchPlanners page, tab param switch, one live walk). /dispatch/planners/loads: real 2-row load timeline (L-20260829-0338 TIO PERFUMES Laredo TX, L-20260808-0050 TIO PERFUMES Laredo TX), each plotted on its correct date column. healthz=965789a. | — |
| `DISP-S30` | **PASS** | Surface /dispatch/planners/timeline renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-PLANNERS (5 items, one shared DispatchPlanners page, tab param switch, one live walk). /dispatch/planners/timeline: the canonical Timeline tab of the shared planners page, same real shared date-range header as S27-S29/S31. healthz=965789a. | — |
| `DISP-S31` | **PASS** | Surface /dispatch/planners/truck renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- GROUP-DISP-PLANNERS (5 items, one shared DispatchPlanners page, tab param switch, one live walk). /dispatch/planners/truck: real per-unit availability grid (unit 01 all 'shop', CODEX-LEGAL-UNIT-20260816-1506 all 'rsv', 30-day columns). healthz=965789a. | — |
| `DISP-S32` | **PASS** | Surface /dispatch/pod-review renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/pod-review: 'POD review + BOL', real 1-row queue (L-20260816-0168, TEST DriverTESTMTDP79YF, TEST DATA keep recipient, Pending Review, Approve/Reject actions). healthz=965789a. | — |
| `DISP-S33` | **PASS** | Surface /dispatch/pre-settlements renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/pre-settlements: real 1-driver batch (Rafael Rogelio Rivero Reynoso, 08/10/2026-08/16/2026, L-20260810-0003, $297.60), Total payout this batch $297.60 (an earlier 2s-wait check misread this as 'Loading...' -- a longer wait showed the real content, my own check's timing, not a defect). healthz=965789a. | — |
| `DISP-S34` | **PASS** | Surface /dispatch/settings renders, is entity-scoped, and shows an honest empty state | Historical acceptance definition. Page component: apps/frontend/src/pages/dispatch/DispatchSettingsPage.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PASS 2026-08-09 — DispatchSettingsPage need-company + ListErrorBanner on prefs; guard pack --selftest exit 0. / PASS 2026-08-09 — apps/frontend/src/pages/dispatch/DispatchSettingsPage.tsx renders 4 preference sections (default view, sort, alert thresholds, auto-routing). Default view is a per-user preference (GET/PATCH /api/v1/dispatch/preferences — auth-session scoped, not company-scoped by design, same as any other user-settings surface); sort/thresholds/auto-routing are stored in localStorage, both intentionally not entity data (no backing table that could be "empty"). FIXED a swallowed-error gap: getDispatchPreferences failing silently fell back to the "home" default with no indication the saved preference never loaded. Added a named error message ("Couldn't load your saved default view...") shown on fetch failure. Regression test: DispatchSettingsPage.test.tsx (4 tests, all green) covers render, prefs-load, PATCH-persist, and the new honest-error case. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA selected — /dispatch/settings rendered default-view, sort, thresholds, and auto-routing controls; GET /api/v1/dispatch/preferences returned 200. Preferences are intentionally authenticated-user scoped and localStorage-backed, not company data, so no entity-table empty state applies; contemporaneous entity-bearing app calls carried operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80 and returned 200. Existing DispatchSettingsPage tests cover the named load-error path. | — |
| `DISP-S35` | **PASS** | Surface /dispatch/settlements renders, is entity-scoped, and shows an honest empty state | Historical acceptance definition. Page component: apps/frontend/src/pages/Dispatch.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PASS 2026-08-09 — /dispatch/settlements quicklink honest empty + need-company; Driver Finance is the list home; guard pack --selftest exit 0. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/settlements rendered without error; all entity-bearing requests returned 200, including /api/v1/driver-finance/settlements?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80. The surface honestly explains that runs, acknowledgements, and payouts live in Driver Finance and provides View all settlements → /driver-finance/settlements rather than a silent empty list. | — |
| `DISP-S36` | **PASS** | Surface /dispatch/trip-pairing renders, is entity-scoped, and shows an honest empty state | COMPONENT RESOLVED on origin/main and live SHA 4e5db76: apps/frontend/src/routes/trip-pairing-board.routes.ts mounts TripPairingBoardPage at /dispatch/trip-pairing. Earlier live click-through was pending (DOD-A/B/C on TRANSP+TRK+USMCA). Prior text said UNRESOLVED — that was stale vs code. / PASS 2026-08-09 — TripPairingBoardPage need-company + ListErrorBanner + board honest empty; company-gated getTripPairingBoard; guard pack --selftest exit 0. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/trip-pairing resolved to TripPairingBoardPage and rendered a populated board (29 active trucks, 7 assigned trips, linked units/drivers/loads); GET /api/v1/dispatch/trip-pairing-board?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80 returned 200. The empty branch was not invoked by populated live data; existing guard coverage proves company gating, ListErrorBanner, and the named honest-empty board. | — |
| `DISP-S37` | **PASS** | Surface /dispatch/trip-profitability renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-29 (CC-3) -- /dispatch/trip-profitability: 'Trip Profitability' Company Settlement Report, real 15-row NB+SB roll-up (Trips 15, Revenue $13,824.50, Driver Pay $1,335.40, Fuel $40.00, Net Profit $12,028.10), incl. our own scenario trip S-20260828-0022 (Javier Vargas Solis, L-20260828-0022, $50.00 revenue, 100% margin). healthz=965789a. | — |

Desktop audit: —
