# Module completion — Dispatch — acceptance checklist

**PROGRESS: 6 of 37** · complete: `false` · as_of: 2026-08-29T16:40:00Z · live_sha: `4e5db76`

| Status | Count |
|---|---:|
| PASS | 6 |
| HOLD | 0 |
| OPEN | 0 |
| FAIL | 0 |
| UNVERIFIED | 31 |

| ID | Status | Title | Evidence | PR |
|---|---|---|---|---|
| `DISP-S01` | **UNVERIFIED** | Surface /dispatch renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch DispatchBoard entity-scoped loads list; owner-proven Book→assign→dispatch path (L-0085 class). Empty-state follow-on on board false-empty still tracked in verify-list-empty-settled sweep. | — |
| `DISP-S02` | **UNVERIFIED** | Surface /dispatch/alerts renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — DispatchAlertsPage company-gated queries (accidents/cash/late/in-transit); ListErrorBanner on failure; data-testid=dispatch-alerts-honest-empty when all counts are known 0; guard scripts/verify-disp-s02-alerts-surface.mjs --selftest exit 0. | — |
| `DISP-S03` | **UNVERIFIED** | Surface /dispatch/alerts/late-arrivals renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — LateArrivalsPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S04` | **UNVERIFIED** | Surface /dispatch/assignment-history renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — AssignmentHistoryPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S05` | **UNVERIFIED** | Surface /dispatch/assignments renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch/assignments via DispatchSecondaryTabRoute; same proven assignment path. | — |
| `DISP-S06` | **UNVERIFIED** | Surface /dispatch/at-risk renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — AtRiskQueuePage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S07` | **UNVERIFIED** | Surface /dispatch/book-load renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch/book-load Book Load wizard — OWNER PROVEN live (Neftali/Baytown rescue packet); do not re-audit. | — |
| `DISP-S08` | **UNVERIFIED** | Surface /dispatch/border-crossing renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — BorderCrossingWizardPage company-scoped submit + brokers; ports/brokers load errors surfaced; need-company + ports/brokers honest empty testids; guard scripts/verify-disp-s08-border-crossing-surface.mjs --selftest exit 0. | — |
| `DISP-S09` | **UNVERIFIED** | Surface /dispatch/border-crossing/history renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — BorderCrossingHistoryPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S10` | **UNVERIFIED** | Surface /dispatch/borders/geofence-history renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — borders/BorderCrossingHistory ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S11` | **UNVERIFIED** | Surface /dispatch/chat renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — DispatchChatPage company-gated listChatThreads; ListErrorBanner; need-company + threads honest empty testids; guard scripts/verify-disp-s11-chat-surface.mjs --selftest exit 0. | — |
| `DISP-S12` | **UNVERIFIED** | Surface /dispatch/detention renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — DetentionBoardPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S13` | **UNVERIFIED** | Surface /dispatch/equipment-transfers renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — EquipmentTransferRequests ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S14` | **UNVERIFIED** | Surface /dispatch/factoring-packets renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch/factoring-packets Navigate→/accounting/factoring (wired redirect, no orphan chrome). | — |
| `DISP-S15` | **UNVERIFIED** | Surface /dispatch/factoring-queue renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — FactoringQueuePage list-state/ParityTable settled empty (already guarded). | — |
| `DISP-S16` | **UNVERIFIED** | Surface /dispatch/geofencing renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — GeofencesPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S17` | **UNVERIFIED** | Surface /dispatch/in-transit-issues renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — InTransitIssuesPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S18` | **UNVERIFIED** | Surface /dispatch/incidents renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch/incidents Navigate→/dispatch/alerts (wired redirect). | — |
| `DISP-S19` | **PASS** | Surface /dispatch/layovers/driver/:driverId renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-09 — apps/frontend/src/pages/drivers/DriverLayoverHistoryPage.tsx renders the driver's name in the header, entity-scopes via operating_company_id (DriverLayoverHistory's fetch includes operating_company_id + driver in the query string), and honestly names 3 empty/blocked states: no company selected, no driver in the route, and (inside ParityTable) "No layovers detected in this period." for a real zero-row result — never a bare number or silent blank. Fetch failure shows ListErrorState with retry. Regression test: DriverLayoverHistoryPage.test.tsx (4 tests, all green) covers render+scope, honest-zero, no-driver, and honest-error. / Historical acceptance definition. Page component: apps/frontend/src/pages/drivers/DriverLayoverHistoryPage.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PROD-VERIFIED 2026-08-09 Neon br-fancy-credit-akjnd07a + lucia bypass + USMCA opco 5c854333-6ea5-4faa-af31-67cb272fef80: dispatch.driver_layovers usmca_n=1 (uuid=1fd3131e-fe0e-4b97-951a-2074434979ba driver_uuid=a32a35c8-7cd5-4368-83f0-35e185092433 Neftali Coronado Urbano; previous_load=495793b8… L-0074 class; next_load=5c2d7446… L-20260809-0001; duration_hours=22.19). Route /dispatch/layovers/driver/:driverId → DriverLayoverHistoryPage; API GET /api/v1/dispatch/layovers?operating_company_id&driver registered (routes.ts). USMCA active drivers=16. healthz/shallow version=49ff971. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/layovers/driver/a32a35c8-7cd5-4368-83f0-35e185092433 rendered Driver Layover History for Neftali Coronado Urbano. GET /api/v1/dispatch/layovers?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80&driver=a32a35c8-7cd5-4368-83f0-35e185092433 and the scoped driver detail GET returned 200. Live zero-row result showed the named honest empty state No layovers detected in this period, 0 rows, 0 of 0; no console errors or warnings. DriverLayoverHistoryPage tests cover scope, zero, missing-driver, and error paths. | — |
| `DISP-S20` | **UNVERIFIED** | Surface /dispatch/loads renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch/loads board list — live USMCA loads exist; entity-scoped useLoadsList. | — |
| `DISP-S21` | **UNVERIFIED** | Surface /dispatch/loads/:id renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — /dispatch/loads/:id detail — live drill on rescued loads; EntityLinks. | — |
| `DISP-S22` | **PASS** | Surface /dispatch/loads/:id/banking renders, is entity-scoped, and shows an honest empty state | PASS 2026-08-09 — apps/frontend/src/pages/dispatch/LoadBankingLinkagePage.tsx renders header + LinkedBankTransactionsPanel; the linkage fetch (getBankTransactionsByLinkage) carries operating_company_id + load_id and is gated enabled: Boolean(companyId && linkage.id). Honest empty state is already named: "No bank transactions tagged to this load yet. Tagging happens on Banking → Transactions → Categorize (persisted links only)." shown only after a successful zero-row response (never on pending/failed fetch); fetch failure shows an error banner with Refresh. Regression test: LoadBankFeedLinkagePage.test.tsx (3 tests, all green) covers entity-scoped fetch, honest-zero, and honest-error. / PROD-VERIFIED 2026-08-09 Neon br-fancy-credit-akjnd07a + lucia + USMCA: mdata.loads usmca_n=26 (sample L-20260809-0001 id=5c2d7446-b54d-4b63-83e4-bc9cc65e924f). banking.bank_transactions USMCA n=189; matched_load_id IS NOT NULL =0; categorization_load_id IS NOT NULL =0 → reverse by-linkage returns honest zero for load drill (Empty-state DoD OK — density not required). Route /dispatch/loads/:id/banking → LoadBankingLinkagePage + GET /api/v1/banking/transactions/by-linkage (categorization.routes.ts rateLimit). healthz/shallow version=49ff971. | — |
| `DISP-S23` | **UNVERIFIED** | Surface /dispatch/map renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — MapView company-gated Samsara positions; need-company + positions honest empty + map-not-configured honest empty (no fake pins); ListErrorBanner; guard scripts/verify-disp-s23-map-surface.mjs --selftest exit 0. | — |
| `DISP-S24` | **UNVERIFIED** | Surface /dispatch/notify-preferences renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — NotifyPreferencesPage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S25` | **UNVERIFIED** | Surface /dispatch/ocr-queue renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — OcrQueuePage ParityTable settled empty; verify-list-empty-settled. | — |
| `DISP-S26` | **PASS** | Surface /dispatch/planner renders, is entity-scoped, and shows an honest empty state | Historical acceptance definition. Page component: apps/frontend/src/pages/dispatch/PlannerCalendarPage.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PASS 2026-08-09 — PlannerCalendarPage company-gated getDispatchPlannerWeek; need-company + drivers honest empty + ListErrorBanner; guard scripts/verify-disp-s26-planner-surface.mjs --selftest exit 0. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/planner rendered the populated weekly planner (driver/load/HOS rows); GET /api/v1/dispatch/planner/week?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80&week_start=2026-08-24 returned 200. The empty branch was not invoked by populated live data; scripts/verify-disp-s26-planner-surface.mjs --selftest proves company gating, named honest empty, and ListErrorBanner. | — |
| `DISP-S27` | **UNVERIFIED** | Surface /dispatch/planners renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — deliberate Navigate /dispatch/planners → /dispatch/planners/timeline; target DISP-S30 meets Empty-state DoD; guard scripts/verify-disp-s27-s31-planners-pack.mjs --selftest exit 0. | — |
| `DISP-S28` | **UNVERIFIED** | Surface /dispatch/planners/driver renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — DriverPlanner need-company; SafetyDriverSchedulerGrid company-gated + ListErrorBanner + honest empty; guard pack --selftest exit 0. | — |
| `DISP-S29` | **UNVERIFIED** | Surface /dispatch/planners/loads renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — LoadsPlanner need-company + ListErrorBanner + loads honest empty; company-gated week fetch; guard pack --selftest exit 0. | — |
| `DISP-S30` | **UNVERIFIED** | Surface /dispatch/planners/timeline renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — UnifiedTimelinePlanner need-company + ListErrorBanner + drivers honest empty; company-gated; guard pack --selftest exit 0. | — |
| `DISP-S31` | **UNVERIFIED** | Surface /dispatch/planners/truck renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — TruckPlanner need-company + ListErrorBanner + units honest empty; company-gated queries; guard pack --selftest exit 0. | — |
| `DISP-S32` | **UNVERIFIED** | Surface /dispatch/pod-review renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — PodReviewPage ParityTable settled empty (already guarded). | — |
| `DISP-S33` | **UNVERIFIED** | Surface /dispatch/pre-settlements renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — apps/frontend/src/pages/Dispatch.tsx renders PreSettlementsPanel for subTab=pre_settlements; preSettlementsQuery.queryFn (listSettlements) carries the active operating company id and is gated enabled: Boolean(defaultCompanyIds[0]). FIXED a swallowed-error gap: the panel's isError prop was never wired, so a failed fetch left rows at [] and rendered the SAME "No pre-settlements ready right now." text as a genuine zero-row result (a zero presented as a fact). Wired preSettlementsQuery.isError through; the panel now shows a named error ("Couldn't load pre-settlements...") and suppresses the fabricated $0 total-payout row on failure. Regression test: PreSettlementsPanel.test.tsx (4 tests, all green) covers honest-zero, honest-error, no-fabricated-total-on-error, and real-row render. / PASS 2026-08-09 — /dispatch/pre-settlements via DispatchPage; company-gated listSettlements; need-company + ListErrorBanner + PreSettlementsPanel honest empty; guard verify-disp-s33-s36-surface --selftest exit 0. / PROD-VERIFIED 2026-08-09 Neon br-fancy-credit-akjnd07a + lucia + USMCA: driver_finance.driver_settlements n=8 (open=4, closed=3, presettle=1 display_id=S-2026-0001). Route /dispatch/pre-settlements → DispatchSecondaryTabRoute subTab=pre_settlements → PreSettlementsPanel; listSettlements / open-by-driver APIs company-gated. healthz/shallow version=49ff971. | — |
| `DISP-S34` | **PASS** | Surface /dispatch/settings renders, is entity-scoped, and shows an honest empty state | Historical acceptance definition. Page component: apps/frontend/src/pages/dispatch/DispatchSettingsPage.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PASS 2026-08-09 — DispatchSettingsPage need-company + ListErrorBanner on prefs; guard pack --selftest exit 0. / PASS 2026-08-09 — apps/frontend/src/pages/dispatch/DispatchSettingsPage.tsx renders 4 preference sections (default view, sort, alert thresholds, auto-routing). Default view is a per-user preference (GET/PATCH /api/v1/dispatch/preferences — auth-session scoped, not company-scoped by design, same as any other user-settings surface); sort/thresholds/auto-routing are stored in localStorage, both intentionally not entity data (no backing table that could be "empty"). FIXED a swallowed-error gap: getDispatchPreferences failing silently fell back to the "home" default with no indication the saved preference never loaded. Added a named error message ("Couldn't load your saved default view...") shown on fetch failure. Regression test: DispatchSettingsPage.test.tsx (4 tests, all green) covers render, prefs-load, PATCH-persist, and the new honest-error case. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA selected — /dispatch/settings rendered default-view, sort, thresholds, and auto-routing controls; GET /api/v1/dispatch/preferences returned 200. Preferences are intentionally authenticated-user scoped and localStorage-backed, not company data, so no entity-table empty state applies; contemporaneous entity-bearing app calls carried operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80 and returned 200. Existing DispatchSettingsPage tests cover the named load-error path. | — |
| `DISP-S35` | **PASS** | Surface /dispatch/settlements renders, is entity-scoped, and shows an honest empty state | Historical acceptance definition. Page component: apps/frontend/src/pages/Dispatch.tsx (resolved from routes/manifest.tsx, local wrappers followed). PASS REQUIRES, on TRANSP + TRK + USMCA: (1) the route renders without error or blank frame; (2) every data call it fires carries operating_company_id for the active entity and returns 2xx; (3) when the backing table has no rows for that entity the surface shows an HONEST EMPTY STATE — named, explaining what would populate it — never a silent blank, a spinner that never resolves, a zero presented as a fact, or a swallowed error. Row density is NOT required and NOT sufficient. Prior bar and why it changed: see the module note and docs/trackers/DISP-MAINT-SURFACE-SWEEP-BLOCKED-ON-DATA-2026-07-31.md. / PASS 2026-08-09 — /dispatch/settlements quicklink honest empty + need-company; Driver Finance is the list home; guard pack --selftest exit 0. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/settlements rendered without error; all entity-bearing requests returned 200, including /api/v1/driver-finance/settlements?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80. The surface honestly explains that runs, acknowledgements, and payouts live in Driver Finance and provides View all settlements → /driver-finance/settlements rather than a silent empty list. | — |
| `DISP-S36` | **PASS** | Surface /dispatch/trip-pairing renders, is entity-scoped, and shows an honest empty state | COMPONENT RESOLVED on origin/main and live SHA 4e5db76: apps/frontend/src/routes/trip-pairing-board.routes.ts mounts TripPairingBoardPage at /dispatch/trip-pairing. Earlier live click-through was pending (DOD-A/B/C on TRANSP+TRK+USMCA). Prior text said UNRESOLVED — that was stale vs code. / PASS 2026-08-09 — TripPairingBoardPage need-company + ListErrorBanner + board honest empty; company-gated getTripPairingBoard; guard pack --selftest exit 0. / PROD-VERIFIED 2026-08-28 GO-0023 on healthz 4e5db76, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80 — /dispatch/trip-pairing resolved to TripPairingBoardPage and rendered a populated board (29 active trucks, 7 assigned trips, linked units/drivers/loads); GET /api/v1/dispatch/trip-pairing-board?operating_company_id=5c854333-6ea5-4faa-af31-67cb272fef80 returned 200. The empty branch was not invoked by populated live data; existing guard coverage proves company gating, ListErrorBanner, and the named honest-empty board. | — |
| `DISP-S37` | **UNVERIFIED** | Surface /dispatch/trip-profitability renders, is entity-scoped, and shows an honest empty state | REOPEN 2026-08-29 OWNER: unbound prose evidence is not a live proof (no Neon/HTTP/browser artifact). prod_verified false until GUARD packet + live_verified_sha. Prior: PASS 2026-08-09 — TripProfitability ParityTable settled empty; verify-list-empty-settled. | — |

Desktop audit: —
