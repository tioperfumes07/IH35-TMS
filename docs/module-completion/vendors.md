# Module completion — Vendors

**PROGRESS: 7 of 9** · complete: `false` · **in-progress (bar-2)** · as_of: 2026-08-30T01:20:00.000Z · live_sha: `pending-cert-01`

| Status | Count |
|---|---:|
| PASS | 7 |
| HOLD | 0 |
| OPEN | 2 |
| FAIL | 0 |
| UNVERIFIED | 0 |

| ID | Status | Title | Evidence | PR |
|---|---|---|---|---|
| `VEND-S01` | **PASS** | /vendors roster active count matches Neon mdata.vendors | LIVE 2026-08-29 Neon lucia br-fancy-credit-akjnd07a: USMCA total=142 active=123 inactive=19 (1 sample, 95 driver-linked, 28 non-driver, 0 QBO-mirrored). TRANSP total=958 active=571 inactive=387. TRK total=1873 active=1873. Stale 2026-08-03 stamp said USMCA=4 — FALSE, live Neon=123. Vendors.tsx uses listAllVendors (wraps listVendors with pagination) + active_company_only=true. Guard verify-vend-s01-roster-active-filter.mjs fixed to accept listAllVendors + updated Neon counts. CLOSED THE PENDING CHROME CLICK-THROUGH (CC-1, 2026-08-29, SHA b276443): live /vendors (USMCA Freight Solutions Inc context) shows All (148) / Active (129) / Inactive (19); same-turn Neon read (current_user=neondb_owner, control mdata.drivers=271 nonempty) confirms USMCA total=148 active=129 inactive=19 on the SAME SHA — exact parity, roster count grew from the prior snapshot (new vendors created since 08-24, inactive count unchanged at 19), no drift between UI and Neon. | — |
| `VEND-S02` | **PASS** | Vendor detail factor schedule uses factoring.factor not notes parse | 2026-08-03 Cursor #4211: FactoringHome profile PATCH advance_rate/fee_rate/reserve_rate + remittance_details on factoring.factor; VendorDetail strips factoring notes block + relocates schedule UI to /factoring. Guard verify-fact-dual-canonical-profile step 2242. Neon Faro TRANSP flat rates 0.97/0.015/0.015. RE-VERIFIED LIVE (CC-2, 2026-08-29, SHA b2448ce): guard re-run PASS; Neon lucia (bypass_rls) direct read factoring.factor id=5240378c-1fcb-42e3-8c83-05c52ce44435 "Faro Factoring Full Recourse V1" TRANSP: advance_rate=0.9700 fee_rate=0.0150 reserve_rate=0.0150, exact match, not voided. | 4211 |
| `VEND-LINK-01` | **PASS** | Bills/allocations EntityLink uses vendor_uuid not legacy vendor_id text | 2026-08-04 Cursor: BillsPage, BillDetailPage, BillPaymentsListPage, BillPaymentDetailPage, AllocationsPage EntityLink kind=vendor id=mdata_vendor_id (label may fall back to legacy vendor_id text). Backend bills.service + allocations.service expose mdata_vendor_id. Guard verify-vend-link-01-vendor-uuid-entitylink composes verify-bill-vendor-link-canonical-uuid step 1969. Step 2356. RE-VERIFIED LIVE (CC-2, 2026-08-29, SHA b2448ce): both guards re-run — verify-bill-vendor-link-canonical-uuid OK (every bill/payment/allocation vendor drill-through uses the canonical uuid), verify-vend-link-01-vendor-uuid-entitylink OK. | — |
| `VEND-S03` | **PASS** | Vendor create dedup guard scoped per operating company | 2026-08-03 Cursor: vendors.routes resolves operating_company BEFORE dedup; match is lower(btrim(vendor_name))+opco+deactivated_at IS NULL (G6-2). Guard verify-vend-s03-s04-dedup-and-types + step 2238. RE-VERIFIED LIVE (CC-2, 2026-08-29, SHA b2448ce): direct source read of apps/backend/src/mdata/vendors.routes.ts confirms vendorNameConflictExists (line 257) queries `lower(btrim(vendor_name)) = lower(btrim($1)) AND operating_company_id = $2::uuid AND deactivated_at IS NULL` exactly as claimed, under setScopedCompanyContext (membership-asserted). Create path (line 492-498) calls resolveOperatingCompanyId BEFORE vendorNameConflictExists; PATCH path (line 705) does the same via patchScopedCompanyId. Guard re-run: OK. | — |
| `VEND-S04` | **PASS** | Vendor types catalog-backed (catalogs.vendor_types populated) | 2026-08-03 Neon lucia: catalogs.vendor_types 8/TRANSP · 8/TRK · 8/USMCA. VendorCreateModal + NewVendorDrawerForm createKind=vendor_type → catalogs.vendor_types. Guard step 2238. LIVE RE-VERIFIED (CC-1, 2026-08-29, SHA b276443): raw count(*) for USMCA is now 10, not 8 — investigated before trusting either number (never-guess law): Neon shows the 8 original is_system=true/is_active=true rows (Fuel/Repair/Tires/Towing/Insurance/Permit/Toll/Other) unchanged, PLUS 2 is_active=false rows created 2026-08-22/23 by another seat ("CC3 Test Vendor Type", "CC3 TEST Vendor Type", is_system=false) — TEST fixtures, correctly deactivated not deleted (void-not-delete), not a regression. Live Chrome /vendors?create=1 (USMCA Freight Solutions Inc) Vendor type dropdown shows exactly the same 8 active options, confirming the picker correctly filters is_active=true and the 2 inactive TEST rows are properly hidden from create flow. UI-to-DB parity confirmed at the ACTIVE-only layer, which is the layer this item's acceptance actually depends on. | — |
| `VEND-S05` | **PASS** | Inactive vendor filter excludes 387 inactive from default Active view | 2026-08-03 Neon lucia TRANSP inactive=387 exact. Vendors.tsx default listStatus="active" filters deactivated_at==null; data-list-status-filter=vendors. Guard verify-vend-s01-roster-active-filter + step 2236. RE-VERIFIED LIVE (CC-2, 2026-08-29, SHA b2448ce): structural re-read of Vendors.tsx confirms parseVendorListTab(searchParams.get("listTab") ?? "active") defaults to active, listStatus==="active" filters vendor.deactivated_at==null, data-list-status-filter="vendors" present. Neon lucia (bypass_rls) direct count this turn: TRANSP active=571 inactive=387 total=958 -- inactive count matches exactly. Guard re-run: OK, TRANSP active=564 inactive=387 (guard's own live query, same inactive count). | — |
| `VEND-VERIFY-01` | **PASS** | Vendors module VERIFY-1..8 TRANSP + USMCA | 2026-08-04 Cursor: meta guard verify-vend-verify-01 composes S01 roster (TRANSP 564/USMCA 4 lucia), S03/S04 dedup+types, S02 fact-dual canonical profile, VEND-LINK-01 bill/allocation vendor uuid EntityLink; manifest /vendors + /vendors/:id; VendorDetail vendor-factor-schedule-relocated + emptyFactoringProfileMeta. Steps 2252 + 2356. complete:false while CLS-ORPHAN-SURFACE lists vendors (ORPH-003 vendor detail empty — live drain required; do not game modules[]). RE-VERIFIED LIVE (CC-2, 2026-08-29, SHA b2448ce): guard re-run PASS (7 of 7 checklist; complete_flag per false-green law). ORPH-003 remains a genuine, separate OPEN data-model gap (vendor has no ACH/bank field at all, owner decision pending) -- does not contradict this item's own VERIFY-1..8 composition, not claimed here as module completeness. | — |
| `VEND-CERT-01` | **OPEN** | Fully-Wired 7–12 on current healthz (URL tabs, responsive master-detail, leftover audit PATCH closed) | OPEN 2026-08-29 CERT-01: item stamps VEND-S01..VERIFY-01 are not module certify. Master-detail must stack (Customers pattern). Tabs URL-addressable. FW 12 only after 1–11 Built on then-current healthz. Do not set complete:true until this item PASS + prod_verified by CC-2. 2026-08-30 Cursor: additive proofs[] (SPA /vendors + API GET mdata/vendors, mount≠404) for shadow-report only — typed OPEN stays; no proven_at_sha so derived cannot be PASS; Live Chrome FW 8–12 still owed on then-current healthz. 2026-08-30 CC-3 (live Chrome, authenticated, USMCA): confirmed 3 specific sub-items live — (1) master-detail /vendors?tab=vendor_details is genuinely URL-addressable (tab persists across navigate/reload, verified via window.location); a selected ROW is not (reload falls back to the first row) — same gap exists on /customers today, not a Vendors-specific regression, not yet fixed anywhere. (2) resized 1400px->480px: master-detail correctly STACKS the detail card below the list at narrow width (list, then selected-vendor detail, then Summary) — matches the required Customers-pattern behavior, no defect found. (3) the 'leftover audit PATCH' item is CLOSED: navigated to the standalone /vendors/:id detail page (not the master-detail drawer — a separate route with Profile/A-P/Documents/Audit History/Tasks/W-9-1099 tabs, also URL-tab-addressable via ?tab=audit), confirmed live that vendor d9bb802f-fad4-421c-a41e-a1e834e33362's Audit History tab now shows its real 3 events (created/updated x2) instead of the prior false-empty state (fixed earlier this session, 85e3a3f7e9 / PR #18296). NOT YET CHECKED this pass: the remaining FW 7-11 surface-bar/chrome-law/pickers/security items across the rest of the module (this was a targeted spot-check of the 3 items named in this row's own evidence text, not a full 12-item sweep). Still OPEN, still not self-certifying — status/prod_verified left to CC-2 per this row's own rule. 2026-08-30 CC-3 continued (same session, live Chrome, item 12 create->save->reload->reverse-drill proof): also found+fixed a real defect while spot-checking — both /vendors and /customers master-detail selected ROW (not just tab) was plain in-memory state, resetting to the first row on reload; fixed to a URL param mirroring the existing tab param mechanism (scripts/verify-master-detail-selected-row-url-addressable.mjs, PR #18442, Live=UNVERIFIED pending deploy). Then ran a full create/save/reload/reverse-drill cycle on a real TEST vendor (id 51e7280b-6af9-4126-ae6d-d956e6a74314, 'CC3 Vend-cert-01 Verify TEST', held per CREATE-TEST-THEN-VOID law, not voided): Create Vendor wizard (Vendor Type combobox '+ Add new vendor type' first row, Escape restores prior value without forcing a bad selection, Default expense account combobox '+ Add new account' first row) -> Save -> vendor.created audit event appears immediately on its own Audit History tab (1 row, mdata.vendors.created) -> reload of the standalone /vendors/:id detail page persists correctly (this route IS id-in-URL, unlike the master-detail drawer's gap just fixed) -> roster search 'CC3 Vend-cert-01' finds it, All/Active counts increment by 1, master-detail auto-selects it showing the entered Notes verbatim. Also walked all 6 standalone-detail-page tabs (Profile/A-P/Documents/Audit History/Tasks/W-9-1099) on the pre-existing CC3 Verify Vendor record: all show honest empty states ('No bills for this vendor', 'No tasks linked to this record yet', etc.), correct '+ Create'/'+ Upload' vocab, W-9/1099 panel is notably honest about its own read-only/unverified-category caveats. One residual, not-fixed observation: the Default-expense-account picker lists a 'ZZ-SAMPLE A' test account alongside real ones -- catalogs.accounts has no is_sample_data column to filter it, matching the known test-data-in-prod-tables class needing an owner ruling, not a unilateral filter. Still not self-certifying this item -- this is now substantial FW-12-shape live evidence, but items 7/8/9/10/11 have not been swept exhaustively across the whole module, only spot-checked incidentally while doing the above. | — |
| `VEND-TIEOUT-01` | **OPEN** | Sum of open bills equals AP control account balance | OPEN 2026-08-30 bar-2. Auto_check executable exists; FAIL until live expected cents match (R2 empty never PASS). Do not stamp prod_verified. Bar-1 items unchanged. | — |

Desktop audit: ~/Desktop/IH35-CURSOR-AUDIT/AUDITOR-RUN-2026-07-31/modules/factoring-deep-2026-08-01.md
