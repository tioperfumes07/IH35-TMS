[
  {
    "id": "LAW-2026-08-06-CI-CONCURRENCY-SHA-SCOPED",
    "title": "A workflow may not key its concurrency group on a bare ref — a queued run that never executed can otherwise be cancelled into a permanently dead required check, leaving the PR unmergeable with no code failure",
    "source_file": ".github/workflows/ci.yml",
    "guard": "scripts/verify-ci-concurrency-sha-scoped.mjs",
    "type": "enforced"
  },
  {
    "id": "LAW-2026-08-06-KANBAN-DROPSTATUS-CONTRACT",
    "title": "Every dispatch Kanban lane dropStatus must be a status loadStatusSchema accepts — a lane whose drop the API rejects is a silent dead end, and the frontend lane list and backend enum live in separate files owned by separate lanes so they drift independently",
    "source_file": "apps/frontend/src/components/dispatch/DispatchKanban.tsx",
    "guard": "scripts/verify-kanban-dropstatus-contract.mjs",
    "type": "enforced"
  }
]
