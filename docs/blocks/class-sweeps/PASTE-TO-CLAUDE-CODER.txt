================================================================================
PASTE → CLAUDE CODER  (UI / operational lane)
IH35-TMS class-sweeps · built to the 8-Layer Connected-Closure Standard · 2026-07-26
================================================================================

You own the UI/UX + operational classes. Deploys are unblocked (#3558 merged,
main = be5bf0b). Build the SHARED PRIMITIVES first — they make the rest one-line sweeps.

YOUR BLOCKS, IN ORDER:
  1. C7 — create surfaces use the shared 480px right DRAWER, not a centered modal.
         Build the drawer variant ONCE on the shared component → propagates to 38 surfaces.
  2. C1 — EntityPicker: replace every raw-UUID input (inline +Add-new first row, writes
         the same canonical table it reads). Composes with the C7 drawer.
  3. C3 — shared DatePicker: replace ~20 native type=date/datetime-local inputs.
  Then ride the primitives:
  4. C5 — canonical load navigation (EntityLink kind=load → /dispatch/loads/:id).
  5. C8 — no dead KPI cards (required-drill card component).
  6. C9 — no rendered field dropped on save (form round-trip contract; HELD where a
         missing column is needed — e.g. reorder_threshold, claim_number).
  7. C10 — every defined route mounted (route-manifest parity; 401 not 404).
  8. C4 — server-persist business settings (HELD if a settings table/columns are new).
ALSO: finish the demo-purge #3557 (recursive cascade) packaging — GUARD re-verifies on a
fresh branch before Jorge applies; close #3530 (superseded). Rebase #3556 on main (#3558).
Full spec for each is in blocks/C7-*.txt … blocks/C4-*.txt.

NON-NEGOTIABLE RULES (Law of the Land — read it in the repo/project, do not restate it):
  • GUARD-FIRST: ship the failing verify-step guard (scripts/verify-steps/NNNN-*.mjs,
    --selftest capable of failing) BEFORE the fix. Its red output IS your worklist.
    Never weaken a guard to go green — ratchets only tighten.
  • 8 LAYERS, every block: score L1 data · L2 accounting(N/A for pure-UI, with reason) ·
    L3 API (route mounted 401-not-404, typed, authZ, atomic where multi-write) · L4 UI
    (drawer, picker law, DatePicker, click-through, empty/loading/error, EN/ES, a11y) ·
    L5 linkage both-way · L6 security (no uuid in URL, RLS) · L7 guard+component+e2e ·
    L8 closure. Each layer = PASS(live proof) or N/A(reason). No silent skips.
  • PICKER LAW: inline "+ Add new" is the FIRST ROW inside the dropdown, opens that
    entity's wizard, WRITES the same canonical table the picker reads, survives reload,
    company-scoped. A raw-UUID text input is an automatic FAIL.
  • CLICK-THROUGH EVERYWHERE: every row/KPI/card navigates or opens the editable drawer —
    no dead clicks. Honest UI: a number is live-backed or clearly "—".
  • CANONICAL: read/write the canonical table for each kind (Part A §4) — never a RETIRE
    table, never the QBO mirror. Any schema change is HELD (Jorge applies on Neon).
  • PR body carries the Rule 16 evidence block: ROOT CAUSE / FIX / GUARD / LIVE PROOF
    (on TRANSP AND USMCA, or "UNVERIFIED + named blocker") / REMAINING. One block per PR.
  • You never self-review your own block (Rule 11). GUARD verifies live before any merge.

DELIVER PER BLOCK: the guard PR (red inventory) → then the fix PR (guard green + 8-layer
evidence + live proof on TRANSP AND USMCA). Stop and flag GUARD on any UNVERIFIED.
================================================================================

────────────────────────────────────────────────────────────────────────────────
MERGE + BRANCH + NO-COLLISION (Claude Coder) — full rules in 00-RULES-OF-ENGAGEMENT-NO-COLLISION.txt
────────────────────────────────────────────────────────────────────────────────
YOU merge your own PRs. Jorge only applies the JORGE-APPROVED label (when a PR is HELD/financial/
  migration); then YOU click squash-merge. Self-mergeable UI/docs PRs: merge on green. YOU fix your
  own CI failures and resolve your own conflicts. Never merge Cursor's PR.

FILES YOU OWN: apps/frontend/** incl. the shared components you build (Modal/CreateDrawer, EntityPicker,
  forms/DatePicker.tsx), all frontend sweep call-sites, the backend app-router registration index (C10),
  the server settings endpoint + table (C4), scripts/verify-steps NNNN band 1550–1599.
FILES YOU MUST NOT TOUCH: db/migrations/*.sql that are financial/canonical/RLS, backend money routes,
  the posting service, RETIRE-repoint code. If a sweep needs a money/schema change, tell GUARD — do not
  reach across. (Your own C4/C9 schema migrations are fine, in YOUR migration band, HELD.)
SHARED HOT FILES (serialize via GUARD, rebase before merge): db/migrations/.held-migrations.json,
  scripts/lib/migration-checksum-overrides.json, scripts/lib/prod-migration-ledger-checksums.json.
MIGRATION NUMBERS (only C4/C9 if a column is missing): your band 20260911xxxx–20260920xxxx; verify max
  on all refs first; one per PR; never edit an applied migration; HELD (Jorge applies on Neon).
BRANCHES: git fetch origin && git switch -c sweep/c7-<slug> origin/main. One block = one branch = one PR.
BEFORE MERGE: git fetch && git rebase origin/main; resolve your conflicts; CI all green.
FIRST, clear the hot-file queue: rebase #3556 (registry truth-up) on main (#3558) and merge it FIRST
  (it edits .held-migrations.json — merging it first unblocks the others). Then #3549 (LST-RLS-01, file
  == ledger 1deda060) once Jorge labels. THEN build C7 (drawer) → C1 → C3.
================================================================================
