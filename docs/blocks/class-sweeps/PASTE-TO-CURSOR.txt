================================================================================
PASTE → CURSOR  (financial / canonical / RLS lane)
IH35-TMS class-sweeps · built to the 8-Layer Connected-Closure Standard · 2026-07-26
================================================================================

You own the LEDGER-INTEGRITY + STOP classes. Deploys are unblocked (#3558 merged,
main = be5bf0b). Work these in order. Each is GUARD-FIRST and HELD.

YOUR BLOCKS, IN ORDER:
  1. C6 — money-table INSERT must post a balanced JE via the existing poster (HIGHEST).
         Your guard #3555 is already open — finish it, then wire each flagged write.
  2. C2 — forbid live writes to RETIRE schemas; repoint the ~5 live writers to canonical.
  3. C11 — same-entity split-brain: one canonical table per entity; repoint + backfill.
Full spec for each is in blocks/C6-*.txt, blocks/C2-*.txt, blocks/C11-*.txt.

NON-NEGOTIABLE RULES (Law of the Land — read it in the repo/project, do not restate it):
  • GUARD-FIRST: ship the failing verify-step guard (scripts/verify-steps/NNNN-*.mjs,
    --selftest capable of failing) BEFORE the fix. Its red output IS your worklist.
    Never weaken a guard to go green — ratchets only tighten.
  • 8 LAYERS, every block: score L1 data · L2 accounting · L3 API (ATOMIC txn +
    idempotency/exactly-once + authZ/WF-064) · L4 UI · L5 linkage both-way · L6
    security+ALERT on money-post/sync failure · L7 guard+tests+$0.05 live smoke ·
    L8 closure. Each layer = PASS(live proof) or N/A(reason). No silent skips.
  • MONEY: reuse the EXISTING posting service — NO new GL math. Balanced JE (header+lines).
    Sub-ledger reconciles to GL. Reversal = reverse-and-replace. Flags default OFF.
    Reconcile-only — NEVER write back to QBO. Honor owner money-locks (ask-rails,
    expense-vs-capitalize, deductible Option C).
  • CANONICAL: never write a RETIRE table. Fix the WRITER onto canonical (Linkage Law §1).
    Rule 07 — archive the retired table, never DROP. Backfill verified (counts reconcile).
  • HELD: you CANNOT run prod DDL. Deliver the migration in the PR; Jorge applies it on
    Neon (neondb_owner) and ledger-backfills; GUARD re-proves the ledger row. Order:
    migration → prove schema → THEN ledger stamp (never stamp before schema is proven).
  • PR body carries the Rule 16 evidence block: ROOT CAUSE / FIX / GUARD / LIVE PROOF
    (on TRANSP AND USMCA, or "UNVERIFIED + named blocker") / REMAINING. One block per PR.
  • You never self-review your own block (Rule 11). GUARD verifies live before any merge.
  • #3551 (safety company-paid civil fine → no ledger) is a C6 instance — fold it into C6
    unless GUARD rules to keep it a standalone domain block.

DELIVER PER BLOCK: the guard PR (red inventory) → then the fix PR (guard green + 8-layer
evidence + live $0.05 smoke proof). Stop and flag GUARD on any UNVERIFIED, don't guess.
================================================================================

────────────────────────────────────────────────────────────────────────────────
MERGE + BRANCH + NO-COLLISION (Cursor) — full rules in 00-RULES-OF-ENGAGEMENT-NO-COLLISION.txt
────────────────────────────────────────────────────────────────────────────────
YOU merge your own PRs. Jorge only applies the JORGE-APPROVED label; then YOU click squash-merge.
YOU fix your own CI failures and resolve your own conflicts. Never merge Claude Coder's PR.

FILES YOU OWN: db/migrations/*.sql (financial/canonical/RLS), backend money routes + posting-
  service wiring, RETIRE-repoint code, scripts/verify-steps NNNN band 1520–1549.
FILES YOU MUST NOT TOUCH: apps/frontend/** shared components (Modal/CreateDrawer, EntityPicker,
  forms/DatePicker.tsx), frontend sweep call-sites, the app-router registration index, the
  settings endpoint. If your block needs one of those, tell GUARD — do not reach across.
SHARED HOT FILES (serialize via GUARD, rebase before merge): db/migrations/.held-migrations.json,
  scripts/lib/migration-checksum-overrides.json, scripts/lib/prod-migration-ledger-checksums.json.
MIGRATION NUMBERS: your band 20260901xxxx–20260910xxxx; verify max on all refs first; one per PR;
  never edit an applied migration. GUARD already authored 202608160000 (Mexico 6890) — you commit it.
BRANCHES: git fetch origin && git switch -c sweep/c6-<slug> origin/main. One block = one branch = one PR.
BEFORE MERGE: git fetch && git rebase origin/main; resolve your conflicts; CI all green; migration
  delivered in the PR for Jorge to apply on Neon (migration → prove → ledger); GUARD re-proves live.
FIRST, help clear the hot-file queue: after #3556 merges, rebase + merge #3554 (LINK-02, file already
  == ledger 4d66df41) once Jorge labels it. THEN start C6.
================================================================================
