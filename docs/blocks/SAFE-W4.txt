═══════════════════════════════════════════════════════════════════════════════
SAFE-W4 — SAFETY/PWA / TASK — Safety/PWA wave W4
PR URL: <paste PR link here>
═══════════════════════════════════════════════════════════════════════════════
STATUS: VERIFY — W4 shipped #877–883; safety module LOCKED (CLAUDE.md §7).

NOTE: inventory-derived (coder's repo mining), NOT GUARD-live-verified. This is a TRACKING placeholder so
the work is not forgotten. Before building: confirm CURRENT live state + the relevant docs/specs/ spec +
approved-screen PNG + the exact tracker phase/task ID (RESPOND-BEFORE-CODE). Do not invent scope.

LOCK: additive-only; per-entity (TRK/TRANSP/USMCA share nothing); reuse shared table + parity components;
Samsara/Block-20/timezone laws as applicable; North-Star.

DO: Safety + driver PWA wave W4 (scope per safety roadmap). Decompose into sub-blocks against the spec once current state is confirmed; one PR each.

ACCEPTANCE (GUARD live): each sub-surface renders real data per entity to the spec; gated where it posts.

STANDING ORDERS (Cursor): foreground only, no subagents. No retries — STOP and paste the exact error. Live updates every 5 min CST (Laredo). Confirm worktree pwd + git status + rev-parse first. Show diff --staged --stat before commit. Stop on anything unexpected.
═══════════════════════════════════════════════════════════════════════════════

--- ARTIFACTS ON MAIN (evidence for reconcile classifier) ---
STEP-0 evidence sweep 2026-06-26: this block is BUILT on main. Real signature artifacts (verified present):
  - apps/backend/src/safety/medical-cards.routes.ts
  - apps/backend/src/safety/reminders.routes.ts
  - apps/backend/src/telematics/hos-tracker.routes.ts
