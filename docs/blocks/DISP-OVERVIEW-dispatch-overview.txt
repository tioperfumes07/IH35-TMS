═══════════════════════════════════════════════════════════════════════════════
DISP-OVERVIEW — DISPATCH / TASK Dispatch Overview — Dispatch Overview
PR URL: <paste PR link here>
═══════════════════════════════════════════════════════════════════════════════
STATUS: ✅ DONE — verify only (overview, PR #752 + #1106, row 567).

NOTE: inventory-derived (coder's repo mining), NOT GUARD-live-verified. Confirm CURRENT live state +
the DISPATCH-MODULE-SPEC.md / approved-screen PNG before building (RESPOND-BEFORE-CODE).

LOCK: additive-only; per-entity; reuse shared table + parity components; America/Chicago.

DO: Dispatch overview dashboard (KPIs, open loads, exceptions). Build to the spec/approved screen; reuse locked components.

ACCEPTANCE (GUARD live): surface renders real data per entity to the spec; no bespoke table (use shared).

STANDING ORDERS (Cursor): foreground only, no subagents. No retries — STOP and paste the exact error. Live updates every 5 min CST (Laredo). Confirm worktree pwd + git status + rev-parse first. Show diff --staged --stat before commit. Stop on anything unexpected.
═══════════════════════════════════════════════════════════════════════════════

--- ARTIFACTS ON MAIN (evidence for reconcile classifier) ---
STEP-0 evidence sweep 2026-06-26: this block is BUILT on main. Real signature artifacts (verified present):
  - apps/frontend/src/pages/dispatch/DispatchOverview.tsx
  - apps/backend/src/dispatch/arch-tabs.routes.ts
