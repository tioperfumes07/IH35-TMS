# Screenshots (placeholders)

PNG files in this folder are **minimal placeholders** so links in the user guides resolve in Git.

**Before MVP (May 22):** replace each file with a **real capture** from `https://app.ih35dispatch.com` or `https://driver.ih35dispatch.com`, using a test account where possible.

**Rules**

- No customer names, phone numbers, full addresses, SSNs, bank balances, or live settlement dollar amounts unless blurred.
- Prefer **staging** if it mirrors production UI.
- Keep filenames stable so markdown links do not break.

**Assign captures by persona**

- `driver/` — Driver PWA (mobile width).
- `dispatcher/` — Office app as Dispatcher role (portrait or narrow desktop optional for “field” shots).
- `owner/` — Office app as Owner / Administrator (desktop or tablet).

_Last updated: 2026-05-14_
