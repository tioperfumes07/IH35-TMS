# Security Audit — 2026-06-05

**Block:** CLOSURE-19-SEC-AUDIT (Agent B, Lane B)  
**Branch:** `closure/sec-audit`  
**Scope:** RLS, auth flows, secrets, dependency CVEs, CORS/CSP — audit scripts only (no production code changes)

## Executive summary

Security audit tooling and CI workflow added. Cross-tenant isolation probe **passes** on live DB (TRANSP fixture invisible under USMCA scope). One RLS gap documented (`identity.email_verifications`). No secrets detected in bundle scan patterns. Zero critical/high CVEs in production dependency trees (`npm audit --omit=dev`). CORS allowlist is explicit; security response headers (CSP, HSTS, X-Frame-Options, Referrer-Policy) are **not** configured in application code — documented as accepted exceptions in [security-baseline.md](../security-baseline.md).

---

## 1. RLS policies (`sec-audit-rls-policies.mjs`)

**Schemas audited:** `app`, `mdata`, `identity`

| Check | Result |
|-------|--------|
| Static guards present (`verify:rls-operating-company-scope`, `verify:no-cross-carrier-data-leak`) | PASS |
| Tables audited (live DB) | 49 |
| RLS disabled | **1 gap:** `identity.email_verifications` |
| Missing policies | **1 gap:** `identity.email_verifications` |
| Weak policy names (manual review) | 33 tables flagged — policy names lack obvious tenant keyword; most use `operating_company_id` column via separate CI guard |
| Cross-tenant probe (TRANSP → USMCA) | **PASS** |

### Gap detail: `identity.email_verifications`

Pre-authentication email verification tokens. No `operating_company_id` column. Accessed only through Lucia bypass paths. **Recommendation:** add RLS deny-all + bypass policy in a future migration block (not in CLOSURE-19 scope).

---

## 2. Auth flows (`sec-audit-auth-flows.mjs`)

| Check | Result |
|-------|--------|
| Session cookies httpOnly | PASS (OAuth PKCE + Lucia clear options) |
| Secure + SameSite | PASS — prod: `Secure` + `SameSite=None`; dev: `lax` |
| JWT algorithms | N/A — Lucia opaque session IDs (no client JWT) |
| Password reset one-time tokens | PASS — UUID, 1h expiry, `used_at` on confirm |
| Refresh token rotation | N/A office auth; QBO refresh tokens server-side only |
| Logout invalidates session | PASS — `lucia.invalidateSession` + cookie clear |
| Google OAuth PKCE | PASS |
| OAuth returnTo allowlist | PASS |

---

## 3. Secrets scan (`sec-audit-secrets-scan.mjs`)

| Surface | Result |
|---------|--------|
| `apps/frontend/dist/*.js` | No matches (when built) |
| `apps/driver-pwa/dist/*.js` | No matches (when built) |
| Git history (gitleaks) | Informational in CI — findings require Jorge-approved rotation |

**Policy:** Document only; no auto-fix or history rewrite.

---

## 4. Dependency CVEs (`sec-audit-deps-cve-scan.mjs`)

**Command:** `npm audit --omit=dev` on backend, frontend, driver-pwa

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 0 |
| Moderate | 7 |
| Low | 0 |

### Critical/high upgrade plan

None required at this time.

### Moderate items (sample)

| Package | Dependency | Advisory | Upgrade path |
|---------|------------|----------|--------------|
| backend | `ws` | GHSA-58qx-3vcg-4xpx (moderate) | Bump transitive via parent package update; low breaking-change risk |
| frontend | (transitive) | various moderate | `npm audit fix` where semver-safe; review Vite/plugin chain |

---

## 5. CORS / CSP / headers (`sec-audit-cors-csp.mjs`)

| Header | Backend | Frontend HTML | Driver PWA HTML |
|--------|---------|---------------|------------------|
| CORS allowlist (not `*`) | PASS | — | — |
| Content-Security-Policy | **NOT FOUND** | **NOT FOUND** | **NOT FOUND** |
| Strict-Transport-Security | **NOT FOUND** | — | — |
| X-Frame-Options | **NOT FOUND** | — | — |
| Referrer-Policy | **NOT FOUND** | — | — |

**Note:** Render may inject HSTS at the edge for `*.onrender.com` / custom domains. Application-level helmet middleware recommended for a future hardening block.

### PWA service worker

- Cache name: `ih35-driver-shell-v2`
- Cached URLs: static shell only (`/`, `index.html`, manifest, icons)
- **No API response caching detected** — PASS

---

## 6. CI artifacts

| File | Purpose |
|------|---------|
| `.github/workflows/security-checks.yml` | PR + nightly security pipeline |
| `scripts/verify-no-secrets-in-bundle.mjs` | Fail PR on secret patterns in dist JS |

---

## Forensic 5-point

1. **Manifest first:** `.block-ready.json` / `.block-ready.agent2.json` updated for CLOSURE-19-SEC-AUDIT  
2. **Allowed files only:** No production code modified  
3. **CI guard:** `verify-no-secrets-in-bundle.mjs` + `security-checks.yml`  
4. **Cross-tenant:** PASS on live probe  
5. **Owner sign-off:** [security-baseline.md](../security-baseline.md) — Jorge, 2026-06-05

---

## Verification commands

```bash
node scripts/sec-audit-rls-policies.mjs
node scripts/sec-audit-auth-flows.mjs
node scripts/sec-audit-secrets-scan.mjs
node scripts/sec-audit-deps-cve-scan.mjs
node scripts/sec-audit-cors-csp.mjs
npm run build:frontend && npm run build:driver-pwa
node scripts/verify-no-secrets-in-bundle.mjs
```
