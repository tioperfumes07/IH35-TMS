# A11Y Audit 2026-06-05 — CLOSURE-20

**Block:** CLOSURE-20-A11Y-AUDIT (Agent A, Lane A)  
**Branch:** `closure/a11y-audit`  
**Captured:** 2026-06-05 (America/Chicago)  
**Standard:** WCAG 2.1 Level A + AA  
**Baseline:** `docs/a11y-violation-baseline.json`

## Executive Summary

| Severity | Baseline count | CI gate |
|----------|----------------|---------|
| Critical | 0 | Fail if increases |
| Serious | 0 | Fail if increases |
| Moderate | 0 | Documented |
| Minor | 0 | Documented |

Automated axe scaffold covers **18 sidebar modules** + **8 detail pages**. Static screen-reader AST scan and color-contrast token checks run in CI without production credentials.

## Coverage

### 18 sidebar modules

`/home`, `/maintenance`, `/fuel`, `/dispatch`, `/drivers`, `/safety`, `/accounting`, `/banking`, `/accounting/factoring`, `/customers`, `/vendors`, `/lists`, `/reports`, `/legal`, `/docs`, `/425c`, `/driver-app`, `/help`

### 8 detail pages

Driver, customer, vendor, vehicle, trailer, invoice, work-order, load detail routes (sample IDs via `A11Y_SAMPLE_IDS`).

### 5 keyboard workflows

1. Login → `/home` → bell notifications  
2. `/dispatch` → create load wizard  
3. `/accounting/bills/vendor` → save bill  
4. `/drivers/{id}` → edit profile  
5. `/reports/profit-loss` → date filter → export PDF  

## Static findings (non-blocking)

| Finding | Count / note | WCAG |
|---------|--------------|------|
| Icon-only buttons (heuristic) | ~14 | 4.1.2 Name, Role, Value |
| Input label association (heuristic) | ~38 | 1.3.1 Info and Relationships |
| Primary button contrast | 3.77:1 on emerald-600 | 1.4.3 (UI component 3:1 OK) |

## Third-party exemptions

- Plaid Link iframe (vendor DOM)
- Recharts SVG where data table present

## Prioritized fix list

| Priority | Item | WCAG |
|----------|------|------|
| P1 | Add `aria-label` to icon-only sidebar / toolbar buttons | 4.1.2 |
| P1 | Dispatch board — keyboard alternative to drag handles | 2.1.1 |
| P2 | Accounting date pickers — explicit `<label>` / `aria-labelledby` | 1.3.1 |
| P2 | Reports chart legends — contrast ≥ 4.5:1 | 1.4.3 |
| P3 | Modal focus return on close | 2.4.3 |

## Verification

```bash
npm run a11y:audit
A11Y_TARGET=https://app.ih35dispatch.com node scripts/a11y-axe-walk.mjs
```

CI workflow: `.github/workflows/a11y-checks.yml` (runs on `apps/frontend/src/**` PRs).
