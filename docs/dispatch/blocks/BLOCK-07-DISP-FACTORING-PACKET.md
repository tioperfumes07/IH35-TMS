AGENT-2 · Block 7 of 13 — PHASE Dispatch / TASK <add tracker row DISP-FACTORING-PACKET> — Factoring packet auto-assembly + FARO reserve tracker
SO: prepend BOX 0. CITES docs/specs/FACTORING-PACKET-AUTO-ASSEMBLY.md (authoritative).
SCOPE (ADDITIVE): Trigger Load.status=delivered AND POD uploaded+signed → auto-assemble packet (Invoice, BOL, POD, Rate Con, Accessorials). Statuses: NOT_FACTORED → PACKET_READY → SUBMITTED → ADVANCE_RECEIVED → RESERVE_RELEASED → CHARGED_BACK. Dispatcher must APPROVE before submit (never auto-submit). Factoring view = queue + FARO Reserve Tracker (invoices submitted count+$, advances received $, reserve held/Faro Escrow $, fees paid YTD $, chargebacks pending $, reserve release schedule). Load drawer Factoring tab (packet checklist + status + Approve&submit).
ACCOUNTING (no new financial code): ADVANCE_RECEIVED → auto JE per CPA Option A/B (FACTORING-ACCOUNTING-STRUCTURE.md); RESERVE_RELEASED → release JE; CHARGED_BACK → insurance.refund_obligation. Reuse Block-24/25 poster + listFactorReserveBalances + factoring.routes /summary + factoring batch.service.
RECONCILE: map per-load packet status (this spec) ↔ batch status (draft/submitted/funded/rejected in batch.shared.ts) — document the mapping, do not duplicate.
FILES: apps/frontend/src/pages/factoring/* (extend FactoringHome), components/dispatch/LoadDetailDrawer.tsx (add Factoring tab — see Block 12), backend factoring services (reuse). Migration only if a packet-status column is missing (timestamped, self-contained GRANTs).
ACCEPTANCE: packet auto-creates on delivery+POD; approve→submit; statuses post correct JEs via existing poster; reserve tracker totals reconcile to factoring views.
LANE LOCK: factoring files; LoadDetailDrawer tab coordinated with Block 12 (one writer for the drawer file per cycle).
