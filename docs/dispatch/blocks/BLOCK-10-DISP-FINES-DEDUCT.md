AGENT-1 · Block 10 of 13 — PHASE Dispatch / TASK <add tracker row DISP-FINES-DEDUCT> — Driver fines (internal + DOT) → settlement auto-deduction confirm/defer
SO: prepend BOX 0. No new financial code — reuse existing services.
SCOPE (ADDITIVE): when a fine is logged (Safety internal fine OR DOT fine), prompt how to deduct: ONE payment or SPLIT across N settlements. Create a driver_finance.auto_deduction_policies row (deduction_type='fine') — table + types already exist (migrations/0392-auto-deductions.sql; settlements/auto-deductions/apply.ts; useAutoDeductionPolicies.ts). On each settlement compute, auto-apply via settlement-deduction-cap.service.ts (net-floor cap; all-or-nothing roll-over to next settlement when it doesn't fit). In the settlement, dispatcher/payroll CONFIRMS or DEFERS each pending deduction via escrow-deduction-pending.service.ts (approvePendingDeduction / rejectPendingDeduction). Audit driver_finance.deduction.deferred_over_cap. Surface a "Fines & deductions" section in the Settlement tab + DeductionsSection.tsx.
FILES: Safety/Dispatch fine-entry → policy creation hook; LoadDetailDrawer Settlement tab confirm/defer UI (coordinate Block 12); reuse all named services. Migration only if fine→policy link column missing.
ACCEPTANCE: fine creates policy with chosen schedule; appears on settlements (one or rolled); per-settlement confirm/defer works + audits; respects net floor.
LANE LOCK: fine-entry + driver-finance UI; backend services reuse-only.
