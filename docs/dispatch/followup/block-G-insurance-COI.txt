Block G (follow-up) — PHASE <add tracker phase> / TASK <add tracker row, real ID> — Certificate of Insurance (COI) PDF generation   [RECOMMENDED — needs Jorge GO to dispatch]
RBC TARGET: insurance module backend (read-only) + PDF render + frontend download button
DEPENDS: Block 5 merged (policy + policy_unit data present).
MODEL: Sonnet 4.6 medium (read + PDF render; no financial writes).

STANDING ORDERS: foreground only, no subagents; no retries — STOP and paste the exact error; live updates every 5 min with CST/Laredo timestamp + real measured data, no guesses; confirm worktree pwd, git status, git log, git rev-parse HEAD; show diff --staged --stat before commit; stop on anything unexpected.

WHY: produce a printable Certificate of Insurance for a policy (carrier, policy number, coverage, term dates, covered units) to send to customers/brokers.

LANE LOCK: insurance module only, READ-ONLY against policy/policy_unit. No financial writes, no bill changes. Forbidden: detention, driver-finance, sidebar-config, accounting.

RESPOND-BEFORE-CODE: post (1) the COI field layout (what reads from policy vs policy_unit vs company profile), (2) the PDF render approach (reuse existing PDF/export util if one exists — confirm which), (3) the endpoint + download wiring. WAIT for Jorge GO.

SCOPE:
- GET endpoint returns a generated COI PDF for a policyId (RLS-scoped to operating company).
- Frontend "Download COI" button on the policy detail.
- Pure read; no schema change expected. If a render util is added, no new schema.

ACCEPT: COI renders with correct policy + unit + company data; RLS prevents cross-company access; tsc -b green; CI green.

PAUSE: RECOMMENDED block — do NOT dispatch until Jorge says GO. Then RESPOND-BEFORE-CODE, then wait for code GO. Squash-merge holds for explicit GO.
