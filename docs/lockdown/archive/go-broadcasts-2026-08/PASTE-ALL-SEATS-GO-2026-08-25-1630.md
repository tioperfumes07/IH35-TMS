# PASTE ALL SEATS — GO 2026-08-25 16:30 CT

**Idle = defect. You already have instructions. Read INBOX TOP. ACK in OUTBOX this turn.**

Live API **`e59f66a`**. Deploy **LANDED**. Hard-reload USMCA. **Nobody `trigger_deploy`.** Skip **#15546**. U14 never restamp. Launch ~4h.

`git pull --ff-only origin main`. Reuse `L-20260824-0007`. Do not remake Complete / Close / `/425c`.

---

## Mail read (do not wait on Jorge)

| Seat | OUTBOX | NOW |
|------|--------|-----|
| **CC-1 9223** | **STALE. No ACK on invoice#=load#.** Last self-ACK is old U14 leftover. | **#1 `INVOICE-DISPLAY-ID-EQUALS-LOAD-NUMBER` then 2–10 serial. Money clone (not `IH35-TMS-clean`). Blocks Program Complete.** ACK `GO-1630` this turn. Never `/425c`. Never `trigger_deploy`. |
| **CC-2 9224** | ACK GO-1540. Back-arrow class **exhausted** (#15860/#15866/#15871/#15882/#15900). | Unique leftover `/reports` `/cash-flow` `/tasks`. Item **26 cash-flow Proforma still BLOCKED on CC-1 #1–2**. Do not remake scroll/back. ACK `GO-1630`. |
| **CC-3 9225** | Stale vs GO-1625. | **If #15909 underscore-combobox already on `origin/main` — do not remake.** Next leftover **62–75**. `#64` waits CC-1 #6. Else finish 9774 feature. Never remake title-case #15905/#15906. Never `trigger_deploy`. ACK `GO-1630`. |
| **Codex 9226** | Shipped #15902 Book Load HOS silent fallback. | **Live is `e59f66a` now — prove Dispatch `eld_certified` this turn.** hop.assign **UI only** (mint = CC-1). ACK `GO-1630`. |
| **Cascade** | **STALE (Aug 16).** Idle = defect. | **WALK `/program` now.** FINDING or AUDIT-PASS. OUTBOX same turn. No product PR. No U14 restamp. |
| **Devin-A / Devin** | Stale vs 16:25. | Same as Cascade. Not PARKED. Items 126–150. OUTBOX same turn. |
| **Cursor 9222** | Lead. | Unique leftover **`BOOKLOAD-STOP-NAME-ADDRESS-NOT-TITLE-CASED`**. Do not steal CC-1. Do not second-kick. Skip #15546. |

Fully wired = items **1–12**, Live Chrome last. Unique 500 / dead / silent — never defer.
