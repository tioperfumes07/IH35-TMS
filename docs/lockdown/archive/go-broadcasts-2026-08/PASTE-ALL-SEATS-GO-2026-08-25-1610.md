# PASTE ALL SEATS — GO 2026-08-25 16:10 CT

**Launch window ~4 hours.** Idle = defect. Hard-reload **USMCA** when `healthz/shallow` leaves `6141951` — API deploy **`dep-da70a5q6iojc7381oo6g`** IN FLIGHT (tip `10957be`, includes #15860 + #15895). **Nobody else `trigger_deploy`.** Skip **#15546**. U14 never restamp.

`git pull --ff-only origin main`. Reuse load `L-20260824-0007`. Do not remake Complete / Close / `/425c`.

---

## Law (do not remake)

- **#15860 `9d24536`:** ScrollToTop + PageHeader smart-back. CC-2 waves #15871/#15882 too.
- **CC-1 money 1–10 still OPEN** on the money clone (invoice#=load#, cash-flow Proforma labels, JEs). That **blocks Program Complete**. Cursor does not steal it.

## Every seat NOW

1. **Program `/program`** — unique 500/dead/silent. Fully-Wired 1–12. CREATE-TEST-THEN-VOID.
2. **CC-1** — items **1–10 serial**. Never `/425c`. Never `trigger_deploy`.
3. **CC-2** — remaining **no-header** ← only. Unique leftover. Never remake ScrollToTop.
4. **CC-3** — remaining underscore comboboxes + any create path not yet using `properPersonOrPlaceName`.
5. **Codex** — live-prove Dispatch HOS `eld_certified` on an assigned Samsara driver.
6. **Cascade / Devin-A** — walk `/program` **now**. OUTBOX same turn.

Fully wired launch quality: surface + picker + F+R + money terminus where owed + Live Chrome last. Never defer a unique FINDING.
