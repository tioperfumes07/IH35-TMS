# PASTE ALL SEATS — GO 2026-08-25 15:40 CT

**Idle = defect.** Hard-reload **USMCA** after API deploy `dep-da6vs5c9v7es739mjcbg` goes live (`healthz/shallow` ancestry includes **`9d24536`** / #15860). U14 never restamp. PCMILER/geofence owner-gated. **Nobody `trigger_deploy` except Cursor lead.** Skip **#15546**. Tonight owner voids TEST data and starts **real** dispatch / accounting / banking / factoring input.

**Not a 15th plan.** Same GO-1350 25-item lists. `git pull --ff-only origin main`.

Reuse load `065538c8-…` (`L-20260824-0007`), T-LIVE `1a3c98da-…`, WO `850e2cc4-…`. Do **not** remake Complete / Close / `/425c`.

---

## Law from CC-2 (do not remake)

**PR #15860 squash `9d24536` — FIXED AND MERGED.** Live after this deploy.

1. **Scroll-to-top on module click** — `ScrollToTop` in `App.tsx`. PUSH/REPLACE → `window.scrollTo(0,0)`. **POP (browser back/forward) leaves scroll alone.** Do not add a second reset in Shell.
2. **Back arrow uses real history** — both `PageHeader`s: `hasInAppHistory` → `navigate(-1)`; hardcoded `backHref` only on direct load/refresh.

**Follow-ons already on main:** #15871 maintenance leaves + DefectDetail; #15882 24 safety/dispatch/finance leaves.

**Still OPEN (CC-2 continue):** pages with **no header at all** (missing ←). Audit individual leaves. Do not re-patch shared PageHeader/ScrollToTop.

---

## Owner now (every seat)

1. **Program scenarios** — unique 500 / dead / silent; **Fully-Wired 1–12**, Live Chrome last.
2. **CC-1 money clone** — items **1–10** serial. **Not** Cursor lead tree. Never `/425c`.
3. **CC-2** — remaining **no-header** back arrows only. Prove leftover unique. Never remake #15860.
4. **CC-3 FE** — title-case names/addresses on **create payload**; remaining underscore comboboxes. No money.
5. **Codex** — prove **Samsara HOS on Dispatch** (`DriverHosClockValue` + `eld_certified`).
6. **Cascade / Devin-A** — **LIVE WALK `/program` now**. OUTBOX same turn. Idle = defect.

---

## Cursor this hour

- Deploy in flight: `dep-da6vs5c9v7es739mjcbg` tip `6141951` (includes #15860). **Do not second-kick.**
- Unique leftover: print/CSV **Account # hidden** unless CoA toggle ON; break-even **Excel lenders + vehicles**; last-module fallback on refresh; Lists hub POP-only scroll restore.
- Skip #15546. Do not steal CC-1 1–10.
