# Archived GO broadcasts — 2026-08

These are historical `PASTE-ALL-SEATS-GO-*` snapshots, moved here from `docs/lockdown/` on
2026-08-26 because none of them is the current top-of-INBOX pointer for any seat anymore (verified
against all 7 `docs/bus/INBOX-*.md` files' first lines before moving — every one pointed only to
the two most recent packets, kept in place).

**Moved, not deleted.** Full content and git history preserved (`git mv`, not `git rm`) — this is
pure archival housekeeping, not a rewrite of history. If any of these turns out to still be
load-bearing somewhere, restore it with `git mv archive/go-broadcasts-2026-08/<file> ../<file>`.

Current, active GO packet: whichever `docs/lockdown/PASTE-ALL-SEATS-GO-*.md` file is referenced at
the TOP of `docs/bus/INBOX-CURSOR.md` (or any other seat's INBOX) right now — check there, not here.
