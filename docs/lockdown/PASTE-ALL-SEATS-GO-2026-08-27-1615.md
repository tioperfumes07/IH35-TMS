# GO-1615 — OWNER: ALL CODERS STILL IDLE · 2026-08-27 16:15 CT

**THIS IS NOW.** Owner said seats are **stuck/idle**. Watching INBOX after N=0 is a defect. No second-kick API. Live SHA `GET /api/v1/healthz/shallow`. Skip #15546. Never restamp U14. Do not void TEST.

**Cursor is shipping FUEL-F6910** (shared Combobox `loading` passthrough — SPA TS2322). Codex do **not** remake it. After SPA green: Live Chrome `/fuel` unique leftover.

ACK: `SEAT | ACK | GO-1615 | PORT=<n> | NOW=<exclusive> | SHA=<healthz> | GO`

---

## Exclusive NOW (do not steal)

| Seat | Port | NOW |
|------|------|-----|
| Cursor | 9222 | lead · FUEL-F6910 · leftover POST unique 500/dead/silent |
| CC-1 | 9223 | `/accounting` create labeled TEST (do not void). Named OPEN=0 is not idle. |
| CC-2 | 9224 | **Do not watch INBOX.** Unique FINDING on `/reports` `/cash-flow` `/finance` `/tasks` or file N=0 + next leaf create TEST. Never GL. |
| CC-3 | 9225 | unique leftover `/legal` `/compliance` `/lists` — next 500/dead/silent. Do not remake 9 shipped IDs. |
| Codex | 9226 | do not remake Combobox. Next silent-cap + Live Chrome after SPA. |
| Cascade | MCP | `/dispatch` `/driver-hub` unique hunt on **current** healthz |
| Devin | MCP | `/vendors` unique FINDING — last walk N=0 is not park |

---

## BOX

```
SEAT=<you> GO-1615 IDLE=DEFECT. Owner: all coders still idle.
NOW=exclusive URL above. ACK OUTBOX this turn. Do not watch INBOX.
ACK: SEAT | ACK | GO-1615 | PORT=n | NOW=<url-or-id> | SHA=<healthz> | GO
```
