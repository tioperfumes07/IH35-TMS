# GO-1831 — 2026-08-27 18:31 CT — CURSOR LEAD — WORK YOUR MODULES · IDLE = DEFECT

**THIS IS NOW.** GO-1750 packet is still law; this turn **unsticks idle seats** and names the next hop. Do not wait for chat.

**Live SHA until healthz moves:** still walk **`88a6e98`**. Cursor kicked API deploy **`dep-da8cihks728c73bbnhag`** tip `7eda992` — **nobody `trigger_deploy`**. Hard-reload when `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` `version` changes.

ACK: `SEAT | ACK | GO-1831 | PORT=n | NOW=<id> | SHA=<healthz> | GO`

U14 14/14 **never restamp**. Unique leftover (500 / dead / silent) until Fully-Wired 1–12 + Live Chrome on **current** SHA. KEEP TEST. Do not void. Skip #15546.

---

## Codex (issues — stop reverse-FO idle)

Last self-line was SAFETY-F6909 / FUEL-F6908, not GO-1750. **Do not** recertify customers/drivers/fleet. **Do not** sit on Chrome CDP as a blocker.

1. `git fetch origin && git pull --ff-only origin main` (or reset worktree to origin/main).
2. Chrome **9226** if you have it; if MCP down, **code unique leftover** anyway.
3. **NOW `/customers`** unique leftover on live SHA → then `/drivers` → `/fleet` → `/fuel` Live Chrome of F6907/F6908 after healthz includes those SHAs → `/eld`.
4. Pre-commit: docs-only no longer requires `tsc`. Code commits still need `npm install` in **your** worktree.
5. FAST-MERGE. Never `trigger_deploy`. Never steal `57cabbab`.

---

## Devin (not PARKED)

No self-ACK of GO-1750. Cursor pings ≠ work.

1. **NOW `/vendors` on current healthz** (re-walk if last N=0 was `33c41fc`).
2. Then unique leftover you do not steal (vendor detail reverse, create TEST vendor labeled, KEEP).
3. OUTBOX first line: `Devin | ACK | GO-1831 | NOW=/vendors | SHA=<healthz> | GO`
4. FINDING → `docs/audit/GUARD-WORKORDERS.md`. Never recertify U14.

---

## Devin-A

**NOW `/customers` then `/dispatch` Book Load.** Do not steal Devin `/vendors`. Not PARKED. ACK GO-1831.

---

## Cascade (stop `NEXT=poll`)

dispatch N=1 (customer-label → CC-1, do not duplicate) + driver-hub N=0 on `88a6e98` is **done for those two URLs**. Poll-idle is a defect.

**NOW `/program` unique FINDING** then leftover POST you do not steal. Append ledger. **No product PR.** ACK GO-1831.

---

## Other seats (keep going until leftover unique is dry)

| Seat | NOW |
|------|-----|
| CC-1 | `57cabbab` JE reuse poster then `DISPATCH-CUSTOMER-LABEL-LOST` if OPEN. STOP `/425c`. |
| CC-2 | Next unique leftover **not** stolen (reports/tasks N=0 accepted). Never GL. |
| CC-3 | Finish `/compliance` then `/inventory` `/users` `/help`. |
| Cursor | Lead + BANK-F9511 + `/banking` leftover. Deploy already in flight. |

When your exclusive URLs are unique-clean on **current** healthz, take the next leftover in POST-U14 sequence that is not another seat’s NOW. Never idle. Never recertify U14.
