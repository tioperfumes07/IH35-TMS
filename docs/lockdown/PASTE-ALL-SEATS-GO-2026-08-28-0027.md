# GO-0027 — ALL SEATS THIS IS NOW (idle = defect)

`git pull --ff-only origin main`
Instruction = `docs/bus/FEED/NOW-<SEAT>.md`

**Deploy in flight** `dep-da92hg942hec73egp88g` tip `13a97574`. Live healthz still `4e5db76` until that deploy is live. Work against **current `origin/main`**. After healthz moves, re-prove on the new SHA.

**Nobody except Cursor `trigger_deploy`.** Skip PR **#15546** **#16895**. U14 never restamp. KEEP TEST. No PROG-01. No flag flips. ACCT-F9877 is **not** owner-gated — posting-engine already shipped (#17256); do not ask Jorge.

ACK: `SEAT | ACK | GO-0027 | NOW=<from your FEED> | SHA=<healthz> | GO`

| Seat | NOW |
|------|-----|
| **CC-1** | `BANK-TRANSFER-BALANCE-DUAL-WRITER-CONFLICT` (OPEN board) then leftover `/accounting` unique money. Never idle. |
| **CC-2** | Unique leftover `/home` then `/fuel` (500/dead/silent/unchecked UPDATE). Do not recap GO-0022. Never idle. |
| **CC-3** | Banking items still `PASS`+`prod_verified:false` — **USMCA live proof** on healthz (not a flag hunt). Then `/eld` unique leftover. Never idle. |
| **Codex** | Continue `/dispatch` unique leftover drain. DISP-S19..S36 already live-clicked — do not restamp U14. Never idle. |
| **Cascade** | Unique FINDING only on live healthz. Append ledger. Skip #15546 #16895. Never idle. |
| **Devin** | Paste `docs/lockdown/PASTE-DEVIN-GO-2026-08-28-0027.md`. `/vendors` VEND-S01 USMCA=123 not 4. KEEP TEST. |
| **Devin-A** | Unique FINDING `/customers` then `/driver-hub`. Do not steal `/vendors`. Never idle. |
| **Cursor** | Lead. Census. FAST-MERGE this bus. Deploy already kicked. |
