AGENT-1 · Block <N> of <M> — PHASE <tracker-phase> / TASK <set-from-latest-tracker> — Dispatch board: List=Kanban format + Booked/Assigned/Unassigned + reserve-a-load
RBC TARGET: branch feat/dispatch-board-assignment-view

[!] Set a real tracker TASK id before dispatch.
STANDING ORDERS: foreground only, no subagents; no retries — STOP, paste exact error; live updates every 5 min CST/Laredo + real measured data, no guesses; confirm worktree pwd, git status, log, rev-parse; show diff --staged --stat before commit; stop on unexpected.
LOCKS: ADDITIVE ONLY, never remove/reorder existing views or columns. Keep Kanban + List; Units is an added view. There is NO Northbound/Southbound board section. Do not touch the Book Load wizard. RESPOND-BEFORE-CODE (RULE 6): reply first with as-built vs spec inventory, screens/blueprint matched, deltas, NEW spec. Wait for GO.
LANE LOCK: one writer per magnet file per cycle (sidebar-config.ts, App.tsx, verify-*). Migrations sequential, one agent. Do not edit shared registries beyond what this block names.
SCOPE (additive) per DISPATCH-BOARD-VIEW-LOGIC.md:
- List view renders the SAME grouping/logic as Kanban, in list form.
- Assignment view stacked sections: (1) BOOKED LOADS (reserved, no unit/driver) (2) ASSIGNED UNITS (unit has a load) (3) UNASSIGNED UNITS (idle/available).
- RESERVE-A-LOAD: allow creating/accepting a load with NO unit and NO driver (schedule weeks ahead); it appears in BOOKED until assigned. Reserving must not require unit/driver.
- Six status columns (Pending Assignment, Assigned, In Transit, Delivered, Completed, Cancelled) define the universal six-column sizing reference (see global block).
CI GUARD: assert a load can persist with null unit + null driver (reserved) and surfaces in Booked; assert List view groups identically to Kanban.
GATES (Std Order #16 v2): build:backend EMIT, frontend tsc -b, verify:arch-design, full backend vitest for runtime/calc paths, migrations self-contained w/ GRANTs + drift-capture (CI is fresh-DB). verify+commit+push as ONE step.
ACCEPTANCE: List mirrors Kanban; Booked/Assigned/Unassigned sections; reserve-a-load works and shows in Booked.
PAUSE after RESPOND-BEFORE-CODE. PAUSE before merge — Claude verifies live.
