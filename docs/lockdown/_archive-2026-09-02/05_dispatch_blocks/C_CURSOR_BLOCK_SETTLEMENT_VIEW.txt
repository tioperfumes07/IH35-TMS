AGENT-1 · Block <N> of <M> — PHASE <tracker-phase> / TASK <set-from-latest-tracker> — Pre-settlement / settlement trip-linking VIEW
RBC TARGET: branch feat/presettlement-trip-linking-view

[!] Set a real tracker TASK id before dispatch.
STANDING ORDERS: foreground only, no subagents; no retries — STOP, paste exact error; live updates every 5 min CST/Laredo + real measured data, no guesses; confirm worktree pwd, git status, log, rev-parse; show diff --staged --stat before commit; stop on unexpected.
LOCKS: ADDITIVE ONLY. Core triangulation logic already exists — do NOT change it; build the VIEW + the "add to it?" affordance if missing. No new financial code; settle through existing functions. RESPOND-BEFORE-CODE (RULE 6): reply first with as-built vs spec inventory, screens/blueprint matched, deltas, NEW spec. Wait for GO.
LANE LOCK: one writer per magnet file per cycle (sidebar-config.ts, App.tsx, verify-*). Migrations sequential, one agent. Do not edit shared registries beyond what this block names.
SCOPE (additive) per SETTLEMENT-TRIP-LINKING.md:
- Enforce/confirm MUST 8a.0.5.12: exactly ONE open pre-settlement per driver; second load auto-adds.
- Flow VIEW: NB delivery auto-creates pre-settlement (composed); dispatch board row shows "Driver has open pre-settlement · add to it?"; clicking adds the SB load; "Settle and pay" enables on return to Laredo; final settlement shows BOTH loads + deductions + escrow + debt-alert before payment.
- Per driver/truck: linked trips (NB+SB legs), running totals, deductions, escrow, debt alert, add-load + settle actions.
- Border-routing must be != 'pending' before compose() includes a border-crossing load (WF-014).
CI GUARD: assert second open pre-settlement per driver is blocked (auto-add); assert settle blocked while driver not returned/locked.
GATES (Std Order #16 v2): build:backend EMIT, frontend tsc -b, verify:arch-design, full backend vitest for runtime/calc paths, migrations self-contained w/ GRANTs + drift-capture (CI is fresh-DB). verify+commit+push as ONE step.
ACCEPTANCE: one open per driver; NB auto, SB joins, settle on return; view shows linked trips + totals + deductions + escrow + debt alert.
PAUSE after RESPOND-BEFORE-CODE. PAUSE before merge — Claude verifies live.
