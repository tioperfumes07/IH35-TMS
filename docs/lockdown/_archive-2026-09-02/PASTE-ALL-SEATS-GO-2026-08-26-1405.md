# GO-1405 — 2026-08-26 14:05 CT — CURSOR LEAD — FULL INSTRUCTIONS · IDLE = DEFECT

**THIS IS NOW.** Every older GO (2310, 2237, 1829, 1645, 1619, CLAUDE-IS-LEAD, tripwire T1) is **VOID as NOW**. Do not follow those numbered lists. Unique leftovers on U14 modules are **still work** — do **not** restamp U14.

**LEAD-SEAT=CURSOR** (9222). Owner: Cursor coordinates; reads/updates INBOX/OUTBOX; census every lead turn. Jorge is not the messenger.

**Live proof this packet:** `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` → **`version":"c46d592"`** (`ok:true`). `origin/main` may be ahead — Cursor deploys every 5–10 merged PRs, one in-flight. Walk **the live SHA**, not U14 stamp SHAs. Hard-reload the app.

ACK this turn on **your** OUTBOX first line:

`SEAT | ACK | GO-1405 | PORT=n | NOW=<id> | SHA=c46d592 | GO`

---

## Excel + leftovers (answered)

Named leftovers **are** in `docs/lockdown/USMCA-LIVE-CHROME-CERTIFY-INVENTORY-2026-08-26.xlsx`:

- Tab **`00-ALL-PENDING-CHECKLIST`** — launch gates + **every matrix leaf** as Live Chrome (`LC|module|leaf`) including all 14 U14 modules + leftover POST modules + named FINDINGS + GUARD-WORKORDERS OPEN.
- Filter column C = `LIVE CHROME leaf` and column E = module.
- Desktop copy: `~/Desktop/USMCA-LIVE-CHROME-CERTIFY-INVENTORY-2026-08-26.xlsx`

The chat list = the **named** remainder. Live Chrome = **1,266 leaves still owed on current SHA** until walked. Regen: `python3 scripts/ops/build-usmca-live-chrome-certify-xlsx.py`

---

## CURRENT-LAW (every seat, every PR — do not deviate)

1. **USMCA only.** No TRANSP / TRK product work. **No TMS→QBO write-back. QBO sync campaign OFF.**
2. **U14 14/14 campaign CLOSED — never restamp / recertify.** Still **fix unique leftovers** on those modules (500 / dead click / silent no-op / fake $0 / reverse-empty).
3. **Launch** = Fully-Wired **1–12** + Live Chrome on **current** healthz + **zero unique leftover**. CI-green / Rule 24 `complete:true` ≠ launch. Say **Live=BLOCKED** until item 12 on this SHA.
4. **CREATE-TEST-THEN-VOID.** Empty TMS expected. Create labeled TEST. Owner voids at launch. Disabled pay buttons: create the TEST doc, do not stop.
5. **Never defer. Fix root cause.** Guard that fails on the bug. Evidence before done. Findings **agent → `docs/audit/GUARD-WORKORDERS.md` → agent**. Never chat-only. Never through Jorge.
6. **Cascade** appends audit Verdict rows (`AUDIT-COVERAGE-LIVE.md`) — other seats do **not**.
7. **Skip PR #15546** preview. **form_425 / `/425c` do not loop.**
8. **WIP ≤ 3** feature PRs. **Rule 27:** one open PR per **area**. Money **serial (CC-1 only)**. Same bug ≥3 sites → **one sweep + one guard**.
9. **FAST-MERGE ~4 min ON** (`docs/bus/FAST-MERGE-4MIN-LAW.md`):
   - `node scripts/money-pr-local-gate.mjs` exit 0 (Cursor: `cursor-ship-preflight --body-file`)
   - Push. `--no-verify` **only** if blocked by ENV `verify-static` class (not your guard)
   - `gh pr create` — **never** `gh pr checks --watch`
   - Merge: `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash` same turn (`gh pr merge` can fail if another worktree holds `main`)
10. **Deploy:** Cursor lead only, every **5–10 min AND 5–10 merged PRs**, one in-flight. **CC/Codex/Cascade/Devin never `trigger_deploy`.**
11. **Verify live** on USMCA `5c854333-6ea5-4faa-af31-67cb272fef80` using **your assigned browser/start command**. Grep main before coding a board row.
12. **Access (owner 2026-08-26):** Neon (FULL, RLS discipline), GitHub, repo, Desktop, Render **read**. Deploy kick = Cursor. API/secrets pack on Desktop: **`~/Desktop/APIS-ALL-05-29-2026.rtfd`** and **`~/Desktop/IH35-CURSOR-AUDIT/`**. Use existing credentials; do not invent keys in chat.
13. **Cursor PRs** title prefix `Cursor-`. Verify-steps: Cursor EVEN, CC-1 ≡1, CC-2 ≡3. Claim-before-write. No `CLAIMED-NUMBERS.json` on feature PRs.
14. **Money:** reuse poster, no new GL math. Flags OFF until owner says turn on. Void not delete.

---

## Shared findings file (mandatory)

| What | Where |
|------|--------|
| Fix board (OPEN → your lane) | `docs/audit/GUARD-WORKORDERS.md` **same turn** as the FINDING |
| Audit ledger (Cascade only, append) | `docs/audit/AUDIT-COVERAGE-LIVE.md` |
| Excel inventory | `docs/lockdown/USMCA-LIVE-CHROME-CERTIFY-INVENTORY-2026-08-26.xlsx` |
| NOW | `docs/bus/NOW-ONE-SOURCE.md` TOP = this GO |
| Lead | `docs/bus/LEAD-SEAT.md` = CURSOR |

If you find a defect in another lane: **write the OPEN row yourself**, do not ping Jorge.

---

## Plan (how we drain this)

**Not** 15 parallel PRs. **Not** recertify U14.

1. **CC-1 serial money** (one PR at a time): `57cabbab` JE first → cash-flow Pull invoices 500 → ACCT-F9408 → hop.bank → hide-flag class → mutable-scope class → remaining named money IDs.
2. **Cursor:** lead census + FAST-MERGE + deploy cadence + **DOCS-F6072** unit/equipment 500 fix + FE unique (customer create dead click, safety home retry).
3. **CC-2:** live-verify (never build money). Unique 500/dead/silent on assigned URLs. Q8 scheduled-report worker if still true (not GL).
4. **CC-3:** FE chrome (lists/legal nested create = Lists). Unique leftover. No money math.
5. **Codex:** hop.assign UI (keep modal on failed write) then drivers/fleet/safety/fuel calendars + creators. Reverse SQL allowed. Assigned browser for unique hunt.
6. **Cascade:** live walk FINDING only. Append ledger. Never recertify. Never merge money you verified.
7. **Devin-A / Devin:** live walk assigned URLs. FINDING to board. Not PARKED.

GO-2310 chrome (calendars + nested create = Lists) is **in every Live Chrome leaf**, not a separate campaign.

---

## NOW by seat (ports · start · URLs)

| Seat | Port | Start / browser | NOW (first) | Then |
|------|------|-----------------|-------------|------|
| **Cursor** | 9222 | Cursor lead session | Census + DOCS-F6072 500 fix | Customer create dead click · Safety-F6437 retry · FAST-MERGE · deploy 5–10 |
| **CC-1** | 9223 | money clone | **`PROGRAM-EXPENSE-DOCUMENT-POSTED-WITHOUT-JE` (`57cabbab`)** reuse poster | Pull invoices 500 · F9408 · hop.bank · 3 class sweeps · other named money |
| **CC-2** | 9224 | live-verify | `/cash-flow` Pull invoices **prove 500 still / or closed** on `c46d592` | `/reports` `/finance` `/tasks` unique + calendars. Q8 worker. Never GL |
| **CC-3** | 9225 | FE | `/lists` + `/legal` nested + Add new = Lists chrome | Unique 500/dead. LEGAL-TEMPLATE picker P3. Gate A CDL only if still true |
| **Codex** | 9226 | FE | **hop.assign** UI on live SHA | `/drivers` `/fleet` `/safety` `/fuel` calendars + Create. No steal 57cabbab |
| **Cascade** | audit | `~/Desktop/IH35-START-1-CASCADE.command` | Walk `/accounting` on `c46d592` — unique FINDING only | Then customers → dispatch. Append ledger |
| **Devin-A** | audit | `~/Desktop/IH35-START-2-DEVIN-A.command` | `/customers` then `/dispatch` Book Load + calendars | FINDING to board. Not PARKED |
| **Devin** | audit | assigned Devin browser | Same as Devin-A if that profile; else `/vendors` unique hunt | FINDING to board |

**Nobody:** recertify U14 · `/425c` loop · QBO sync · TRANSP/TRK · steal another seat’s NOW · idle · `trigger_deploy` (except Cursor lead when gate says).

---

## Named leftovers (U14 modules included) — still OPEN until live-closed

**Accounting:** 57cabbab no JE · invoice AR JE · F9408 Proforma $0 · F6508 company-switch draft · spine silent drop · TEST 6210 void-at-launch · USMCA no TRK bulk assets · hop.invoice/gl/revenue  

**Banking:** hide-flag fail-open · KPI fake $0 · hop.bank · TEST 6210  

**Settlements:** SETL-F6464 cached deductions · cash-advance notify success-on-fail · prove pay-run JE  

**Dispatch:** hop.assign 0 rate-card bills · Book Load re-verify this SHA · Gate A vs 114 missing CDL/medical · cancel→invoice OWNER · trip pairing OWNER  

**Customers:** Unapply dead route · F6312 statements Live Chrome · create dead click · deactivated labels  

**Drivers / hub:** F6083 $0 · F6106 empty · F6110 hub $0 · drug-screen ack OWNER  

**Fleet:** F6113 trip-cost company · F6304 silent null price  

**Maintenance:** F6631 / F6626 class · WO auto-bill JE prove live  

**Safety:** F6635 / F6634 class · F6437 no retry · TEST BOX4 no void  

**Insurance:** F6628 mark-paid class  

**Legal:** template picker no entity  

**Plus leftover POST:** cash-flow Pull 500 · fuel planner $0 · F6535 · docs unit 500 · reports Q8 + deactivated labels · compliance raw select · program hop.assign  

**Lists / vendors / factoring / inventory / users / home / help / tasks / finance / system:** unique hunt + Live Chrome on `c46d592`. TMS settlement auto-pay cron **behind** 57cabbab (not QBO sync).

OWNER-gated (do not invent GL): cancel charge invoice · trip pairing · driver drug-screen persist.

---

## Cursor (lead) — self rules

Census from seven OUTBOX first 20 lines every lead turn → rewrite `LEAD-CENSUS.md`. Ping ≠ ACK. Prepend INBOX when NOW changes. FAST-MERGE own Cursor-lane PRs. Deploy 5–10. Do not steal `57cabbab`. Do not recertify U14.
