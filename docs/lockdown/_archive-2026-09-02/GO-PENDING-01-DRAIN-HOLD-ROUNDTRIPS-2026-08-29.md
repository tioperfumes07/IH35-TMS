# GO-PENDING-01 — drain trust leftovers; HOLD Round Trips UI (owner 2026-08-29)

Round Trips D-5 / calendar redesign is **OWNER HOLD**. Do not edit `RoundTrips.tsx`. Packet `GO-DWELL-01` stays reference only until Jorge returns.

Cascade TURBO waves **CLOSED**: #17717 #17736 #17750 #17765 #17772. Unique product defect **50208** shipped #17789. Do not re-run those waves. Unique FINDING only.

CC-3 Batch 5 **CLOSED** (#17787). Next = GR-1 `failingNames` slice from `docs/audit/VERIFY-STATIC-BASELINE.json` (~150 remaining). Do not idle “waiting for a new packet.”

## Seat NOW

| Seat | NOW |
|------|-----|
| **CC-1** | INBOX was stale (GO-DWELL/GO-MODAL). ACK this GO. Money: (1) `verify-audit-void-reversal-subject-resolver` finding (2) Wave-3 reverse-and-repost first slice (3) D-3/D-4 **writers only** — detention_event → `driver_finance.settlement_lines`; populate `utilization.*` from real stop times. **No Round Trips UI.** Never `trigger_deploy`. Never stamp `complete:true` JSON. |
| **CC-2** | **T-07:** WAVE-2 REOPEN 50108–50124 — bind or un-`complete:true` only you stamp. Evidence packets. Skip #15546. |
| **CC-3** | Next GR-1 names from baseline (T-06 ratchet: shrink-only, never `package.json`/`ci.yml`). Then **T-08** IANA `America/Chicago` label **CT** never CST. **No D-5.** |
| **Codex** | **D-1** stop arrive/depart + source. Duplicate-departure guard after EVEN claim on main. **No D-5.** |
| **Cascade** | Unique FINDING. GR-1 already seeded. Do not recertify U14. Skip #15546. |
| **Cursor** | T-01 auth-helper TS2339 (this ship). Deploy 5–10 not per-merge. Live proofs T-01–T-04 wait until `healthz/shallow` is descendant of T-03/T-04 tips. Round Trips HOLD. |

Live SHA last seen: `ed4e2f2`. Skip #15546. Customers = Devin.
