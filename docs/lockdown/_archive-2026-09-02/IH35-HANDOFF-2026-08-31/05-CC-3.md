# CC-3 — NOT THE BOOKS
## FREE LANE — start now
1. `scripts/tieout/vendors-ap-aging.mjs` — sum of open bills == AP control account,
   **tolerance 0**. Currently a 13-line stub. 80 AP bills exist live, so there is real data.
2. **VEND-CERT-01** — the second open vendors item. With the tie-out this closes vendors
   entirely (7 of 9 → 9 of 9).

Rules: tolerance 0 · record observed pass or fail · empty is never a pass · report the
difference and its cause, never adjust the expected value.

Stay off the money path. Do not recertify U14. Never `complete: true`. Never `trigger_deploy`.
