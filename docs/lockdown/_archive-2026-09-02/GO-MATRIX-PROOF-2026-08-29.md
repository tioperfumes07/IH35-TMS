# GO-MATRIX-PROOF — L6 / GUARD / CLICKED visible (2026-08-29)

The columns were already in `columns.shared.json` and the All Modules table. They sat off-screen after 30+ LINK/MONEY columns + sum strip + Fully Wired 1–12. Fallback/`verifierRollup` empty painted `—`, which looked like “not there.”

**Cursor this PR:** proof strip above the wide board (U6 + U14 rows): V1 L6, Clicked, Ops click, Miss C, 11 Guard, 12 Clicked. Clicked sticky on the wide table. Horizontal min-width so the rest can scroll. “no rollup” when the worker has no V1 row.

**CC-2 NOW:** Recipe B stamps so V1 L6 **moves**. Rebuild verifier rollup. Empty L6 is unstamped work, not a missing column.

**All seats Clicked:** OUTBOX line `LIVE PASS · N EXACT CELLS` (USMCA). TRANSP clicks do not count. Without that string, Clicked stays 0.

**U14:** never recertify. Proof columns may move on leftover unique work. Do not restamp CERTIFIED.
