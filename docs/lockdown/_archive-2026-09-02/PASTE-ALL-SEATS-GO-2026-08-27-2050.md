# GO-2050 — 2026-08-27 20:50 CT — CURSOR LEAD — WORK NOW · IDLE = DEFECT

**THIS IS NOW.** Do not wait for chat. Jorge is not the messenger. 15 minutes idle is a defect.

**Live SHA:** `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` → **`7eda992`** (`ok:true`). Hard-reload. Older SHA walks (`88a6e98`, `33c41fc`) do not count for this hop.

ACK first line of **your** OUTBOX:

`SEAT | ACK | GO-2050 | PORT=n | NOW=<id> | SHA=7eda992 | GO`

Then **work**. KEEP TEST. **Do not void.** Skip #15546. U14 14/14 **never restamp**. CC never `trigger_deploy`. FAST-MERGE ~4 min.

---

## NOW by seat

| Seat | Port | NOW (first) | Then |
|------|------|-------------|------|
| **CC-1** | 9223 | Money: invoice **billing + cash legs** — load-sourced invoice must `DR A/R / CR Unbilled` (reclass, not a second CR Revenue). Reuse poster. **Do not void** INV-2026-00037/00044/00045 or the other live USMCA invoices. File/fix on board. STOP `/425c`. Opening-balance **entity** is owner-only — do not move the OB JE. | Next OPEN money row on `GUARD-WORKORDERS.md` |
| **CC-2** | 9224 | Next unique leftover you do not steal. Never GL. | `/reports` `/finance` `/tasks` already N=0 on prior SHA — re-spot **`7eda992`** then leftover |
| **CC-3** | 9225 | Finish `/compliance` `/inventory` `/users` `/help` unique leftover on **`7eda992`**. | Nested +Add new = Lists |
| **Codex** | 9226 | Unique leftover `/customers` → `/drivers` → `/fleet` → `/fuel` `/eld` on **`7eda992`**. Do **not** recertify U14. Chrome 9226 if up; **code if MCP down**. | Silent-cap only if unique |
| **Cascade** | audit | STOP poll. Unique FINDING `/program` on **`7eda992`**. No product PR. | Next leftover you do not steal |
| **Devin** | audit | Not PARKED. Re-walk `/vendors` on **`7eda992`**. KEEP TEST. | FINDING to board |
| **Devin-A** | 9227 | `/customers` then `/dispatch` Book Load. Do not steal `/vendors`. | KEEP TEST |
| **Cursor** | 9222 | Lead + `/banking` leftover (match after last closed statement). BANK-F9511 if still OPEN. | Census every turn |

**Nobody:** wait · recertify U14 · steal another NOW · void TESTs · second-kick Render this turn.
