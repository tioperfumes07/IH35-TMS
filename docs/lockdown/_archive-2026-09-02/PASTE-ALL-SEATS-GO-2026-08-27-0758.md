# GO-0758 — PASTE BOXES · FULLY-WIRED 1–12 · VERTICAL · 7D TRUTH · 2026-08-27 07:58 CT

**THIS IS NOW.** Live **`0340406`**. Hard-reload. Skip #15546. Nobody `trigger_deploy`. Never restamp U14.

**Owner asked:** paste-ready instructions; verify 7-day findings; **system-wide complete** fixes (Fully-Wired 1–12, all Required scoreboard columns, vertical class — not a one-screen patch).

**Honest census (Cursor lead, this turn):** I did **not** click-prove every OUTBOX line from every coder for 7 days. I **did** grep `origin/main` on Cascade’s dispatch 7, Devin vendor class, Codex money OPEN vs code, and CC-2/CC-3 shipped vs leftover. Board `OPEN HANDOFF` count is **stale** (102 Codex rows; e.g. F6822A nested BEGIN is **already removed** in `safety-v5.routes.ts`). **Grep main before remake.**

ACK: `SEAT | ACK | GO-0758 | NOW=<id> | SHA=0340406 | GO`

---

## LAW (every seat — paste this first)

```
LAW (do not skip):
- Complete fix = root cause + canonical R=W + F+R links + mutation-proven guard + live prove on CURRENT healthz
- Fully-Wired items 1–12 for the leaf (docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md). Live Chrome LAST.
- Vertical: same class/column across ALL owed modules, not this one screen (VERTICAL-WIRING-LAW).
- All Required matrix columns for that leaf (connectivity reverse picker qbo_chrome driver unit trailer load vendor customer + money cols owed). No leafRe:.*
- CREATE-TEST-THEN-VOID. Empty TMS expected. Do not remake proven TESTs.
- No patch. No defer. No toast-only. No hide-500. No route-only void.
- Grep origin/main before remake. Board stamp ≠ code.
- Exclusive URL. Skip #15546. Never trigger_deploy. Never recertify U14.
- Same turn: board + OUTBOX FINDING | ID | routed=<seat>
```

---

## 7-DAY TRUTH (grep-verified)

| Source | Implemented? | Still OPEN (complete / vertical) |
|--------|----------------|----------------------------------|
| Cascade dispatch 7 | PATCH 500 **yes** (#16616). Wait-times **live 200, INSERT still unsafe**. Driver-label **resolver absent**. Status-400 **not mapped**. Trip-pairing ghost GET **gone from page**. Commodity CREATE **still not persisted**. HOS concat: Cascade says fixed — re-prove. | CC-1 wait-times wrap + driver resolver + commodity class. CC-3 status-400 system-wide + FE label consume + trip-pairing ratchet. |
| Devin `/vendors` | Deactivate/Reactivate/SAFER **class on main**. HEADER-CREATE **wired** (do not remake). | Unique FINDING only. Re-prove `0340406`. TEST `63a9a2d1`. |
| Codex 7d | Many driver/fleet/safety FAST-MERGEs (history caps, HOS unbounded #16614, etc.). F6851A **guard aligned** (#16652). F6822A nested txn **already gone** — do not remake. | **CC-1:** F6797 DELETE still on WO lines; F6803A race; F6843A mutable wizard. **F6788A owner-gated.** Grep F6810A/F6750A/F6741. **Codex:** unique verticals + GUARD-F6809A if still red. |
| CC-3 7d | Lists/catalog/commodity PATCH shipped. ACK GO-0738 status-400 **started**. | Finish status-400 **all callers**, not one badge. |
| CC-2 7d | Audit-subject / void-reversal / cash-advance labels shipped. | `DEDUCTION-TRAIL-MISSING-AUDIT-EVENTS-SINK` OPEN — complete shared helper, all 7 siblings. |
| CC-1 7d | Money serial shipped (UTC reports, escrow counts, vendor balances, etc.). Last STATUS “no CC-1 rows” **false**. | Wait-times + driver resolver + commodity + remaining Codex money. |

---

## BOX — CC-1 (money / schema / RLS) · 9223

```
SEAT=CC-1 PORT=9223 GO-0758 SHA=0340406
Paste INBOX-CC-1 TOP + ACK OUTBOX-CC-1.
Never trigger_deploy. Never /425c loop. Grep origin/main before remake.

LAW: Fully-Wired 1–12 on every money leaf you touch. Vertical class (all consumers), not one route. Reuse poster. No new GL math. Void-not-delete. Flags OFF until owner says ON.

NOW serial (complete, not patch):
1) DISPATCH-BORDER-CROSSING-WAIT-TIMES-RLS-500
   Cache INSERT in cbp-wait-times.service.ts still unwrapped. lucia / SECURITY DEFINER for ALL writers (GET + cron refresh). Guard fails if INSERT runs as ih35_app without bypass. Live: all 5 ports 200 AND row in cache. Hiding 500 = patch.

2) DISPATCH-DRIVER-LABEL-LOST — SYSTEM-WIDE deactivated-driver labels
   Add mdata.resolve_driver_label_same_company (same family as customer/vendor). Wire EVERY list/API that shows driver names (dispatch list, reports, HOS, settlements consumers that already have the gap). Not a FE string fallback. Guard + selftest.

3) DISPATCH-LOAD-COMMODITY-CREATE-SILENT-NOOP — one architecture
   Persist commodity/weight (canonical columns + Book Load + board + PATCH + reverse) OR remove fields from UI+API+schema end-to-end. Half field = FAIL.

4) Then Codex money (grep first): MAINT-MONEY-F6797 void-not-delete WO lines (DELETE still present); F6803A lock+CAS; F6843A immutable policy+bills. Skip remake F6822A (nested BEGIN already removed). F6788A OWNER-GATED — do not build parts-inventory credit.

5) CC-3 filed: DROP stale SELECT ALL on catalogs.equipment_types + driver_load_statuses.

ACK: CC-1 | ACK | GO-0758 | PORT=9223 | NOW=wait-times-wrap | SHA=0340406 | GO
```

---

## BOX — CC-3 (FE / catalogs / dispatch chrome) · 9225

```
SEAT=CC-3 PORT=9225 GO-0758 SHA=0340406
Stop lists. Exclusive leftover FE. Never trigger_deploy. Do not remake #16616 / Reactivate / HEADER-CREATE.

LAW: Fully-Wired 1–12 + surface bar on every dispatch/list leaf you touch. Vertical class: every caller of the same API/enum/label, not one badge. Fail loud. Guard + selftest.

NOW serial:
1) DISPATCH-LOAD-STATUS-FILTER-ENUM-MISMATCH-400 — SYSTEM-WIDE
   Map EVERY FE status (list, badges, board, round-trips, analytics querystrings) to backend enum. Fail loud. Guard: no stale tokens (delivered|draft|planned|at_pickup|paid|booked|closed|at_delivery|invoiced) in dispatch load GET query.

2) DISPATCH-TRIP-PAIRING-EXPENSES-ENDPOINT-404
   Page already uses getTripPairingBoard. Ratchet: TripPairingBoardPage MUST NOT call /api/v1/accounting/expenses. Live prove. Do NOT invent that GET.

3) DISPATCH-DRIVER-LABEL-LOST FE consume
   After CC-1 resolver (or in parallel if API already returns label): list must show name, not "Driver — not visible". EntityLinkOrTombstone. HOS RetryRetryRetry: one Retry, 404 fail-loud. Vertical: same deactivated-label chrome on dispatch list.

ACK: CC-3 | ACK | GO-0758 | PORT=9225 | NOW=status-filter-400-systemwide | SHA=0340406 | GO
```

---

## BOX — CC-2 (reports / cash-flow / settlements leftover) · 9224

```
SEAT=CC-2 PORT=9224 GO-0758 SHA=0340406
Never GL. Never trigger_deploy. Watching INBOX = idle = defect.

LAW: Fully-Wired 1–12 on leftover reports leaves. Vertical: shared auditSubjectProjection / joins — all 7 /reports/audit/* siblings, not one report.

NOW:
1) DEDUCTION-TRAIL-MISSING-AUDIT-EVENTS-SINK — complete sink + reverse, all siblings, guard, live prove.
2) Unique leftover on /settlements then /cash-flow /reports /finance /tasks. CREATE-TEST-THEN-VOID.
3) Do not remake void-reversal / cash-advance-label / activity-by-module already on main.

ACK: CC-2 | ACK | GO-0758 | PORT=9224 | NOW=deduction-trail-complete | SHA=0340406 | GO
```

---

## BOX — Codex (drivers / fleet / safety / fuel / maint / insurance FE) · 9226

```
SEAT=CODEX PORT=9226 GO-0758 SHA=0340406
Never restamp U14. Never trigger_deploy. Grep origin/main before remake. Money OPEN → CC-1.

LAW: Fully-Wired 1–12 on YOUR leaf. Vertical: if you fix a silent cap / double pager / lifecycle, finish the CLASS across all owed driver/fleet/safety tabs, not one history table. All Required matrix columns for that leaf. Guard leaf-specific.

NOW:
1) Finish in-flight history/pager/lifecycle PRs as COMPLETE verticals (range+pager+empty+error+reload).
2) GUARD-F6809A parser if still red (typed filters). Do not weaken tenant/load asserts.
3) Unique FINDING only on /drivers /fleet /safety /fuel /maintenance /insurance. Dump leftover IDs on OUTBOX.

ACK: Codex | ACK | GO-0758 | PORT=9226 | NOW=/drivers-vertical | SHA=0340406 | GO
```

---

## BOX — Cascade (audit /dispatch + /driver-hub)

```
SEAT=CASCADE GO-0758 SHA=0340406
Unique FINDING only. Never recertify U14. Never stamp FIXED unless origin/main grep shows the write path.

NOW:
- Re-prove commodity PATCH + wait-times on 0340406. Wait-times 200 ≠ complete while INSERT is unwrapped.
- Walk /dispatch + /driver-hub. Board + OUTBOX FINDING | ID | routed=<seat> same turn.
- Dump remaining 7d unique IDs not in GO-0758.

ACK: CASCADE | ACK | GO-0758 | NOW=/dispatch | SHA=0340406 | GO
```

---

## BOX — Devin (/vendors only)

```
SEAT=DEVIN GO-0758 SHA=0340406
Exclusive /vendors. git fetch && reset --hard origin/main. Hard-reload.

NOW:
- Re-prove Inactivate/Reactivate/SAFER on 0340406. TEST 63a9a2d1. Void at launch.
- HEADER-CREATE already wired — do not remake.
- Unique FINDING only (500 / dead / silent). Board + OUTBOX same turn.

ACK: Devin | ACK | GO-0758 | NOW=/vendors | SHA=0340406 | GO
```

---

## BOX — Cursor (lead 9222)

```
SEAT=CURSOR PORT=9222 GO-0758 SHA=0340406 LEAD
Census OUTBOX every turn. Skip #15546. trigger_deploy only on 5–10 gate (not this hop — live still 0340406).

NOW:
- Keep seats on GO-0758 boxes. FAST-MERGE bus.
- Overflow: GUARD-F6809A if Codex not on it; HEADER-CREATE ratchet only if dead again on 0340406.
- Do not steal CC-1 GL.

ACK: Cursor | ACK | GO-0758 | PORT=9222 | NOW=lead | SHA=0340406 | GO
```
