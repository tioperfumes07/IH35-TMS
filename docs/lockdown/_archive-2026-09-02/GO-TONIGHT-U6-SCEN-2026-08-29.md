# GO-TONIGHT-U6-SCEN — close Urgent 6 + leftover U14 unique · scenarios named (2026-08-29)

**THIS IS NOW.** Live `healthz` at send: read `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` — do not use a table from an earlier turn. Skip **#15546**. Nobody but Cursor `trigger_deploy`. KEEP TEST. **PAY-01 BLOCKED.**

## U14 tonight — what “close” means

Urgent 14 exclusive certify is **already 14/14 CERTIFIED. Never recertify. Never restamp.**

Tonight U14 work = **unique leftover FINDING only** (500 / dead click / silent no-op) on leftover POST surfaces. Zero unique FINDING = that leftover is closed. **Do not print OPEN hops for the 14 CERTIFIED modules.** Law: `docs/lockdown/POST-URGENT-14-MODULE-SEQUENCE-2026-08-23.md` + `docs/bus/NOW-ONE-SOURCE.md`.

## Urgent 6 tonight — honest board (Recipe B; only CC-2 writes `prod_verified`)

Counts from `docs/module-completion/*.json` on origin/main at packet write (PASS vs `prod_verified`):

| Module | complete | PASS | prod_verified | Who moves it tonight |
|---|---|---:|---:|---|
| accounting | false | 25/39 | 25 | **CC-1** remaining money items → evidence packet → **CC-2** stamp |
| banking | false | 19/19 | 16 | **CC-2** stamp the 3 unbound PASS (or REOPEN) |
| settlements | false | 8/9 | 8 | **CC-1** the UNVERIFIED item + GL codes |
| factoring | false | 9/10 | 9 | **CC-1** the UNVERIFIED item |
| dispatch | false | 37/37 | **6** | **Cursor + Codex** Live Chrome packets on **current** healthz → **CC-2** stamps (31 errands, not new chrome) |
| vendors | true | 7/7 | 7 | leftover unique FINDING only — **do not recertify** |

`complete: true` without `prod_verified` on U6 is **illegal** for the six (`URGENT_6_COMPLETION_IDS`). Dispatch is the gap: chrome is PASS, stamps are not.

## WHO TESTS SCENARIOS (SCEN-01) — not optional, not Codex for money

Packet: `docs/lockdown/SCEN-01-ACCIDENT-CHAIN-GO-2026-08-29.md`. Flags ON. Screenshot ≠ GL. **CC-1 must assert JE account codes** from `scenario-registry.ts` `je:` — `n > 0` is not done.

| Scenario / hop | Creates TEST (Live Chrome) | Money / GL codes | Stamps scenario “done” |
|---|---|---|---|
| `scenario.accident` hops 1–3, 5, 7 | **CC-3** | hops 4, 6 **CC-1** | **CC-2** only after linkage query **and** account-code assert |
| `scenario.insurance` | **CC-3** claim | **CC-1** recovery + codes | CC-2 |
| `scenario.deductions` hop 10 | **CC-3** | **CC-1** settlement deduction + codes | CC-2 |
| `scenario.settlement` | — | **CC-1** | CC-2 |
| `scenario.roadside_ap` | **CC-3** WO if needed | **CC-1** bill+JE | CC-2 |
| `hop.assign` | — | **CC-1 only** (driver bill, not UI assign) | CC-2 |

**Codex:** leftover unique dispatch/fuel. **Do not** own `hop.assign`.  
**Cascade:** unique FINDING; GUARD-2 non-money; **no** scenario-done on a screenshot.  
**Devin:** customers only.  
**Cursor:** lead + deploy cadence + dispatch Live Chrome packets for U6 dispatch stamps + SCEN create if a hop is blocked on UI.

## Seat NOW (ACK this GO id)

| Seat | NOW |
|---|---|
| CC-1 | SCEN hops 4/6 + settlement + hop.assign + remaining accounting/settlements/factoring UNVERIFIED. DOC-01 schema after first money hop lands. Never `trigger_deploy`. |
| CC-2 | Stamp U6 Recipe B (banking 3, dispatch packets as they arrive). Scenario close only with query+codes. F-A baseline if still yours. |
| CC-3 | SCEN create hops **tonight** (accident 1–3,5,7 · insurance · hop 10). Then CUST-01. |
| Codex | DISP-01 detention/at-risk/KPI then unique leftover. No hop.assign. No new verify-steps. |
| Cascade | Unique FINDING. T-08 inventory #17851. Never restamp U14. |
| Devin | customers 0/10. |
| Cursor | Dispatch U6 Live Chrome → CC-2 packets. Deploy 5–10. Next product = leftover unique / DOC-01 UI after schema. |

ACK: `SEAT | ACK | GO-TONIGHT-U6-SCEN | SHA=<healthz> | NOW=<one line> | GO`
