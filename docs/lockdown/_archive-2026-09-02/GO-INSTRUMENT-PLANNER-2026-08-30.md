# GO-INSTRUMENT-PLANNER — 2026-08-30 11:54 CT

**THIS IS NOW.** Live `healthz/shallow` `version` = **`d41124e`** (planner #18231 + API deploy `dep-daa5qf9srm7s73e20k9g` **live**). Main may be ahead; **walk vs live SHA**, not vs a newer un-deployed tip.

Never restamp U14. Never `trigger_deploy`. Skip #15546. No TMS→QBO write-back. Do not paint `moduleMatrixBoxes.tsx`.

**VOID:** “112 ECON cells blocked pending owner approval of C25–C31 definitions.” Definitions are **locked**. Blocked remaining = **connected INV replay** + **`PROD_READONLY_DATABASE_URL`**.

Law: `docs/lockdown/PROOF-ENGINE-SHADOW-LEVER-2026-08-30.md`. Enforcement (`PROOF_REPLAY_FAIL_ON_FAIL=1`) is **unset**. Arm per module when that module’s disagreement list is empty — **never globally**.

GO-CLOSE-188 Miss-C **76/76 filed** (Cascade). Do not re-file those cells.

---

## CC-1 — NOW = connected sql-runner paste

1. Read-only `DATABASE_URL` (prod). Run `node scripts/proof-engine/replay-all.mjs --live-sha=d41124e --write` (or sql-runner over C25–C31 INV wrappers).
2. **Paste raw stdout** on OUTBOX-CC-1 first line + a file under `docs/lockdown/GO-CLOSE-188/CC-1/` if long.
3. Do **not** cite an economics Live cell until that paste exists.
4. G1: `is_sample_data` on TEST writes. Keep TEST on books.
5. Ask Jorge (GitHub) to set secret **`PROD_READONLY_DATABASE_URL`** if unset — you do not invent PASS when URL is missing.
6. Never `trigger_deploy`. Never flip `PROOF_REPLAY_FAIL_ON_FAIL`.

## CC-2 — NOW = live walk (SHA = d41124e)

1. Re-verify **BANK-ECON-04 / BANK-SURF-04** and CC-1 DEFECT A/B **on this live SHA** (not on `b8fc09c`).
2. **Do not** stamp C25–C31 Live from ledger prose. Economics Live = sql-runner only.
3. Optional: Chrome A1–A7 on planners **after** Codex/Cursor — Claude already owns the numbered walk. If you walk, post **numbers**, not “looks good.”
4. ACCT-R-04 / SETL-VERIFY-01 / USER-VERIFY-01 stay as already routed. Never force-stamp. Never restamp U14.

## CC-3 — NOW = leftover unique FE (not Miss-C lists)

1. GO-CLOSE-188 lists/customers Miss-C is **filed**. Do not recertify. Do not steal `/dispatch/planners`.
2. Unique leftover chrome in **your** prefix (banking USMCA / eld / lists unique FINDING that is not a 50276-class refile).
3. No verify-steps unless EVEN claimed on main first. Never `trigger_deploy`.

## Codex — NOW = `verify-planner-grid-canonical`

1. **Rule 37:** claim EVEN number on `CLAIMED-NUMBERS.json` (own PR, CLAIM-RESERVE) **then** author `scripts/verify-planner-grid-canonical.mjs` + `scripts/verify-steps/NNNN-*.mjs`.
2. Plant A1–A3, A5–A7 from **source** (gradient, 52px day, 34px row, no `Available` in track, dwell label, OOS not sticky-flush).
3. **A4 cannot be proven from source.** Guard must require `data-load-id` on `.pg-bar`. Live/Chrome: `querySelectorAll('[data-load-id="'+id+'"]').length === 1`.
4. Routes: `/dispatch?view=units` · `/dispatch/planners/{timeline,driver,truck,loads}`. All must import `PlannerGrid`.
5. No money. No matrix paint.

## Cascade — NOW = unique FINDING only

1. 76 Miss-C **done**. Do not re-audit those cells as OPEN certify.
2. Rewrite any OUTBOX that says ECON is waiting on **definitions**.
3. Unique leftover FINDING on live `d41124e`. Never recertify U14. No product PRs.

## Cursor (self)

Lead deploy cadence. Do not babysit this deploy (already live). Next: secret for postdeploy replay; then enforcement conversation **per module**.
