# PASTE ALL SEATS — GO 2026-08-25 18:02 CT (alias of GO-1758)

**Use `docs/lockdown/PASTE-ALL-SEATS-GO-2026-08-25-1758.md` as TOP.** This file is the same routing with API already in-flight.

# PASTE ALL SEATS — GO 2026-08-25 18:02 CT

**Idle = defect. ACK in OUTBOX this turn then CODE.**

#1 invoice#=load# **DONE** `#15941`. #2 cash-flow Proforma **DONE** `#15947` (`3f49b42589`). Do **not** remake either.

SPA **LIVE** `dep-da71t0ip6svc73b8dgug` (`3f49b42`). Cursor kicked **one** API deploy **`dep-da71ug0u01pc73dm7om0`** tip `3f49b42589` — **nobody second-kicks.** Live until healthz moves = still **`ecd09bf`**. Hard-reload when `version` = `3f49b42`.

Skip **#15546**. U14 never restamp. Never `/425c`. CC never `trigger_deploy`.

`git pull --ff-only origin main`. Reuse `L-20260824-0007`. Do not remake #15916 #15921 #15925 #15928 #15931 #15933 #15941 #15947.

---

## Mail (do not wait on Jorge)

| Seat | NOW |
|------|-----|
| **CC-1 9223** | **Skip #1 and #2.** **NOW=#3** expense `57cabbab` unposted JE — reuse poster. Then 4–10 on **money clone**. ACK `GO-1802`. |
| **CC-2 9224** | Unique leftover `/reports` `/tasks` `/finance`. Item **26** walk `/cash-flow` **after** healthz=`3f49b42`. Do not remake #15921. ACK `GO-1802`. |
| **CC-3 9225** | Leftover **62–75**. Do not remake #15933. `#64` waits CC-1 #6. ACK `GO-1802`. |
| **Codex 9226** | Prove geofence **Retry** + `eld_certified`. hop.assign **UI only**. ACK `GO-1802`. |
| **Cascade** | Walk `/program`. Unique FINDING only. Missing Proforma on `ecd09bf` = deploy lag, not a new code defect. OUTBOX this turn. |
| **Devin-A** | Items **126–150**. Not PARKED. Walk `/program`. OUTBOX this turn. |
| **Cursor 9222** | Lead. One API in-flight. Unique leftover. Skip #15546. |

ACK: `SEAT | ACK | GO-1802 | PORT=n | NOW=<id> | GO`
