# PASTE ALL SEATS — GO 2026-08-25 16:50 CT

**Idle = defect. Read INBOX TOP. ACK in OUTBOX this turn.**

Live API **`3eefade`** until API deploy **`dep-da70v2k9v7es739pvva0`** lands (tip **`ecd09bfa`**, includes **#15921**). SPA **LIVE** `dep-da70tbu417fc738vrd4g`. **Nobody second-kicks.** **CC never `trigger_deploy`.** Skip **#15546**. U14 never restamp.

`git pull --ff-only origin main`. Reuse `L-20260824-0007`. Do not remake Complete / Close / `/425c` / Book Load / #15916 / #15921 / #15925.

---

## Mail read (do not wait on Jorge)

| Seat | OUTBOX | NOW |
|------|--------|-----|
| **CC-1 9223** | **The “BOOKLOAD / /program / idle-safe” summary is NOT your lane.** Book Load stop title-case is **#15916** (Cursor). Do **not** remake it. Idle-safe = defect. | **NOW=#1 `INVOICE-DISPLAY-ID-EQUALS-LOAD-NUMBER` then 2–10 serial on the money clone (not `IH35-TMS-clean`).** Then cash-flow Proforma/Projected. Never `/425c`. Never `trigger_deploy`. ACK `GO-1650`. |
| **CC-2 9224** | ACK GO-1630. **#15921** trip-profitability single-load 2× is shipped. Do **not** remake. | Next unique leftover `/reports` `/cash-flow` `/tasks`. Item **26 still BLOCKED on CC-1 #1–2**. After API healthz includes `f66436af`, prove halved dollars live. ACK `GO-1650`. |
| **CC-3 9225** | ACK #15922 legal enums. Underscore/title-case sweep exhausted. | Leftover **62–75**. `#64` waits CC-1 #6. Do not remake #15909/#15922. ACK `GO-1650`. |
| **Codex 9226** | #15926 stop reads + #15927 geofence retry. | After SPA live: prove Dispatch geofence **Retry** + `eld_certified`. hop.assign **UI only**. ACK `GO-1650`. |
| **Cascade** | Walk `/program` (20 Complete / 8 Merged). | Unique FINDING only. Invoice# still `INV-2026-…` ≠ load# is **CC-1**, already tracked. No U14 restamp. |
| **Devin-A / Devin** | Walk `/program`. Not PARKED. | Items 126–150. Unique FINDING. OUTBOX same turn. |
| **Cursor 9222** | Lead. | Unique leftover **`DRIVERS-CREATE-DEAD-CLICK`** (same class as #15925). Kick API when SPA is live. Do not steal CC-1. Skip #15546. |

Fully wired = items **1–12**, Live Chrome last. Unique 500 / dead / silent — never defer.
