# PASTE ALL SEATS — GO 2026-08-25 17:15 CT

**Idle = defect. Read INBOX TOP. ACK in OUTBOX this turn or CODE.**

Owner in chat: **nobody idle.** Cursor is **authorized in the money lane** on **#1** because CC-1 was idle. **CC never `trigger_deploy`.** Skip **#15546**. U14 never restamp.

Live API **`ecd09bf`** (`dep-da70v2k9v7es739pvva0` LIVE, includes **#15921**). SPA autoDeploy **IN FLIGHT** `dep-da716d52g9dc73881p6g` tip `465f46cb` — **nobody second-kicks API or SPA.**

`git pull --ff-only origin main`. Reuse `L-20260824-0007`. Do not remake Complete / Close / `/425c` / Book Load / #15916 / #15921 / #15925 / #15928 / #15931 / #15933.

---

## Mail read (do not wait on Jorge)

| Seat | NOW |
|------|-----|
| **CC-1 9223** | **Grep `origin/main` `from-load.ts` for `const displayId = loadNumber` BEFORE you code #1.** If present (Cursor shipping), **skip to #2 `CASHFLOW-PROFORMA-PROJECTED-LABELED`** then 3–10 on the **money clone**. Do not dual-author from-load. Never `/425c`. Never `trigger_deploy`. ACK `GO-1715`. |
| **CC-2 9224** | Do **not** remake **#15921**. Prove halved dollars on live API `ecd09bf`. Next unique leftover `/reports` `/cash-flow` `/tasks`. Item **26 waits #2** (cash-flow labels). ACK `GO-1715`. |
| **CC-3 9225** | Leftover **62–75**. Do **not** remake **#15933** WO-AUTO-BILL-NEVER-POSTS-GL-JE. `#64` waits money #6. ACK `GO-1715`. |
| **Codex 9226** | Prove Dispatch geofence **Retry** + `eld_certified` on live API `ecd09bf` (SPA catching up). hop.assign **UI only**. ACK `GO-1715`. |
| **Cascade** | Unique FINDING only. Invoice#=load# is **in flight / Cursor money** — do not restamp U14. Walk `/program`. OUTBOX same turn. |
| **Devin-A / Devin** | Items **126–150**. Unique FINDING. Not PARKED. OUTBOX same turn. |
| **Cursor 9222** | Lead. **NOW=#1 `INVOICE-DISPLAY-ID-EQUALS-LOAD-NUMBER`** (owner authorized money). Then Neon apply CHECK. Deploy only when SPA idle AND 5–10 min / 5–10 PRs. Skip #15546. |

Fully wired = items **1–12**, Live Chrome last. Unique 500 / dead / silent — never defer.
