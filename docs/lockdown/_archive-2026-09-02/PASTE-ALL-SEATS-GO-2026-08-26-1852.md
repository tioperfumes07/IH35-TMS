# GO-1852 — IDLE = LIVE WALK — VERTICAL LEFTOVER (18:52 CT)

**THIS IS NOW.** Jorge is not the messenger. Pull INBOX TOP. Ping ≠ ACK.

Live **`f12ab6e`**. Hard-reload. Nobody `trigger_deploy`. Skip #15546. U14 never restamp.

## Idle law (permanent this GO)

If you have no open FINDING in-hand **this minute**, you are not allowed to wait. Same turn:

1. Open **your next leftover URL** (table below).
2. **Live-click** every control / or CREATE labeled TEST then leave it (CREATE-TEST-THEN-VOID).
3. Unique 500 / dead click / silent no-op → file `GUARD-WORKORDERS.md` + OUTBOX one line + fix if your lane.
4. CLEAN → OUTBOX `UNIQUE-FINDING-CLEAN | url=... | SHA=f12ab6e` and **immediately take the next URL in your vertical**. No HOLDING.

## Vertical leftover POST (never U14). Take the next unused row in YOUR column.

| Seat | Vertical (in order — skip already CLEAN this cycle) |
|------|-----------------------------------------------------|
| CC-1 | Board OPEN money serial. **NOW=`BANK-ACCOUNT-HIDE-CAPABILITY-FAILURE-FAILS-OPEN`**. Then next OPEN CC-1 row same turn. STOP `/425c`. Never GL-new-math. |
| CC-2 | `/reports` → `/cash-flow` → `/finance` → `/tasks` unique. Do not remake #16375 if already on main. Never GL. |
| CC-3 | `/program` → `/system` → `/eld` unique. Do not remake /docs /compliance /home /help. |
| Codex | Keep Driver/Fleet/Safety/Fuel unique. If no code FINDING: live-walk next fuel/driver/fleet/safety leaf + CREATE TEST. Do not remake F6687–F6690. |
| Cascade | `/dispatch` unique on **f12ab6e** (customers FINDING already filed). Then leftover POST `/driver-hub`. FINDING only. |
| Devin | `/vendors` live CREATE-TEST then `/dispatch`. One Devin. Devin-A VOID. |
| Cursor | Lead. Leftover FE overflow only if bus green. |

ACK: `SEAT | ACK | GO-1852 | PORT=n | NOW=<url-or-id> | SHA=f12ab6e | GO`
