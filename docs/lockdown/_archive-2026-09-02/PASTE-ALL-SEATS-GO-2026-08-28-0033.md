# GO-0033 — Safety TEST + Factoring chrome + Fleet

`git pull --ff-only origin main`
Queue index: `docs/lockdown/GO-QUEUE-0030-0054-INDEX.md`
THIS packet: `docs/lockdown/PASTE-ALL-SEATS-GO-2026-08-28-0033.md`

**NOW = this GO.** After FAST-MERGE/ACK, immediately start **GO-0034** without waiting for Jorge or Cursor chat. Idle=defect.

USMCA only. Chrome + labeled TEST DATA create. Unique FINDING = 500/dead/silent. KEEP TEST. U14 never restamp. PROG-01 SKIP. CC-1 money does **not** gate other seats. Nobody except Cursor `trigger_deploy`. Skip #15546 #16895.

ACK: `SEAT | ACK | GO-0033 | NOW=<one line> | SHA=<healthz> | GO`

## Per coder (do this, this GO)

### CC-1 (9223)
`/factoring` chrome. Reserve accounts owner-manual (Rule 19). Unique money leftover.

### CC-2 (9224)
`/finance` unique leftover. Honest flag-off is NOT a FINDING.

### CC-3 (9225)
`/safety` CREATE labeled TEST event or fine. Chrome every tab.

### Codex (9226)
`/fleet` unique leftover + TEST unit/trailer create if wizard lives.

### Cascade
Walk `/safety` `/fleet`. Unique FINDING.

### Devin
`/vendors` leftover unique.

### Devin-A
`/customers` leftover unique.

### Cursor (9222)
Lead. Safety spawn-WO chrome if dead.

## Forbidden
HOLD. Waiting for a new GO. Waiting for CC-1. PROG-01. U14 restamp. TRANSP/TRK/QBO write-back. Dual Devin.
