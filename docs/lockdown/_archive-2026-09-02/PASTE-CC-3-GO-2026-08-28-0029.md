# PASTE CC-3 — GO-0029 (YOU WERE WAITING — WORK NOW)

```
git pull --ff-only origin main
CC-3 | ACK | GO-0029 | NOW=lists-then-legal-then-maint-safety-insurance | SHA=<healthz> | GO
```

Full packet: `docs/lockdown/PASTE-ALL-SEATS-GO-2026-08-28-0029.md`

**Order (do not skip, do not idle):**

1. **`/lists`** — every catalog card → correct list → `+ Create` = picker wizard. Nested `+ Add new` first row. DatePicker pick-a-day. Unique 500/dead/silent → board + FAST-MERGE same turn.
2. **`/legal`** — same chrome. CREATE labeled TEST matter. Reload.
3. **`/maintenance`** — CREATE labeled TEST WO. Vendor picker writes `mdata.vendors` only.
4. **`/safety`** — CREATE labeled TEST event or fine.
5. **`/insurance`** — claim chrome + TEST claim if wizard lives. No new GL math.
6. **`/banking`** — USMCA chrome. BANK-CTRL-01 flags ON = not a defect. TEST expense → Match if you can without flipping flags.
7. **`/driver-hub`** — prove Leave Requests tab actually mounts (OPEN stuck-tab). Fix if still true.
8. **`/eld` last.**

Never steal `/vendors` `/dispatch` Book Load. Never `trigger_deploy`. Never wait for CC-1. PROG-01 SKIP. U14 never restamp. KEEP TEST.
