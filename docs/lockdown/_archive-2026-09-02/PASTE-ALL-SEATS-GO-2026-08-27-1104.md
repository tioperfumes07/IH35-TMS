# GO-1104 — OWNER: DEPLOY + EVERY SEAT WORK NOW · IDLE = DEFECT · 2026-08-27 11:04 CT

**THIS IS NOW.** Owner: deploy + get current + **nobody idle**.

**API deploy LIVE** `dep-da860rqfngtc73bgpm4g` · healthz **`8e4380a`**. Hard-reload. Cursor may kick the next 5–10 gate when `origin/main` is ahead. CC never `trigger_deploy`. Skip #15546. Never restamp U14.

Canonical: Fully-Wired 1–12 (`docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md`) + vertical + unique FINDING only (500 / dead / silent). Watching INBOX = **idle = defect**.

ACK: `SEAT | ACK | GO-1104 | NOW=<id> | SHA=<healthz> | GO`

---

## LAW

```
LAW: Fully-Wired 1–12. Vertical class. Root cause + R=W + F+R + guard + Live Chrome on CURRENT healthz.
No patch. No defer. Grep origin/main before remake. Exclusive URL. Skip #15546. Never trigger_deploy. Never recertify U14.
Idle / HOLDING / watching INBOX = defect. Work NOW.
```

---

## LIVE vs MAIN (measured 11:04 CT)

- Live: `15857b1` (ok:true) until this deploy lands
- origin/main: `8e4380a` — **41 commits** including commodity persist #16693, F6797 void-not-delete, F6803A, F6843A, lists/safety leftover. Board OPEN rows for those three money IDs are **stale** — grep main (DELETE work_order_lines **gone**).

---

## BOX — CC-1 · 9223

```
SEAT=CC-1 PORT=9223 GO-1104 IDLE=DEFECT
Never trigger_deploy. Never /425c. Grep origin/main before remake.

NOW:
1) When healthz=8e4380a: Live Chrome Book Load commodity/weight persist (ACCT-F9508 on main). CREATE-TEST-THEN-VOID.
2) Mine NEXT unique CC-1 money OPEN that still exists on grep (F6797/F6803A/F6843A already merged — do not remake).
3) Exclusive /accounting /banking /settlements /factoring leftover unique only.

ACK: CC-1 | ACK | GO-1104 | PORT=9223 | NOW=commodity-live-chrome | SHA=<healthz> | GO
```

---

## BOX — CC-2 · 9224

```
SEAT=CC-2 PORT=9224 GO-1104 IDLE=DEFECT
Never GL. Never trigger_deploy. Watching INBOX = defect.

NOW: unique FINDING (500/dead/silent) on exclusive /cash-flow /reports /finance /tasks. Fully-Wired 1–12. After healthz=8e4380a re-prove deduction-trail. Do not remake #16654 / void-reversal.

ACK: CC-2 | ACK | GO-1104 | PORT=9224 | NOW=unique-leftover | SHA=<healthz> | GO
```

---

## BOX — CC-3 · 9225

```
SEAT=CC-3 PORT=9225 GO-1104 IDLE=DEFECT
Never trigger_deploy. Do not remake status-400 / trip-pairing / HEADER-CREATE / tire-registry.

NOW: Live Chrome driver labels on CURRENT healthz (resolver on live+main). Then exclusive unique leftover /lists /legal /compliance. Fully-Wired 1–12.

ACK: CC-3 | ACK | GO-1104 | PORT=9225 | NOW=driver-label-live-chrome | SHA=<healthz> | GO
```

---

## BOX — Codex · 9226

```
SEAT=CODEX PORT=9226 GO-1104 IDLE=DEFECT
Never restamp U14. Never trigger_deploy. Money OPEN → CC-1 after grep.

NOW: next unique 500/dead/silent vertical on /drivers /fleet /safety /fuel /maintenance /insurance. Fully-Wired 1–12. Do not remake F6797/F6803A/F6843A.

ACK: Codex | ACK | GO-1104 | PORT=9226 | NOW=/safety-vertical | SHA=<healthz> | GO
```

---

## BOX — Cascade

```
SEAT=CASCADE GO-1104 IDLE=DEFECT
Unique FINDING only. Never recertify U14. Never stamp FIXED without grep+live on CURRENT healthz.

NOW: walk /dispatch + /driver-hub on 15857b1 now; again on 8e4380a when healthz moves. Board+OUTBOX same turn.

ACK: CASCADE | ACK | GO-1104 | NOW=/dispatch | SHA=<healthz> | GO
```

---

## BOX — Devin + Devin-A

```
SEAT=DEVIN / DEVIN-A GO-1104 Exclusive /vendors. IDLE=DEFECT
Unique FINDING only. HEADER-CREATE wired — do not remake. Re-prove Inactivate/Reactivate/SAFER on 8e4380a.

ACK: Devin | ACK | GO-1104 | NOW=/vendors | SHA=<healthz> | GO
```

---

## BOX — Cursor lead · 9222

```
SEAT=CURSOR PORT=9222 GO-1104 LEAD
Deploy already kicked — nobody second-kick. Census. Overflow leftover FE unique. Skip #15546.

ACK: Cursor | ACK | GO-1104 | PORT=9222 | NOW=lead | SHA=<healthz> | GO
```
