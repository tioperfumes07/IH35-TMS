===============================================================================
CURSOR WORK ORDER — 2026-08-30. TWO TASKS + DEPLOY LANE.
===============================================================================
DISCRIMINATOR: run_sql_transaction with
  ["SET LOCAL app.bypass_rls = 'lucia'", "<query>"], je count >= 2218.
Rule 42: you are the only seat that deploys.
Rule 37: CLAIM -> MERGE -> AUTHOR. Cursor takes EVEN verify-step numbers.
FAST-MERGE: deploy every 5-10 PRs, never past 10.

-------------------------------------------------------------------------------
TASK 1 — GUARD. Wire AFTER CC-1's FACT-RESERVE-01 merges.
-------------------------------------------------------------------------------
Author scripts/verify-steps/NNNN-factoring-reserve-derives-from-reserve-pct.mjs
It must FAIL if either of these reappears in
apps/backend/src/accounting/factoring-advances.routes.ts:
  * reserve computed as (invoiceTotalCents - advanceAmount)   [the complement bug]
  * factor_fee_cents written as a literal 0 / omitted from the INSERT

WHY A STATIC GUARD IS MANDATORY HERE: this defect BALANCES.
  185000 - 5550 - 0 = 185000 - 2775 - 2775 = 179450
Cash is identical either way, so no debits=credits check, no reconciliation, and no
smoke test will ever catch it coming back. Only a static source guard will.

-------------------------------------------------------------------------------
TASK 2 — FARO DIGITAL BANK ACCOUNT. Secondary. Do NOT block CC-1's P0 on this.
-------------------------------------------------------------------------------
LIVE: 0 rows in banking.bank_accounts matching %faro% or %factor% for USMCA.
The factoring tables exist. The faucet does not.

Create "Faro Factoring - USMCA" with its OWN GL account.
NOT shared with USMCA FREIGHT. NOT shared with Relay.

ACCEPTANCE - the statement shape must reconcile:
  AR 95,075.00  -  escrow reserve 1,426.13  -  cash reserve 5,000.00  =  NFE 88,648.87
And the August proceeds tie:
  face 95,075.00 - reserve 1,426.13 - fee 1,426.13 - wire 120.00 - cash rsv 5,000.00
    = 87,102.74  = Faro "Payments to You"   EXACT

-------------------------------------------------------------------------------
STANDING CORRECTION TO YOUR LAST VERIFICATION PACKET
-------------------------------------------------------------------------------
Your verified table said: "SETL-VERIFY-01 - 17 / 0 bills / 0 payments."
That reads the driver_settlements HEADER columns. The system says otherwise.

S-2026-0002 (Rafael Rivero Reynoso, $297.60, load L-20260810-0003) in
driver_settlement_gl_runs / gl_bills:
  driver_vendor_id      2ba67a02-6bc1-4c3e-907f-0a410fef93c7
  driver_bill_id        31f155f3
  accounting_bill_id    35b8ce38
  bill_journal_entry_id b7575a45
  cash_bill_payment_id  d6f22f71
  cash_journal_entry_id 8bc9947e
  run_status            reversed (2026-08-21T17:46:35Z)

The poster fired, produced a complete chain, and was reversed. Three seats (CC-2, Claude,
Cursor) independently made the same call from the same empty header columns. CC-1 task 5
fixes the write-back that caused it.

ANSWER TO YOUR QUESTION ("confirm Close pay-run writes both bill IDs"):
IT DOES NOT. The only writer of those header columns is
apps/backend/src/payroll/driver-settlement.service.deprecated.ts:653-654, and payroll.*
is RETIRE under the linkage law. The live poster writes them only into
driver_settlement_gl_bills. That is the defect, assigned to CC-1 task 5.

Your two corrections to me were both right and are accepted: Manager active-with-password
is 2 (not 3 - I reported the columns without intersecting them), and Rule 37 is
CLAIM -> MERGE -> AUTHOR on a verify-step EVEN number, not a GitHub PR number.
