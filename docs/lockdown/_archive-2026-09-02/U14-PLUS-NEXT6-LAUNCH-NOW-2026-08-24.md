# U14 + NEXT 6 — LAUNCH-READY NOW (owner 2026-08-24 12:12 CT)

**Not a second U14 exclusive campaign. Do not restamp the 14/14 table.**

**Bar (already law):** Fully-Wired 1–12 + Live Chrome on **current** `healthz/shallow` + **zero unique leftovers** + CREATE-TEST-THEN-VOID (labeled TEST; owner voids at launch).

**Scope this hour**

| Set | Modules | Routes |
|-----|---------|--------|
| Urgent 14 | accounting · bank · settlements · factoring · dispatch · vendors · customers · drivers · fleet · lists · maintenance · safety · insurance · legal | `/accounting` `/banking` `/driver-finance` `/factoring` `/dispatch` `/vendors` `/customers` `/drivers` `/fleet` `/lists` `/maintenance` `/safety` `/insurance` `/legal` |
| Next 6 | cash-flow · finance · driver-hub · reports · tasks · compliance | `/cash-flow` `/finance` `/driver-hub` `/reports` `/tasks` `/compliance` |

`/425c` = **do not loop.** ELD stub = not missing.

**CREATE TEST NOW.** Empty is not a stop. Memo/name = `TEST DATA` / `TEST-LAUNCH-<seat>-<date>`. Void later. No TMS→QBO write-back.

**Stamp CERTIFIED COMPLETE** only on current live SHA after you click the hops and unique 500/dead/silent = none. Historical U14 SHA alone ≠ launch-ready.

**Seat NOW**

| Seat | Modules this turn |
|------|-------------------|
| Cursor | Fuel spend fail-loud · `/customers` Statements/Recurring/Late Fees TEST · stamp only what you prove |
| CC-1 | `/accounting` create TEST invoice/bill if needed · bills Claim/WO still live |
| CC-2 | `/reports` + `/tasks` + `/cash-flow` — TEST then unique |
| CC-3 | `/lists` + `/maintenance` + `/legal` — TEST then unique |
| Codex | `/drivers` + `/fleet` + `/fuel` after spend-fix deploy — TEST then unique |
| Cascade | `/banking` `/settlements` `/factoring` `/finance` — unique FINDING if still true |
| Devin-A | `/customers` 3 money tabs + `/vendors` + `/dispatch` — create TEST, not PARKED |

Idle = defect.
