# UNIFIED_BLUEPRINT_ADDITIONS.md — append (GLOBAL sort + UI contract)

## 2026-06-07 — GLOBAL SORT + UI CONTRACT (whole software)

1. SORTABLE COLUMNS EVERYWHERE: every column header in every list, catalog, bills, invoices, charts,
   categories, and any table across the ENTIRE software is clickable to sort. First click = ascending,
   second click = descending (toggle). Example: click "Loads" -> order by load number asc; click again -> desc;
   click "Unit" -> order by unit number asc; etc. QuickBooks-style. Active sort column shows an asc/desc caret.
2. FILTER-TYPEAHEAD lists (not bare dropdowns): type to filter to related matches; collapse on blur; allow
   unselect/clear; never block advancing when left empty.
3. UNIVERSAL SIX-COLUMN SIZING: tiles/click-boxes size to the six status columns (Pending Assignment, Assigned,
   In Transit, Delivered, Completed, Cancelled) grid.
4. CURRENCY format (4800 -> 4,800.00) and WEIGHT format (24600 -> 24,600) + kg/lbs; centered headers; equal
   paired field sizes; date/appointment/window = calendar type-and-adjust.
5. NEVER DELETE, ALWAYS ADD. ARCHIVE never DELETE.
