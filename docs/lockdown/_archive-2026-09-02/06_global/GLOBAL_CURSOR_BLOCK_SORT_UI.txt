AGENT-1 · Block <N> of <M> — PHASE <tracker-phase> / TASK <set-from-latest-tracker> — Global sortable columns + UI contract
RBC TARGET: branch feat/global-sortable-columns

[!] Set a real tracker TASK id before dispatch.
STANDING ORDERS: foreground only, no subagents; no retries — STOP, paste exact error; live updates every 5 min CST/Laredo + real measured data; confirm worktree pwd, git status, log, rev-parse; show diff --staged --stat before commit; stop on unexpected.
LOCKS: ADDITIVE ONLY; remove nothing. Do not touch the Book Load wizard. RESPOND-BEFORE-CODE (RULE 6): inventory current table component(s) + which lists already sort vs not; deltas; NEW spec. Wait for GO.
LANE LOCK: one writer per magnet file per cycle; shared table component is the single writer here.
SCOPE (additive) per GLOBAL_SORT_AND_UI.md:
- Make the shared table/list component support click-header sort: 1st click asc, 2nd desc, caret indicator. Apply across lists/catalogs/bills/invoices/charts/categories app-wide (opt-in per column via header config; default on for data columns).
- Filter-typeahead selection component (collapse on blur, unselect/clear, non-blocking) as the standard.
- Currency + weight + date formatters as shared utils; centered headers; equal paired-field sizing tokens; six-column sizing token.
CI GUARD: assert the shared table exposes a sortable header API and toggles asc/desc; formatter unit tests (currency/weight); lint that flags bare <select> where a typeahead is required.
GATES (Std Order #16 v2): build:backend EMIT, frontend tsc -b, verify:arch-design, vitest for sort + formatters. verify+commit+push as ONE step.
ACCEPTANCE: any data column header sorts asc/desc on click app-wide; typeahead standard; formatters applied.
PAUSE after RESPOND-BEFORE-CODE. PAUSE before merge — Claude verifies live.
