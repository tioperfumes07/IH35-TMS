# CC-2 — GRADES. DOES NOT BUILD.
## FREE LANE — start now, no deploy needed
`scripts/tieout/faro-factoring-statement.mjs`. It is a 30-line stub with one `fail()` and no
comparison logic. Expected, already verified to the cent — **do not re-derive**:

```
face             95,075.00        (33 invoices, INCLUDING 016)
escrow reserve    1,426.13
discount fee      1,426.13
wire fees           120.00
cash             92,102.74        = face - escrow - fee - wire
NFE              88,648.87        = face - 1,426.13 - 5,000.00 cash reserve
```
Tolerance 0 · record the observed value pass or fail · an empty result is never a pass ·
**never lower the expected value to make it pass.**

Expect it to fail by **$3,800** until CC-1 rebuilds 016. That failure is correct — record it
with its cause, do not "fix" it by dropping 016 from the expected face.

## Grading rules for 016
Missing because CC-1 skipped it = **CC-1 miss**. Missing because the factoring screen cannot
pledge net = **P0 against factoring**. Grade the cause, not the symptom.

## Defect register — add these
`FACTORING-PLEDGE-IGNORES-CREDIT-MEMOS` (P0, fixed #18393 — verify live) ·
`NO-DILUTION-CLOSEOUT-PATH` (P0) · `QBO-MIRROR-STALE` (P1 — `mdata.qbo_ar_invoices` last
mirrored 2026-08-14, stops at doc 013 while live QBO runs to 036; never cite it as proof of
an absence) · `CREDIT-MEMO-REASON-ENUM-INCOMPLETE` (P2).

Hard Rule 5 unchanged: **only CC-2 writes `prod_verified`.** Idle is a defect.
