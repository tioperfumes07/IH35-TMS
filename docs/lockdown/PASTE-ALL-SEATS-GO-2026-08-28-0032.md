# GO-0032 — Maintenance TEST WO + Settlements chrome + Planner leftover

`git pull --ff-only origin main`
Queue index: `docs/lockdown/GO-QUEUE-0030-0054-INDEX.md`
THIS packet: `docs/lockdown/PASTE-ALL-SEATS-GO-2026-08-28-0032.md`

**NOW = this GO.** After FAST-MERGE/ACK, immediately start **GO-0033** without waiting for Jorge or Cursor chat. Idle=defect.

USMCA only. Chrome + labeled TEST DATA create. Unique FINDING = 500/dead/silent. KEEP TEST. U14 never restamp. PROG-01 SKIP. CC-1 money does **not** gate other seats. Nobody except Cursor `trigger_deploy`. Skip #15546 #16895.

ACK: `SEAT | ACK | GO-0032 | NOW=<one line> | SHA=<healthz> | GO`

## Per coder (do this, this GO)

### CC-1 (9223)
`/settlements` chrome + leftover money unique. TEST settlement hop if wizard lives.

### CC-2 (9224)
`/cash-flow` unique leftover. Proforma labels: grep main before remake.

### CC-3 (9225)
`/maintenance` CREATE labeled TEST WO → `maintenance.work_orders`. Vendor = `mdata.vendors`.

### Codex (9226)
`/dispatch` planner/POD/BOL unique leftover. No remake of shipped DSP-F71xx.

### Cascade
Walk `/maintenance`. Unique FINDING.

### Devin
Vendor picker from WO path if you hit it — still `/vendors` owner. Unique leftover.

### Devin-A
`/customers` leftover unique. KEEP TEST.

### Cursor (9222)
Lead. WO vendor picker chrome if CC-3 files it.

## Forbidden
HOLD. Waiting for a new GO. Waiting for CC-1. PROG-01. U14 restamp. TRANSP/TRK/QBO write-back. Dual Devin.
