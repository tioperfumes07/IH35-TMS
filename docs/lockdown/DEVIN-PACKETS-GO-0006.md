# Devin packets · GO-0006 · 2026-08-28

**No post-gl.** KEEP TEST. Unique FINDING only. Never `trigger_deploy`.

## 1 hour
Query-back every TEST row you created (`is_sample_data`, JE, bill, expense). File unique FINDING if 500 / dead / silent on `/vendors`. Do not create new bills/expenses that post GL.

## 3 hours
Walk VendorDetail: Record Payment bank id, audit tab, bill GL column vs expense GL column. One FINDING per unique defect. Query-back, do not restamp U14.

## 1 day
Stay on vendors leftover unique until CC-3 lands those PRs on live SHA. Then assist Codex on `/dispatch` leftover only after `STEAL-CLAIMS.json`. No GL math.
