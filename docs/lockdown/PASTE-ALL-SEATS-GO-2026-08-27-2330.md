# GO-2330 · 2026-08-27 23:30 CT · WAKE IDLE · FORBIDDEN NEXT=poll

**THIS IS NOW** for wake/SQL/vendors/Book Load. Live healthz **`7eda992`**. **GO-2340 amends seed/steal:** no Event-2 POD seed; steal after `STEAL-CLAIMS.json`. Owner: Devin + Cascade were idle.

**FORBIDDEN:** `NEXT=poll`. A walk with N=0 is not a stop — CREATE labeled TEST that turn.

ACK: `SEAT | ACK | GO-2330 | NOW=<below> | SHA=7eda992 | GO`

| Seat | NOW (do this immediately) |
|---|---|
| **Cascade** | **STOP poll.** (1) SQL USMCA earn vs bill latch counts + invoices with earn and no bill — paste query+counts on OUTBOX. (2) Unique FINDING `/fuel` leftover POST (500/dead/silent). CREATE TEST fuel if empty. No product PR. Start `~/Desktop/IH35-START-1-CASCADE.command` if that file exists. |
| **Devin** | **STOP ping-wait.** CREATE labeled TEST vendor if needed (KEEP). Then `/vendors` unique FINDING on `7eda992`. **Do not capture/approve POD.** Not PARKED. |
| **Devin-A** | CREATE TEST if picker empty. `/customers` then `/dispatch` Book Load **KEEP**. Lifecycle Chrome (not waiting on Cascade). Not PARKED. |
| **CC-1** | 1–4 then B. If merge-blocked: TEST asset+loan+bank txn with `is_sample_data` on the whole chain. |
| **CC-2** | Claim ≡3 empty-gate guard. If blocked: steal leftover **after STEAL-CLAIMS.json**. |
| **CC-3** | Seed TEST detention + factoring batch. **No POD until B is live.** |
| **Codex** | `/customers` leftover or steal **after STEAL-CLAIMS.json**. No POD. |
| **Cursor** | This wake + Wave 0 declare/bind (this PR). Then next money-leaf C25 bind. |

SQL Cascade must run (lucia + opco USMCA):

```sql
SELECT event, count(*) FROM accounting.load_revenue_recognition_postings
 WHERE is_active GROUP BY 1;
```

USMCA only. Never recertify U14. Never `trigger_deploy`.
