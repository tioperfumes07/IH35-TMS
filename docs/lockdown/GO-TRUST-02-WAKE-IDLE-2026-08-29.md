# GO-TRUST-02 — WAKE IDLE · 2026-08-29 16:40 CT

Idle = defect. ACK on OUTBOX then work. Skip #15546. Customers = Devin. Only CC-2 stamps JSON. Nobody else `trigger_deploy`.

**Law:** Box 3 empty = `not_built` = FIX (vertical by COLUMN). Box 4 empty + Box 3 green = `built_unproven` = ERRAND (observe live SHA; never copy stamps). U6 then leftover unique.

**Live:** deploy `dep-da9l10142hec7387ofu0` tip `627b2a3` (T-03 emptyWhy + currentAuthUser null). Wait for `healthz/shallow` `version` ancestor of `627b2a3` before live 401/PATCH proofs.

| Seat | NOW |
|------|-----|
| CC-1 | Wave-1 money evidence packets (remaining unbound). FAST-MERGE same 15s. No `prod_verified` JSON. Period-close if Wave-1 packet empty. |
| CC-2 | Stamp or REOPEN CC-3 ~70 packets + BANK-ECON-05 REOPEN. After live SHA: T-01 no-cookie matrix 401; T-02 vendor PATCH + audit row. |
| CC-3 | Next chrome/stale-guard packet. No customers. No new verify-steps unless claimed. |
| Codex | Dispatch/drivers/fleet leftover unique. Recipe C. |
| Cascade | Seed GR-1 `failingNames` if unseeded; unique FINDING only. |
| Devin | Customers only. Vendors do not recertify. |
| Cursor | T-04 worker interval (this PR) then T-05 rows key. |

ACK: `SEAT | ACK | GO-TRUST-02 | SHA=<healthz> | GO`
