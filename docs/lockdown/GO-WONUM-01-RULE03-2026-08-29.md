# GO-WONUM-01 — Rule 03 WO numbers (owner 2026-08-29)

**Format stays.** Withdrawn: short sequential identity; withdrawn: delete `refresh_wo_display_id`.

**Owner D2 (this chat, written):** **(a) Lock V5 on first non-`PEND0` set.** Invoice corrections live in columns; the WO display id does not change again before completion. Edit Rule 03 so “lock once set” is the only V5 sentence (drop the contradicting refresh-on-invoice-change sentence). Cite this GO.

## CC-1 NOW

**D1** — refuse WO create when unit has no `unit_number` (`E_UNIT_HAS_NO_NUMBER`). Remove `COALESCE(unit_number, id::text)` UUID fallback. Do not silently renumber completed WOs. Do not void TEST/SAMPLE/WAVE3 WOs.

**D2** — implement lock-on-first-set. Guard: V5 must not change after a non-`PEND0` value.

**D3** — Rule 03 `{UNIT}` → `mdata.units.unit_number` (not `master_data.units.display_id`). `git grep master_data .cursor/rules/` and report every other hit in the PR body.

Guard ≡1, claim on main first, Rule 17. Fail on UUID-in-display_id, regex mismatch, refresh on completed WO.

Never `trigger_deploy`. Skip #15546. Recipe B — CC-2 stamps.
