# CERTIFIED MEANS ZERO UNIQUE LEFTOVER (owner 2026-08-24 11:13 CT — LAW)

**Answered = closed. Do not invent a second bar.**

If a module has leftover unique work, it is **NOT CERTIFIED COMPLETE**. Calling it CERTIFIED while leftovers sit OPEN is **contradictory and forbidden**.

---

## THE DEFINITION (every seat, every session)

**CERTIFIED COMPLETE** on USMCA means all of these are true **at the same time**:

1. Fully-Wired items **1–12** (`docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md`) — create→canonical, money when owed **or honest flag-off**, forward+reverse, surface bar, chrome, pickers, RLS, guard, **Live Chrome last**.
2. Live Chrome on **current** `healthz/shallow` `version` — unique **500 / dead click / silent no-op / reverse-empty (Neon has rows, UI 0)** = **none**.
3. **Zero unique OPEN leftovers** on that module (`docs/lockdown/LAUNCH-READY-UNIQUE-REMAINDER-2026-08-24.md` + board). If one is OPEN, the module is **NOT COMPLETE**.
4. Proof is **live SHA + Chrome + Neon (lucia same txn when counting) + repo on `origin/main`**. CI-green / Rule 24 `complete:true` / matrix Box 4 / a historical stamp SHA **alone ≠ CERTIFIED COMPLETE**.

**Not leftover (do not treat as incomplete):** empty TMS expected · CREATE-TEST-THEN-VOID · posting flags OFF until Jorge says turn on · Finance Hub honest flag-off · ELD hidden stub · `/425c` do-not-loop unless a **new** 500/dead/silent.

USMCA only. No TRANSP/TRK. No TMS→QBO write-back. FAST-MERGE. CC never `trigger_deploy`. Cursor deploys 5–10 min AND 5–10 PRs, one in-flight.

---

## Sequence (do not pause)

**Phase A — U14 nothing pending.** Close every unique leftover on the 14. Live-verify on current SHA. Then those 14 are CERTIFIED COMPLETE.

**Phase B — remaining sidebar modules.** Same definition, one module at a time per seat URL, Live Chrome, unique FINDING fix same turn, then CERTIFIED COMPLETE. Do not idle until all visible sidebar modules are done.

Idle / HOLD / “waiting for instructions” = defect. You have this file + `INBOX-<SEAT>.md` TOP.

ACK: `SEAT | ACK | ZERO-LEFTOVER-LAW | PORT=n | NOW=<id> | GO`
