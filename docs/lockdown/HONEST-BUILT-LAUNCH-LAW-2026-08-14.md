# HONEST BUILT + LAUNCH LAW (owner-locked 2026-08-14 — PERMANENT)

**Answered = closed. Do not re-ask. Do not soft-read. Do not invent a parallel definition.**

**Who this is for:** owner + **every** coder seat — Cursor · Claude Code / CC-1 · CC-2 · CC-3 · Codex · ChatGPT desktop · Cascade · Devin · GUARD · Windsurf.

**Authority:** This file + `FULLY-WIRED-COMPLETE-BAR-2026-08-13.md` together are the **only** meaning of launch-ready / complete / wired / Built-honest.  
If a seat claims done from a stale INBOX, an old HTML artifact, a green scoreboard cell, or a prior chat summary that contradicts this file — **that claim is a defect**.

**Companions (do not replace):**  
`FULLY-WIRED-COMPLETE-BAR-2026-08-13.md` · `VERTICAL-WIRING-LAW-2026-08-12.md` · `DEFINITION-OF-DONE.md` · `CODER-PASTE-INSTRUCTIONS-2026-08-12.md`

---

## Owner one-liner

> **Launch-ready without Live Chrome** = Fully-Wired items **1–11** true for **every** module / leaf / owed control, with Box 3 **Built** proven by **leaf-specific** guards only.  
> **Live Chrome (item 12) is last.** Soft “yes / wired / done / money complete” without that = process defect.  
> **Do not add scoreboard columns** on Required.json or a 5th Verified Box. Fix lying Built.  
> **Owner 2026-08-21:** the Program **system** scoreboard MUST show **launch-ladder columns** (Wave · Vertical COL · FAST-MERGE · FW 1–11 · Live 12 · Certify) so seats cannot forget Urgent 6 → rest of urgent. That is not a 5th Box and not a new Required leaf.

---

## Boot order (stop reading stale files)

Every session, **in this order**:

1. `docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md` — what “fully wired” means (1–12).
2. **This file** — honest Built + seat lanes + sequence (so you cannot deviate).
3. Your seat INBOX only: `docs/bus/INBOX-CURSOR.md` · `INBOX-CC-1.md` · `INBOX-CODEX.md` · `INBOX-CC-2.md` (as assigned).
4. `docs/bus/OUTBOX-<seat>.md` last ship line — then continue; never invent a new plan.

**Forbidden as source of truth:** old Claude scratch HTML, prior session summaries that say a column is “Done” while `leafRe:".*"` / `|.*` / word-regex Built tags remain, scoreboard % alone, `module-completion/*.json complete:true` alone.

If two docs conflict: **MORE PROTECTIVE wins** → Fully-Wired + this file beat INBOX nostalgia.

---

## What “Built honest” permanently means

| Illegal Built (theater) | Legal Built |
|---|---|
| `leafRe:".*"` / `".+"` / empty | Exact leaf id(s) or anchored prefix that names a real family (`^bills\\.`) |
| Trailing / OR match-all: `…\|.*` or `.*\|…` | No `|.*` anywhere in the claim |
| Word blankets: `.*(create\|modal\|drawer\|wizard).*` | Leaves the guard actually mutates/proves |
| Wrong-direction assert (child→parent credited as reverse_link) | Guard proves the column’s **Required meaning** |
| Legacy WAVE-A/B/C `.*` floor still on disk called “Done” | Broad tag removed or narrowed; Required honesty-dropped if never owed |

**Runtime:** `apps/backend/src/program/matrix-built-auto.ts` `isLeafSpecific()` must reject the illegal shapes above.  
**CI:** `scripts/verify-matrix-built-leaf-specific.mjs` — new/changed broad claims **hard-fail**; legacy broad count **ratchet only DOWN** (target **0**).

**Scoreboard green is not independent verification.** Independent column sweeps (connectivity / reverse_link style) are **scratch evidence for fix PRs**, not a permanent 5th Box.

---

## Launch-ready without Live Chrome (whole product)

For **100% of the software** (not money-lane only):

1. Fully-Wired **1–11** on every leaf (create→canonical · money when owed · F+R · matrix · **surface bar** · chrome · pickers · RLS · guard/evidence).
2. Every Required matrix cell: honest Built **or** honesty_audit drop (never theater Required).
3. Zero illegal `leafRe` Built claims (baseline → 0).
4. Filed real gaps fixed or explicitly not-Required with evidence.
5. **Surface-bar inventory completeness (item 7):** every real modal / popup / drawer / ParityDrawer / wizard / nested-create / toolbar control in code either has a matrix leaf **or** is proven N/A. A column sweep of existing `required.json` leaves does **not** prove the inventory is complete — Cursor owns the leaf-existence audit. **Create-drawer ratchet:** `scripts/verify-surface-bar-create-drawer-inventory.mjs` (LAW-2026-08-14-SURFACE-BAR-CREATE-DRAWER-INVENTORY) — every FE create drawer must have exact `leaf.surface_path` or `FILE_OWNED_BY_LEAF` / `ALLOWED_NESTED` (no fuzzy match). **Toolbar ratchet:** `scripts/verify-surface-bar-toolbar-leaf-inventory.mjs` (LAW-2026-08-14-SURFACE-BAR-TOOLBAR-LEAF-INVENTORY) — every `chrome.toolbar_*` leaf must have allowlisted `surface_path` (no null-path theater). **Wizard ratchet:** `scripts/verify-surface-bar-wizard-inventory.mjs` (LAW-2026-08-14-SURFACE-BAR-WIZARD-INVENTORY) — every FE `*Wizard*.tsx` must map to `leaf.surface_path` or `FILE_OWNED_BY_LEAF`. **Modal ratchet:** `scripts/verify-surface-bar-modal-inventory.mjs` (LAW-2026-08-14-SURFACE-BAR-MODAL-INVENTORY) — every FE `<Modal open=>` host must map to `leaf.surface_path` or `FILE_OWNED_BY_LEAF` / `ALLOWED_NESTED`. **ParityDrawer ratchet:** `scripts/verify-surface-bar-paritydrawer-inventory.mjs` (LAW-2026-08-14-SURFACE-BAR-PARITYDRAWER-INVENTORY) — every FE `<ParityDrawer open=>` host must map to `leaf.surface_path` or `FILE_OWNED_BY_LEAF` / `ALLOWED_NESTED`. **Combobox ratchet:** `scripts/verify-surface-bar-combobox-inventory.mjs` (LAW-2026-08-14-SURFACE-BAR-COMBOBOX-INVENTORY) — every FE `<Combobox>` host must map to `leaf.surface_path` or `FILE_OWNED_BY_LEAF` / `ALLOWED_NESTED`.

**Then** Live Chrome (item 12). Until then: say **`Built progress / Live=BLOCKED`**. Never “fully wired” / “launch tomorrow” / “McLeod-ready.”

---

## Permanent seat lanes + sequence

Parallel theater-purge + gap-fix is allowed **across seats**. No seat invents a fourth method.

### CC-1 / Claude Code (money)

| Order | Work |
|---:|---|
| 1 | Kill money theater tags (`ap_bill` `leafRe:".*"` first; any money-col `|.*` / floors) |
| 2 | Fix **money-path** reverse gaps (settlements / factoring / banking / accounting) from filed board rows |
| 3 | Honest sweep + fix: `gl_je` → `liability` → `ap_bill` → `expense` → `bank` → `invoice` / `payment` / `settlement` / `factor` / `escrow` |
| 4 | Never claim money “complete” while theater or filed money gaps remain |

### Cursor (FE / chrome / bus / surface bar)

| Order | Work |
|---:|---|
| 1 | Kill **qbo_chrome** + **picker_law** theater (`|.*`, word-regex Built); harden `isLeafSpecific` with CC if needed |
| 2 | **Surface-bar leaf-existence audit** (Fully-Wired item 7): code surfaces missing from `required.json` → add leaf or document N/A |
| 3 | Fix FE reverse/connectivity leftovers that are EntityLink / drawer / chrome (non-GL) |
| 4 | Bus: INBOX/OUTBOX/STATUS truth; rewake idle seats; never idle waiting for Jorge |
| 5 | Live Chrome **only after** Built honest for the agreed gate |

### Codex / ChatGPT desktop (mechanical)

| Order | Work |
|---:|---|
| 1 | Filed **connectivity** product gaps (API→canonical depth beyond route-mount) |
| 2 | Filed **reverse_link** non-money UI gaps (lists, EntityLink sections, filters) |
| 3 | Entity-column honesty: remove WAVE-A `leafRe:".*"` floors; keep/add leaf-specific guards only |
| 4 | Never paint Built with broad tags; never claim Live |

### CC-2 / Cascade / Devin / GUARD

- Obey this file + Fully-Wired.  
- CC-2: GUARD / live-verify lane — do not “complete” columns by editing Required alone.  
- Cascade/Devin: merge + scribe; do not invent launch definitions in chat.

---

## Forbidden language (every PR / OUTBOX / chat to owner)

| Forbidden | Required instead |
|---|---|
| “Done / complete / fully wired / launch-ready” for a column or product | “Required corrected · N real gaps filed · Live=BLOCKED” |
| “Entity columns Done” while WAVE-A `.*` remains | “Narrow guards landed; broad tag still debt until removed” |
| “Connectivity Done, 0 gaps” because route-mount guard passes | “Route-mount proven; API+table depth still owed if gaps filed” |
| “8–10 sweeps and we’re launch-ready” | Name remaining theater + filed product gaps |
| New scoreboard columns / permanent 5th “Verified” Box | Kill lying Built; keep 22 columns |

Every Claude-green PR **REMAINING:** line must include:  
`Live=BLOCKED · theater_broad_remaining:<n> · filed_gaps_remaining:<m or board ids>`

---

## Enforcement (LAW = guard or it is not law)

| Mechanism | Guard / file |
|---|---|
| Fully-Wired bar present on all boots | `scripts/verify-fully-wired-complete-bar-present.mjs` |
| This law present + cited + seat lanes | `scripts/verify-honest-built-launch-law-present.mjs` |
| Leaf-specific Built (incl. no `|.*` / word blankets) | `scripts/verify-matrix-built-leaf-specific.mjs` + `matrix-built-auto.ts` |
| Registry | `docs/law/LAW.json` → `LAW-2026-08-14-HONEST-BUILT-LAUNCH` |

---

## Board

Standing class: `SCOREBOARD-BUILT-SELF-DECLARED-NOT-VERIFIED` in `docs/audit/GUARD-WORKORDERS.md`.  
Status may not say product/column **complete** while illegal Built shapes remain or filed gaps for that column are OPEN.

---

*Permanent. Owner-locked 2026-08-14. Supersedes any softer “Done” wording in prior INBOX waves, Claude artifacts, or chat.*
