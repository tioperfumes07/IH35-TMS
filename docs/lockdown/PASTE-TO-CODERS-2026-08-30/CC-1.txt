===============================================================================
CC-1 WORK ORDER — 2026-08-30 (SUPERSEDES EVERY EARLIER PACKET)
SIX TASKS, IN THIS ORDER. DO NOT REORDER.
===============================================================================
DISCRIMINATOR — every Neon read must use run_sql_transaction with
  ["SET LOCAL app.bypass_rls = 'lucia'", "<your query>"]
and must return count(*) accounting.journal_entries >= 2218.
A zero-row read without that value is an RLS false-empty. Discard it and re-run.

RULE 37 (CLAIM -> MERGE -> AUTHOR): claim your verify-step EVEN number, let the claim
MERGE to origin/main, THEN author scripts/verify-steps/NNNN-*.mjs.
Never claim+author in the same PR. Only chore/claimed-regen may land both.
WORM everywhere: reverse, never erase, never UPDATE a posted row.

ALREADY DONE — DO NOT REDO:
  FACT-RESERVE-01 (#18318) landed and is VERIFIED CORRECT and DEPLOYED (API live 8f86683).
  reserveAmount = Math.round(invoiceTotalCents * reserve_pct / 100)
  feeAmount     = Math.round(invoiceTotalCents * factor_fee_pct / 100)
  factor_fee_cents added to the INSERT at $11, parameters renumbered correctly.

-------------------------------------------------------------------------------
TASK 1 — FACT-INPUTS-01. P0. The defect MOVED from the math into the defaults.
-------------------------------------------------------------------------------
The formula is now right. The numbers fed to it are still ungoverned.

  apps/frontend/src/pages/accounting/SubmitFactoringModal.tsx:38-40
    advanceRatePct  useState("92")
    reservePct      useState("8")      <- Faro is 1.5%
    factorFeePct    useState("0")      <- Faro is 1.5%

  apps/backend/src/accounting/factoring-advances.routes.ts:47
    factor_fee_pct: z.coerce.number().min(0).max(100).optional().default(0)

activeFactor cannot resolve reliably today: factoring.customer_factor_assignment is still
empty, and the modal's fallback is just activeFactors[0]. So a submit can still land
reserve 8% / fee 0%.

On the real August schedule that is:
  reserve  $7,606.00  instead of  $1,426.13
  fee      $0.00      instead of  $1,426.13
An omitted factor_fee_pct reproduces the exact factor_fee_cents = 0 bug just fixed.

FIX: percentages come from the RESOLVED FACTOR ROW, not from UI state and not from a zod
default. Delete the 92 / 8 / 0 seeds. Delete .default(0) from factor_fee_pct.
If no factor resolves for the customer, the submit must FAIL CLOSED with a clear error.
NEVER price money at a default. Same principle as the batch defaults already removed.

-------------------------------------------------------------------------------
TASK 2 — WORM-REVERSE FAC-2026-00001. Still wrong on the books right now.
-------------------------------------------------------------------------------
Live this minute:
  FAC-2026-00001  status 'advanced'
    reserve_amount_cents 5550   (should be 2775)
    factor_fee_cents        0   (should be 2775)
Reverse it. Do not UPDATE it. Do not delete it.

-------------------------------------------------------------------------------
TASK 3 — FACT-RESOLVER-03. Resolver ignores void and active state.
-------------------------------------------------------------------------------
apps/backend/src/factoring/factor.service.ts:227-234 (getFactorForCustomer) filters only on
tenant, customer and effective dates. A VOIDED assignment or a DEACTIVATED factor still
resolves and still prices money.

FIX: add to the WHERE clause
  AND a.voided_at IS NULL
  AND f.voided_at IS NULL
  AND f.active IS TRUE

-------------------------------------------------------------------------------
TASK 4 — MDATA-COPY-04. Copy customers + vendors from TRANSPORTATION to USMCA.
-------------------------------------------------------------------------------
OWNER RULING: USMCA serves the same customers as IH 35 Transportation. Copy both lists.

DO NOT GO TO QUICKBOOKS. The data is already in the TMS. Verified live:
  IH 35 Transportation   1,257 real customers (all with qbo_customer_id, all active)
                           951 real vendors  (918 with qbo_vendor_id, 564 active)
  USMCA                     19 real customers, 121 real vendors
In-database copy. No QBO API, no OAuth, no write-back risk.

*** CRITICAL — DO NOT COPY THE QBO IDs ***
qbo_customer_id / qbo_vendor_id belong to TRANSPORTATION's QuickBooks company file.
Copying them onto USMCA rows falsely links USMCA records to TRANSP's QBO entities and
corrupts both sides of the reconciliation. Set them NULL on every copied row.
Set source_system / source = 'COPY_FROM_TRANSP_2026-08-30'.

DATA-QUALITY GATES — a blind copy drags Transportation's mess into USMCA permanently:
  CUSTOMERS: 1,257 real rows contain 61 NORMALIZED-NAME DUPLICATE GROUPS.
    The is_duplicate flag reads 0 because nobody ever set it. DO NOT TRUST THAT FLAG.
    Confirmed example: "Sajacks Freight" AND "Sajacks Freigth" (typo) both exist.
    -> Produce the 61-group list for the owner. Copy survivors only.
  VENDORS: copy the 564 ACTIVE only. Do not copy the 387 deactivated.
    EXCLUDE the 32 vendors with driver_id NOT NULL - those are Transportation's DRIVERS.
    USMCA drivers get their own vendor rows via the driver-vendor link service.
    Copying them creates cross-entity driver-vendor corruption.
    3 normalized-name duplicate groups here too - dedupe.

  Write mdata.customers and mdata.vendors (CANONICAL).
  NEVER write mdata.qbo_vendors (RETIRE lane).
  is_sample_data = FALSE and factoring_eligible = TRUE on every copied customer.

FARO DEBTORS: 15 of the 26 already exist in Transportation and come across in this copy.
4 must be created new. 7 are AMBIGUOUS and the owner is answering those separately.
See data/FARO-DEBTOR-MATCH-TO-TRANSPORTATION.csv. DO NOT GUESS on the 7.

TRAP: USMCA has a test row "Semares Forwarding Services". It is NOT Faro's
"S E Mares Forwarding Service LLC". DO NOT MERGE ON NAME SIMILARITY. Create fresh.

-------------------------------------------------------------------------------
TASK 5 — FACT-ASSIGN-05. Assign EVERY USMCA customer to Faro.
-------------------------------------------------------------------------------
OWNER RULE, LOCKED, VERBATIM:
  "EVERY CUSTOMER MUST GO THROUGH FACTORING. IF ONE DOES NOT, I WILL UPDATE THAT ONE."
Assign ALL USMCA customers by default. The owner carves out exceptions individually.
Do NOT restrict this to the Faro debtor set.

LIVE: factoring.customer_factor_assignment = 0 rows, ALL companies.
WHY IT IS THE KEYSTONE: getFactorForCustomer resolves ONLY through that table. Empty means
every batch gets factor_id = NULL, every reserve preview is null, and the
mixed_factors_not_allowed guard is vacuous (one NULL bucket always passes). It is also why
task 1's fail-closed rule has nothing to resolve against until this lands.

INSERT one row per USMCA customer:
  factor_id       40b3690b-f1d4-44b4-90cf-c1cfd4f79c33   (Faro, USMCA)
  effective_from  2026-08-10
  effective_to    NULL
  voided_at       NULL

mdata.customers.factoring_eligible is already NOT NULL DEFAULT true - the schema already
implements the owner rule. 22 of 31 current USMCA customers sit at FALSE only because coder
test seeds flipped them (apps/frontend/src/test-utils/factories.ts still sets it false).
Add a guard so seeds stop writing factoring_eligible = false.

-------------------------------------------------------------------------------
TASK 6 — SETL-TRACE-06. Header write-back + build one fully traceable settlement.
-------------------------------------------------------------------------------
PART A — THE WRITE-BACK DEFECT. This made THREE seats misdiagnose the module as dead.

CONFIRMED, do not re-investigate:
The ONLY writer of driver_finance.driver_settlements.accounting_bill_id /
accounting_bill_payment_id is
  apps/backend/src/payroll/driver-settlement.service.deprecated.ts:653-654
and payroll.* is RETIRE under the linkage law.
The LIVE poster (apps/backend/src/accounting/settlement-posting/
settlement-bill-payment-posting.service.ts) writes those IDs ONLY into
driver_finance.driver_settlement_gl_bills (L647-650, L722-726), never back to the header.

PROOF the poster itself works: S-2026-0002 (Rafael Rivero Reynoso, $297.60,
load L-20260810-0003) posted a COMPLETE chain -
  driver_bill 31f155f3 -> accounting_bill 35b8ce38 -> bill JE b7575a45
  -> cash payment d6f22f71 -> cash JE 8bc9947e
run_status 'reversed' (2026-08-21T17:46:35Z). The engine is fine. The back-link is not.

FIX: the LIVE poster must write the header back-link. DO NOT resurrect the deprecated
payroll service - that drags a RETIRE-lane writer into the canonical path.

ALSO REPORT BACK (do not fix blind): apps/backend/src/system/transaction-health.service.ts
lines 386-389 use (s.accounting_bill_id IS NOT NULL) as a linkage signal, with a comment
claiming the live poster writes it. That comment is false. If that predicate is live,
transaction health has been scoring EVERY settlement unlinked. Report the exact predicate
before changing anything.

PART B — BUILD THE TRACE SUBJECT. No existing settlement can serve:
  USMCA settlements                                        17
  with gross_pay > 0 AND is_sample_data = FALSE             1  (already posted + reversed)
  settlement lines ever approval_status = 'approved'        0 of 7   <- gate never exercised
  settlement lines carrying posting_account_id              0 of 7
  deductions ever applied to a settlement                   0  (2 exist, both pending)
  deduction JE / deduction bill payment ever posted         NEVER
  USMCA loads at status 'delivered'                         1
The one 'approved' settlement has gross_pay $0.00 - a zero-dollar bill fails
createJournalEntry's debits===credits>0 assertion. It proves nothing.

BUILD through the app UI, not raw SQL:
  driver: real active USMCA driver, is_sample_data = FALSE
  load:   real delivered USMCA load, is_sample_data = FALSE
  lines:  ALL THREE types, each with posting_account_id SET and approval_status 'approved'
            - earnings (loaded miles x rate, show the math in the description)
            - a real deduction (apply one of the 2 pending driver_settlement_deductions)
            - a reimbursement
  REQUIRED: gross > 0, deductions > 0, net > 0, and NET MUST NOT EQUAL GROSS.
  A zero-deduction settlement only re-proves what S-2026-0002 already proved. The point is
  to fire the deduction leg, which has NEVER posted:
  gl_runs.deduction_journal_entry_id and gl_bills.deduction_bill_payment_id are both NULL
  system-wide.

NOTE: settlement_lines.approval_status = 'approved' has NEVER been achieved (0 of 7).
Expect that gate to break. Fix root cause. Do not bypass the gate.

The driver is a real person owed real money. The amount must be correct BEFORE posting.
