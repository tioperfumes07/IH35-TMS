# GO-WAKE-ALL — 2026-08-30 13:48 CT

Idle = defect. Skip #15546 #16895. Never recertify U14. Never `trigger_deploy`. USMCA only. Jorge is not the bus.

**Live API:** `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` → `version` **`485c52d`**.

`PROD_READONLY_DATABASE_URL` is **already set** (Cursor). Do not wait on Jorge.

ACK: `SEAT | ACK | GO-WAKE-ALL | SHA=485c52d | NOW=<one line> | GO`

## CC-1
DEFECT-A (USMCA invoice payment must debit cash). Then `reconciliation_sessions` unique per account+period (no cancel route — you own schema). Wrappers/R7 **done**. Never `PROOF_REPLAY_FAIL_ON_FAIL` globally.

## CC-2
THIS IS NOW: live Chrome **USMCA** `/legal/matters` row `MAT-LAWSUI-TESTMTDQ164H` SOL/hearing **≠ `—`**. BANK-ECON-04 / SURF-04 stay FAIL. Recon unique = CC-1. Do not stamp on SHA 5071217/455a32f.

## CC-3
Do **not** start DRIVER-F7334 schema (lane-barred). NOW=next **chrome-only** unique leftover FAIL in your prefix (500 / dead click / silent no-op). No isError mass-wire. No planners. Hearing list already shipped — do not rebuild.

## Codex
THIS IS NOW: `SAFETY-PHOTO-COMPARISON-UNIT-DRIVER-NOT-VISIBLE` live click on current SPA. GR1 money guards = CC-1.

## Cascade
STOP filing 50277–50344. Detector 1–3 then B. SUPERSEDE false grep class (directory-scope, not GNU `|`). 50309 not proven FAIL.

## Cursor
Lead. Period close HOLD next session. Overflow leftover unique FE only.
