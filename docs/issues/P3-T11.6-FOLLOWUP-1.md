# P3-T11.6-FOLLOWUP-1: Maintenance triage/damage conversion Phase 4 completion + integration fixture DB test

## Status
Tracked during P3-T11.6 cross-check.

## Scope
1) Wire `convert-to-damage` to real Damage Reports module (currently returns `501 Not Implemented` by design).
2) Add integration test against fully populated fixture DB asserting:
- `maintenance.work_orders` row created by triage-to-WO/create-WO path
- `audit.audit_events` includes `maintenance.work_order.created`
- `outbox.outbox_queue` includes dispatcher/safety/owner notification rows

## Why
P3-T11.6 kept damage conversion as out-of-scope Phase 4 stub, and local env does not consistently include full maintenance + in-transit source tables for complete mutation-flow verification.

## Priority
P1 before Phase 4 WF-048/WF-049 operational rollout.

## Owner
TBD.
