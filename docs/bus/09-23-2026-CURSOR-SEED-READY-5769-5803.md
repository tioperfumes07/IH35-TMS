# CURSOR — SEED READY: AlwaysTrack 5769–5803

Owner: delete USMCA transaction data, then re-seed from settlement PDFs. Cursor is clear of JE remediation (handed to CC-1). This note is seed prep only.

## Truth corpus (signed — never re-parse PDF for money)

Repo: `data/alwaystrack/settlements-truth-2026-09-13.json`

| Side | Count in 5769–5803 | Notes |
|---|---:|---|
| company | **34** | missing company-side **5782** only (known) |
| driver | **35** | includes 5782 |
| both sides | **34** | feed unit = company doc + matching driver doc |

Company rollup (range): 76 loads · 171 fuel rows · 178 expense rows.

## Seed sequence (per document, one day at a time)

1. Preflight — CC-2 `feed-day-preflight` (five checks; sixth = GL ties to driver_net still owed).
2. Create load(s) from company `loads[]` (numbers free after void renumber).
3. Chain: load → proforma invoice → driver bill → expenses → fuel → settlement → factoring (factoring last).
4. Reconcile immediately: `node scripts/verify-alwaystrack-parity.mjs` scoped to that document — six dimensions in cents, zero tolerance.
5. Close day only when clean; then advance cursor.

## App surface already built

- `POST` feed-day route: `apps/backend/src/driver-finance/historical-feed-day.routes.ts`
- Executor: `historical-feed-day.service.ts`
- Parity gate: `scripts/verify-alwaystrack-parity.mjs`

## Standing laws

- USMCA only · no seat fixtures · banking.bank_transactions stays 1133 · WORM while rows exist; owner delete path is owner-owned · no QBO write-back.

## Status

Cursor **ready to seed** the moment owner/CC-1 confirm transaction tables are empty for USMCA. Standing by for go.
