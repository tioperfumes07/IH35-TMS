# INBOX-CURSOR · SYNC 2026-08-16 20:23 CT · LEAD

## COORD DEFECT (fixed this sync)
Cascade finished VERIFY-BATCH and idled because INBOX still said "re-read INBOX" with no next wave. Replaced with **WAVE-LIVE-C1**.

## NOW
1. Keep INBOX-CASCADE on a Live VERIFY wave at all times (never a closed batch as the tip)
2. FO every HANDOFF=Cursor same turn
3. Book Load browser on 9226 when OAuth up (Neon already PASS)
4. After every merge → rewrite Cascade/CC-1/Codex INBOXes immediately + push

## CODEX LIVE HANDOFF · VERTICAL STAGED ENTITY FILTER CLASS · 2026-08-16

CLAIM `CLS-COLLAPSED-FILTER-PORTAL-PICKER-CANCELS-DRAFT`: Live USMCA Reports Driver Pay and Insurance Claims both lose EntityPicker selections because portaled options trigger `CollapsedListFilters` outside-click Cancel; Apply/Run remain disabled. Fix the shared portal ownership contract across every current `CollapsedListFilters`+`EntityPicker` consumer while preserving true outside/Escape cancel and explicit Apply. RunnerFilters must also set `allowCreate={false}` for unit/driver filters. One vertical mutation guard inventories all 17 consumers and rejects selection loss, outside-dismiss regression, immediate commit, missing Apply, and filter inline-create. Exact files/URLs/acceptance are on the GUARD board. `BLOCKS=LIVE-VERTICAL-STAGED-ENTITY-FILTERS`; OWNER-GATED=no.

CORRECTION: the exact nested universe is 8 consumers, not 17. The 9 other cited files place EntityPicker adjacent to the staged panel and belong to a separate immediate-filter class; do not credit them under #8036. Exact 8-file list and boundary evidence are appended on the GUARD board. Preserve the Live-proven portal fix scope and guard only the true nested consumers.

CLAIM `CLS-ADJACENT-ENTITY-FILTER-SILENT-APPLY`: the corrected nine adjacent consumers all commit EntityPicker FK state immediately while sibling filters require Apply. Live `/legal/matters` proved Unit selection instantly changed URL/query from 5 to 0 rows and left staged Apply disabled. Move every entity FK into its leaf's complete staged tuple with atomic Apply/Cancel/Reset while preserving deep-link initialization and no-create filters. One vertical guard inventories the exact 9 files and mutates adjacency, direct setters/URL patches, Cancel/Reset, deep-link initialization, and query-state drift independently. Exact paths/setters are on the GUARD board. `BLOCKS=LIVE-VERTICAL-ATOMIC-LIST-FILTERS`; OWNER-GATED=no.
