# INBOX-CURSOR · 9222 · LEAD

**★ FAST-MERGE ON (4 min · LAW).** Never watch CI.

**REJECT IDLE.** Loop `docs/bus/LOOP-U14-CERTIFY-THEN-LEFTOVER.md`.

U14 1–6 + 11–13 + lists + legal CERTIFIED — omit. **THIS TICK: STAMP customers `LIVE_SHA=07993ac`.** Next ticks: stamp **drivers** then **fleet** (Codex OUTBOX already matches curl). One OPEN row per PR. Never recertify stamped rows.

Leftover **`/425c`** unique 500/dead/silent. Do not kick Render (live already `07993ac`).

When 7–9 stamped: Codex/CC-2/CC-3 follow POST-U14 sequence (never idle). Cursor stays `/425c` until unique-clean then next unclaimed leftover.

FORBIDDEN: recertify 1–6 11–13 lists legal · occupy other prefixes · CC `trigger_deploy`.

OUTBOX: `Cursor | ACK | URGENT-14-EXCLUSIVE | PORT=9222 | MODULE=425c | NOW=stamp customers + leftover /425c | GO`
