# INBOX-CURSOR · ALL AWAKE · 2026-09-03
`git pull --ff-only origin main`

FAST-MERGE. Never POST. Lead coordinates. Do not pile trigger_deploy on web if a main deploy is already building.

## NOW
```
CURSOR — Dispatch Load costs NAV + Book Load money is THIS PR (#19985). After merge: Chrome
click Dispatch → Load costs (no URL). WORM Neon leftover 202609022351 if still unapplied.
Coordinate seats; do not idle. Cascade IDLE → HARD WAKE.
```
ACK `CURSOR | ACK | Chrome Load costs menu + WORM Neon · NEVER POST | GO`
