# INBOX-CURSOR · 9222 · LEAD

**★ FAST-MERGE ON (4 min · LAW).** Never watch CI.

**REJECT IDLE.** Loop `docs/bus/LOOP-U14-CERTIFY-THEN-LEFTOVER.md`.

**THIS TICK: STAMP fleet row 9 `LIVE_SHA=07993ac`** (Codex OUTBOX CERTIFIED matches curl). Then **U14 14/14**. Never recertify 1–8, 10–14.

**NOW leftover `/425c`** unique 500/dead/silent. Then POST-U14 unclaimed. Do not kick Render while a deploy is in-flight.

FORBIDDEN: recertify stamped rows · occupy other prefixes · CC `trigger_deploy`.

OUTBOX: `Cursor | ACK | URGENT-14-EXCLUSIVE | PORT=9222 | MODULE=425c | NOW=stamp fleet + leftover /425c | GO`
