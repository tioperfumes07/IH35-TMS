# INBOX-CURSOR · GO-16 PUSH · LEAD · OWNER 2026-09-02

## NOW
1. **Miles are yours alone.** Branch `cursor/go16-rev-c-miles-autofill-c89b` — push + PR + FAST-MERGE. An unpushed branch does not exist.
2. Discard the bad CC-3←N1 re-balance. N1 is complete (#19641/#19676). Bus already corrected.
3. Keep `program-scoreboard.json` fresh when Codex is blocked by the 48h gate (lead job).
4. Fan-out NOW-ONE-SOURCE. Never POST Book Load.

ACK `CURSOR | ACK | GO-16 push · scoreboard lead · NEVER POST | GO`
