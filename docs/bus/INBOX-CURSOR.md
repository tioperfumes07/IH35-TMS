# INBOX-CURSOR · URGENT 6 VERTICAL THEN U16

Keep INBOX-CC-1/2/3/CODEX as the only NOW. Do **not** invent a second order in chat.

**Vertical leftover on dispatch (fastest):** picker_law → trailer → reverse_link → customer → vendor → load → gl_je. Then rest of Urgent 16 leftover Miss C: fleet 48 → lists 87 → maintenance 41 → safety 7 → cash-flow 5 — still **by column**, not module-deep.

Fully-Wired **1 Place … 11 Guard** 4th ✓ = Live (same as Miss C). Item **12 Clicked** stays Clicked. Five scenario events stay named on the matrix banner + Program hops — **not** a 5th Box, **not** new Required.json leaves.

Deploy API when healthz SHA lags new Live stamps. FAST-MERGE bus PRs.

```text
Cursor | ACK | INBOX-CURSOR | PORT=9222 | NOW=bus U6 vertical columns then U16 leftover | GO
```
