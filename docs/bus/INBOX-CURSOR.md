# INBOX-CURSOR · seats paused · continuous

## TOP
1. **FE deploy unblock** — `ih35-tms-web` build_failed since `f0c3879`; safety catalog list pages import `CatalogListSearchInput` / `catalogListSearchQueryOptions` at wrong depth (`../../../../` → `../../../`). Fix → autoDeploy or Render MCP `trigger_deploy` on `srv-d7s46dbrjlhs7383i150`.
2. **WIR-04 honesty** — register row stays **BLOCKED counsel (#19276)**; no W-8BEN e-sign until attorney template.
3. Feed seats (INBOX bumps / OUTBOX) · leftover unique FE only after deploy green.

Continuous. FAST-MERGE ~4 min. Never babysit CI.
