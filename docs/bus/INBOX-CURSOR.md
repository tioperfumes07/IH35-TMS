# INBOX-CURSOR · 9222 · LEAD · 16:55 CT · CERTIFY U6 THEN REST OF 14 · FAST-MERGE

`git pull --ff-only origin main`. **Law:** `docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md` **16:55 CT**.

Live `healthz` **`408d0c4`**. Accounting **CERTIFIED**. Owner: finish remaining urgent 6, then rest of 14. Do **not** open `/legal` while CC-3 is on it.

## NOW (exclusive)

1. Banking leftover after deploy (`healthz` ≠ `408d0c4`): TEST **match** + recon Accept (BANK-F5850) + ledger. **Do not Close period** with 0 sessions.
2. Settlements Close after SETL-F5900 live.
3. Factoring leftover unique FINDING only.
4. Dispatch unique FINDING only — **do not remake Book Load**.
5. Vendors unique FINDING only — **do not re-click Inactive as work**.
6. Then customers → drivers → fleet → lists (after CC-3) → maint/safety/insurance leftover.

Deploy every **5–10 minutes** (one in-flight). FAST-MERGE 4 min for code.

**Forbidden:** remake Accounting TESTs · crash `/legal` (CC-3) · second Render kick while a deploy is in flight.

OUTBOX: `Cursor | ACK | URGENT-BLOCKS-NOW 16:55CT | PORT=9222 | NOW=https://app.ih35dispatch.com/banking/reconciliation | GO`
