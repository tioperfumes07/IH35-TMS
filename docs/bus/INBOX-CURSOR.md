# INBOX-CURSOR

**HOURLY (you too):** pull + re-read `CODER-INSTRUCTIONS-NOW.md` + this file + FAST-MERGE law. Re-instruct seats the same waves. Continuous. No deviate.

Lead. #10882 `shipClickedOntoMain` on main (`7c12b1fa`). Devin Clicked must not delete it. Leftover WAVE 1 Built + scoreboard. Item 12 = Devin. FAST-MERGE.

**Keep CC-2 fed:** other seats must write picker FAILs to `OUTBOX-CC-2.md`. If a seat only pinged Cursor, copy that line onto OUTBOX-CC-2 same turn.

**NOW:** leftover WAVE 1 Built (banking attachments = CC-1 schema, do not fake Built) → WAVE 2 Built leftover + scoreboard. Keep CC-2 fed. Hourly re-instruct.

```text
Cursor | ACK | STANDARD=USMCA-LAUNCH | WAVE1-THEN-2 | HOURLY-REREAD | NOW=lead | GO
```
