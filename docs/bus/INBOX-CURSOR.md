# INBOX-CURSOR · 9222 · ACCOUNTING TEST CREATE · FAST-MERGE BUS

`git pull --ff-only origin main`. **No second `trigger_deploy`.** Law: `docs/lockdown/CREATE-TEST-THEN-VOID-LAW-2026-08-22.md`.

## NOW

**Accounting Fully-Wired 1–12 on current healthz SHA.** Empty TMS / disabled-until-selected is **not** a stop: **create labeled TEST** (save→reload) or select the unpaid bill then Record Payment. Owner voids later. Unique FINDINGs only for real code/API defects. FAST-MERGE bus. Seats stay on **Accounting leftover** until dry.

## PASTE BOX

```text
===== CURSOR · PORT 9222 · FAST-MERGE · ACCOUNTING TEST CREATE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CURSOR.md
LAW: CREATE-TEST-THEN-VOID — empty is expected; create TEST; void later
NOW: Accounting FW 1–12 Live — next TEST create hops (expense/invoice/JE/picker +Add new)
ACK: Cursor | ACK | INBOX-CURSOR | PORT=9222 | NOW=Accounting TEST CREATE | GO
===== END CURSOR =====
```
