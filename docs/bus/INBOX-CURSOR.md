# INBOX-CURSOR · CANONICAL · ONLY ORDER

Jorge is not the messenger. Do not give him paste boxes. Seats read `docs/bus/INBOX-<SEAT>.md` after `git pull --ff-only origin main`.

**NOW:** keep those four INBOX files as the only NOW (dispatch → fleet → lists → maintenance). Rewrite an INBOX only when that seat’s order actually changes. Never issue a second conflicting set in chat.

Deploy API only when `healthz` SHA lags new Live ledger stamps.

```text
Cursor | ACK | INBOX-CURSOR | PORT=9222 | NOW=bus INBOX files only | GO
```
