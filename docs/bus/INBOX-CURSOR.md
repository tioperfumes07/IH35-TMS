# INBOX-CURSOR · 2026-08-19T10:50Z · LEAD + SAFETY ACTIVE

Execute `docs/trackers/OWNER-EXECUTION-PLAN-2026-07-22.md`. Fix never defer. FAST-MERGE. Continuous. No idle.

## BOOT
```bash
cd <your Cursor worktree>   # e.g. IH35-TMS-clean
git pull --ff-only origin main
# read: docs/bus/STATUS-NOW.md → this INBOX TOP → OUTBOX-CURSOR
```
CDP **9226** · `~/.chrome-mcp-cursor` · USMCA · healthz `https://api.ih35dispatch.com/api/v1/healthz/shallow`

## OWN NOW (seq 3 ACTIVE)
1. **safety** FE residual — ranked OPEN / Live defects / chrome / pickers / silent-apply / EntityLinks
2. Tip **all seats** every wave (STATUS + INBOX tops)
3. Append AUDIT Leaves when Devin/Codex tip PASS
4. Overflow: lists (seq 4) · maintenance FE if Devin starved · dispatch residual only after safety thin

## SHARE LATER (same order)
insurance · legal · dispatch · drivers · fleet FE · last-wave chrome — **only when prior seq closed or Jorge parallel-authorizes non-overlap**

## FORBIDDEN
Money GL / posting invent · Lists create theater · invent load FKs (Rule 32) · idle · skip safety to random modules

## SHIP
`cursor-ship-preflight --body-file` PASS → push → `gh pr create` → `gh pr merge --squash --delete-branch --admin`  
OUTBOX: `Cursor | SHIPPED #N … | NEXT=… | Live=BLOCKED`

## DO NOW
Safety residual FE OR Leaves from Devin tip → FAST-MERGE → tip seats → next FO same turn.
