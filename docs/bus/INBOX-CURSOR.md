# INBOX-CURSOR · 9222 · ACCOUNTING LIVE ON CURRENT SHA

`git pull --ff-only origin main`. FAST-MERGE 4–5 min. **One** API batch may still be `update_in_progress` (`dep-da4iiqek1f9s73efd6g0`). **Do not kick a second.**

## HARD

Other seats are **not** gated on Accounting CERTIFIED. You do **not** idle waiting for healthz ≠ `0cec933` while the **web** app is already serving newer chrome (autoDeploy). Certify = Fully-Wired **1–12** Live Chrome on the **SHA that healthz reports**, plus honest web vs API split.

**Live 2026-08-21 23:55 CT (do not regress):**

- API `GET /api/v1/healthz/shallow` → `{"ok":true,"version":"0cec933"}` HTTP **200** (not 502).
- USMCA JE list: **0** “not visible”; memos **Expense posting** / **Fuel transaction**.
- Audit trail: **Invoice Payment** without tombstone; vis=0.
- Factoring labels: INV-2026-00038 / TC Freight LLC (not UUID / Unknown Customer).
- `/accounting/chart-of-accounts` → Lists CoA (heading Chart of Accounts).

**Accounting is still NOT CERTIFIED** (FW 1–12 not fully walked; API SHA still `0cec933`; money view may still be CC-1).

## NOW

1. Keep Live-clicking remaining Accounting Fully-Wired 1–12 leaves on **current** healthz SHA (hub, bills, expenses, invoices, A/R register, remaining chrome). File unique FINDINGs. Never reuse ACCT-F5732/5733/5740/5741/5743.
2. When healthz version **changes** and stays JSON 200: re-click the tombstone set + remaining leaves on **that** SHA before saying CERTIFIED.
3. Rewrite other seats’ INBOX if they idle. No second Render kick.

## PASTE BOX

```text
===== CURSOR · PORT 9222 · ACCOUNTING LIVE CURRENT SHA =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CURSOR.md
LAW: USMCA · FAST-MERGE 4MIN · NO second trigger_deploy
CHROME: 9222

SEATS MUST NOT WAIT: CC-1 view _cents · CC-2 /factoring INV-2026-00038 · CC-3 customers→… · Codex banking reverse

YOUR NOW: Accounting FW 1–12 Live on healthz SHA (now 0cec933). CERTIFIED only if all applicable items pass on that SHA. Do not idle for batch.
ACK: Cursor | ACK | INBOX-CURSOR | PORT=9222 | NOW=Accounting Live current SHA | GO
===== END CURSOR =====
```
