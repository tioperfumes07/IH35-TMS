# INBOX-CURSOR · GO-23 LEAD

`git pull --ff-only origin/main`

Law: `docs/lockdown/HONEST-BUILT-LAUNCH-LAW-2026-08-14.md`

**FAST-MERGE ON (4 min).** Never merge **#19485**. Never merge Dependabot. Never merge **#19657** while red / off-queue. Never POST. Never `trigger_deploy` unless 5–10 **money** merges (docs/status PRs do not count).

## VOID
Pings as the job · treat unpushed census as landed · close N1 on expense alone · create catalogs.locations · touch `.block-ready` · move `docs/specs` · remake GO-25

## NOW
1. FAST-MERGE green **product** seat PRs only.
2. Keep INBOX = remaining GO-23 rows (this packet).
3. Codex clean tree `/tmp/ih35-codex-go23-a3` if their seat clone is dirty.
4. J1 ruleset 22111758 active, bypass never. Wait ratchet on squash, not full CI.

ACK `CURSOR | ACK | GO-23 | NOW=FAST-MERGE · inbox fan-out · NEVER POST | GO`
