# P0 COMPILER BLOCKER · CI-F6318 · CURRENT MAIN `e5ddbfec24`

`/425c` is **not done as a build dependency**: `node scripts/generate-module-completion-data.mjs && (cd apps/frontend && npx tsc -b)` fails TS18047 at `apps/frontend/src/pages/form425c/lib/buildPrintHTML.ts:193` because nullable `netCash` is compared using `netCash >= 0`. Fix the null-aware render/color contract with a ratcheting print guard before `/docs`. No cast/non-null assertion. BLOCKS Codex FLT-F6317/F6319/F6320 normal pushes.

# INBOX-CURSOR · 9222 · LEAD

**★ FAST-MERGE ON (4 min).** Never watch CI.

**YOU HAVE INSTRUCTIONS.** `docs/bus/NEXT-8-LEFTOVER-CERTIFY-NOW-2026-08-24.md` then this TOP.

**NOW:** leftover `https://app.ih35dispatch.com/docs` — leftover module **#8 of 8**. Unique 500/dead/silent. Do **not** loop `/425c` (#15053–#15279 already shipped).

**THEN (wave 2):** `/users` → `/home` → `/help` → `/program` → `/system`.

Lead: FAST-MERGE + deploy every **5–10 min AND 5–10 PRs**, one in-flight. Never per-merge. Never saturate. Small PRs only.

FORBIDDEN: recertify 1–14 · occupy `/cash-flow` `/finance` `/fuel` `/compliance` `/customers` `/drivers` `/fleet` `/lists` `/legal`

OUTBOX: `Cursor | ACK | NEXT-8 | PORT=9222 | NOW=/docs | GO`
