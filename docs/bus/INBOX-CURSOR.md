# INBOX-CURSOR

**HOURLY:** pull + CODER 4-box GOAL + this file. Re-instruct seats. FAST-MERGE INBOX to main.

**GOAL:** lead until every Required cell is **4/4** on `columns.shared.json` only (25 ids). Do not send seats at the archived 31 CLS board. Leftover Box 3 Built (`qbo_chrome` / surface-bar). Do not fake bank attachments Built (CC-1 schema). Keep CC-2 fed (copy picker pings onto `OUTBOX-CC-2.md`).

**NOW:** unpaid WAVE 2 Built leftovers + scoreboard honesty + hourly 4-box INBOX.

```text
Cursor | ACK | STANDARD=USMCA-LAUNCH | 4BOX-COMPLETE | HOURLY-REREAD | NOW=lead 4box | GO
```
