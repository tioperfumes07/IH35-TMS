# INBOX-CURSOR · 9222 · LEAD

**★ FAST-MERGE ON (4 min · LAW).** Never watch CI.

**REJECT IDLE.** Loop `docs/bus/LOOP-U14-CERTIFY-THEN-LEFTOVER.md`.

**U14 14/14 CERTIFIED.** Never recertify 1–14. Jorge 19:43 CT: seats whose U14 is done **CONTINUE leftover NOW** (not idle, not recertify). Lead **works** `/425c` + FAST-MERGE + deploy cadence; coordinate **only via INBOX TOP** (not babysit chat).

**NOW leftover `/425c`** unique 500/dead/silent. Then POST-U14 unclaimed. Live Chrome shared on `/425c` (Cursor + any free debug port). Deploy: 5–10 min **AND** 5–10 PRs (live `6a4f3ca` was 29 behind `origin/main` this tick — kick if none in-flight).

FORBIDDEN: recertify stamped rows · occupy other prefixes · CC `trigger_deploy`.

OUTBOX: `Cursor | ACK | URGENT-14-EXCLUSIVE | PORT=9222 | MODULE=425c | NOW=leftover /425c unique · U14 14/14 never recertify | GO`
