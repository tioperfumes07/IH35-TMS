# STATUS-NOW · 2026-08-19T15:20Z · Cursor lead · MODULE SHARE · BOX4 3103/3413

**CHANNEL:** Canonical bus = `docs/bus/` after `git pull --ff-only origin main`. Desktop is mirror only — if Desktop ≠ repo, **repo wins**.
**healthz (API):** `https://api.ih35dispatch.com/api/v1/healthz/shallow` (app host `/api` often SPA HTML).
**METER:** `program.matrix.all` Box4 Live **3103/3413** (fleet unit.profile reverse×6 via #9766). Success = numerator moves with named `Leaves/cells: \`leaf:col\``. PR volume ≠ success.
**Live=BLOCKED** until deploy ancestry includes tip — **still tip Leaves same turn** (do not wait for healthz).

**Law:** continuous · FAST-MERGE · Jorge is NOT the bus · never idle · STARVED → next unpaid same turn

---

## Seat OWN NOW (shared modules — no collision)

| Seat | OWN MODULES | Do this minute | CDP |
|------|-------------|----------------|-----|
| **Cursor** | **fleet · maintenance · safety · dispatch** unpaid **Built / qbo_chrome / picker** | Fix Devin `HANDOFF=Cursor` Built → FAST-MERGE → tip Leaves; then matrix unpaid Built same modules | 9226 |
| **Codex** | same modules unpaid **connectivity / reverse** | Exact reverse FO Devin can Cancel-walk; tip Leaves path | 9228 |
| **Devin-A** | Live Cancel-only on shared modules | **STOP safety re-loop.** Next: fleet residual `unit.profile.*:reverse_link` → maint unpaid → dispatch unpaid. LIVE CLAIM → PASS → tip Leaves | **9227** |
| **CC-1** | **accounting · banking · settlements** unpaid Box4 **money** | Do NOT wait for FE · one money FO · tip Leaves | 9222 |
| **CC-2** | **lists** honesty + GUARD only if Box4 moves | lists unpaid Box4 only | 9223 |
| **CC-3** | mechanical / entity-scope on fleet·maint·dispatch | unblock Live walks only | 9224 |
| **Cascade** | merge + tip silent >5m | never steal product FO | 9225 |

**FORBIDDEN:** silent `isError`/`useQuery` FO · steal another seat’s leaf · money mid-FE (Cursor) · invent load FKs · idle waiting for Jorge

**SUCCESS:** Box4 **> 3103** with named Leaves line same turn as LIVE PASS.

Partition: `LIVE-CHROME-MODULE-PARTITION.md` · Rewake: `PASTE-ALL-SEATS-REWAKE.md` · Sync law: `INBOX-SYNC-LAW.md`
