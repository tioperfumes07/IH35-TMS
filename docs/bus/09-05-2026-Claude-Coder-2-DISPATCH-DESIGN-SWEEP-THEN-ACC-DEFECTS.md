# 09-05-2026 · CLAUDE CODER 2 · DISPATCH DESIGN SWEEP, J1 TO ZERO, THEN THE ACC DEFECTS
USMCA only `5c854333-6ea5-4faa-af31-67cb272fef80`. FAST-MERGE. You are the only seat that writes the
verified-live flag. `git pull --ff-only origin main`.

## VERIFIED (owner, 2026-09-05 01:30 UTC)
Tokens landed (#20397). GLB-11/12/13 closed with getComputedStyle proof. ACC-13 test/sample GL
accounts archived + create-guard (#20422, #20423, #20424) merged. Good work — but your OUTBOX has no
ACK of SEQUENCE 2.0 and no STEP checkoffs. Post `CC-2 | ACK | SEQUENCE 2.0 | GO` and a
`STEP-2.1 DONE | #20397` line now, retroactively, then work in order.

## THE SEQUENCE
**2.2 — Dispatch reads the tokens, ONE guarded sweep (rule 9.0.17).** Every dispatch data table
header = `--th-bg #EEF2F6`, `--th-ink #1F2937`, centered, 11px/700/uppercase, `--th-border` between
every column, zebra even rows, sticky header, sticky first identity column, horizontal scroll inside
the container, no truncated header label. KPI cards `--kpi-bg #F4F7FA` + `--kpi-border #C7D2DC`,
centered, ≤101px (target 93px). Surfaces still deviating — verify each LIVE, fix each:
`DispatchKanban.tsx` lane headers · planner grids (`PlannerGrid.css`) · `DispatchLoadCostsPanel.tsx` ·
any `#14314F` / `bg-slate-700` header row on a data table · `LoadCostsBoardPage.tsx` filter pills
(`rounded-full` → 2px token; Cursor/CC-1 own the file, you own the ratchet — file the line, do not
edit their file). Rail and top banner STAY navy — they are not data headers.
Ratchet: `verify-ui-design-system-ratchet.mjs` FAILS if a navy table header returns. Live Chrome
proof with getComputedStyle numbers per surface, as you did for GLB-11/12.

**2.3 — J1 to ZERO.** `off_locked_scale_sizes` and `trapping_picker_total` to 0/0 on dispatch files
first, then system-wide. Post before/after counts per PR. J1 closes at 0/0 — not "green ratchet".
Also still yours from the 09-04 order: GLB-05, GLB-07 UPLOAD on every create/edit screen, GLB-09 dead
grey boxes → real dropdowns with `+`, GLB-10 control sizes 28px. #18 LOCATION casing: not
reproducible and not in grep — closed, say so in one line.

**2.4+ — THE MONEY DEFECTS, one complete vertical each, in number order** (schema → backend rule →
endpoint → screen → guard → live proof; not a layer). ACC-01..12, ACC-14..18, ACC-20 remain open.
Live numbers win over the packet: uncategorized bank = 352/343, not "~369". The matched-state DB
CHECK is CC-1's — do not re-raise. ACC-18 (`is_sample_data` not set by create paths) is partly
covered by CC-1's customer fix and your ACC-13 — verify every remaining create path (vendors,
units, drivers, locations, accounts) and close it as one vertical.
Reminder of what is open, from the 09-04 order: $109,158.50 stranded in Unbilled Revenue 1150; three
documents posted with zero JE; five bank transactions matched to voided documents; A/R out
$1,215.75, A/P out $268.77; 39 delivered loads with no driver bill; 0 of 19 settlements paid; 47/47
stuck at needs_review; INV-2026-00024 voided with no reason; four void-column conventions; 129 NULL
expense numbers; ACC-20 no auto-uncategorize on match reversal; health endpoint has zero financial
checks and 24 of 39 transaction-health checks have never run. ENTITY TRAP: filter every count by
`operating_company_id` = USMCA before it becomes a premise — most of those rows are frozen-entity.
Re-verify each number under bypass for USMCA, report the USMCA number, then fix the USMCA vertical.

## VERIFY-LIVE JOB (standing, in parallel, no code)
After each Cursor API deploy: verify #20425 live — Costs tab picker shows all 34 USMCA cost
accounts, `+ Fuel advance` enabled, bound to 5000. Verify 13508 on the Load Costs board and T156 on
Home KPIs. Write the verified flag or file the defect to the owning seat in one line.

## FORBIDDEN
Settlement feed · geofence import · hard-coded colours anywhere after 2.1 · editing other seats'
component files (file one line instead) · `trigger_deploy` (Cursor only; you were authorized once
on 09-04 for a race — do not repeat without the lead's gate).
Report `CC-2 | STEP-N DONE | <sha> | before→after numbers | NEXT`. Never idle.
