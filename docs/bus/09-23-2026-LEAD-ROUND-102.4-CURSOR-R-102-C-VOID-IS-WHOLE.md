# LEAD — ROUND 102.4 — R-102-C (relayed to Cursor, 2026-09-23), committed verbatim so `LANE_CROSS=` can cite it

```
TO: CURSOR — ROUND 102.4 — R-102-C — THE BOTH-WAY VOID INVARIANT, BASELINED BEFORE THE RUN
DEADLINE: 2026-09-23 20:00 UTC (15:00 Laredo CT). MISSED → CC-2.

BUILD scripts/verify-void-is-whole.mjs. Live DB required, fail closed, exit 1. A money guard
that cannot connect is a FAIL, never a pass — settled, not relitigated.

Proves BOTH directions, per document family, USMCA 5c854333-6ea5-4faa-af31-67cb272fef80:

DIRECTION 1 — NO SILENT VOID. Every document whose journal entries are ALL dead under the
five-column liveness test —
  je.status='posted' AND je.voided_at IS NULL AND je.reversed_by_je_id IS NULL
  AND je.reverses_je_id IS NULL AND p.reversed_by_line_id IS NULL
— carries voided_at, a non-empty void_reason, and a real voided_by_user_id. A dead ledger under
a live header is exactly what production would look like today if E10 ran as written. That is
the failure the owner's ruling exists to prevent.

DIRECTION 2 — NO STRANDED POSTING. Every document carrying voided_at has ZERO live journal
entries under that same test. A stamped header over a live posting is worse than direction 1:
the screen says VOIDED and the trial balance disagrees.

REPORT every violation with family, document number, id, and BOTH counts. Never a total alone.
"N violations" is not a finding.

FAMILIES: loads, invoices, expenses, bills, credit memos, vendor credits, payments, driver
bills, driver settlements, settlement lines, factoring advances, fuel purchases.
loads, factoring_advances and fuel_transactions will FAIL on a missing column until CC-1's
R-102-A migration lands — measured live this hour, all three return ZERO of voided_at /
void_reason / voided_by_user_id. THAT IS THE CORRECT OUTPUT, not a reason to skip them. Name
the missing column and keep going.

banking.* IS NOT A FAMILY. Do not read it, count it or report on it.

BASELINE: run it on PRODUCTION today and publish the number BEFORE E10 runs anywhere. That
number is the before-picture and the only thing that lets anyone prove afterwards that the run
did what it said. It is NOT provisional and it does not wait for the purge — the purge never
deletes, so nothing this guard counts disappears.
Today's live headers for your tie-out: loads 138/142 · invoices 81/119 · expenses 237/506 ·
factoring 69/120 · fuel 589/627 · journal entries 1,181/3,218.

UNCHANGED AND STILL YOURS: 1 E7 batch 2 (154 guards, 118 mechanical, 36 individual, 45 go
DB-conditional; the 18 red carry PROVISIONAL ceilings per Round 86) — finish it first.
2 I-DEDUCT invariant, after this. Item 4 stays RETRACTED: M2 is not built, those rows die.
```
