# CC-1 → CC-2 findings — 2026-09-04

FIND IT / FILE IT / DO NOT FIX IT. CC-2 owns Banking; CC-1 does not edit banking categorization
surfaces.

---

TO: CC-2
FILE: banking categorization flow (whichever route/component resolves a bank_transaction's
categorization reason — CC-2's own surface, not enumerated here since it is not mine to trace)
WHAT IS WRONG: SET-24 (owner order 2026-09-04) named a real, live ambiguity: 14 wire-in bank
transactions in August all carry a masked description ("WIRE TYPE:WIRE IN ... TRN:XXXXXXXXXX620478")
that the bank feed cannot distinguish between a Faro factoring remittance and a broker advance
against a load's receivable. CC-1 just shipped `accounting.broker_advances` (PR #20247, migration
202613630001, live on Neon prod) — the table that RECORDS a broker advance receipt — but nothing
lets a categorizer say "this specific bank transaction IS that broker advance" at categorization
time. Without that link, an operator categorizing one of these 14 wire-ins has no way to tell them
apart, and no way to connect a matched wire-in to the `accounting.broker_advances` row it actually
represents.
WHAT IT SHOULD DO: at bank-transaction categorization time, when the categorizer picks "broker
advance" as the reason/category for a wire-in, let them select (or the system auto-suggest, by
load_id/amount/date proximity) which `accounting.broker_advances` row it corresponds to, and record
that link (a new nullable FK column on whatever table already tracks a categorization's resolved
reason, or a join table — CC-2's own schema convention to choose). This is the disambiguation
mechanism the owner asked for: "whatever you build must let the owner tell them apart at
categorization time."
EVIDENCE: `accounting.broker_advances` (live, migration 202613630001) has no inbound reference from
any `banking.*` table today (confirmed via `grep -rln "broker_advances" apps/backend/src/banking`
→ no matches). The 14 masked-description wire-in rows are described directly in the owner's SET-24
instruction (2026-09-04); CC-1 has not independently re-run that count against banking.
bank_transactions since it is not CC-1's read-scope to build categorization UI against.
