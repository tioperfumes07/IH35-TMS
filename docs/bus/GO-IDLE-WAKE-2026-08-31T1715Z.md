# GO — IDLE WAKE · 2026-08-31 17:15Z (12:15 CDT)
Author: Claude (orchestrator). Owner reported 4 seats idle. Verified.

## IDLE PROOF (OUTBOX mtime vs now 12:15 CDT)
| seat | last OUTBOX | idle |
|---|---|---|
| CASCADE | 11:05 | 70 min |
| CC-1 | 11:13 | 62 min |
| CURSOR | 11:26 | 49 min |
| DEVIN-A | 11:37 | 38 min |
| CODEX | 11:45 | 30 min |
| CC-3 | 11:58 | 17 min |
| CC-2 | 12:09 | 6 min (ACTIVE) |

Queues were NOT empty. Idle was seats not popping, not starvation.
NEVER-IDLE-SEAT-LAW applies: a seat with OPEN items and no OUTBOX line in 30 min is in breach.

## LIVE EVIDENCE — Neon prod, USMCA, read 17:14Z
```
AUG_JE_UNFLAGGED    236   <- month-end guard baseline HOLDS
FUTURE_JE_TOTAL      62
FUTURE_JE_UNFLAGGED  57   <- DEFECT: test-policy amortization posting as REAL
ESCROW_BALANCES       3
ESCROW_LEDGER         3
DRIVERS_ACTIVE      175   <- 172 drivers have no escrow account
```

Loads created today (all `is_sample_data = true` — G1 inheritance CONFIRMED WORKING):
```
load          status                     bills  lines
L-0002        completed_docs_received      0      0    <- DEFECT A
L-0003        delivered_pending_docs       1      1    <- settled EARLY
L-0004        completed_docs_received      1      0    <- DEFECT B
L-0006        dispatched                   1      0
L-0010        completed_docs_received      1      1    <- correct
L-0013        unassigned                   0      0
L-0015        dispatched                   1      0
```

## THE TWO DEFECTS THIS PROVES — CLASS LEVEL, NOT PER-LOAD

**DEFECT A — MINT-SKIP.** L-0002 reached `completed_docs_received` with **zero** driver_bills.
Every other load at `dispatched` or beyond minted one. The mint did not fail loudly — it did not
run. A driver worked a load and the system has no record it owes him anything.

**DEFECT B — SETTLE NON-DETERMINISM.** Of the 3 loads at the terminal settle-eligible state
`completed_docs_received`, exactly **1 of 3** produced a settlement_line. Meanwhile L-0003, which
is only at `delivered_pending_docs` and is NOT settle-eligible, produced one. Settlement fires on
loads that should not settle and skips loads that should.

Same money, opposite errors. Under the blast-radius law neither is a per-load remint. State N
(the population of every load ever reaching a settle-eligible state), fix all of N, ship a guard
with a selftest that fails on a planted skip.

---

# ASSIGNMENTS

## CC-1 — MONEY (idle 62m)
1. **DEFECT A root cause.** Why did L-0002 mint zero bills? Find the guard/branch that returned
   without minting and without erroring. Silent skip is the defect; the missing bill is the symptom.
2. **DEFECT B root cause.** What makes settle fire on `delivered_pending_docs` (L-0003) and skip
   `completed_docs_received` (L-0002, L-0004)? Report the predicate. Do not remint until the
   predicate is named.
3. **Guard + selftest** covering both. Plant a skip, prove exit 1.
4. Factoring: replace hardcoded `0.95 / 0.025` in `batch.service.ts:179-180` with the profile —
   **97.00% advance, 1.50% discount fee, 1.50% escrow, $10 flat wire fee.** Derived exactly from
   the 33 Faro invoices: face 95,075.00 − 1,426.13 − 1,426.13 − 120.00 = 92,102.74. Three wrong
   values in one line; fix all three.

## CC-2 — ESCROW (you are active; this is next after your current item)
5. **172 of 175 drivers have no escrow account.** 12 drivers have already had $1,100 deducted at
   $25/deduction against accounts that do not exist. Escrow is a **liability we owe the driver**,
   not our cash — a deduction with no ledger row is money taken with no record of the obligation.
   Auto-create on driver activation, backfill the 12 from their existing deductions, and prove the
   ledger sums to the balance for all 3 existing + all backfilled rows.

## CASCADE — MECHANICAL, NO CHROME (idle 70m)
6. `docs/audit/NAVY-SUBNAV-INVENTORY.md` — all 178 routes, 3 currently ticked. Inventory first.
7. Convert to `NavyPageSubNav` (`apps/frontend/src/components/layout/NavyPageSubNav.tsx`, tokens
   already locked: `bg-[#1A1F36] text-white text-[11px] overflow-x-auto`). One PR per module,
   commit message `navy subnav: X of 178`. Do not invent a second banner component.
8. Guard + selftest. Claim the EVEN-numbered step first so you do not collide with CC-3.

## DEVIN-A — CHROME A (idle 38m)
9. Drive **L-0004** from `completed_docs_received` through settle in Live Chrome. It has a bill and
   no line — this is DEFECT B reproduced in the UI, which is the evidence CC-1 needs.
10. Book the next TEST load with Sample **ON**; assert `is_sample_data = true` at every hop and
    that AT is never NULL. Today's 7 loads all carry the flag — hold that streak.
11. One batched bus commit per hour. Hard requirement, not aspirational.

## CURSOR — LEAD (idle 49m)
12. Refill any QUEUE-* under 3 OPEN.
13. **57 future-dated JEs are unflagged.** They are the amortization schedule of
    `POL-TESTMTDQ164H` from insurer "TEST DATA insurer keep" — verified: all 5 USMCA insurance
    policies are test rows, and the owner confirmed USMCA carries no bound insurance yet. Flag all
    57 `is_sample_data = true`. **Reverse-and-reflag, do not DELETE** — WORM holds.
14. **Do not touch the 236 August unflagged JEs.** That is the month-end baseline and it has held
    all day. Owner closes the period, nobody else.

## CODEX — BANK (idle 30m)
15. Phase 7: first-ever bank txn → settlement match, or LOAD-5 short-pay. Either is a first.

---

## STANDING — do not violate
- NO VOIDS on `INV-2026-00049..00081`. Owner ruling: real transactions, to be reconciled.
- Nobody closes the August period. Owner only.
- Only CC-2 writes `prod_verified`.
- Never `trigger_deploy` — Cursor only, Rule 42.
- USMCA only. TRANSP/TRK frozen.
- Reverse, never erase.

## PENDING OWNER INPUT — do not act yet
Owner is uploading signed insurance documents and values. The EDSA proposal on file
(`EDSA_USMCA_Integrated_Insurance_Proposal_2026`, Aug 21 2026, $271,280.41 combined) is a **quote,
not a binder** — "coverage is not bound until confirmed in writing." Three open discrepancies were
found and are NOT to be entered into the system until the owner supplies the signed values:
14 vs 15 power units (a $37,400 unit gap), the schedule footer that misses its own sum by
$383,580, and $2,532.18 between the carrier quotes and the finance agreement.
**No insurance JE, policy row, or prepaid/note-payable entry until the owner uploads.**
