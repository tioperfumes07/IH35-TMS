# Item e: deeper than the FK gap — using the named engine would double-post live GL money — CC-1 — 2026-09-26 02:24Z.
Prior content archived: `docs/bus/archive/NOW-CC-1-2026-09-26-04.md` (WORM).

CC-1 | R-187 G1 | DONE | AUTH-049 | reposted with item_id, live JEs pasted | item e: named engine would double-post, 2 more rows now flagged | G3a escalated, real misapplication.

**G1 DONE (AUTH-049, CONSUMED)**: Lead ruled the "NOT 5000" text in R-187 withdrawn — USMCA's live
catalog already carries QBO item "Driver Reimbursement-Company Vehicle Fuel"
(catalogs.items e93a0c79-...) whose own default_expense_account_id resolves to 5000 Fuel & Diesel,
confirmed live. AUTH-046's original attempt was reversed under AUTH-048 not because 5000 was wrong, but
because it never set item_id/quantity/rate_cents/unit_of_measure (all four required together by
expense_lines_item_qty_rate_amount_check). Reposted both lines correctly: item_id set, qty 1, rate
$10.00, load-attributed (13582-5, 13597-5). Both JEs live, balanced, verified: Dr 5000 (via the item) /
Cr the driver's own 2175-00-NNN leaf. status/posting_status correct at insert (ACCT-F2026092595).

**G3a (FLS 525.00/load 13513) — BLOCKED, real finding, no write attempted.** R-187 asked me to "post
the Faro purchase through the factoring engine ... then the Faro receipt, so the invoice closes" for
invoice `ca5c386d-...` (load 13513, customer "FLS TRANSPORTATION SERVICES LIMITED", $525.00,
`factoring_status='not_factored'`, 0 paid — matches `.faro-map.json`'s own flagged issue "paid 0.00 !=
faro receipts 525.00"). Before writing anything, I checked the PURCHASE REPORT (Faro inv 8, PO
5772267, 8/14/26, FLS Transport Inc., purchase 525.00, cash rsv 7.88, discount 7.88, net adv 509.24 —
the standard 1.5%/1.5% contract split) against what's already live, and found this exact transaction
has ALREADY been fully processed — just attached to the WRONG invoice. `accounting.factoring_advances`
row `FAC-2026-00009` carries `faro_invoice_number='8'`, `faro_purchase_date=2026-08-14`,
`reserve_amount_cents=788`, `factor_fee_cents=788`, `advance_amount_cents=50924` — an EXACT match to
this purchase-report row — but it's linked to invoice `f59a3468-...` on load 13515, customer
"FLS Transport Inc." (a DIFFERENT customer_id than "FLS TRANSPORTATION SERVICES LIMITED", despite the
near-identical name). Payment `PMT-2026-00003` (reference "FARO-INV-8", $525.00, notes: "ROUND 153
item 2 -- Faro AGING: invoice fully closed") is applied to that SAME wrong invoice. Load 13515 does not
appear anywhere in `.faro-map.json` — Faro's own reconciliation never found a purchase for it — so this
looks like a genuine cross-customer misapplication from ROUND 153's AGING reconciliation work, not two
coincidentally-identical $525 transactions.

Creating a fresh advance+receipt for the CORRECT invoice (ca5c386d/13513) as G3a's text literally asks
would DOUBLE-COUNT Faro's one real $525.00 payment — Faro only purchased and paid this invoice once.
The right fix is almost certainly to UNWIND FAC-2026-00009 + PMT-2026-00003 from the wrong invoice
(f59a3468/13515/"FLS Transport Inc.") and correctly reattach the same funding+receipt to the right one
(ca5c386d/13513/"FLS TRANSPORTATION SERVICES LIMITED") — a bigger, two-invoice, two-customer correction
than "post one missing transaction," with real risk to whatever ROUND 153's AGING reconciliation
already built on top of the wrong pairing. Escalating rather than reversing/reattaching on my own
judgment. No write attempted; both invoices, the advance, and the payment are untouched.

**SELF-CORRECTION (AUTH-048, CONSUMED)**: this session's own AUTH-046 items a/b (the 2 missing Honda
gas expenses, settlements 5805/5808) were posted Dr 5000 Fuel & Diesel / Cr 2175-<driver>. That debit
account is WRONG — R-187's own G1 spec (a document I had already read this session) explicitly
requires a distinct "Company Vehicle Fuel" account ("NOT 5000 and NOT an IFTA gallon") and says to
BLOCK, not invent one, if it doesn't exist. Confirmed live: it doesn't exist in USMCA's chart. Caught
this myself re-reading G1's exact text before starting it as a follow-on task — should have
cross-checked before running AUTH-046, not after. Both JEs reversed live and verified
(reversing JEs 7538eee2.../77206ffc..., expenses now status='void'/posting_status='reversed', the
credit side was correct and is unaffected). G1 (both lines) is BLOCKED again, reported to Lead,
pending the account-creation ruling. AUTH-046's own CONSUMED block is marked RETRACTED with a pointer
here rather than left standing uncorrected.

**Root cause fixed** (ACCT-F2026092595, PR #22794): 4 independent `accounting.expenses` posting
writers (`expenses.routes.ts` x2, `tour-close-posting.service.ts`, `maintenance/two-section-service.ts`)
each flipped `posting_status='posted'` on a successful GL post but never touched the separate `status`
column — confirmed via `audit.row_changes` (11 updates on one row, `status` stuck at 'draft' through
the exact update that set `journal_entry_id`). No CHECK constraint links the two columns. My own
AUTH-046 script had the same gap, worse (never set `posting_status`/`journal_entry_id` either). All
four writers now move `status` in lockstep with `posting_status`.

**15 headers corrected live** (AUTH-047, CONSUMED): 13 rows needed `status` flipped only; my own 2
rows (13582-4, 13597-4) needed the full correction, ground-truthed against `journal_entry_postings`
(not the also-wrong `expenses.journal_entry_id` column). Live counts before -> after: draft 15 -> 0,
posted 502 -> 517, void 801 -> 801 unchanged.

**Item e — went deeper per Lead's "then item e," found the named engine cannot be used at all
without double-posting real GL money already recorded.** The 12 advances confirmed live ($2,275.96
across 10 distinct `driver_finance.driver_bills`, exact match to ROUND 202's total). Continued from the
FK-mismatch finding already reported (`linked_driver_bill_id` -> `driver_finance.driver_bills(id)`, not
`linked_bill_id` -> `accounting.bills(id)`; zero `accounting.bills`/`driver_settlement_gl_bills` rows
exist for any of the 10 bills).

Checked WHY these advances show `status='recovered'` with no bill payment: their recovery is ALREADY
posted, inside each advance's own settlement JE, as a credit to **1245 Driver Cash Advances
Receivable** for the advance's own amount — verified row-by-row against `journal_entry_postings`
joined on each advance's `recovered_in_settlement_id`. **8 of the 10 previously-"clean" rows match
exactly** (CA-2026-0001/0002/0003/0004/0006/0007, CA-2026-TIE-5800/5801 — $1,847.96 total): their
$-amount already sits as a 1245 credit in a posted, balanced settlement JE. Creating a NEW bill+payment
through `createBill`/`payBill` (the only code that builds an `accounting.bills` row for a driver
context — it has its own `driver_id` column, confirmed) would post a SECOND, duplicate JE: both
functions call `postSourceTransactionInClientTx` whenever `BILL_PAYMENT_GL_POSTING_ENABLED` is on, with
**no parameter to suppress it** — confirmed reading `PayBillInput`'s full shape. There is no
"record-only, GL already accounted for" mode in this engine; using it here would double-count real
cash-advance recoveries.

**2 MORE rows now fail the same check** (beyond the already-known CA-2026-0008/0009): CA-2026-0005
($148.00) and CA-2026-TIE-5807 ($78.01) — their claimed `recovered_in_settlement_id` JE contains a 1245
credit for a DIFFERENT amount ($151.99 and $280.00 respectively, not their own), meaning either their
`recovered_in_settlement_id` is wrong or their `status='recovered'` is itself unearned. **4 of 12 rows
are now flagged**, not 2. No write attempted on any of the 12 — the 8 clean ones need an engine that
doesn't yet exist (a records-only linkage, or a genuinely new bill-payment leg that nets against the
already-posted 1245 credit rather than duplicating it), and the 4 flagged ones need their own
settlement-linkage correction first.

## Still open
Item e (needs a Lead/owner call on: (a) a records-only bill-payment mechanism that doesn't double-post
the 8 already-GL-recovered advances, (b) the 4 flagged rows' settlement-linkage errors). G3a (BLOCKED —
needs a Lead/owner call on unwinding+reattaching FAC-2026-00009/PMT-2026-00003 across two customers).
ROUND 202 items c/d (flagged previously, still open) and STEP 3 (blocked behind c/d). G3b, G3c, G3e,
G4's escrow remainder (8/10, 8/12, 8/13, 8/14). R-185 steps 2-6 (repost the 27-row driver-paid-expense
list from 1000/2000 to their own 2175 children — NOTE: check each row's own item/category against
catalogs.items' own default_expense_account_id, same as G1, before reposting; 5000-via-item is the
pattern now, not necessarily every row's own current account).

CC-1 | 02:24Z | G1 DONE and CONSUMED (AUTH-049). Item e went from "wrong FK" to "the named engine would
double-post, and 2 more rows don't even reconcile to their claimed settlement" — real findings, not
declined work; no write attempted on any of the 12. Writer root cause (ACCT-F2026092595) + AUTH-047's
15-row backfill stand unaffected. Continuing to G3b/G3c/G3e/G4/R-185 while awaiting Lead's calls on
item e and G3a.
