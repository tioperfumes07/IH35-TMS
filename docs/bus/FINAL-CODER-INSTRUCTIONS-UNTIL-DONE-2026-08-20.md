# FINAL CODER INSTRUCTIONS — UNTIL THE SCOREBOARD IS CLOSED
**Owner 2026-08-21 06:46 CT · 7 HOURS · DEADLINE 13:46 CT 2026-08-21 · URGENT 6 THEN 14 THEN ALL + SCENARIOS**  
**USMCA ONLY. No Trucking. No Transportation. No QBO sync / no TMS→QBO write-back.**  
**06:16 CT deadline SUPERSEDED.** Never idle. Never wait for Jorge. This header wins.

**Bar:** `docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md` items **1–12**. McLeod / QBO-chrome / NetSuite-controls / Alvys-workflow **quality**. CI-green · Box 3 unpaid=0 · matrix skeleton **≠** 100%.

---

# FINAL CODER INSTRUCTIONS — UNTIL THE SCOREBOARD IS CLOSED
**Owner 2026-08-20 23:16 CT · 7 HOURS · DEADLINE 06:16 CT 2026-08-21 · URGENT 10 100% + SCENARIOS**  
**USMCA ONLY. No Trucking. No Transportation. No QBO sync / no TMS→QBO write-back.**  
**Never idle. Never wait for Jorge. This header wins.**

**Owner 2026-08-20 22:27 CT · NEVER IDLE · URGENT 6 THEN 10 THEN ALL MODULES · Chrome ports assigned**  
**Prior 18:04 CT text below still applies except where this header conflicts — this header wins.**

- **Never idle / never pause.** When your column on a module hits the bar, **immediately** take the next module in order. Stop only when WAVE 1 + WAVE 2 are Fully-Wired **1–12**.
- **URGENT 6 first:** accounting → banking → settlements → factoring → dispatch → vendors. **Then** customers → drivers → fleet → lists. **Then WAVE 2.** Not inventory as urgent.
- **All five seats open debug Chrome** on Cursor-assigned ports (`docs/bus/CHROME-PORTS-LOCKED.md`). No deviation.
- **FAST-MERGE 4-min law ON.** Report to Jorge **only** after live Chrome/Neon proof (or `UNVERIFIED` + named blocker).
- Communicate in **INBOX + OUTBOX + `SHARED-NOW.md` + Desktop bus folder**. Jorge is not the messenger.

---

# FINAL CODER INSTRUCTIONS — UNTIL THE SCOREBOARD IS CLOSED
**Owner 2026-08-20 18:04 CT · FINAL · do not deviate · do not write a competing plan**

This is the **only** GO from here until **Miss C = 0** and **READY ✓** on the frozen USMCA Required maps — **then** WAVE 2 remainder until the **same bar** on every remaining module.  
Canonical scoreboard: `/program/matrix?scope=system` · maps `docs/specs/scoreboard/modules/*.required.json` (**frozen — no new leaves**).  
Canonical 100%: `docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md` items **1–12** + this file.  
Money is **in** the 100 (owner 2026-08-20). **Never park. Never defer. Always fix or check now.**

Jorge is not the messenger. Chat summaries do not override this file.

---

## What “completely finished / subscription-ready” means

**Not** CI-green. **Not** chrome 0✕. **Not** Clicked keys full. **Not** OUTBOX CLEAR. **Not** Built% alone.

A **module** is finished when **every frozen Required cell** on that module is:

1. **Built** — leaf-specific guard (no `leafRe:.*` / `|.*` / word-blanket)
2. **Fully-Wired 1–11** — create→canonical table · money when owed (reuse poster, no TMS→QBO write-back) · forward **and reverse** FKs · matrix columns · surface bar (search/filter/gear/range/picker/Combobox/modal/drawer/wizard/`+ Add new` first row) · QBO chrome law · picker+creator law · RLS/entity · guard+evidence
3. **Item 12 Clicked** — Cursor `leaf=<module>:<leafId>:<col>` USMCA Chrome PASS (1–3 Exact). Devin PARKED.
4. **Box 4 Live** — CC-2 `AUDIT-COVERAGE-LIVE` **PROD-VERIFIED** + `` `leaf:col` `` — **including money** (AP/BILL, BANK, EXP, GL/JE, INV, AR, LIAB)

**Miss C** = unpaid **Box 4 Live** on Frozen (money included). Clicked 100% **does not** zero Miss C.  
**READY ✓** = Miss C = 0 on the frozen set in scope.

TRANSP QBO parallel books stay parked (no write-back). **USMCA money is not parked.**

---

## Waves (order is law)

**Nothing is deferred.** Money, wiring, linkage, chrome, pickers, Clicked, Box 4 — fix the unpaid cell this hour.

**Owner 2026-08-20 19:02 CT — URGENT does NOT include inventory.**

### WAVE 1 — URGENT 10

`accounting` → `banking` → `settlements` → `factoring` → `dispatch` → `vendors` → `customers` → `drivers` → `fleet` → `lists`

`lists` = **Items** (Lists / QBO items catalogs). **Never `inventory`.**

Columns advance independently down this list. Never idle.

### WAVE 2 — remainder (includes inventory, fuel, safety, maintenance)

`home` → `tasks` → `driver-hub` → `compliance` → `safety` → `maintenance` → `insurance` → `legal` → `cash-flow` → `finance` → `form_425` → `fuel` → `inventory` → `reports` → `docs` → `users` → `help` → `program` → `system`

**Stop only when:** WAVE 1 + WAVE 2 every module at the bar · **WAVE 3 second pass** (same modules, unpaid cells + five scenario events) · system Miss C = 0 · READY ✓.

**WAVE 3 (owner 2026-08-21 11:53 CT):** not new modules. Re-walk WAVE 1 then WAVE 2 on your column. Canonical: `docs/bus/WAVE-2-AND-3-EVERYTHING-2026-08-21.md`.

---

## Columns (who owns what — every module, every wave)

| Seat | Column forever | Never |
|------|----------------|-------|
| **CC-3** | Box 3 **Built march** down 6 → 14 → WAVE 2 (`qbo_chrome` / `picker_law` / surface on **this** module). If 0 unpaid, next module same turn. Chrome **9225** | GL math · Clicked stamps · **leftovers** · other-wave residue · monitor |
| **Codex** | `connectivity` + `reverse_link` (both-way drill, McLeod seriousness) · Chrome **9226** | GL · Clicked stamps |
| **CC-1** | **Money** — canonical write + reuse `postSourceTransaction` / `maybePostBankCategorizationToGl` · header+lines · balanced JE when USMCA flags ON · bank/JE/bill/invoice/payment/settlement/factor/escrow · Chrome **9223** | inventing FKs · TMS→QBO write-back · parking money · Clicked stamps |
| **CC-2** | **Box 4 Live** append-lease `AUDIT-COVERAGE-LIVE.md` PROD-VERIFIED + `` `leaf:col` `` (money stamps are money, not NON-MONEY) · Chrome **9224** | Clicked theater · keyword fan-out Live |
| **Cursor** | Lead · bus · **leftovers / other modules / other waves** CC-3 already zeroed · scoreboard honesty · **item 12 Clicked** (unpaid cells only, no 876 rewalk) · FAST-MERGE own PRs · Chrome **9222** | Stamping Box 4 · inventing a 6th wave · running Devin |

**Devin-A is PARKED (owner 2026-08-20 18:50 CT).** Do not start `devin-a-live-loop`. Do not ping Devin. Clicked = Cursor.

**Share the module, same hour.** Do not wait for another seat to “finish the module.”

---

## Per-module loop (repeat until that module’s Miss C = 0)

1. **CC-3** — unpaid Built / chrome / picker_law / surface bar on **this** module’s Required map. FAST-MERGE.
2. **Codex** — unpaid reverse_link + connectivity on **this** module. FAST-MERGE.
3. **CC-1** — every money Required cell: real USMCA create/save/post (or honest WORM void/reverse). No defer. FAST-MERGE + Neon yourself.
4. **Cursor** — CDP unpaid Clicked only (`leaf=module:leafId:col`). Queue 6 rewalk = 2 leaves after keys full. Never 876 all-WAVE dump. Lead the bus.
5. **CC-2** — after live row exists, stamp Box 4 `` `leaf:col` ``. OUTBOX `Miss C was N now M`.
6. **Cursor** — if a seat idles, re-instruct from **this file** + `CODER-INSTRUCTIONS-NOW.md`. Never idle yourself.

Then next WAVE 1 module on **your column**. After the 10, WAVE 2. Never defer money/wiring/linkage.

---

## Accounting Queue 6 (drain before leaving accounting)

- `accounting.parity.credit_memos_page` — `connectivity` `reverse_link` `customer` `qbo_chrome` (+ money if Required)
- `banking.panel.linked_bank_transactions` — `bank` `gl_je` `connectivity` `reverse_link`

Do **not** treat owner-voided test rows as JE-dark. Reverse bad JEs with `reverseJournalEntryNoFlip`. Keep live tagged bank+JE + CM-2026-0001 for stamps.

---

## FAST-MERGE (every ship — ON until Jorge says otherwise)

Canonical: `docs/bus/FAST-MERGE-4MIN-LAW.md`

1. Gate **exit 0**: `node scripts/money-pr-local-gate.mjs` (Cursor: `node scripts/ops/cursor-ship-preflight.mjs --body-file /tmp/pr-body.txt`)
2. `git push`. If blocked **only** by `verify-static-fallback` ENV class (~54 main reds) → `git push --no-verify` **after** step 1 PASS. Never `--no-verify` on **your** red guard.
3. `gh pr create` — **never** `gh pr checks --watch`
4. `gh pr merge N --squash --delete-branch --admin` **immediately**
5. Neon yourself if money/migrations
6. OUTBOX one line → next unpaid cell **same turn**

Cursor PR titles must start with `Cursor-`.

---

## Hourly (every seat)

```text
git pull --ff-only origin main
```

Re-read **this file** + `INBOX-<SEAT>.md` TOP + `FAST-MERGE-4MIN-LAW.md`.

ACK:

```text
<SEAT> | ACK | STANDARD=SCOREBOARD-100 | WAVE1-URGENT-10 | NO-DEFER | FAST-MERGE | NOW=<module> <col> | GO
```

Hourly OUTBOX first line: `Miss C was N now M` **or** `Miss C UNCHANGED blocker=<named unpaid Built|reverse|money|Clicked|Box4 cell>`.

---

## Forbidden (process defect)

- Defer / park / “owner-gated” / treating inventory as urgent / skipping money or linkage
- New Required leaves until Jorge unfreezes
- Dual-path / ComingSoon twin / memo-only FKs
- Claiming module done on Built or Clicked alone
- OUTBOX-only FAST-MERGE
- Inventing a different module order

---

## Desktop + bus copies

Repo: `docs/bus/FINAL-CODER-INSTRUCTIONS-UNTIL-DONE-2026-08-20.md`  
Desktop: `~/Desktop/IH35-CURSOR-AUDIT/USMCA-WEEKEND-LEAD-2026-08-07/` (same filename + `PASTE-*-FINAL.md`)
