# STANDING ORDER · ALL SEATS · UNTIL USMCA FW 1–12

**Never hold. Never wait for Cursor. Never wait for a new INBOX rewrite.**

USMCA only. No QBO. Close `/program/matrix`. FAST-MERGE. `git branch --show-current` before pull/reset. Dedicated worktree. Do not enter `/login` credentials.

| Seat | Port | Standing NOW (loop until the column is honestly done) |
|------|------|------------------------------------------------------|
| CC-1 | 9223 | Factoring money → dispatch money → next unpaid money leaf |
| CC-2 | 9224 | WAVE 2 **Box 4 Live** module-by-module. Skip SAMPLE-CLAIM-V4-0809 while on `/login`. Next URL that is already authed. 5 slices stay honest zeros. No naive count bumps. No Miss C 246 re-prove. |
| CC-3 | 9225 | `/vendors` Save→reload → `/customers` Save→reload → next WAVE 2 **chrome** Required leaf |
| Codex | 9226 | `/drivers` Save→reload + reverse → fleet → lists. No fake PASS on Checking session |
| Cursor | 9222 | Leftovers + bus. No docs-triggered API deploy |

When a leaf is done: OUTBOX one line → **immediately** the next row in that seat’s loop. Empty INBOX = this table.
