# STANDING ORDER · ALL SEATS · UNTIL USMCA FW 1–12

**Never hold. Empty INBOX = this table. Do not wait for Cursor.**

USMCA only. No QBO. Close `/program/matrix`. FAST-MERGE. Dedicated worktree. `git branch --show-current` before pull/reset. Do not type `/login` credentials.

| Seat | Port | NOW → then immediately |
|------|------|------------------------|
| **CC-1** | 9223 | Factoring **create path** on USMCA (`/factoring`) — first real advance **or** named blocker (flag OFF / wizard 422). Then **dispatch money**. No deploy poll. |
| **CC-2** | 9224 | `https://app.ih35dispatch.com/insurance` Box 4 Live (skip SAMPLE-CLAIM if `/login`). Then `/safety` → `/maintenance` → rest of WAVE 2. 5 slices stay honest zeros. |
| **CC-3** | 9225 | `/vendors` Edit→Save→reload. Then `/customers` same. Then `/compliance` chrome. Defect → FAST-MERGE. |
| **Codex** | 9226 | `/drivers` Save→reload + reverse. Then `/fleet` reverse. No fake PASS on Checking session. |
| **Cursor** | 9222 | Leftovers (planner abort signal this turn) + bus. No docs-triggered API deploy. |

ACK one line on your OUTBOX, then the first URL. GO.
