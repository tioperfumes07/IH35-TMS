# STANDING ORDER · NEVER IDLE · EVERY MODULE · UNTIL FW 1–12

**Owner 2026-08-21 11:17 CT.** This file wins over any older INBOX that says “holding,” “poll,” or “wait for Cursor.”

**Forbidden:** idle · holding · waiting for a new INBOX · waiting for another seat · waiting for Jorge · waiting for deploy · waiting for login (skip that URL, next URL same turn) · waiting for Cursor to rewrite this file.

**Stop only when:** WAVE 1 + WAVE 2 every module is Fully-Wired **1–12** (USMCA). Empty INBOX = keep marching this ladder.

USMCA only. No Trucking. No Transportation. No QBO sync. Close `/program/matrix` after one glance. FAST-MERGE. Dedicated worktree. `git branch --show-current` before pull/reset. Do not type `/login` credentials. One URL → prove → **close the tab** → next cell.

## Ladder (law)

**WAVE 1:** `accounting` → `banking` → `settlements` → `factoring` → `dispatch` → `vendors` → `customers` → `drivers` → `fleet` → `lists`  
**WAVE 2:** `home` → `tasks` → `driver-hub` → `compliance` → `safety` → `maintenance` → `insurance` → `legal` → `cash-flow` → `finance` → `form_425` → `fuel` → `inventory` → `reports` → `docs` → `users` → `help` → `program` → `system`

If your column on a module is already 0 unpaid: **skip it and take the next module in the same turn.** Honest zeros / 0 TMS rows = N/A + next leaf — not idle.

## Your column on every module

| Seat | Port | Forever (do this on the current module, then the next) |
|------|------|--------------------------------------------------------|
| **CC-1** | 9223 | Money write + reuse poster. Next unpaid money leaf on the ladder. No Render poll. |
| **CC-2** | 9224 | Box 4 Live. Next unpaid Live cell. Skip `/login` URLs. Five scenario slices stay honest zeros — do not wait on them. |
| **CC-3** | 9225 | Built / qbo_chrome / picker / Save→reload chrome. Next unpaid chrome leaf. No status-only PRs. |
| **Codex** | 9226 | `connectivity` + `reverse_link` live drill. No fake PASS on Checking session. Next module reverse. |
| **Cursor** | 9222 | Clicked leftovers + bus. No docs-triggered API deploy. |

ACK one line on your OUTBOX. Then work. Jorge is not the messenger.
