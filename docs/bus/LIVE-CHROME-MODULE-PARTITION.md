# LIVE CHROME MODULE PARTITION · 2026-08-15 (Cursor lead)

Owner: Box 3 Built 3446/3446 closed · Box 4 Live in flight · seats must not re-walk the same module.

| Seat | Live Chrome modules (USMCA · app.ih35dispatch.com) | Do NOT touch |
|------|-----------------------------------------------------|--------------|
| **Cursor** | `lists` · `safety` · `dispatch` · `fleet` · `fuel` · `maintenance` (Lists/catalogs + non-money chrome) · `customers` · `vendors` · `drivers` (Lists + hub chrome) · `docs` · `tasks` · `compliance` | accounting money posters · settlements GL · factoring money · banking JE/match economics |
| **Codex** | `insurance` · `legal` · `inventory` · `reports` · `home` · `program` · `system` · `cash-flow` · `form_425` · `finance` (non-money hops) | Cursor modules above · CC-1 money leaves |
| **CC-1** | `accounting` (money leaves: bills/expenses/JE/invoices) · `banking` (categorize/match/recon/GL) · `settlements` · `factoring` | Cursor Lists create chrome · Codex connectivity-only modules |

**Rules**
1. Claim in your OUTBOX before walk: `LIVE CLAIM <module> · leaves: …`
2. PASS lines: `LIVE PASS <module>:<leaf> · evidence`
3. Failures → GUARD-WORKORDERS OPEN + OUTBOX same turn (Finding Law)
4. Product-wide still `Live=BLOCKED` until Box 4 certified — per-leaf PASS ≠ product Live
5. If another seat already has LIVE CLAIM on a module, skip it

**Cursor already PASS (do not re-walk):** Lists accounting create set (CoA/classes/items/…), preferred hub creates (safety/dispatch/drivers/fuel/fleet/maint OEM+labor-rates), banking statement-import + settings chrome sample.
