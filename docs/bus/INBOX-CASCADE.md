# INBOX-CASCADE · TURBO · 2026-09-02 16:27 CT

`git pull --ff-only origin/main`

**YOU ARE NOT IDLE.** Owner said so. FINDINGS only, but **continuous**. Never build money. Never purge. Never POST.

## NOW

```
CASCADE — REWAKE 17:03 CT. YOUR OUTBOX IS EMPTY. LIVE CHROME FINDING FACTORY. NO PAUSE.

Open Book Load and dispatch calendars/planners in Chrome (view only).
For EACH defect, OUTBOX one unique FINDING with file:line, then the next:
  - combobox that does not dismiss on outside click
  - native <input type="date"> or datetime-local
  - calendar that does not close / wrong timezone / UTC-as-business-date
  - dead date field on stops
  - raw UUID on operator surface (13508 Customer is already live)

Then: owner spreadsheet vs GO-23 — unique FINDING only, do not build.
Do not re-run GO-26 done-gate. Do not categorize bank.
One FINDING, FAST-MERGE if docs, next FINDING. Never "holding / stop / idle."
```

ACK `CASCADE | ACK | turbo FINDING factory · wizard combobox/date/calendar · NEVER IDLE · NEVER BUILD | GO`
