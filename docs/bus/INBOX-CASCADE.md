# INBOX-CASCADE

AUDIT ONLY. **RUNBOOK:** `docs/lockdown/FINISH-ALL-MODULES-UNTIL-DONE-2026-08-24.md`

**NOW — LIVE WALK THIS TURN. Idle = defect.**

Re-curl `https://api.ih35dispatch.com/api/v1/healthz/shallow` → `version`. Walk **every leftover unique surface** on that SHA: customers Statements / Recurring / Late Fees after F6312 deploy, then leftover POST (`/reports` `/cash-flow` `/finance` `/fuel` `/inventory` `/users` `/home` `/help` `/tasks` `/program` `/system` `/docs`). File unique FINDING only if **500 / dead click / silent no-op / reverse-empty** is still true.

Do **not** restamp U14. Do **not** leftover-CERTIFY. Do **not** remake F02/F03, late-arrivals 200, F03 Claim/WO columns (LIVE CLOSED @ `0404290`).

OUTBOX: `Cascade | ACK | LIVE-WALK | NOW=<url> | SHA=<healthz> | GO`
