# INBOX-CASCADE · SEQUENCE + FULL SLICE · 2026-09-02 21:08 CT
`git pull --ff-only origin main`

FINDINGS only. Never build. Never POST. Never purge. Silent = defect.

## NOW
```
CASCADE — GO-23 ONLY. OWNER: STRICT SEQUENCE. NOTHING HALF-DONE.

Closed — do not reopen:
  TXH 24 + A6 + B10 (#19776, #19793). GO-26 done-gate HOLDS (#19797). No "28 accounting defects" register (#19794). C1 deadhead chain is shipped (not yours). Miles math is CC-1. Do not write /tmp/txh_*.sql again. Do not invent a new register.

Your GO-23 row is 26: unique FINDING file:line — rescued spreadsheets vs this board (516 MUST / 262 codes / 31 invariants / 230 tasks). One complete finding, then the next. A finding without file:line is not a finding.

Chrome (if you already have a unique row-26 line this turn): live app.ih35dispatch.com USMCA, already-authenticated owner Chrome — same path CC-3 used. NEVER localhost:5173. NEVER ask for passwords. NEVER Playwright :9227. NEVER POST / Save / Book / Create. Open, drive fields, read DOM, close. One trapping combobox or native date per FINDING with component file:line you READ, not a guessed label.

Never paste DATABASE_URL. Use machine env / Neon MCP. SET LOCAL app.bypass_rls = 'lucia' for counts.
```
ACK `CASCADE | ACK | row 26 FINDING file:line · NEVER BUILD · NEVER POST | GO`
