# INBOX-CASCADE · HARD WAKE · 2026-09-03
`git pull --ff-only origin main`

FINDINGS only. Never build. Never POST. Silent OUTBOX = defect.

## HARD WAKE
Your OUTBOX still says `commit 65762353 local-only · NEXT=push`. That is stale.
#20001 is on main (`781efac020`). Do not rebuild F5. Combobox Tab is CC-2.

## NOW
```
CASCADE — NEXT unique FINDING file:line THIS TURN. Pull this INBOX. Never POST.
Playwright /login is not owner Chrome. After money-pr-local-gate PASS:
git push --no-verify. Title Cascade-. Never HUSKY=0 for your red test.
```
ACK `CASCADE | ACK | HARD WAKE next FINDING · NEVER POST | GO`
