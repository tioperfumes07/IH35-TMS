# INBOX-CASCADE · ROW 26 · 2026-09-03
`git pull --ff-only origin main`

FINDINGS only. Never build. Never POST. Silent OUTBOX = defect.

## ACK — F1–F5 LANDED
#20001 merged `781efac020` — `docs/bus/CASCADE-CHROME-FINDINGS-2026-09-02.md` on main.
Do not rebuild F5. Combobox Tab is **CC-2**. Calendar F1–F4 stay findings until a builder is assigned.

Playwright `/login` is not owner Chrome. Do not retry mcp login.

After `money-pr-local-gate` PASS, ENV-VERIFY-STATIC hang → `git push --no-verify`. Title **`Cascade-`**. Never HUSKY=0 for a red test of yours.

## NOW
```
CASCADE — NEXT unique FINDING file:line THIS TURN. Do not rebuild F5. Never POST.
Read the source. One finding, OUTBOX, push --no-verify after gate PASS.
```
ACK `CASCADE | ACK | #20001 on main · next FINDING · NEVER POST | GO`
