# INBOX-CASCADE · SYNC 2026-08-16 20:23 CT · LIVE VERIFY NEVER STOPS

**0 open green PRs is NOT a stop condition.** You VERIFY live chrome toward certified 30/30. FAST-MERGE is a side loop, not the job.

Chrome **9225** · entity **USMCA** · app `https://app.ih35dispatch.com`  
Desktop file only (this path). After every OUTBOX line → take next leaf immediately.

## FORBIDDEN (process defect)
- `awaiting INBOX` · `awaiting green PR` · `0 PRs idle` · `heartbeat only`
- Stopping because VERIFY-BATCH is done
- Re-running the closed 20:08 batch as your only work

## OUTBOX NOW (paste this first)
`Cascade | WORKING · LIVE CLAIM WAVE-LIVE-C1 · chrome=9225 · USMCA`

## WAVE-LIVE-C1 — do in order · one OUTBOX line each · never pause between

### A · Close prior FAILs with CORRECT proof (do first · ≤10 min)
1. **#8025 Program Tracker GATED** — open `/program/tracker` · Pending tab · prove tooltip/banner text `no owner approval is required` OR source on `origin/main` `ProgramTrackerPage.tsx` contains that string + StatCard `includes legacy GATED tags` · OUTBOX `LIVE PASS #8025 Tracker GATED` or FAIL with evidence
2. **Book Load** — Neon only via `load_number` (NOT `display_id`): row `L-20260816-0168` status=`dispatched` USMCA opco `5c854333-6ea5-4faa-af31-67cb272fef80` · OUTBOX `LIVE PASS BookLoad Neon L-20260816-0168 dispatched` · then open Dispatch board in browser if logged in

### B · Independent Live VERIFY (this advances certified — keep going when A done)
For each leaf: CLAIM → click path → PASS with URL+what you saw OR FAIL+HANDOFF=Cursor|CC-1 + board row.

3. `/program/final-additions` — legacy GATED disclosure still honest
4. `/program` (Scenario Tracker home) — loads · not empty theater
5. `/program/modules` — module completion renders
6. `/home` — home tiles navigate (sample 3 clicks)
7. `/reports` — pick one report · filters required · Run enabled only when required filled (#8014 class)
8. `/insurance/policies` — list loads · open one detail · EntityLinks land
9. `/insurance/claims` — list · create affordance present
10. `/legal` — list/create chrome
11. `/inventory` — list · create if owed
12. `/cash-flow` — filters Apply/Cancel/Reset · no raw validation dump
13. `/finance` non-money hops — no money poster
14. `/system` — settings leaves sample
15. `/form_425` — opens · no crash

### C · Side loop every 3 leaves (not instead of B)
Poll open PRs → FAST-MERGE only if mergeable+green · if 0 greens → continue B · never OUTBOX "idle awaiting PR"

### D · When WAVE-LIVE-C1 ends
OUTBOX `Cascade | WAVE-LIVE-C1 done · PASS=N FAIL=M · next=WAVE-LIVE-C2`  
then **immediately** start WAVE-LIVE-C2 = re-walk any FAIL + spot-check Cursor partition read-only: `/lists` `/safety` `/dispatch` `/fleet` `/fuel` `/maintenance` (FO HANDOFF=Cursor).

## Law
FAST-MERGE-4MIN-LAW · HONEST-BUILT-LAUNCH · Live=BLOCKED until certified 30/30  
Jorge ≠ messenger · Cursor rewrites this INBOX after merges — you never wait for chat
