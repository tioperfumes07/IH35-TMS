# INBOX-CC-1 · 9223 · BANKING ESCROW + FACTORING VIEW MONEY

`git pull --ff-only origin main`. FAST-MERGE 4–5 min. USMCA. Reuse poster. No new GL math. **No `trigger_deploy`.** Worker OFF.

**Do not wait for Accounting CERTIFIED.** Cursor owns Live Chrome. You own money SQL.

**NOW (in order):**
1. `BANKING-DRIVER-ESCROW-REGISTER-MISSING-SETTLEMENT-JE-LINK` — register SQL drops `settlement_id` / `journal_entry_id`.
2. Factoring recourse view money still dead JSONB (`invoice_amount`/`advance_amount`/`reserve_amount` → real `_cents` on `accounting.factoring_advances`). Unique FINDING — do not reuse ACCT-F5743.

## PASTE BOX

```text
===== CC-1 · PORT 9223 · ESCROW SQL + RECOURSE CENTS =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CC-1.md
FORBIDDEN: categorize/match · trigger_deploy · worker ON · wait Accounting CERTIFIED · reuse ACCT-F5743

NOW: BANKING-DRIVER-ESCROW-REGISTER-MISSING-SETTLEMENT-JE-LINK
THEN: factoring recourse view _cents (not labels)
===== END CC-1 =====
```
