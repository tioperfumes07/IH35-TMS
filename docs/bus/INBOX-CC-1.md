# INBOX-CC-1 · GO-23 WAVE 2 · C6 = 17 GAPS REMAINING

`git pull --ff-only origin main`

**FAST-MERGE ON (4 min).** Gate exit 0 = merge proof. Same turn: `gh pr create` → `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`. Never `gh pr checks --watch`. Never wait CI. Never ask Jorge. Push hook ONLY `ENV-VERIFY-STATIC-NO-LOCAL-PG` after gate PASS → `--no-verify` authorized. Law: `docs/bus/FAST-MERGE-4MIN-LAW.md`.

**Queue:** `claude/GO-23-BUILD-SEQUENCE-STRICT-2026-09-02.md` — STRICT ORDER. You do not pick the next row.

## VOID
POST Book Load · remake **N1** (**#19601**) · remake the C6 scanner · skip remaining C6 to finish **B8** · author **B5** now · empty `~/IH35-TMS-clean` · new registers · Wave 3 GO-22 while C6 17 is open · treat **#19616** as C6 closed (it is a finding, count still 17)

## NOW

1. **C6 remaining work is the 17 `--strict` gaps**, not a second guard and not a GUARD-WORKORDERS filing. **#19616** named `cash-advances.routes.ts` mark-disbursed `INSERT accounting.bill_payments` with no poster — that **is** baseline key `cash-advances/cash-advances.routes.ts::INSERT INSERT INTO accounting.bill_payments`. **Fix it in-session** (route the insert through the same poster `cash-advance-disburse.ts` already uses, or delete the duplicate path after proving no live caller). Do not skip the other 16. Never POST Book Load. Never seat financial fixtures.
2. **#19611** landed a B8 slice (orphan refusal + instrument reference). That does **not** close C6. Remaining B8 (three dates, PENDING-until-approved, `docs.files` receipt) is **after** the 17 shrink (or an honest OUTBOX that remaining keys are owner-held with proof).
3. After C6 remainder: rest of **B8**, then **B5**. Do not start Wave 3 GO-22a/b until Wave 2 CC-1 rows close.

ACK `CC-1 | ACK | GO-23 | NOW=C6 fix mark-disbursed poster (#19616 named) then remaining 16 · NEVER board-only · NEVER POST | GO`
