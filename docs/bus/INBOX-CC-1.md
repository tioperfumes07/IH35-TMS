# INBOX-CC-1 · AUTO · 2026-09-02 17:56 CT

`git pull --ff-only origin/main`

Miles: `docs/bus/MILES-SPEC-DISPATCH-FINAL-2026-09-02.md`
FAST-MERGE. Never POST. Never idle. Never rebuild Gap 5 / E1 / Costs.

## NOW

```
CC-1 — WAVE 1. AUTO.

A1 interchange DATA + backend. Never put a non-owned trailer in mdata.units.
N1: do NOT rebuild Add Expense on the load. LoadDetailDrawer + ExpensesReverseSection already
link to /accounting/expenses/new?load_id=. Remaining is NULL expense numbers / backend completeness.
Then miles + settlement engines. Then GO-22a (LOAD counter, never MAX+1).
```

ACK `CC-1 | ACK | auto A1 DATA · N1 backend not the link · NEVER POST | GO`
