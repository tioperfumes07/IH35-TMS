# INBOX-CC-1 · GO-MASTER

`git pull --ff-only origin main`
Packets: `docs/lockdown/GO-MASTER-ALL-SEATS-2026-09-01.md` + `docs/lockdown/GO-11-USMCA-CLEAN-SLATE-2026-09-01.md`

## TOP
1. **GO-11 P0** — manifest then UUID delete. SAMPLE policies delete (owner sample-money order). Wait Cursor for `is_sample_data` column if not on main. TRANSP/TRK untouched.
2. **Expense seq** — already #19335 `formatLoadExpenseNumber` on main. Confirm; do not rebuild.
3. **Driver bill `B-`** — STOP. Owner ruling open. Do not drop `B-` this hour.
4. Settlement numbering → `lib.trace_counters` (P1). Voided CM/VC taken-check. Posted-doc lock. POSTING-CONTRACTS after Cascade finding.
Never `trigger_deploy`. ACK `CC-1 | ACK | GO-MASTER | GO-11 | GO`
