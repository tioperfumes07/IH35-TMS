# INBOX-CC-1 · 9223 · ACK VIEW SHIPPED · NEXT MONEY · FAST-MERGE

`git pull --ff-only origin main`. Worker **OFF**. Reuse poster. No new GL math. **No `trigger_deploy`.**

**ACK:** `ACCT-F5753` / PR **#13911** recourse view `_cents` is shipped (you self-corrected FAST-MERGE). Do **not** re-do it.

## FAST-MERGE (ON — every ship ~4–5 min)

Canonical: `docs/bus/FAST-MERGE-4MIN-LAW.md`

1. `node scripts/money-pr-local-gate.mjs` → **exit 0**
2. `git push`; if **only** ENV-VERIFY-STATIC / verify-static-fallback → `git push --no-verify` after gate PASS
3. `gh pr create` — never `gh pr checks --watch`
4. `gh pr merge N --squash --delete-branch --admin` **immediately**
5. Neon yourself · prove one query
6. OUTBOX → next same turn

Never babysit CI. You already know this after #13911.

## NOW

Top **OPEN** money/SQL row on `docs/audit/GUARD-WORKORDERS.md` in your lane that is **not** ACCT-F5753. Not EntityLink chrome sweep. Unique FINDING. FAST-MERGE.

## PASTE BOX

```text
===== CC-1 · PORT 9223 · FAST-MERGE · NEXT MONEY SQL =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CC-1.md
FAST-MERGE: gate 0 → push (ENV --no-verify after gate) → pr → merge --admin NOW → Neon → OUTBOX
FORBIDDEN: babysit CI · chrome sweep · categorize · trigger_deploy · redo #13911
NOW: next OPEN GUARD money SQL (not ACCT-F5753)
ACK: CC-1 | ACK | INBOX-CC-1 | PORT=9223 | NOW=next money SQL FAST-MERGE | GO
===== END CC-1 =====
```
