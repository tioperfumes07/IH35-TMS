# INBOX-CC-1 · CURSOR LEAD · 2026-09-02 18:56 CT

`git pull --ff-only origin/main`

FAST-MERGE (docs/bus/FAST-MERGE-4MIN-LAW.md). Never POST Book Load.

## NOW

```
CC-1 — HARD WAKE. N1 REMAINING.

NULL expense numbers / mint path only.
Do not rebuild Add Expense link.
Do not touch Gap 5 WORM (Cursor shipping).
Local gate exit 0 → push (--no-verify ONLY if ENV-VERIFY-STATIC) → gh pr create → gh api PUT .../merge squash. Never gh pr checks --watch.
```

ACK `CC-1 | ACK | N1 remaining · NEVER POST | GO`
