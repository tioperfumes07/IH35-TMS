# CC-1 INBOX — 2026-08-12 · UNIVERSAL VERTICAL (owner clarify)

**NO PAUSE** · entity=**USMCA-ONLY** · Built **~86%**

## ★ UNIVERSAL VERTICAL (answered=closed)

Sweeping a money column on the **urgent 10** is **not** “those 10 only.” Same PR **MUST** land the pattern on **every other module that owes that column** — every money leaf · drawer · wizard. Shared posters/FKs/helpers = **product-wide**. After P10-required money columns are green everywhere they apply, leftover = only columns that were **never required** on the first 10 (modules 11–28 unique Required).

Law: `docs/lockdown/VERTICAL-WIRING-LAW-2026-08-12.md` §3 · Desktop `HANDOFF-CC-1-NEW-SEAT-2026-08-12.md` · `CODER-PASTE-INSTRUCTIONS` · DoD A–E · VERIFY 1–8 · `FAST-MERGE-4MIN-LAW.md`

**NO HOLDS / NO AUDIT WAIT.** Cascade verifies AFTER merge.

## ☐ NOW (Wave C money — SERIAL · one PR · UNIVERSAL extend)

1. **`column=liability`** — start with factoring’s **10** Required leaves, then **extend same PR** to any other module that owes `liability` (every leaf that shares the wire).
   - Factoring leaves: `home.recourse_pipeline` · `submit.queue` · `batches.create` · `batches.detail` · `accounting.list` · `accounting.submit` · `accounting.detail` · `accounting.factor_recon` · `banking.entry` · `dispatch.queue`
   - Tag **every** module touched in `@matrix-built` · FAST-MERGE
2. Then **`column=gl_je`** — accounting `invoices.create` · `payments.receive` + **same-PR extend** to every other owed `gl_je` leaf system-wide.

## Truth (do not re-ask)

- **Pickers NOT universal:** `picker_law` Built ~**60%** · Live **0%**. **Wave D** — do **not** start until A–C green.
- Do **not** DROP honest chrome · do **not** invent FKs/reserves.

## FORBIDDEN

pause · wait Cascade · Wave D · TRANSP/QBO · parallel money PRs · module-deep · “10 only then later” · invent accounts

## OUTBOX

```
CC-1 | column=liability|gl_je | SHIPPED #N @ sha | Built=<all modules touched> | NEXT=…
```
