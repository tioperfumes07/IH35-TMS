# INBOX-CC-1 · 9223 · CERTIFY URGENT 6 MONEY NOW

`git pull --ff-only origin main`. FAST-MERGE 4–5 min. Never idle. Never `/tasks`. USMCA only. Reuse poster. No new GL math.

**Owner 2026-08-21 16:05 CT: CERTIFY the Urgent 6 modules RIGHT NOW.** Fleet/lists/WAVE2 are AFTER those six.

## PASTE BOX (copy everything in the fence)

```text
===== CC-1 / CLAUDE CODER 1 · PORT 9223 · CERTIFY U6 NOW =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CC-1.md
LAW: USMCA · reuse poster · FAST-MERGE 4MIN · never HOLD · ANSWERED=CLOSED
CHROME: 9223 · prove → CLOSE TAB · never leave /program/matrix open

FAST-MERGE 4–5 MIN (ON — do not deviate):
  LAW: docs/bus/FAST-MERGE-4MIN-LAW.md
  1) node scripts/money-pr-local-gate.mjs  MUST exit 0
  2) git push; if ONLY ENV verify-static AFTER gate PASS → git push --no-verify
  3) gh pr create — NEVER gh pr checks --watch
  4) gh pr merge N --squash --delete-branch --admin NOW
     if checkout-main fails: gh api -X PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash
  5) Neon yourself if money/migrations
  6) OUTBOX one line → next cell SAME TURN
  Forbidden: babysit CI · ask Jorge to merge · idle · standing-by

CERTIFY = Fully-Wired items 1–12 on EACH module (item 12 = Live Chrome last).
No 5th Box. No new Required.json leaves. Jorge is not the messenger.

U6 MODULES IN ORDER (finish one, then next — VERTICAL):
  accounting → banking → settlements → factoring → dispatch → vendors
COLUMNS: picker_law → trailer → reverse_link → customer → vendor → load → gl_je
  You own money termini: gl_je / invoice / expense / bill / factoring advance. Not chrome.

NOW (do not go to fleet):
  A) Author TONU/cancellation accessorial CoA wiring (claim 202612950000 + steps 4201/4205 already on main).
     Bound role → use it. Unbound → BOARD OPEN + keep B. Do not invent accounts.
  B) CREATE the next missing USMCA money event ON AN U6 MODULE (TEST DATA $1,200 labeled)
     via existing poster. Prove header+lines+balanced JE. Prefer dispatch/factoring/accounting.
  C) Re-walk unpaid money cells on those 6 modules. OUTBOX CERTIFY money when a module has
     no remaining unpaid money terminus (honest N/A named).
  D) THEN rest of urgent money: customers → drivers → fleet → lists.

Standing-by / WAVE complete = REJECTED.

IF chrome: OUTBOX-CC-3. Reverse: OUTBOX-CODEX. Live stamp: OUTBOX-CC-2.

OUTBOX: CC-1 | FAST-MERGE | MOD=<u6-module> | COL=<leaf:col> | JE=<uuid|N/A> | NEXT=<id> | GO
ACK: CC-1 | ACK | INBOX-CC-1 | PORT=9223 | NOW=CERTIFY U6 money accounting→…→vendors | NEVER HOLD | GO
===== END CC-1 =====
```
