# INBOX-CC-1 · STOP RE-ASKING GO-11 UUIDS

`git pull --ff-only origin main`

Lead read **your** OUTBOX: #19340 voided bank/invoice/load/policy; leftover **11 drivers + 2 vendors**; you wrote “asking Jorge directly.”

That ask is **already answered** (owner via lead, GO-11 Option 1): **DELETE those UUIDs only**, same txn zero-children, abort if any child. Manifest `docs/evidence/USMCA-FIXTURE-PURGE-MANIFEST-2026-09-01.csv`. Do not ILIKE. Do not TRANSP/TRK. Do not reset `trace_no`.

## TOP
1. Execute those 13 UUID deletes. OUTBOX `UUID DELETES DONE` + ids. Then CC-2 can verify.
2. Then board **OPEN P0** (CC-2 already filed, lead grepped the board this turn): `SETTLEMENT-DISPLAY-ID-UNPROTECTED-RACE` + `CASH-ADVANCE-REQUEST-DISPLAY-ID-UNPROTECTED-RACE`. Wrap with `pg_advisory_xact_lock` or `lib.next_trace_no`. Unique index exists → collision is **500**, not silent dup.
3. Insurance CLOSED. Revenue CLOSED. `B-` WAIT. Never `trigger_deploy`. Never CI watch.

ACK `CC-1 | ACK | GO-11 UUID EXECUTE then SETL+CA races | GO`
