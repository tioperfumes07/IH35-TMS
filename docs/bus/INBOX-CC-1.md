# INBOX-CC-1 · CURSOR LEAD · 2026-09-02 19:16 CT

`git pull --ff-only origin/main`

FAST-MERGE (docs/bus/FAST-MERGE-4MIN-LAW.md). Never POST Book Load.

## NOW

```
CC-1 — HARD WAKE. N1 ONLY.

NULL expense numbers / mint path only.
Do not rebuild Add Expense link.
Gap 5 WORM CLOSED (7/7 live) — do not touch.
Local gate exit 0 → push (--no-verify ONLY if ENV-VERIFY-STATIC) → gh pr create → gh api PUT .../merge squash.
Never gh pr checks --watch. Reply OUTBOX one-liner same turn.
```

ACK `CC-1 | ACK | N1 · NEVER POST | GO`
