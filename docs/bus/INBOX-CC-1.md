# INBOX-CC-1 · URGENT 6 THEN REST OF URGENT 16 · COUNTED

`git pull --ff-only origin main` then execute **this file**. FAST-MERGE (local gate PASS → push → PR → squash `--admin` ~4 min). Never idle. Never `/tasks`. Never Trucking/TRANSP. No TMS→QBO. USMCA only. Reuse poster.

**U6 leftover (live tip `c03b65c`):** dispatch **14/307** unpaid Live. accounting/banking/settlements/factoring/vendors Miss C **0/346, 0/89, 0/97, 0/94, 0/148**.

## YOUR PENDING (show this every OUTBOX)

**Urgent 6: 1 of 1 money cell + 5 of 5 scenario events = 6 of 6**

1/1 `load.banking:gl_je` — create a real USMCA load-tagged bank path + balanced JE (reuse poster) **or** OUTBOX `UNCHANGED blocker=load.banking:gl_je honest-zero` and still do the 5 events.
2–6/6 five events (do not skip because Miss C is 0): revrec · invoice+evidence · bank-path · real fuel · factoring advance.

**STOP leaving U6** until 6/6 are SHIPPED or named UNCHANGED with the exact `leaf:col`.

**Then rest of Urgent 16 money (only unpaid Live money cells):** fleet **48** → lists (0 money — skip lists money) → maintenance **41** money leaves → safety **7** money (`accidents.create:liability`, `fine_detail:gl_je`) → cash-flow **5**. Skip modules at Miss C 0 except the 5 events.

OUTBOX every ship: `CC-1 | U6  k/6 | NEXT=<leaf:col> | GO`

```text
CC-1 | ACK | INBOX-CC-1 | PORT=9223 | PENDING=6/6 U6 | NOW=load.banking:gl_je then 5 events | GO
```
