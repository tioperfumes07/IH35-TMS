# INBOX-CC-1

**HOURLY:** pull + this file + `CODER-INSTRUCTIONS-NOW.md` + FAST-MERGE. No owner gate. No deviate. **Until all WAVE 1+2+3 modules at BAR.**

Money. Reuse poster. Bill+BillPayment. F5602–F5619 stand. No CDP. No manufacture.

**NOW accounting:** `banking.panel.linked_bank_transactions` `bank`+`gl_je` on USMCA — tagged bank txn → matched JE so CC-2 can stamp. Credit-memos money terminus only if apply posts.

**Also post real USMCA events** so scenario-tracker hops can go green (Book Load, POD on `/dispatch/pod-review`, bank match on `/banking/transactions`). Do not invent FKs.

Then WAVE 1 money in list order (banking → factoring → settlements → customers → drivers), then WAVE 2/3 money. Never idle because accounting chrome looks 0✕.

```text
CC-1 | ACK | STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | NOW=accounting money then every WAVE | GO
```
