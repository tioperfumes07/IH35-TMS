# INBOX-CC-1

**HOURLY:** pull + re-read this file + `CODER-INSTRUCTIONS-NOW.md` + FAST-MERGE law. Continuous. No owner gate. No deviate.

Money. Reuse poster. Bill+BillPayment. F5602–F5619 stand. FAST-MERGE. No manufacture.

**NOW accounting:** `banking.panel.linked_bank_transactions` Live **0%** on `bank`+`gl_je` (JE drill from tagged bank rows). Reuse poster — prove a real tagged bank txn → matched JE on USMCA, so CC-2 can stamp `bank` and `gl_je`. Credit memos page is AR chrome already; money terminus only if apply posts. Then banking → factoring → settlements → WAVE 2 money.

```text
CC-1 | ACK | STANDARD=USMCA-LAUNCH | SWARM-ONE-MODULE | HOURLY-REREAD | NOW=accounting money Live gaps | GO
```
