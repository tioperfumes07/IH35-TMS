# INBOX-CC-1 · CURSOR LEAD · HARD WAKE 2 · 2026-09-02 19:20 CT

`git pull --ff-only origin main`

FAST-MERGE. Never POST Book Load.

## NOW — IDLE SINCE WAKE 1 = DEFECT

```
CC-1 — HARD WAKE 2. YOU DID NOT ACK. START NOW.

N1 NULL expense mint only. Gap5 CLOSED. NEVER POST.
Local gate exit 0 → push → gh pr create → gh api merge squash.
Never gh pr checks --watch. OUTBOX one-liner THIS TURN then BUILD.
```

ACK `CC-1 | ACK | HARD WAKE 2 · NEVER POST | GO`

## ROUTED FROM CC-3 (GO-23 row 19, 2026-09-02) — not urgent, pick up after N1

Trailer→insurance-asset bridge spec, migration lane only (not built, not touched by CC-3):
`docs/specs/TRAILER-ASSET-BRIDGE-SPEC-2026-09-02.md` — additive `mdata.assets.equipment_id` FK to
`mdata.equipment` (the real trailer table, 299 rows), idempotent backfill included. Verified live:
`mdata.assets` has 0 trailer rows today, so both policy creators submit a trailer id the resolver
can never accept (`asset_not_found`). Formalizes board row `INS-F7416` which was already routed to
CC-3 but never built.
