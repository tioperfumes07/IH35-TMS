# INBOX-CC-1 · 9223 · CERTIFY U6 MONEY · NO RENDER KICK

`git pull --ff-only origin main`. FAST-MERGE 4–5 min. USMCA. Reuse poster. No new GL math.

**LEAD 2026-08-21 17:39 CT:** API was 502 because **seats deployed after every merge**. Stop that. Certify money on U6. Cursor certifies Live Chrome.

## PASTE BOX

```text
===== CC-1 · PORT 9223 · U6 MONEY · NO DEPLOY =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CC-1.md
LAW: USMCA · reuse poster · FAST-MERGE 4MIN · never HOLD
FORBIDDEN: trigger_deploy · render deploys create · Dashboard Deploy · MCP deploy
           “kick API because healthz SHA lags” · buy Render storage (not the defect)

FAST-MERGE: gate exit 0 → push → gh pr create → squash merge. NO deploy step.

U6 VERTICAL (finish money on one, then next):
  accounting → banking → settlements → factoring → dispatch → vendors
THEN: customers → drivers → fleet → lists. NOT WAVE2.

NOW:
  1) Settlement close on USMCA TEST DATA if 0 closed (honest empty until you close one).
  2) Next missing U6 money event $1,200 labeled TEST via EXISTING poster. Header+lines+balanced JE.
  3) TONU/cancellation CoA: bound role → use it. Unbound → BOARD OPEN. Do not invent accounts.
  4) OUTBOX CERTIFY money per module when unpaid money terminus is 0 or N/A(named).

IF chrome freeze (native confirm): OUTBOX-CC-3. Reverse: OUTBOX-CODEX. Live stamp: OUTBOX-CC-2.

OUTBOX: CC-1 | FAST-MERGE | MOD=<u6> | COL=<leaf:col> | JE=<uuid|N/A> | NEXT=<id> | GO
ACK: CC-1 | ACK | INBOX-CC-1 | PORT=9223 | NOW=U6 money NO RENDER KICK | GO
===== END CC-1 =====
```
