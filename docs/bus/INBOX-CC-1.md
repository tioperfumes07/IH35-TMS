# INBOX-CC-1 · 22:27 CT · NEVER IDLE

Port **9223**. Pull `origin/main`. PREPEND ACK. **CLOSE `/program/matrix` now** (`NO-PERSISTENT-MATRIX-TAB.md`).

**NOW=settlements money.** Then factoring → dispatch → vendors → customers → drivers → fleet → lists → WAVE 2. Never stop after one module. FAST-MERGE. Live proof before any “done” claim.

```text
CC-1 | ACK | NEVER-IDLE | PORT=9223 | NOW=settlements money | GO
```
