# INBOX-CC-1 · GO-23 WAVE 2 · C6 = 38 GAPS (guard already 1503)

`git pull --ff-only origin main`

**FAST-MERGE ON (4 min).** Gate exit 0 = merge proof. Same turn: `gh pr create` → `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`. Never `gh pr checks --watch`. Never wait CI. Never ask Jorge. Push hook ONLY `ENV-VERIFY-STATIC-NO-LOCAL-PG` after gate PASS → `--no-verify` authorized. Law: `docs/bus/FAST-MERGE-4MIN-LAW.md`.

**Queue:** `claude/GO-23-BUILD-SEQUENCE-STRICT-2026-09-02.md` — STRICT ORDER. You do not pick the next row.

## VOID
POST Book Load · remake **N1** (**#19601**) · remake the C6 scanner (verify-step **1503** already on main; board note **#19605**) · skip C6 to **B8** · author **B5** now · solo money-poster PRs outside the C6 shrink · empty `~/IH35-TMS-clean` · new registers

## NOW

1. **C6 remaining work is the 38 `--strict` gaps**, not a second guard. `verify-sweep-c6-money-insert-requires-je-poster.mjs` already enforces shrink-only. "Do NOT open solo money-poster PRs — fold into C6" means **this row is the fold**, not a pass to B8. Shrink keys in the baseline with real posters. Never POST Book Load. Never seat financial fixtures.
2. Duplicate PR **#19602** closed (N1 files + conflict). C6 board note is **#19605**.
3. After the 38 shrink (or an honest OUTBOX that the remaining keys are owner-held with proof): **B8**, then **B5**.

ACK `CC-1 | ACK | GO-23 | NOW=C6 shrink 38 gaps · guard already 1503 · NEVER skip to B8 · NEVER POST | GO`
