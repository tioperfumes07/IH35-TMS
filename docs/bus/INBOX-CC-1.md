# INBOX-CC-1 · GO-23 WAVE 2 · C6 = 12 GAPS REMAINING

`git pull --ff-only origin main`

**FAST-MERGE ON (4 min).** Gate exit 0 = merge proof. Same turn: `gh pr create` → `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`. Never `gh pr checks --watch`. Never wait CI. Never ask Jorge. Push hook ONLY `ENV-VERIFY-STATIC-NO-LOCAL-PG` after gate PASS → `--no-verify` authorized. Law: `docs/bus/FAST-MERGE-4MIN-LAW.md`.

**Queue:** `claude/GO-23-BUILD-SEQUENCE-STRICT-2026-09-02.md` — STRICT ORDER. You do not pick the next row.

## VOID
POST Book Load · remake **N1** (**#19601**) · remake mark-disbursed (**#19618** / **#19619**) · remake the C6 scanner · skip remaining C6 to finish **B8** · author **B5** now · empty `~/IH35-TMS-clean` · new registers · Wave 3 GO-22 while C6 12 is open

## NOW

1. **C6 remaining work is the 12 `--strict` gaps.** Mark-disbursed is **done** (`#19618`). Next honest clusters (do not skip): (a) invoice/bill headers — `invoices.service`, `invoice-lines.routes`, `from-load`, `cancellation-tonu-invoice`, `packet-assemble`, `policy-create-atomic` (revrec latch — do not guess); (b) cash-advance booking `cash-advance-create.ts` + `lumper-cash-advance-split.ts`; (c) `escrow-separation`, `negative-settlement-liability`, `safety-v5.routes`, `bills-bulk.routes`. Shrink with real posters. Never POST Book Load. Never seat financial fixtures.
2. **#19611** B8 slice does **not** close C6. Rest of B8 is **after** the 12.
3. After C6 remainder: rest of **B8**, then **B5**. Do not start Wave 3 GO-22 while C6 is open.

ACK `CC-1 | ACK | GO-23 | NOW=C6 remaining 12 · #19618 done · NEVER skip to B8 · NEVER POST | GO`
