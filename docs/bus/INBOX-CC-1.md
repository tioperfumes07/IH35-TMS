# INBOX-CC-1 · GO-23 WAVE 2 · C6 = 22 GAPS REMAINING

`git pull --ff-only origin main`

**FAST-MERGE ON (4 min).** Gate exit 0 = merge proof. Same turn: `gh pr create` → `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`. Never `gh pr checks --watch`. Never wait CI. Never ask Jorge. Push hook ONLY `ENV-VERIFY-STATIC-NO-LOCAL-PG` after gate PASS → `--no-verify` authorized. Law: `docs/bus/FAST-MERGE-4MIN-LAW.md`.

**Queue:** `claude/GO-23-BUILD-SEQUENCE-STRICT-2026-09-02.md` — STRICT ORDER. You do not pick the next row.

## VOID
POST Book Load · remake **N1** (**#19601**) · remake the C6 scanner (verify-step **1503**; board **#19605**; shrink **#19608**) · skip remaining C6 to finish **B8** · author **B5** now · empty `~/IH35-TMS-clean` · new registers · Wave 3 GO-22 while C6 22 is open

## NOW

1. **C6 remaining work is the 22 `--strict` gaps**, not a second guard. `verify-sweep-c6-money-insert-requires-je-poster.mjs` already enforces shrink-only. Shrink keys in the baseline with real posters. Never POST Book Load. Never seat financial fixtures.
2. **#19611** landed a B8 slice (orphan refusal + instrument reference). That does **not** close C6. Remaining B8 (three dates, PENDING-until-approved, `docs.files` receipt) is **after** the 22 shrink (or an honest OUTBOX that remaining keys are owner-held with proof).
3. After C6 remainder: rest of **B8**, then **B5**. Do not start Wave 3 GO-22a/b until Wave 2 CC-1 rows close.

ACK `CC-1 | ACK | GO-23 | NOW=C6 shrink 22 remaining · #19608/#19611 landed · NEVER skip C6 · NEVER POST | GO`
