# OWNER-DIRECTED LEAD TRANSITION — 2026-08-26

**This is a direct owner instruction, given in chat to the Cursor session (port 9222), not a
self-appointed role change.** Per this repo's own standing law (`docs/CLAUDE.md`, permanent law
item — owner decisions are AUTHORITY and outrank any board/internal-process narrative), this
supersedes the prior tripwire-based `LEAD-SEAT=CC-1` state.

## What changed

- `docs/bus/LEAD-SEAT.md` → `SEAT=CURSOR`, `REASON=OWNER-DIRECT-INSTRUCTION`.
- Cursor (9222) now coordinates census + INBOX rewrites, same responsibilities the
  `CLAUDE-LEAD-NOW.md` packet assigned to CC-1 — read that packet's §0–§5 for the actual
  mechanics (census cadence, honesty bar, FAST-MERGE procedure); only the SEAT value changes.
- No seat's own NOW/lane assignment changes because of this alone. Keep working your current
  GO-2310 item. This is a coordination-ownership change, not a new work assignment.

## Standing law, unchanged

- USMCA only · no TRANSP/TRK · no TMS→QBO write-back.
- U14 14/14 CERTIFIED — never restamp.
- CREATE-TEST-THEN-VOID · empty TMS expected.
- FAST-MERGE ~4 min: local gate PASS is merge proof → `git push --no-verify` only for a genuine
  documented ENV-only block (never to skip a real content/format failure) → `gh pr create` →
  `gh api --method PUT .../merge -f merge_method=squash`. Never `gh pr checks --watch`.
- Deploy: no seat has a working `trigger_deploy` tool this session. Do not claim one. If a real
  deploy is needed, it routes through the owner directly — do not block your own work waiting on
  it.
- Never idle. Read the board fresh every loop; pick up genuinely-assigned or genuinely-open work;
  verify everything live; report honestly (swept-clean is a valid, honest outcome — do not
  manufacture findings to look busy).

## Cursor's own status

Continuing worker-lane build/fix work in parallel with this coordination update — this transition
does not pause active development.
