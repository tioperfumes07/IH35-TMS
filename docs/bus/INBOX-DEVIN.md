# INBOX-DEVIN

**AUDIT ONLY. DO NOT FIX. DO NOT CODE. DO NOT OPEN PRODUCT PRs.**  
You inspect CERTIFIED U14 modules and write **final findings + recommendations**. Cursor/CC own repairs.

**Intensity:** deepest audit — security, wiring, routes, double-sided linkage, reverse from every hub, **full connectivity extent**, **does it post to GL**. Gold model = **accident + insurance claim web**.

**Not** a 15th plan. **Not** OPEN prefixes. **Not** recertify. **Not** `trigger_deploy`. **Not** remake TESTs / WOs.

**REJECT HOLD.** `git pull --ff-only origin main` then this TOP.

**MANDATORY READ (before first click):**
1. `docs/audit/ACCIDENT-CLAIM-WEB-AUDIT-MODEL-2026-08-23.md` — H01–H22 + CONNECTIVITY-EXTENT + GL
2. `docs/audit/CASCADE-DEVIN-VERTICAL-CERTIFIED-AUDIT-INSTRUCTIONS-2026-08-23.md`
3. `docs/lockdown/FULLY-WIRED-COMPLETE-BAR-2026-08-13.md` (1–12)
4. `docs/specs/scoreboard/columns.shared.json` (C01–C24)
5. `docs/specs/scoreboard/modules/vendors.required.json` (then maintenance / safety / insurance)
6. `docs/audit/IH35-FULL-SYSTEM-AUDIT-SPEC.md` (A–E, V1–8, **§B9**)
7. `docs/specs/IH35_ARCHITECTURAL_DESIGN.md`

**Trackers:** `docs/audit/scenario-trackers/certified-u14/` — `U14-06` `U14-11` `U14-12` `U14-13` + worksheet per tab.

**Live SHA:** `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` → `version`

NOW (vertical: every tab + gold web, then next):
1. `/vendors` → insurer/shop/tow as vendors · reverse bills/WOs/claims
2. `/maintenance` → repair WO ↔ claim ↔ accident ↔ unit (do not remake WO TESTs)
3. `/safety` → **accident is the root** · police report · at-fault · reverse driver/unit/load
4. `/insurance` → **own the full claim web** (graph, deductible, driver-responsible, salary vs escrow, lawsuit **from claim UI only** — do not occupy `/legal` hub) · recovery capped · GL

OUTBOX each module: CONNECTIVITY-EXTENT + FINDINGS + RECOMMENDATIONS. Then next. Never idle.

FORBIDDEN: `/lists` `/legal` `/customers` `/drivers` `/fleet` · Cascade URLs · apps/ · recertify.

OUTBOX ACK: `Devin | ACK | AUDIT-ONLY | GOLD-WEB | NOW=/vendors | GO`
