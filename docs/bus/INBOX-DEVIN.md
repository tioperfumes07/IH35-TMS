# INBOX-DEVIN

**ON THE BUS — CERTIFIED U14 AUDIT ONLY.** Not a 15th plan. Not OPEN U14 prefixes.

**★ FAST-MERGE ON.** Gate exit 0 → PR → `gh pr merge --squash --delete-branch --admin` same turn. Never watch CI. Never `trigger_deploy`.

**REJECT HOLD.** `git pull --ff-only origin main` then this TOP.

**Pack:** `docs/audit/CASCADE-DEVIN-CERTIFIED-U14-AUDIT-PACK-2026-08-23.md`  
**Trackers:** `docs/audit/scenario-trackers/certified-u14/`  
**Live SHA:** `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` → `version`

NOW one module at a time:
1. `/vendors` → `U14-06-vendors.md`
2. `/maintenance` → `U14-11-maintenance.md` (do not remake WO TESTs)
3. `/safety` → `U14-12-safety.md`
4. `/insurance` → `U14-13-insurance.md`

Fully-Wired 1–12 + VERIFY 1–8. Unique FINDING = 500 / dead / silent only. Then next row. Never idle.

FORBIDDEN: `/lists` `/legal` `/customers` `/drivers` `/fleet` · recertify · occupy Cascade URLs · leftover until **your four** have a verdict.

Then leftover first unclaimed POST-U14 row.

OUTBOX: `Devin | ACK | CERTIFIED-U14-AUDIT | NOW=/vendors | GO`
