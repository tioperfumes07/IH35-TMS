# PASTE — ONE SURFACE PER SEAT · WIZARD HELD · 2026-09-03

`git pull --ff-only origin main`
Never POST Book Load. Click **No** on `cd`/`HEAD~1`. FAST-MERGE. Leave **#20064**.
**FIND IT, FILE IT, DO NOT FIX IT.** File outside your paths to the owning seat.

Book Load wizard + Dispatch screens: **HELD**. Do not edit BookLoad* until Jorge pastes redesign.

Full blocks: `docs/bus/SEAT-OWNERSHIP-AND-SURFACES-2026-09-03.md`

---

## CURSOR

```
CURSOR | NOW=SEAT OWNERSHIP LOCK only | wizard HELD
CODEOWNERS + verify-seat-surface-ownership (CLAIM EVEN step first, Rule 37).
Do not start Book Load STEP 1–4 until Jorge pastes redesign.
ACK: CURSOR | ACK | ownership lock · wizard HELD | GO
```

---

## CC-1

```
CC-1 | SURFACE: LOAD COSTS + MILEAGE | after lock on main
Design + code + money. catalogs.lane_mileage rebuild (bypass_rls). Seed CSV from Downloads.
Do not commit Book Load. File wizard bugs to Cursor.
ACK: CC-1 | ACK | Load Costs + mileage · NEVER POST | GO
```

---

## CC-2

```
CC-2 | SURFACE: BANKING | after lock on main
Categorization queue. banking.* only. Live USMCA: uncategorized+pending, not empty.
Owner click only. No fixtures. No Book Load / Load Costs.
ACK: CC-2 | ACK | banking queue · NEVER POST | GO
```

---

## CC-3

```
CC-3 | SURFACE: SAFETY + COMPLIANCE | after lock on main
NO MONEY. CDL/med intake, duplicate report, RLS aggregate verdict.
No Book Load / Load Costs / banking.
ACK: CC-3 | ACK | safety CDL intake · NEVER POST | GO
```

---

## CODEX

```
CODEX | SURFACE: MAINTENANCE | after lock on main
NO MONEY. If OOS columns already live, prove then fleet scope
(owner_company_id / currently_leased_to). Report sample rows. Never DELETE units.
ACK: CODEX | ACK | fleet scope + OOS prove · NEVER POST | GO
```

---

## CASCADE

```
CASCADE | SURFACE: LISTS + REPORTS | after lock on main
Build only lists/reports. Dispatch nav: map duplicates file:line — do not delete routes.
NEVER money. NEVER purge. NEVER POST.
ACK: CASCADE | ACK | lists/reports + nav map | GO
```
