# SEAT BROWSER + URL LOCK — no clash (owner 2026-08-26 19:27 CT)

Excel: `docs/lockdown/USMCA-LIVE-CHROME-CERTIFY-INVENTORY-2026-08-26.xlsx` tab `00-ALL-PENDING-CHECKLIST`.
**1870 rows · 1851 ☐ OPEN · 1268 Live Chrome leaves.** Filter Cluster=`LIVE CHROME leaf`, Module=YOUR exclusive module below.

**Hard-reload live SHA `2ef0af5`.** One Devin. Skip #15546. Nobody except Cursor lead `trigger_deploy`.

## Exclusive Chrome (do not open another seat’s URL)

| Seat | Browser / CDP | Exclusive routes (ONLY these) |
|------|----------------|-------------------------------|
| Cursor | **9222** | Lead. Overflow only: `/home` `/help` `/users` `/docs` `/inventory`. Never money URLs. |
| CC-1 | **9223** money clone | `/accounting` `/banking` `/settlements` `/factoring` |
| CC-2 | **9224** | `/cash-flow` `/reports` `/finance` `/tasks` |
| CC-3 | **9225** | `/lists` `/legal` `/compliance` `/program` `/system` `/eld` + **mdata deactivate class** (API, not stealing Devin Chrome) |
| Codex | **9226** | `/drivers` `/fleet` `/safety` `/fuel` `/maintenance` `/insurance` (walk; money FINDING → OUTBOX-CC-1) |
| Cascade | `~/Desktop/IH35-START-1-CASCADE.command` | `/dispatch` `/driver-hub` **only**. Not `/vendors`. Customer 500 already filed — do not remake. |
| Devin | assigned Devin browser (not 9222–9226) | `/vendors` **only**. **Not `/dispatch`.** Cascade owns dispatch. |

`/425c` nobody. `/customers` Chrome: **Cascade already filed** `CUSTOMER-INACTIVATE-500`. Fixer = **CC-3** (same class as vendor deactivate). Do not two seats click Inactivate.

If you need a URL you do not own: OUTBOX ping the owner. Do not steal the tab.

Comms: `INBOX-<SEAT>.md` TOP · your `OUTBOX-<SEAT>.md` · money FAIL first line `OUTBOX-CC-1.md` · board `docs/audit/GUARD-WORKORDERS.md`.
