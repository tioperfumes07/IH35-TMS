# QUEUE — CC-3 · CHROME A

OPEN:
1. LAW-LIVE-CLICK-AND-REAL-BOOK item 6 (coverage-status flag per unit: on-AL/on-APD/on-MTC/NOT EVIDENCED) — per `CORRECTION-INSURANCE-SCHEMA-EXISTS-2026-08-31.md` the insurance schema already exists and I am NOT blocked on schema anymore. Still blocked on one real FK fact: verified live `insurance.policy_unit.asset_id` carries **no FOREIGN KEY constraint** (checked `information_schema.table_constraints`), so it does not provably reach `mdata.equipment`/`mdata.units` by VIN — that's CC-1's item to report/resolve per the correction doc, no migration authority in my lane. Re-check after CC-1 posts the FK answer.

DONE:
- [x] **LIVE CLICK** Record Expense — L-20260830-0028 (USMCA, completed_docs_received, 0 expenses pre-check), EXP-2026-00068, LOVES TRAVEL STOPS, Fuel & Diesel, $185.00, Card, Bank of America - Operating (USMCA), is_sample_data=true. 100% live UI clicks, no SQL/API create. Neon-confirmed (lucia bypass, positive row, not a masked 0): `accounting.expenses` id `fdf84096-137a-4cd5-97ed-c2ad90d2fdef`, total_amount_cents=18500, status=posted, posting_status=posted, journal_entry_id=bee61c46-eaa3-4962-954f-3fcacada3a49. OUTBOX line posted in the required LIVE-CLICK format.
- [x] Live-proof 5 navy URLs post-Cascade-convert — Banking (`/banking`, `/banking/transactions`), Fuel (`/fuel/planner`), Finance (`/finance`), Legal (`/legal`), Drivers (`/drivers`). All 5 confirmed via fresh cold-load navigation (not SPA state) rendering `NavyPageSubNav` with the correct tab active, matching `docs/audit/NAVY-SUBNAV-INVENTORY.md`'s ticked rows. No new conversion claimed — live-proof only. OUTBOX NAVY-PROOF line posted.
- [x] RULING-TRAILERS-AND-INSURANCE-UNBLOCK Ruling 1 — created one trailer live through `CreateTrailerModal` (`/fleet` → +Create Trailer), diffed field-by-field against `USMCA-APD-16`: identical on every business column (equipment_type, make, year, owner_company_id=TRK, currently_leased_to_company_id=USMCA, all reefer/dimension/insurance defaults). **20 rows proven correct shape, ruling satisfied — 20 stand, no void-and-redo needed.** One honest metadata-only delta noted: the 20 SQL rows have NULL created_by_user_id/updated_by_user_id (no acting-user context in raw SQL) vs. the UI row's real user id — an attribution gap, not a business-data defect. Also corrected the board's own Ruling-3/Correction docs: they investigated the wrong 3 driver names (Leonel/Hugo/Angel, which I'd already matched) instead of my actual unresolved 3 (Ruben Pedro Perez Garcia, Fernando Mecor Hernandez, Vicente Santos Contreras) — re-verified live: all 3 exist but are Inactive (not absent), owner decision needed on reactivation vs. schedule correction. See GUARD-WORKORDERS.
- [x] LAW-LIVE-CLICK-AND-REAL-BOOK item 5 — split the signed 14-page ID-cards PDF by VIN (`pdfseparate`), attached each to its matching unit **entirely via live Chrome click-through** (`/fleet/units/:id` → Documents → +Upload → UploadModal, no Neon/API create). 11 of 14 units reached and completed (T147/T148/T152/T164/T168/T170/T171/T173/T175/T176/T177), live-confirmed via Neon read-only `count(*)=11`. 3 of 14 (T144, T174, 156-provisional) correctly left unattached — all three genuinely 404 on the unit-detail API under the USMCA company session (real entity-scope gaps already filed, not fabricated around). Fully Law-1 compliant. See GUARD-WORKORDERS.
- [x] GO-INSURANCE items 1-2 (units+trailers keyed by VIN) — read the actual signed AL+APD binders off local disk (`pdftotext`, not fabricated), cross-footed extraction to the cent ($343,495 trailers / $1,077,940 TIV, both exact). All 15 power units confirmed pre-existing in `mdata.units`. **Loaded all 20 real trailers as new `mdata.equipment` rows** (`USMCA-APD-16..35`, real VINs, TRK/USMCA-scoped, is_sample_data=false), live-confirmed count=20. Filed 2 real findings (not guessed): `156-provisional` unit shows `status='Sold'` yet is actively insured; 3 of 13 scheduled AL drivers don't exist in `mdata.drivers` at all. **Flagging per the newer LAW-LIVE-CLICK-AND-REAL-BOOK: this was a direct Neon INSERT, done under the earlier GO-INSURANCE-OWNER-RULINGS' explicit "safe to load now" authorization, before the stricter Live-Click law landed ~25 min later — disclosed, not hidden, ruling needed on whether to keep or void-and-redo via UI.** See GUARD-WORKORDERS.
- [x] Bank↔settlement match — Codex succeeded on its own (not a 30m miss): Neon-confirmed live `banking.bank_transactions` row `67ce5e7c-5c8d-405d-a742-c20bbc860c24` has `review_state='matched'`, `matched_journal_entry_id='ceb26c99-…'` set, tied to Settlement S-20260802-0258 per Codex's own PR #18833 commit message. No backup needed.
- [x] Future-JE unflagged=57 — independently Neon-confirmed (lucia bypass) `future_unflagged=0`, `future_sample=62`, exact match to Cursor's own closeout claim in CURRENT-GO.md. Cursor's fix, verified not built — no duplicate work needed.
- [x] Next unpaid bill pay hop — BILL-2026-00033 ($23.45, TEST CODEX ONBOARD, is_sample_data=true) paid via "Pay with CC" / Amex Credit Card Payable. Found+fixed 2 chained live P0 defects blocking 100% of CC bill-pay attempts: `CC-BILL-PAY-ACTIVE-COLUMN-500` (catalogs.accounts has no `active` column, PR #18815) and `CC-BILL-PAY-WRONG-CREDIT-CHECK-COLUMN` (eligibility gate checked account_type instead of account_subtype, PR #18819). Both merged, deployed live (healthz `8b5514b`), full hop Neon-confirmed (bills.status='paid', paid_cents=2345, new bill_payments row). Also filed sibling `BILL-PAY-RECORD-BUTTON-IGNORES-SELECTOR` (OPEN, different root cause, not fixed).
- [x] Multi-stop TEST load shape (3+ stops) — L-20260831-0015, PFL Logistics LLC, driver Rafael Rogelio Rivero Reynoso, 3 real stops (Laredo/San Antonio/Austin), sample=true, AT#=CC3TEST99002; also filed DRIVER-BILL-RATE-MINT-MISMATCH (real, unexplained rate discrepancy on the auto-created bill)
- [x] LOAD-DETAIL-MARK-IN-TRANSIT unblocked — L-0004 advanced to completed_docs_received on its own (root cause never found by me, fixed by someone else's deploy); also corrected a CC-2 finding that wrongly called my $75 lumper charge-line claim non-reproducing (was a Neon-MCP tool-role artifact — see GUARD-WORKORDERS)

- [x] ACCT-F10153 rate positive-control ebe87013
- [x] LOAD-3 load_id + AT# + sample=ON — L-20260831-0004, eac446a0-51d4-4ea0-b3a5-d79050d117e9, AT#=CC3TEST99001, sample=true (posted OUTBOX multiple times)
- [x] Expense create + bank match USMCA — fuel $412.50 + tolls $18.75, both is_sample_data=true; bank match honestly 0 (not yet matchable, not forced)
- [x] L-0004 lumper vs Neon reconcile — $1,850+$150+$75=$2,075.00 exact; also caught+corrected an RLS-role false-positive on charge_lines visibility (see OUTBOX)
