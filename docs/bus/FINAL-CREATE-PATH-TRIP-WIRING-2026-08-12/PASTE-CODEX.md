# PASTE → CODEX · CREATE-PATH TRIP WIRING (FE SUPPORT)

Canonical: `FINAL-CREATE-PATH-TRIP-WIRING-2026-08-12/00-README.md`  
Quality: Claude.docx — chrome is support, not the product bar while create-path FKs are open.

## FORBIDDEN (primary work)
- Extending matrix Required inventory as “done”
- Wave D system-wide chrome sweeps as primary while ranks 3–5 open
- Touching Combobox/hotfiles Cursor is using for WO trailer PR (serialize)

## NOW (non-stop)
1. Pull `origin/main` (includes #6296/#6300).
2. **Support create-path FE only:** if WO/expense/claim create has box-in-box / raw date / raw money / Combobox dismiss bugs **on those surfaces**, fix with one class + one guard.
3. Otherwise continue **non-hotfile** Wave A/B FE column gaps (driver/unit/trailer/load pickers) that unblock trip stamps — still real wiring, not Required JSON.
4. FAST-MERGE · OUTBOX one line · NEXT.

REPO: `/private/tmp/IH35-devin-b`  
OUTBOX: `Codex | surface=<path> | class=<CLS-…>|column=<id> | SHIPPED #N | NEXT=…`
