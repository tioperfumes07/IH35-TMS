# PASTE → CC-1 · CREATE-PATH TRIP WIRING (MONEY / SCHEMA)

Canonical: `FINAL-CREATE-PATH-TRIP-WIRING-2026-08-12/00-README.md`  
Quality: Desktop `Claude.docx` — never guess; Neon proof; reuse poster; no new GL math; no invent load FKs on import cohort.

## FORBIDDEN
- Matrix Required / scoreboard density PRs
- Backfilling historical QBO fuel/expense `load_id`
- Creating/reclassifying reserve accounts
- Parallel money PRs (Rule 27 — one serial)

## Context (Cursor already shipped FE stamps)
- #6296 expense suggest-load · #6300 ClaimCreate suggest-load · accident CREATE params fixed
- Your lane owns **schema + posting class + office create** so those stamps have somewhere real to land on fuel/expense/accident trailer

## NOW (non-stop) — ranked serial
1. **`fuel.trailer_id`** — migration (Cursor HH band N/A — you own money migrations) + import/create paths + entity scope
2. **Fuel JE / poster class by unit/load** (going-forward only; reuse existing poster)
3. **Fuel office POST create** (today GET+PATCH assign only — verify live before inventing)
4. **`accounting.expenses.trailer_id`** (or canonical FK) + API create accepts it
5. **`safety.accident_reports.trailer_id`** + create/patch accept
6. **Unify active-load resolver** (suggest-load trailer arg must be load-bearing, not dead)

FAST-MERGE on green gate · Neon apply yourself · OUTBOX one line · NEXT same turn.

REPO: `/private/tmp/IH35-devin-b`  
OUTBOX: `CC-1 | create-path=<id> | SHIPPED #N | Neon=… | NEXT=…`
