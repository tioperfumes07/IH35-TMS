# NOW-ONE-SOURCE · 2026-09-02 16:23 CT

**STOP PURGING. BUILD THE ENGINES.** Owner is booking loads now.
Canonical: `docs/bus/OWNER-ORDER-STOP-PURGE-BUILD-ENGINES-2026-09-02.md` · miles: `docs/bus/MILES-SPEC-DISPATCH-FINAL-2026-09-02.md`.
USMCA only. Never POST Book Load. Never seat fixtures. FAST-MERGE: `docs/bus/FAST-MERGE-4MIN-LAW.md`.

| Seat | NOW |
|------|-----|
| **CC-1** | **Miles + settlement engines.** Three stored miles. Two pay lines. `rate_empty` per driver. Deadhead COMPUTE all-entities, never lane empty. Then Wave 1 A1 + N1. Then GO-22a. Stay on `driver_finance` + dispatch miles. Purge leftover = background only. Bank 395 stay uncategorized. |
| **CC-3** | Wave 1 **A2** (mounted wizard) then **GO-24**. No lane-empty autofill. C1 edit Customer UUID on 13508. |
| **CC-2** | **J1 to zero.** Gate 1.3 CLOSED. Stop filling `miles_deadhead` from catalog empty. Chrome-prove blank-or-computed. Never POST. |
| **Codex** | Push Costs tab. Then A3/B12. Do not edit program-scoreboard.json. |
| **Cascade** | **STOP purge.** Unique FINDING only. Never categorize bank. Never build. |
| **Cursor** | Lead. Deploy every 5–10 merges (108 since 17:00Z — batch now). **Stay off `driver_finance` and dispatch miles.** Build: (1) proforma at first pickup only — `book-load.service.ts` ~1938, no other dispatch edits; (2) company settlement table, 8 sections, Settlement 5753 / P&L **2415.11**; (3) GO-06 shared number field remaining create screens; (4) document memo carries **LOAD number** in its own field, never the invoice number (004–013 are untraceable). |
| **Devin-A** | RETIRED. Stop. |
