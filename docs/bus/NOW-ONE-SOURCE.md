# NOW-ONE-SOURCE · AUTO · 2026-09-02 18:13 CT

**IDLE IS A DEFECT.** Owner: auto mode, continuous, deploy. Jorge is not the messenger.
Queue = `claude/GO-23-BUILD-SEQUENCE-STRICT-2026-09-02.md`. USMCA only. Never POST Book Load.

**LIVE:** API deadhead `#19853` SHA `c705b52951` deploy `dep-dacanm7avr4c73fpobmg`. FE QBO expected-adjustments `#19856` SHA `3126c60e7e`. Tip `#19858` `e9b164ecf6`.
**CLOSED (do not rebuild):** E1, GO-06, Costs `#19845`, Gap 5, A1 SCREEN, deadhead `#19853`, QBO expected-adjustments `#19856`. Stale PR `#19854` closed.

| Seat | NOW — work this minute |
|------|------------------------|
| **CC-1** | **N1 remaining** — NULL expense numbers / backend completeness. Add Expense link is already live. Never POST. |
| **CC-2** | **Chrome NOW:** chain-deadhead on live API (`GET /api/v1/dispatch/deadhead-from-chain`). Also Chrome E1, Load costs, GO-06, expected-adjustments. **THEN J1 remainder to zero.** Never POST. |
| **CC-3** | **Wave 4 C1** remaining raw UUIDs. Do not redo `#19812`. Never POST. |
| **Codex** | **A3/B12** Chrome proof `#19571`. Then native-date + trapping-picker guards. Drop Costs — `#19845` already merged. Never POST. |
| **Cascade** | OUTBOX still empty = idle defect. Unique FINDING with file:line (GO-23 row 26). Never build. Never POST. |
| **Cursor** | Lead. API deadhead live — do not pile `trigger_deploy`. Stay off N1 / C1 / J1. |
| **Devin-A** | RETIRED. |
