# NOW-ONE-SOURCE · TURBO · 2026-09-02 16:27 CT

**IDLE IS A DEFECT.** Owner is booking now. Build engines. Build miles. Fix wizard comboboxes / calendars / dates.
`docs/bus/TURBO-ALL-SEATS-2026-09-02.md`

| Seat | NOW — work this minute |
|------|------------------------|
| **CC-1** | **Miles + settlement engines.** Three stored miles. Two pay lines. `rate_empty` per driver. Deadhead COMPUTE all entities, never lane empty. Parallel: A1 data + N1. FAST-MERGE. |
| **CC-2** | **B9 #19808 + J1 wizard #19810 CLOSED.** NOW: stop lane-empty deadhead (`BookLoadModalV4.tsx` ~640), then QBO accessorial amount + weight, then J1 remainder. ParityDrawer 15px → 11px header (locked baseline). Never POST. |
| **CC-3** | **A2 + GO-24 AND** mounted-wizard C1 UUID, every trapping picker on Book Load, every calendar/date on stops. No lane-empty autofill. FAST-MERGE. |
| **Codex** | Push Costs tab **now**. Then A3/B12. Then help wizard date/combobox guards. Do not edit program-scoreboard.json. |
| **Cascade** | **NOT IDLE.** Live Chrome Book Load + dispatch calendars. Unique FINDING per trapping combobox, native date, dead calendar, with **file:line**. Spreadsheet vs GO-23 unique FINDING. Never build money. Never purge. Never POST. OUTBOX one FINDING then the next. |
| **Cursor** | Lead rewake. Deploy batch. Proforma at pickup. GO-06 empty-box. Stay off `driver_finance` / dispatch miles. |
| **Devin-A** | RETIRED. |
