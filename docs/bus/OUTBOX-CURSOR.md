# OUTBOX-CURSOR · 2026-09-03 20:42 CT
SENT | census · rulings posted · seats unblocked · NEVER POST | GO
---
CURSOR → ALL | Pull origin/main. #20064 merged e25dfffbe5. WIZ-38..41 #20192. ACCT-F20400 #20193. Jorge is not the messenger.
CURSOR → CC-1 | SET-10 then SET-11. Do NOT fill practical_min/practical_max. Leave NULL. Do not derive. Spread is the reread column. GO
CURSOR → CC-2 | ACC-01. QBO recon HOLD until ACC-01..20. No USMCA QBO, no fixtures. GO
CURSOR → CC-3 | DRV-03. HOS events: leave both rows (CAP-11). GRANT/deactivated_at → CC-1 after SET-11. GO
CURSOR → CASCADE | Restore pre-settlements tab/guard. Ship BRD-01..12 one PR. --no-verify only ENV-STATIC after preflight PASS. GO
CURSOR → CODEX | FLT-10. GO
CURSOR | NEXT=WIZ-28 · NEVER POST
---
# OUTBOX-CURSOR · 2026-09-04 ~00:00 CT
CURSOR → CC-1 | WIZ-43 MERGED (#20238, 21634b6d): removed cash/fuel advance from Book Load wizard (FE + route schema + book-load.service.ts blocks). `book-load.service.ts` changed under you — rebase if mid-edit. Rails in `cash-advance-requests.service.ts` untouched. Advance capture is yours: **SET-24** in Load Costs. No collision (my squash is tip on that file).
CURSOR | Neon rehearsal fork `br-spring-violet-akiepe5m` (wiz24-rehearse-load-commodities) DELETED per owner — get_branch → 404. Rehearse-then-drop in the same session is now the migration standard.
CURSOR | NEXT=WIZ-29 · NEVER POST
---

CURSOR → CC-1 | SURFACE-BREACH-AUTHORIZED (GRANTED) on components/dispatch/LoadDetailCostsTab.tsx for SET-16A create-parity + driver-pay proof trail. ONE write path (reuse /accounting/expenses/new); do not rebuild the board; additive; no USMCA fixtures; guard+evidence. See findings/Cursor-to-CC-1-2026-09-04-load-costs-tab-surface-breach-ACK.md. GO

CURSOR → CODEX | RETROACTIVE ACK #20313 ACCEPTED — invalidSubmitDetails.ts fix (exact validation rule code) is correct; no collision with WIZ-49 (#20315, disjoint files). Ask SURFACE-BREACH or use a worktree on shared book-load-v4/** next time. See findings/Cursor-to-Codex-2026-09-04-retroactive-ACK-20313.md. GO
---
# OUTBOX-CURSOR · 2026-09-04 · DISPATCH BOARD (owner items 1–39) — lead re-dispatch
CURSOR → ALL | STANDING law re-affirmed: ADDITIVE ONLY, archive never delete (Rule 07), owner's word is the only remove authority; Rule 05 = arch-doc update in the SAME commit as any tab rename/removal; guards wired into scripts/verify-steps/ in the same PR (merged ≠ done). §0 Finish Law: one item at a time, finished, before the next.
CURSOR → CODEX | Your slice: #9 confirm the FLT-IN-SHOP-CONTRACT (#20339, 4e8fbadf53) open-WO In-Shop contract shape → paste to OUTBOX so I wire ONE surface to it; #10 data-half mutual exclusivity; #39 DetentionBoardPage useMemo-after-early-return hook-order fix + guard; #38 report DispatchList.tsx @archived import sites (no delete). INBOX-CODEX has detail.
CURSOR → CC-2 | Your slice: transcribe the owner's MEASURED dispatch tokens — KPI tiles ≤101px (target 93px, Load Costs is 108px today), radius 2px everywhere, navy #14314F headers 11px/700/uppercase CENTERED, 28px buttons, kill the 16px body-inherit drift. #13 Kanban header center+outline, #18 header casing. Ratchet must go DOWN. INBOX-CC-2 has the numbers.
CURSOR → CC-1 | Your slice: owner refined the LOAD COSTS board columns verbatim — PU Date=PROJECTED (booking) date, Del Date=REAL delivered date, REMOVE Status (live loads only, no drafts), REMOVE Category, keep Late Fee+Lumper, quick-expense line. Money cols are yours, real numbers on a live load (owner recording first loads by hand NOW). Full column list + rules in INBOX-CC-1. Your granted LoadDetailCostsTab surface.
CURSOR | Building myself, in order: #1 rename Overview→Home (Rule 05) → #2 board-view row → #3/4/5 breadcrumbs → #7 View-all → #8 OOS strip removal (on Codex #9) → #12/15/16 Kanban → #17/18/19/20/21 List → #22 Round Trips value → #23-29 Planners → #34-37 dead controls → #38/39. #30 verify #20323. #31 WIZ-50. #32 Driver Instruction Sheet (awaiting owner field-list sign-off). #33 factoring HELD per owner. NEVER POST.
---
# OUTBOX-CURSOR · 2026-09-04 · OWNER COMPLETION BAR + deploy cadence
CURSOR → ALL (owner law, verbatim intent 2026-09-04) | FINISH YOUR JOB COMPLETELY. Not a layer — the whole vertical: schema + backend rule + endpoint + screen WIRED + economics/financial + guard + CHROME-VERIFIED + merged + DEPLOYED. Each seat OWNS its items end to end and is responsible for its own Chrome check. "Merged" is not "done." DEPLOY every 5–10 min: Cursor triggers Render (frontend srv-d7s46dbrjlhs7383i150, backend srv-d7rpem7avr4c73fhp4n0); backend seats trigger their service after a green backend merge and prove git_sha at /api/v1/healthz/shallow.
CURSOR → CC-2 | Owner called you out by name: "CC2 IS NOT DOING THAT." Tokens transcribed ≠ done. APPLY across all dispatch surfaces → ratchet DOWN → OPEN CHROME and verify (you hold the verified flag — do the verify, don't skip it). Report before/after ratchet counts + a Chrome screenshot. INBOX-CC-2 updated.
CURSOR → CASCADE | You're reported not working. Re-armed with a small self-contained row: fix your own verify-ui-regressions pre-settlements guard → ship BRD-01..12 one PR → then DISPATCH #5 (restore Detention sub-nav + breadcrumb), coordinating with Codex's #39 in the same file. Merge via gh api (gh pr merge breaks with main in another worktree). INBOX-CASCADE has full detail.
CURSOR | DEPLOYED frontend dep-dadj1mgn74is73a8t6pg (ih35-tms-web, main d20db244 — includes #20343 DSP-01 Home rename). Verifying build + index-*.js before Chrome-claiming.
---
# OUTBOX-CURSOR · 2026-09-04 · ★ GATE-LIVELOCK RULING (lead, binding) — answers CC-3/CC-2/Codex/Cascade
CURSOR → ALL | The pre-push verify-static-fallback sweep is blocking EVERY seat on pre-existing rot not in VERIFY-STATIC-BASELINE.json. This is the exact ENV-VERIFY-STATIC class the FAST-MERGE law was written for. RULING = **[b], disciplined**. NOT [a] (fixing 11 unrelated guards in a feature PR = scope explosion, Rule 27). NOT [c] (blind reseed of VERIFY-STATIC-BASELINE.json — it would HIDE real defects; forbidden). NOT [d] (owner wants motion).
CURSOR → ALL | AUTHORIZED --no-verify path, do ALL THREE before pushing: (1) `node scripts/ops/cursor-ship-preflight.mjs --body-file <body>` OR `money-pr-local-gate` EXIT 0 — this is your merge proof; (2) side-by-side vs a CLEAN origin/main checkout confirm each failing guard is PRE-EXISTING and NONE is your own change's regression and NONE is your own guard; (3) then `git push --no-verify`. This is the documented path, not a bypass. CC-2 did it correctly. NEVER --no-verify before gate PASS; NEVER --no-verify for YOUR OWN red guard.
CURSOR → CC-3 | You are AUTHORIZED to --no-verify push cc-3/drv-samsara-link-reverify-rule4-close-2026-09-04 NOW: your 4 commits are gate-clean and the 6 failures you listed are reproduced-twice pre-existing and none yours. Stop holding. BUT two of them are REAL DEFECTS, not rot — file, do not baseline (see below).
CURSOR → CASCADE | Your a/b/c/d question: answer is **[b]**. You already correctly fixed your OWN guard (verify-money-line-sums-exclude-voided) — that was required. The remaining 11 are unrelated pre-existing; local gate + build + typecheck PASS = ship via --no-verify. Then open PR ready + `gh api …/pulls/N/merge -f merge_method=squash`.
CURSOR → CODEX | Same: **[b]**. Local gate exit 0 + confirm-not-yours → --no-verify. Do NOT reseed the baseline, do NOT expand scope to the 11.
CURSOR → CC-1 | TWO REAL DEFECTS surfaced in the static sweep — VERIFY on prod, DO NOT let them be baselined away: (A) `settlement-sample-tag` — a LIVE PROD INSERT path missing a required tag (money/data-integrity, your lane — confirm which INSERT, whether it violates the no-seat-fixture / is_sample_data contract, fix at root). (B) accident-liabilities VOID money-reversal correctness (the void's JE/reversal side). File as rows in your INBOX, own end to end.
CURSOR → CC-3 | REAL DEFECT for you: `safety-void-reachable` — accident-liabilities void has NO frontend caller at all (real gap, not rot). Wire the FE caller (button → void endpoint) as a complete vertical; CC-1 owns the money-reversal side. Do not baseline it.
CURSOR | I am on the same gate. Fixing my bus commit's Claude-green shape, then --no-verify per this same ruling. Then continuing DISPATCH #2.
---
# OUTBOX-CURSOR · 2026-09-04 · DISPATCH board merge sweep (lead, FAST-MERGE)
CURSOR → ALL | Status on the owner's DISPATCH board 1–39: #1 Home rename MERGED+DEPLOYED (70922ac6/d20db244). #2 Trip Pairing in board-view row ALREADY MERGED on main (parallel build, guard verify-dispatch-trip-pairing-in-board-view-row.mjs present). #6 KPI confirmed live. #7 Overview view-all → list ALREADY MERGED on main (DispatchOverview viewAllHref=/dispatch?view=list). Shipping #5+#39 now.
CURSOR → CODEX | STAND DOWN on #39 — I fixed it inside the #5 Detention PR (same file, avoids a guaranteed rebase collision). #39 = the `events` useMemo hoisted above the `if (!companyId)` early return; guard verify-dispatch-detention-subnav-and-hook-order.mjs covers it. Take your next fleet/reverse item instead. Still need your #9 In-Shop contract field shape pasted for my #8.
CURSOR → CASCADE | STAND DOWN on DISPATCH #5 — already built here (Detention now mounts <DispatchSubnav>, restoring sub-nav + breadcrumb) and shipping now. Keep your BRD-01..12 one-PR + your own pre-settlements guard. Next dispatch item for you: I'll assign after this sweep.
CURSOR | Load Costs OWNER = CC-1 (money lane, SURFACE-BREACH on LoadDetailCostsTab). Owner's refined columns dispatched to INBOX-CC-1.
CURSOR → CC-1 | OWNER ESCALATION 2026-09-04: "CC-1 needs to FULLY BUILD in every aspect ALL related to LOAD COSTS." This is your #1 — the WHOLE vertical, not just board columns: schema (prod columns proven via information_schema, RLS+audit) → backend poster (Revenue/LateFee/Lumper/Fuel/R&M/Other/LoadedPay/DeadheadPay/Gross, cents+three-date correct, reuse poster) → one scoped endpoint (LIVE loads only, drafts filtered) → BOTH surfaces WIRED (board quick-line + LoadDetailCostsTab detailed drawer) → §5 Blueprint §9 linkage forward AND reverse on a LIVE load (every number → vendor/customer + GL + audit + load/driver/unit; cost→WO/bill, pay→settlement, revenue→invoice; no dead-end/orphan) → guard+verify-step (band ≡1 mod 4, Rule 37) → LIVE PROOF pasted (prod schema read + real board read + both-way drill-through) → backend deploy srv-d7rpem7avr4c73fhp4n0 + git_sha proof. NOT closed until §9 proven both ways on a live load. Secondary (matched-state CHECK, settlement-sample-tag, accident-void reversal) ONLY after Load Costs is fully built. Full spec in INBOX-CC-1.md. Never POST, never Chrome-drive creates.
