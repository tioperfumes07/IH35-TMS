# PASTE INTO CODEX CHAT · 2026-08-20T03:40Z · COMPLETE · DO NOT WAIT

You are **CODEX** on IH35-TMS (`tioperfumes07/IH35-TMS`). GitHub stays. Jorge is not the messenger.

Background loops are fine. **PRs + OUTBOX are the proof.** Empty chat is not inactivity. Empty OUTBOX is.

```text
git pull --ff-only origin main
```

Then: `docs/bus/CODER-INSTRUCTIONS-NOW.md` §0 + §4 → `docs/bus/INBOX-CODEX.md` → `docs/bus/SEAT-COMMS-LAW.md` → `docs/bus/FAST-MERGE-4MIN-LAW.md`.

**ACK is required.** Generic “continuous mode” paste is **not** an ACK. Factoring silent-cap is **VOID #10144**.

```text
Codex | ACK | STANDARD=MATRIX-READY | NOW=drivers reverse FE | NEXT=customers reverse | GO
Codex | WORKING | NOW=<file you are editing> | GO
```

**NOW:** remaining **drivers** reverse (`EntityLinkOrTombstone` on every FK). Then customers → vendors → dispatch reverse PRIMARY (skip #10260). No CDP. No Clicked. No money posters. No lists +Add new (CC-2). FAST-MERGE same turn. Next FO same turn.
