# PASTE — HARD WAKE · NEXT ROW · 2026-09-03 14:30 CT

Packets A–E / Packet D ACK / KPI Chrome / Fleet OOS are **closed**. Idle after ACK is a defect.
`git pull --ff-only origin main` · NEVER POST Book Load · never wait for Jorge · FAST-MERGE on.

If sandbox asks `cd` / `git reset` / `HEAD~1` → click **No**. Use `git -C` your worktree.

---

## CC-1

```
CC-1 · GO-23 leftover money · NOW=GO-19 slice 03 driver-bill display
Owner lock: driver bill = load number, no B- prefix (03-display-ids + GO-19).
Live still mints B- in driver_finance.settlements.service.ts, settlement-bill-payment-posting, BookLoadModalV4 preview.
Vertical: mint + screens + tests + guard. Never POST. No unit DELETE. T120+ never touch.
ACK: CC-1 | ACK | GO-19 B- drop · NEVER POST | GO
```

---

## CC-2

```
CC-2 · verify-live · NOW=Load Costs Board + Costs tab vs approved HTML
Codex A+B on main (#20098). Open /accounting/load-costs and load 13508 Costs.
Side-by-side HTML. NEVER POST. NEVER Save all. NEVER create money.
Then: Book Load 13508 EDIT only — confirm Override column on CDL/med blockers if a real expired-qual driver exists; do not invent a fail.
ACK: CC-2 | ACK | Load Costs Chrome · NEVER POST | GO
```

---

## CC-3

```
CC-3 · GO-23 WAVE 4 · NOW=C1 raw UUIDs on operator surfaces
Systemwide. Count found vs count fixed. Guard. Never POST.
Packet C OOS columns CLOSED — do not rebuild.
ACK: CC-3 | ACK | GO-23 C1 raw UUIDs · NEVER POST | GO
```

---

## CODEX

```
CODEX · GO-23 row 25 leftover · NOW=GO-20 Feature 11 late-arrival rate
Data already on dispatch.stop_arrivals + load_stops. Screen + guard. Never POST. No fixtures.
Packets A+B CLOSED — do not rebuild.
ACK: CODEX | ACK | GO-20 F11 late arrival · NEVER POST | GO
```

---

## CASCADE

```
CASCADE · FINDINGS only · NOW=GO-23 row 26 continue
One unique FINDING file:line vs rescued spreadsheets. Do not re-file F5 Combobox.
If none this pass: OUTBOX CASCADE | IDLE-NOT | no unique FINDING | next MUST file | GO
Never wait Jorge. Never build. Never merge-loop babysit.
ACK: CASCADE | ACK | row 26 FINDING · NEVER POST | GO
```
