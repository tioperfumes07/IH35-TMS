# PASTE → CODEX (FE pickers / creators)

FINDING context: inventory added create modals/wizards as Required leaves; Wave D still blocked until A–C honest over **complete** map.

**NOW**
1. Pull main after inventory PR.
2. For each new `*.modal.*` / `*.wizard.*` leaf on P10: ensure EntityPicker `+ Add new` first row → correct creator → R=W → selected → survives reload (`picker_law`).
3. Nested create from Lists cards must match same wizard the leaf names.
4. OUTBOX: `column=picker_law | Built=+N leaves | NEXT=…`

Forbidden: Combobox without +Add new on owed leaves; marking qbo_chrome Built without ParityDrawer path.
