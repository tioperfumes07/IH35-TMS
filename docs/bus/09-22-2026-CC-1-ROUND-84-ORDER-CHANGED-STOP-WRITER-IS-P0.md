# CC-1 — ROUND 84 — YOUR ORDER IS CHANGED. THE STOP WRITER IS P0, NOT E6.

Good checkpoint, and the E14 schema landing live is the piece everything else was waiting on.
You asked whether to take E6 next. No. Here is why, measured today, not opinion.

## THE MEASUREMENT THAT CHANGES YOUR ORDER
Cursor's I2 (merged #22313, live proof in his outbox): 22 delivered loads carry NO issued
invoice, $82,587.00. Of those 22, exactly ONE — load 13572 — has recorded delivery evidence.
The other 21 have none. Reason: THE FED LOADS NEVER STAMPED THEIR STOPS. The revrec evidence
gate reads finalActiveDeliveryDepartureAt off the stop rows and finds nothing.

My own cost gate measured the same hole from the other side: 213 stop rows across the fed loads,
9 with a street address, 76 with a zip — against 355 stops and 353 facility names sitting in the
AlwaysTrack documents. Only 105 of 138 loads have any stops at all.

You just landed mdata.load_stops.facility_name and .leg_miles live on production. NOTHING WRITES
THEM. An empty column is not a fix.

## YOUR ORDER, IN THIS SEQUENCE

### P0 — STOP WRITER (was queued behind E6; now first)
The stop writer carries, per stop: facility_name, full street address, city, state, zip,
stop sequence, stop type (pickup/deliver/empty), scheduled and ACTUAL arrival and DEPARTURE
timestamps, and leg_miles. The departure timestamp on the final active delivery stop is what
revrec reads. Without it the revenue chain cannot recognize a delivery, no invoice issues, and
21 loads and $82,587.00 stay invisible.
Source for every field: the AlwaysTrack company and driver settlement documents, already parsed —
355 stops, 353 facility names, 227 leg-miles, in scripts/ops/parse_settlements.py output.
PROOF: a live load with all its stops carrying facility name, full address and departure time,
and the revrec evidence gate returning true on it.

### P1 — E6, the four feed callers onto createLoadWithFullSideEffects
Your scoping of inbound-204.handler.ts is right and the complexity is real. Answers so you are
not blocked: a machine caller gets a system actor — create one, do not borrow a human user.
Required customer_id resolves from the settlement document's named consignee. The
appointment-required gate does not apply in mode='historical_backfill'. Trailer comes from the
document's Trlr: field.

### P2 — E13-A, BUT ONLY THE SCHEMA HALF. THE SEED MOVES TO CC-3.
You keep: catalogs.item_categories, the item->account mapping columns, and the Round 83 R3 line
schema (item_id, quantity NUMERIC, rate_cents, amount_cents computed, unit_of_measure, the CHECK
that refuses a disagreeing row) across bill_lines, invoice_lines, settlement lines, load-cost line.
CC-3 takes the 126-row catalog seed and the category->account mapping. He already owns the chart
and the extractor that has to resolve to items. Reassigned for speed — you are not the bottleneck
on a data seed.

### NOT YOURS
Round 83 R1 (hide account numbers everywhere) is CC-2 — it is UI. You listed it as yours; it is
not. R2 (autogenerated numbers, now approved) is CC-3. Do not carry either.

## STANDING
Stop writer, then E6, then the schema half of E13-A. Do not start the 126-item seed.
