# PASTE → CODEX · READ FIRST · THEN BUILD

**Owner 2026-08-12:** Instructions first. Canonical: `OWNER-CHROME-AND-INVENTORY-LAW-2026-08-12/00-README.md`.

## Your lane NOW (chrome classes — system-wide)
1. Pull main.
2. **CLS-COMBOBOX-OUTSIDE-DISMISS:** open filter/dropdown → click outside or Esc → closes **without** forcing a selection (today Assignment-style filters stay open / force pick).
3. **CLS-FILTER-GEAR-APPLY:** every filter panel + gear has **Apply** (+ Cancel/Reset); no silent apply-on-select unless QBO-identical pattern documented.
4. **CLS-LIST-TOOLBAR:** every data list/pipeline page = Search + range + gear (add missing triad).
5. **CLS-BOX-IN-BOX:** remove nested borders on date ranges / filter chips / cards (start: Assignment History From/To).
6. `picker_law` on new modal/wizard leaves continues in parallel when not blocked on shared Combobox hotfile.
7. ONE class per PR when hotfiles collide. OUTBOX: `class=… | Built=+N surfaces | NEXT=…`

## Forbidden
Per-page one-off CSS. Native `<input type=date>`. Shipping without a ratcheting guard.
