# PASTE → CURSOR · READ FIRST · THEN BUILD

**Owner 2026-08-12:** Instructions first — you violated this by coding inventory before seat pastes. Corrected: this folder is canonical; seats unblocked.

## Your lane NOW
1. **Bus:** keep INBOX/OUTBOX pointing here; never idle seats without paste.
2. **Finish matrix inventory PR** (push/merge) so Jorge can see leaves+columns on `/program/matrix`.
3. **Extend inventory** for toolbar controls (search/range/gear/filter) if not already leaves — same Required map + guard ratchet.
4. **CLS-RESPONSIVE-SHELL:** app shell + tables/modals adapt laptop→desktop→TV (no clipped chrome); shared layout, one guard.
5. **CLS-QBO-DATEPICKER-SWEEP:** zero native date inputs; Assignment History calendars fixed (no box-in-box, aligned).
6. **CLS-PROPORTION-CHROME:** modal/side-panel/text density vs approved screens — shared tokens.
7. Coordinate hotfiles with Codex (Combobox vs DatePicker — serialize if same file).
8. OUTBOX one-liner only. nudge=OFF while Jorge present.

## Proof
`node scripts/verify-required-surface-inventory-complete.mjs --selftest`  
Plus new/extended guards for dismiss / Apply / responsive / datepicker as each class ships.
