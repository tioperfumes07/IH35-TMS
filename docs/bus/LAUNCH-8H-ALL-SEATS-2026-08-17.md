# LAUNCH · 8 HOURS NON-STOP · ALL SEATS · 2026-08-17T22:15 CT

**Owner order:** continuous auto loop · FAST weekend 4-minute merge · **no idle · no sleep · no pause · no “awaiting”**.  
**Goal:** McLeod + QuickBooks **subscription-grade** product. Scoreboard → **30/30 modules** certified.  
**Bar:** Fully-Wired 1–11 honest Built · Live Chrome last · print/PDF/CSV export = letter-correct (not SPA chrome).  
**Product stays `Live=BLOCKED` until certified — seats never idle while unpaid leaves / FOs remain.**

---

## 0. FORBIDDEN (process defect)

| Forbidden | Do instead |
|-----------|------------|
| Idle / asleep / “standing by” / “awaiting FO” | Read this file + your INBOX → claim → ship |
| Ending turn without NEXT claim | OUTBOX one-liner names next id **same turn** |
| Babysit CI (`gh pr checks --watch`) | Local gate PASS → push → PR → squash `--admin` |
| Invent GL math / flag ON / TMS→QBO write-back | Reuse poster · flags OFF until owner says turn on |
| Soft-reset / stack / claim+guard same PR | Rule 36/37: tip-main + one FINDING + claim→merge→author |
| Asking Jorge what to do next | Search locked files + this packet + matrix |

---

## 1. FAST-MERGE LOOP (~4–5 min) — EVERY SEAT THAT SHIPS CODE

1. `node scripts/ops/cursor-ship-preflight.mjs --body-file /tmp/pr-body.txt` → **exit 0** (or money-pr-local-gate for CC-*)
2. `git push` — if blocked **only** by verify-static ENV class → `--no-verify` **after** step 1 PASS
3. `gh pr create` — Claude-green FINDING body · Cursor titles `Cursor- …`
4. `gh pr merge N --squash --delete-branch --admin` **immediately**
5. Neon yourself if money/migrations
6. OUTBOX one-liner → **next INBOX item same turn**

Never merge with local gate FAIL. Never idle after merge.

---

## 2. QUALITY BAR (McLeod + QBO subscription)

Every surface you touch must match or beat:

| Domain | Standard |
|--------|----------|
| Ops | McLeod seriousness — dispatch sheet, rate conf, BOL, load confirm print letter-correct |
| Books | QBO trust — invoice / bill / payment / statement / aging print + CSV/Excel export correct |
| Reports | P&L · BS · TB · AR/AP aging · cash flow — Print hides chrome; Export CSV/XLSX matches on-screen figures |
| Settlements | Driver settlement PDF letter · pay run confirmations |
| Factoring | Advance / remittance confirmations printable |
| Safety/Fleet | Confirmations & letters printable without sidebar |

**Print law (owner tonight):** `window.print()` on SPA (sidebar visible) = **FAIL**. Open canonical `.html` / print HTML (`?print=1` auto-print) or `@media print` hide chrome for in-app report tables. Export buttons must download real CSV/XLSX, not empty theater.

---

## 3. SEAT PARTITION (no collision)

| Seat | OWN NOW (8h) | Do NOT |
|------|----------------|--------|
| **CC-1** | Money: Built floor → accounting/banking/factoring/settlements Live · Reports **money** gl_je/reverse · **invoice/bill/payment letter HTML + GL density** · G18 expense lines · bank strip · SAMPLE create/void JE S1–S12 | FE Live K-waves (Cursor) |
| **CC-2** | GUARD live-verify AFTER merges · money-critical samples · REOPEN with evidence | Build money math |
| **CC-3** | Mechanical/entity-scope FOs from GUARD-WORKORDERS · CSV/export guards · display-id scope | Money cells · Cursor Live leaves |
| **Cursor** | **Print/PDF stack** (invoices · settlements · dispatch sheets · report `@media print`) · lists/safety/dispatch/drivers Live · FE FOs from HANDOFF | GL math · claim+feature same PR |
| **Codex** | Own Live modules + shared fuel/docs/tasks/compliance/customers/vendors · report **non-money** Live · Export CSV prove | lists/safety/dispatch/drivers · money Built |
| **Devin-A** | fleet · maintenance Live only · HANDOFF FE FAILs | Code · GL · other modules |
| **Cascade** | CANCELLED — do not wait on Cascade | |

Collision: OUTBOX `LIVE CLAIM <module>` before walk. STARVED → next module **same turn**.

---

## 4. 8-HOUR WAVE ORDER (do not invent a fourth method)

### Hours 0–2 — UNBLOCK PRODUCT PATHS
1. Built floor 3441/3441 (CC-1 P0 three cells) if still open on matrix
2. **Print canonical docs** (Cursor): invoice · settlement · dispatch-sheet `.html?print=1` + SPA print CSS for reports
3. Export audit: every Print/Export/CSV button on accounting + reports → real file or letter HTML (CC-1 money docs · Cursor/Codex report chrome)

### Hours 2–5 — LIVE CLIMB (partitions)
- Continuous Live verify per CONTINUOUS-LIVE-NO-STALL.md
- PROD-VERIFIED + Leaves backticks + scoreboard `--write` + FAST-MERGE every batch
- Money zeros first (CC-1) while FE seats climb unpaid Live%

### Hours 5–8 — MODULE CLOSE TOWARD 30/30
- Vertical close weakest Live% modules in your partition
- Print/export regression pass on USMCA: invoice Print · P&L Print · Settlement Print · Dispatch sheet · one CSV export per money report
- OUTBOX every ship; lead refreshes STATUS-NOW each tick

---

## 5. PER-SEAT TEST CHECKLIST (run continuously)

### CC-1 tests (money)
- [ ] Invoice Print opens letter HTML (not SPA) · totals match header+lines
- [ ] Bill / Bill payment / Expense print or PDF path letter-correct or honest N/A
- [ ] Settlement `.html` print · balanced economics when flag OFF still renders
- [ ] P&L / BS / TB / AR / AP Export CSV row counts match UI (entity-scoped)
- [ ] Neon `bypass_rls` + opco completeness discriminator on money counts
- [ ] No TMS→QBO write-back · posting flags OFF unless owner turned on

### Cursor tests (FE / print)
- [ ] Guard `verify-print-opens-canonical-document` FAIL planted / PASS fixed
- [ ] Invoice / Settlement / Dispatch Print → `.html?print=1`
- [ ] Report pages: `@media print` hides sidebar/nav
- [ ] Lists/safety/dispatch/drivers unpaid Live batches → PROD-VERIFIED
- [ ] Accept Codex/Devin `HANDOFF=Cursor` same turn

### Codex tests
- [ ] Shared modules Live walk · Export CSV on non-money reports
- [ ] FE FAIL → HANDOFF=Cursor + keep claiming next leaf

### CC-2 tests
- [ ] After every CC-1 money merge: live sample (money-critical when applicable)
- [ ] OUTBOX prove line · never certify own build

### CC-3 tests
- [ ] Top OPEN mechanical FO → guard + FAST-MERGE
- [ ] Export/print mechanical regressions get ratcheting guards

### Devin-A tests
- [ ] fleet then maintenance unpaid Live only
- [ ] One-line OUTBOX · HANDOFF FE defects

---

## 6. LEAD CADENCE (Cursor)

Every **5m** tick: healthz + ancestry · OUTBOX deltas · bump INBOXes if idle · STATUS-NOW · rewake · next claim.  
Arm: `AGENT_LOOP_TICK_usmca_cursor_lead`.

---

## 7. DONE CRITERIA (honest)

- Scoreboard modules toward **30/30** with honest Built (no `leafRe:.*` theater)
- Print/export paths letter-correct for invoices · reports · confirmations
- Product **Live=BLOCKED** until Box3+Box4 certified — but **zero idle seats** until then
- Every seat OUTBOX shows continuous NEXT claims for the full 8h

**START NOW. Do not wait for chat.**
