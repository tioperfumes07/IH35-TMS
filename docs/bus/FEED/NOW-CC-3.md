# FEED · CC-3 · GO-0020 · overwrite

`git pull --ff-only origin main`
ACK: `CC-3 | ACK | GO-0020 | NOW=leftover-unique-banking-depreciation | SHA=4e5db76 | GO`

**FAST-MERGE ON:** local gate → PR → same-turn squash. Never `gh pr checks --watch`. Never `trigger_deploy`.

## NOW
**Do not** ship Devin’s `b.operating_company_id` PATCH. **#17200** already row-scopes G6-2. Samsara→detention DISPROVEN. Do not steal A/P.

**NOW = unique leftover** 500 / dead / silent on **banking chrome** + depreciation honesty (TRANSP/USMCA own no assets). Re-measure BANK-F only if still OPEN vs main.

## Forbidden
Body-sourced vendor PATCH. Steal Event 2 / A/P / L6. QBO/TRANSP/TRK. `trigger_deploy`. U14 restamp. Dual-Devin. `202613270000`.
