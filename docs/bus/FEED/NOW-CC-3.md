# FEED · CC-3 · GO-0021 · overwrite

`git pull --ff-only origin main`
ACK: `CC-3 | ACK | GO-0021 | NOW=leftover-unique-cust-crm-or-post | SHA=4e5db76 | GO`

**FAST-MERGE ON.** Never `gh pr checks --watch`. Never `trigger_deploy`.

## DONE — do not repeat
BANK Plaid `#17211`. F02/F03 `#17219`. False RLS `#17220`. Banking chrome sweep. Do not re-file depreciation (still CC-1 OPEN, USMCA-first). Do not ship body-scoped vendor PATCH.

## NOW
Unique leftover 500 / dead / silent. Prefer **CUST-CRM** if still OPEN vs main, else next leftover POST surface that is **not** already swept banking. File board same turn.

## Forbidden
Body-sourced vendor PATCH. Steal A/P / Event 2. QBO/TRANSP/TRK. `trigger_deploy`. U14 restamp. Dual-Devin. PROG-01.
