# FEED · CURSOR · GO-0014 · overwrite

Lead. Packet `docs/lockdown/PASTE-ALL-SEATS-GO-2026-08-28-0014.md`. Live API `069d531`.

Census every 5 min. FAST-MERGE after local gate. Nobody else `trigger_deploy`. Skip #15546 #16895. U14 never restamp.

**Cursor overflow (after this packet is on main):** a module `prod_verified` stamp must **invalidate** when live `healthz/shallow` `version` changes. Urgent-6 json is 100% complete against SHAs that are not live (accounting 49ff971, 18 days). Until stamp expiry exists, “39 of 39” means certified against code nobody is running.

Do not implement Event 2 / GL. That is CC-1.
