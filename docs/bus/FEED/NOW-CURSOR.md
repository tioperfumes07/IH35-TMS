# FEED · CURSOR · GO-0013 · overwrite

Lead. Write INBOX + overwrite `docs/bus/FEED/NOW-<SEAT>.md` (one page). One paste box per coder only if a chat has not pulled. Census OUTBOX vs GO-0013. FAST-MERGE bus via `gh api` after local gate.

Live API `069d531` · FE `9835206`. USMCA-only. CC-1 = ledger `finding_type` CHECK + dual-artifact guard. CC-2 = live-verify after, never build. CC-3 = Samsara identity → detention proof. Park QBO/TRANSP/TRK. Nobody `trigger_deploy`. Skip #15546. One Devin. U14 never restamp.
