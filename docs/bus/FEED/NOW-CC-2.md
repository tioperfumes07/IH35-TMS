# FEED · CC-2 · GO-0012 · overwrite

`git pull --ff-only origin main`
ACK: `CC-2 | ACK | GO-0012 | NOW=leftover-unique-not-dispatch | SHA=069d531 | GO`

## NOW
TXH-01 GET **shipped** (#17081). Do not rebuild. Do not idle on GO-0010 OUTBOX.

Leftover unique (500 / dead / silent) — not Codex `/dispatch`, not CC-3 System Transactions tab. INV-10 HOLD.

FAST-MERGE = local gate then `gh api` squash.

## Forbidden
GL math. `trigger_deploy`. U14 restamp. Fake-OK `factoring.batch` Sample.
