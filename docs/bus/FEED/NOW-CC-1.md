# FEED · CC-1 · GO-0011 · overwrite

`git pull --ff-only origin main`
ACK: `CC-1 | ACK | GO-0011 | NOW=G1-CI-then-FAST-MERGE | SHA=069d531 | GO`

## NOW (one serial money stream — do not hold)
1. **#17067 G1 is CI red** (PR body evidence, lucia-bypass-guard, locked-guards, pass-7, build-typecheck). Fix those on that PR. Do not merge red. Then FAST-MERGE #17067 yourself.
2. Then FAST-MERGE **#17069** (dual bill-pay GL, reuse poster) then **#17074** (BANK-F9519). One money area at a time.
3. After G1 is on `origin/main`, OUTBOX `FIXED` with squash SHA so Devin can query-back aging/sample.

## Forbidden
9000 fail-closed. Void-all. New GL math. INV-10. `trigger_deploy`. U14 restamp. Idle “holding for review.”
