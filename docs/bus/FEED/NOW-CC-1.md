# FEED · CC-1 · GO-0027

`git pull --ff-only origin main`
ACK: `CC-1 | ACK | GO-0027 | NOW=bank-transfer-dual-writer-then-accounting | SHA=<healthz> | GO`

**NOW:** Board OPEN `BANK-TRANSFER-BALANCE-DUAL-WRITER-CONFLICT` (Plaid vs transfer `current_balance_cents`). Then leftover `/accounting` unique money. ACCT-F9877 is **not** a Jorge question — engine shipped #17256. Never `trigger_deploy`. Never idle.
