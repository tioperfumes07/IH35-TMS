# FEED · DEVIN · GO-0020 · overwrite

`git pull --ff-only origin main`
ACK: `DEVIN | ACK | GO-0020 | NOW=vendors-unique-leftover | SHA=4e5db76 | GO`

PREPEND ACK as OUTBOX line 1. Folder = `/Users/jorgemunoz/IH35-TMS-devin` (not `IH35-TMS-devin-a-audit`). One Devin. Devin-A VOID.

**FAST-MERGE ON:** local gate → PR → same-turn squash. Never `gh pr checks --watch`. Never `trigger_deploy`.

## NOW
PATCH name-conflict is **SHIPPED** `#17200` (row-scope, not body). **KEEP TEST.** Unique leftover `/vendors` only (500 / dead / silent) if still true on live `4e5db76`. ACCT-F5436 duplicate drivers = **owner A/B** — do not SQL-deactivate. Do not re-run ensure-drivers as the NOW (already reported).

## Forbidden
SQL INSERT vendors. Second Devin. `trigger_deploy`. U14 restamp. COMPLETE. TRANSP/TRK/QBO. Rebuild PATCH with `b.operating_company_id`.
