# CODER INSTRUCTIONS NOW · 2026-08-21 19:07 CT

**Your only order is your INBOX file.** `git pull --ff-only origin main`. **FAST-MERGE 4–5 min.** Never HOLD. Never `/tasks`. USMCA only.

**Owner 19:07 CT: ALL SEATS ON THE SAME MODULE until that module is honestly finished, then the next.** Order: **accounting → banking → settlements → factoring → dispatch → vendors** then customers → drivers → fleet → lists.

**NOW MODULE = ACCOUNTING.** Do not start settlements/factoring/dispatch/vendors/rest-of-urgent until Accounting leftover below is drained (or OUTBOX UNCHANGED with a named blocker). **Product Live remains BLOCKED.** Categorize is not a coder function.

## HARD — every seat

1. **Do NOT call Render deploy.**
2. **Do NOT buy Render storage.**
3. **Do NOT re-enable `ENABLE_SCHEDULED_REPORTS_WORKER` this turn.**
4. **Do NOT pause for Jorge.** Same module, your lane, next leftover same turn.
5. Native `window.confirm` freeze: ConfirmModal / VoidReasonModal.
6. **USMCA only.** No QBO write-back.
7. **Categorize / Match / recon is not a coder function.** Honest labels + wiring only.

| Seat | File | NOW (ACCOUNTING) |
|------|------|------------------|
| **CC-1** | `INBOX-CC-1.md` | Register human `reference` (not source UUID). JE/bill human source labels. Vendor join on expenses (4 live tombstones). Worker OFF. |
| **CC-2** | `INBOX-CC-2.md` | Accounting reverse_link unpaid cells only. Stamp Live after click. No settlements/factoring this tick. |
| **CC-3** | `INBOX-CC-3.md` | Accounting picker_law + chrome (Bills/Expenses/Invoices/JE wizards). Not customers/drivers/fleet. |
| **Codex** | `INBOX-CODEX.md` | Accounting reverse/connectivity unpaid only. No CDP. No deploy. |
| **Cursor** | `INBOX-CURSOR.md` | Live Chrome Accounting. Bus. No per-merge deploy. |

**Honest:** reverse PASS ≠ module CERTIFIED. Accounting CERTIFIED only when Fully-Wired 1–12 proven live on current healthz SHA.
