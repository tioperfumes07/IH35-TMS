# CODER INSTRUCTIONS NOW · NO DEVIATE · CONTINUOUS · HOURLY RE-READ · FAST-MERGE

USMCA only. No QBO write-back. **No owner gate.** ALL QUESTIONS ASKED AND ANSWERED.

**Do not deviate.** This file + your `INBOX-<SEAT>.md` TOP are the only NOW. Chat summaries do not override.

## Method (researched · owner-locked 2026-08-20)

**Swarm one module. Split columns. Do not give each seat an entire module.**

Research: Scrum Book *swarming / one-piece flow*; Pierrain *squads + swarming* vs feature teams; LeSS feature teams still **specialize inside** the slice; merge queues exist so parallel PRs do not collide. This repo already proved the other way is slower: Devin cannot post GL, CC-1 must not CDP, Rule 27 forbids two money PRs, Devin reports “Miss C=0 / queue 0” while accounting Box 4 Live is still open.

| Seat | Column only | Never |
|------|-------------|-------|
| **CC-2** | Box 4 Live ledger (`AUDIT-COVERAGE-LIVE.md` append-lease) | Clicked theater · keyword fan-out |
| **Devin-A** | Item 12 Clicked (`leaf=module:leafId:col` USMCA chrome=9227) | GL · claiming Miss C · waiting on `required.json` |
| **CC-1** | Money write / reuse poster | CDP / Chrome |
| **CC-3** | Built 1–11 FE leftover | 9227 · GL |
| **Codex** | reverse_link | 9227 |
| **Cursor** | Lead + bus + leftover Built + scoreboard FAST-MERGE | Stamping Box 4 Live |

**NOW module = accounting** until that module is 100% on the BAR below. Then **everyone** moves to the **next WAVE 1 name together** (same columns).

## BAR (100% on the NOW module, then software totals)

Money park is **OFF**. #13050: Frozen / Miss C / READY **count money**. Guard fails if anyone excludes money again. Miss C **going up after deploy is the real unpaid money**, not a regression.

1. **Four boxes** — Required · Audited · Built · **Live** all 100% on that module (system target **3365/3365**).
2. **Miss C = Frozen minus Box 4 Live** (#13132). **Clicked 100% cannot zero Miss C.** Devin OUTBOX `LIVE PASS` is **Clicked only**, never Miss C, never Box 4.
3. **Built wire complete** — 0 leftover Built on that module; remaining gap is Live, not more chrome.
4. **Fully-Wired 1–12** — item 12 last (Devin Clicked). Until 12: `Built` / `Live=BLOCKED`.

**Box 4 Live ≠ Clicked.** `cell.live` only from `AUDIT-COVERAGE-LIVE` **PROD-VERIFIED** with explicit leaf id + `Exact cells:` / `Leaves:` `` `leaf:col` ``.

**Devin idle is a defect.** `--rebuild-only` printing `0 leaves` means Clicked **keys** are full. That is **not** drained. Do **not** wait for new `required.json`. Re-walk **accounting Queue 6** money/chrome screens so CC-2 can stamp Box 4. Then keep the watcher; next unpaid Clicked cell starts the loop.

## Module order (do not invent)

Finish **WAVE 1** to the BAR **before** WAVE 2 as NOW.

**NOW:** accounting (jumped first because Live ✕s were here — not because WAVE 1 starts here).

**After accounting 100%:** continue WAVE 1 in this list, skip names already at BAR:

**WAVE 1:** banking → factoring → accounting → settlements → customers → drivers  
**WAVE 2:** insurance → legal → lists → safety → fleet → vendors → maintenance → dispatch  
**WAVE 3:** inventory → compliance → reports → cash-flow → finance → form_425 → users → docs → home → tasks → program → driver-hub → system → fuel  

**eld never.** Fuel/system/driver-hub stay WAVE 3. No QBO write-back.

**Accounting Queue 6 (Built 346/346 · Live 340/346 until CC-2 stamps):**

- `accounting.parity.credit_memos_page` — remaining Live ✕ cols among `connectivity` `reverse_link` `customer` `qbo_chrome`
- `banking.panel.linked_bank_transactions` — `bank` `gl_je` `connectivity` `reverse_link`

Do not idle because Clicked/Miss-C-as-Clicked looks 0 while Box 4 Live < 100%.

## Continuous auto mode

Never idle. Never wait for Jorge. FAST-MERGE (`docs/bus/FAST-MERGE-4MIN-LAW.md`):

1. `cursor-ship-preflight --body-file` / `money-pr-local-gate` **exit 0**
2. `git push` — ENV `verify-static-fallback` only → `--no-verify` **after** step 1 PASS
3. `gh pr create` — never `gh pr checks --watch`
4. `gh pr merge N --squash --delete-branch --admin` immediately (or GitHub merge API if local `main` is locked)
5. Neon yourself if money/migrations
6. OUTBOX one line → next FO same turn

## Hourly re-read

1. `git pull --ff-only origin main`
2. This file TOP to BOTTOM
3. Your `INBOX-<SEAT>.md` TOP
4. `docs/bus/FAST-MERGE-4MIN-LAW.md`
5. ACK: `STANDARD=USMCA-LAUNCH | SWARM-ONE-MODULE | HOURLY-REREAD | NOW=accounting <col> | GO`
6. Same chain — do not invent a queue

## Seats NOW (accounting swarm)

| Seat | NOW |
|------|-----|
| **CC-2** | Stamp Queue 6 Live after live click. Then WAVE 1 next name Box 4. |
| **Devin-A** | chrome=9227. Rebuild includes money. Queue 0 → re-click Queue 6 `leaf=accounting:<id>:<col>`. Never Miss C=0 from PASS count. |
| **CC-3** | WAVE 1 Built leftover if Built < Required; else WAVE 2 Built. Not 9227. Not GL. |
| **CC-1** | `banking.panel.linked_bank_transactions` bank+gl_je USMCA. Reuse poster. F5602–F5619 stand. |
| **Codex** | reverse on those two leaves. No 9227. |
| **Cursor** | This bus. FAST-MERGE. Do not stamp Box 4. |

ACK: `STANDARD=USMCA-LAUNCH | SWARM-ONE-MODULE | HOURLY-REREAD | NOW=accounting <col> | GO`
