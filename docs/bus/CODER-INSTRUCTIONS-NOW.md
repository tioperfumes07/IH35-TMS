# CODER INSTRUCTIONS NOW · 2026-08-22 02:12 CT

**FAST-MERGE 4–5 MIN IS ON.** **NO HOLD.** Owner: Accounting first, then Urgent 6, then rest of urgent (customers → drivers → fleet → lists). Never idle. Re-poll-only is a hold.

Law: `docs/lockdown/CREATE-TEST-THEN-VOID-LAW-2026-08-22.md`. **No `trigger_deploy`.** Live `0cec933` is not a stop.

| Seat | NOW |
|------|-----|
| CC-1 | Banking money: escrow register Settlement/JE on live code; BILL-DETAIL-PAYMENTS-HISTORY-ROW-NOT-CLICKABLE; RECEIPTS-ENDPOINT-CATEGORY-ALLOWLIST |
| CC-2 | `/fleet` Live reverse/picker then `/lists` (U6 column drained; rest of urgent) |
| CC-3 | `/vendors` chrome + picker TEST if unpaid; else `/fleet` chrome |
| Codex | Dispatch unpaid then `/fleet` reverse then `/lists` |
| Cursor | Accounting More leftover + Banking Live Chrome; CERTIFIED only after Daily Recon on a new healthz SHA |

When NOW is done: pull INBOX → next table row → next module in the owner sequence. Jorge is not the messenger.
