# CODER INSTRUCTIONS NOW · 2026-08-21 21:18 CT

**Your only order is your INBOX file.** `git pull --ff-only origin main`. **FAST-MERGE 4–5 min.** Never HOLD. Never `/tasks`. USMCA only.

**Owner 21:18 CT — THE RULE:** If a coder has **truly finished Accounting in their lane**, they **continue to the next Urgent 6 module (banking)**. Do not idle waiting for another seat’s leftover-dry signal. Do not copy CC-3 (already on factoring→dispatch→vendors).

Urgent 6: **accounting → banking → settlements → factoring → dispatch → vendors** then customers → drivers → fleet → lists.

**Honest now:** Accounting is **not CERTIFIED**. Live `healthz/shallow` still `{ok:true,version:"0cec933"}`. Cursor kicked batch `dep-da4gbos9v7es738485kg` → **`pre_deploy_failed`**. Dashboard `preDeployCommand` still runs `ci:boot-api-smoke` + `ci:boot-aggregate-smoke` (law: migrate + critical-runtime only). Product Live remains BLOCKED until Fully-Wired 1–12 on the **current** healthz SHA. Categorize is not a coder function.

**Truly finished (per seat) means:** no remaining unpaid cells **in that seat’s lane** (CC-1 money labels/posters · CC-2 Live reverse/picker except `modal.decide` · Codex reverse/connectivity · Cursor Live Chrome leftover that does not need a new SHA). Deploy lag on another seat’s already-merged fix is **not** a reason to sit idle.

## HARD — every seat

1. **Do NOT call Render deploy** (Cursor lead already used the one batch; it failed pre-deploy).
2. **Do NOT buy Render storage.**
3. **Do NOT re-enable `ENABLE_SCHEDULED_REPORTS_WORKER` this turn.**
4. **Do NOT pause for Jorge.**
5. Native `window.confirm` freeze: ConfirmModal / VoidReasonModal.
6. **USMCA only.** No QBO write-back.
7. **Categorize / Match / recon is not a coder function.** Honest labels + wiring only.

| Seat | File | NOW |
|------|------|-----|
| **CC-1** | `INBOX-CC-1.md` | ACCOUNTING not done in-lane: JE/expense **poster memos** still UUID. Stay until that is truly done, then **banking** money labels. Worker OFF. |
| **CC-2** | `INBOX-CC-2.md` | If unpaid accounting reverse/picker (except `modal.decide`) is empty → **NOW=banking** reverse/picker. Else finish those cells. No idle. |
| **CC-3** | `INBOX-CC-3.md` | **Released.** NOW=factoring chrome/picker, then dispatch, then vendors. |
| **Codex** | `INBOX-CODEX.md` | If unpaid accounting reverse/connectivity is empty → **NOW=banking** reverse. Else finish accounting reverse. No CDP. No deploy. |
| **Cursor** | `INBOX-CURSOR.md` | Bus + Accounting Live Chrome until a **live** SHA carries the leftover batch. Not CERTIFIED. Do not park other seats. |

**Honest:** reverse PASS ≠ module CERTIFIED. Accounting CERTIFIED only when Fully-Wired 1–12 proven live on current healthz SHA.
