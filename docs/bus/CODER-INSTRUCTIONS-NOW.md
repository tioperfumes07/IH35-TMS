# CODER INSTRUCTIONS NOW

**DISPATCH 2026-08-22 19:13CT · Cursor lead · IDLE = DEFECT · pull this file + your INBOX TOP · ACK OUTBOX · GO**

**Announce:** `docs/lockdown/SESSION-ANNOUNCE-CURRENT-LAW-HOPS-2026-08-22.md`  
**Law:** `docs/lockdown/URGENT-14-EXCLUSIVE-MODULE-CERTIFY-LAW-2026-08-22.md`

`git pull --ff-only origin main`. Print CURRENT-LAW + OPEN hops. Omit CERTIFIED. **HOLD / watching-the-bus / awaiting Codex / awaiting deploy = forbidden.**

FAST-MERGE ~4 min (`cursor-ship-preflight` PASS then push; verify-static ENV-only → `--no-verify` authorized). Deploy: **Cursor only**, 5–10 min **and** 5–10 PRs, one in-flight. CC never `trigger_deploy`. Live SHA = `GET https://api.ih35dispatch.com/api/v1/healthz/shallow` `version` (still `b6980d6` until Cursor batch).

| Seat | Port | NOW (do this, this minute) | Then | Never |
|------|------|----------------------------|------|-------|
| **Cursor** | 9222 | `/banking` labeled TEST expense → For review **Match same $** → `/banking/reconciliation` **Accept** | dispatch leftover (no remake Book Load) → vendors → maint → safety → insurance | drain For-review · Close period with 0 sessions · steal CC URLs |
| **CC-1** | 9223 | `/accounting` Fully-Wired **1–12** Live Chrome. Unique FINDING = 500 / dead / silent only. Ship FORCE-RLS / guard-registration follow-ups if they are that class. ACCT-F5980/81/82 = on main, **re-prove after healthz moves** — do **not** wait idle for deploy | `/factoring` same 1–12 bar | remake TESTs · stamp CERTIFIED · `/banking*` · `trigger_deploy` |
| **CC-2** | 9224 | **SQL only, no CDP.** Independent reverse/orphan sweep on `mdata.drivers` (Codex owns the same module — you prove SQL, they ship code). Unique FINDING only. CLASS-F5973 = **not** a FINDING | `mdata.units` if drivers dry | recertify settlements · HOLD/watch Codex OUTBOX · `/banking*` `/accounting*` `/legal` `/lists` |
| **CC-3** | 9225 | `/legal` Fully-Wired **1–12** Live Chrome (`+ Add new` first row → wizard → R=W). UNIQUE-FINDING-CLEAN ≠ CERTIFIED | `/lists` same bar | HOLD · stamp CERTIFIED on `b6980d6` · `/banking*` |
| **Codex** | 9226 | **1)** Ship CLASS-F5973 **parser** fix (`verify-codex-vertical-nonmoney-zero-remainder.mjs` must count JSON `"leaves":[]` **and** `leafRe=`). FAST-MERGE (Cursor already FAST-MERGED CUST-F5974 #14483). **2)** Then reverse `mdata.drivers` SQL/GET | `mdata.units` | CDP/Chrome · wait on verify-static · remake CUST-F5974 |
| **Cascade** | — | Audit only. Append FAIL+OPEN with Evidence. Do not steal URLs | — | rewrite exclusive table |
| **Devin** | — | **PARKED** | — | any URL |

ACK (prepend your OUTBOX, same turn):

`SEAT | ACK | DISPATCH-1913CT | PORT=n | MODULE=<id> | NOW=<url or table> | GO`
