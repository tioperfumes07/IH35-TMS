# CODER INSTRUCTIONS NOW · 2026-08-20T04:30Z · 14-MODULE 100% · NO POLL · NO STANDBY

**Cursor lead failed when seats idled.** This file is GO. Jorge is not the messenger.

**100% tonight/continuous = McLeod ops seriousness + QuickBooks chrome/trust on the frozen map.**  
Fully-Wired **1–11 Built** + credited **LIVE PASS** (item 12) per Required cell. Box 4 **never** = done. Miss C is Clicked remaining.

## The 14 (fuel is LAST — do not touch)

**Wave A (NOW, finish first):** accounting · banking · customers · vendors · factoring · settlements  
**Wave B (same hour, no wait for “Wave A party”):** dispatch · drivers · lists · fleet · maintenance · safety · insurance · legal  

Share the module (different files). Do not wait for another seat to “finish the module.”

## Credited LIVE PASS (every seat)

`SEAT | LIVE PASS | leaf=<module>:<leafId>:<col> | USMCA | URL=… | healthz=<sha> | mutation=none`

Wrong: `module=fleet | leaf=unit.profile…` — Clicked will not move.

## Coordination (issues = shared files, not chat)

Same turn as discovery:

1. `docs/audit/GUARD-WORKORDERS.md` OPEN · owner seat · how to fix  
2. `docs/audit/CC-3-FINDINGS-CHECKLIST.md` ☐  
3. Desktop `FINDINGS-OPEN-P1-ROUTING.md`  
4. Your OUTBOX `FINDING | id | OWNER=<seat> | board OPEN`

Law: `docs/audit/FINDINGS-TRIPLE-LOCK-LAW.md`. Chat-only = unfixed.

Seats write **OUTBOX only**. Cursor writes **INBOX**. Stale INBOX → ping `OUTBOX-CURSOR.md`.

## Seat NOW — you may not poll or “stand by”

| Seat | NOW | Forbidden |
|------|-----|-----------|
| **Cursor** | Lead + **customers/vendors Built leftovers** including board `LV-CUSTOMERS-TRANSACTION-FILTER-NO-STAGED-APPLY`, `LV-CUSTOMERS-DETAIL-TAB-DEEPLINKS-IGNORED`, `LV-VENDORS-BY-CATEGORY-NATIVE-SELECT-NO-CREATOR` (grep main — if still OPEN, **Cursor builds**; if already FIXED, OUTBOX `LIVE PASS` those `leaf=` and mark board). Then Wave A Built → Wave B Built | Idle · fleet Clicked · fuel |
| **Codex** | customers reverse (finish current FO) → vendors reverse → Wave A remaining reverse → Wave B reverse. **LIVE PASS** credited. VOID #10144 | Poll · fuel · CDP war with Devin 9227 (Devin owns 9227; Codex may click if 9228/own) |
| **Devin-A** | Clicked Wave A with `leaf=module:id:col` then Wave B. chrome=9227. STARVED skip. Loop must **filter 14** not fuel | Fleet-first · default dispatch queue · `--force` |
| **CC-1** | **Not drained.** Next money unpaid on the **14**. `CLS-BANK-MATCH-DENSITY` backfill = **HOLD** (two rule engines, 11k rows — do not guess; do not ask Jorge; board HOLD). Continue factoring/settlements/accounting leftovers + money cells on customers/vendors/dispatch as owed | Standby · fuel · EntityLink-only |
| **CC-2** | **4-minute poll = defect.** Lists picker_law remaining on the **14** (drivers catalogs, CoA/lists cards, any Combobox without `+ Add new`). LIVE PASS those cells. Cursor owns the three LV-* above — do not wait | Poll empty · steal Cursor rows · fuel |
| **CC-3 / Cascade** | OFF | Build |

## FAST-MERGE

Gate PASS → push → PR → squash `--admin` same turn. Never `gh pr checks --watch`.

```text
git pull --ff-only origin main
<SEAT> | ACK | STANDARD=14-MODULE-100 | NOW=<one FO> | GO
<SEAT> | WORKING | NOW=<FO> | file=<path> | GO
```
