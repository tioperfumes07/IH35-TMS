# CODER INSTRUCTIONS NOW · 2026-08-22 10:34 CT

**Canonical:** `docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md` — **14 modules**. `git pull --ff-only origin main` **before you speak**.

**LIVE (curl 10:34 CT):** `healthz/shallow` **`version=0cec933`** → Accounting **NOT CERTIFIED**. Daily-recon unauthenticated **401**. Prior Render `dep-da4qm10` **update_failed**. Cursor kicked **one** replacement `dep-da4s3m3l550s738ql90g` — **coders never `trigger_deploy`**. **Idle is forbidden.** Claiming 09:08 “sequence closed” without ACK **10:34CT** is a defect.

USMCA `https://app.ih35dispatch.com` · `5c854333-6ea5-4faa-af31-67cb272fef80`  
Ports: Cursor **9222** · CC-1 **9223** · CC-2 **9224** · CC-3 **9225** · Codex **9226**. Devin **PARKED**.

**FAST-MERGE:** gate 0 → push (ENV `--no-verify` only after gate) → `gh pr create` (no `--watch`) → `gh pr merge --admin` → Neon if money → OUTBOX → **next URL same turn**.

**14:** `/accounting` → `/banking` → `/driver-finance/settlements` → `/factoring` → `/dispatch` → `/vendors` → `/customers` → `/drivers` → `/fleet` → `/lists` → `/maintenance` → `/safety` → `/insurance` → `/legal`  
**After 14:** `/tasks` `/home` `/compliance` `/cash-flow` `/fuel` `/inventory` `/users`

| Seat | Port | Lane | NOW | Then |
|------|------|------|-----|------|
| **CC-1** | 9223 | Money / poster reuse | Board money FAIL **or** unique leftover in 2–14 | After-14 `/fuel` `/inventory` money if named FAIL |
| **CC-2** | 9224 | Live Chrome every tab | First unpaid URL in 2–14 **this pull** (not 09:08 memory) | After-14 `/tasks`… if 14 URLs opened this pull |
| **CC-3** | 9225 | Picker / `+ Create` | `/customers` (OUTBOX was stale 01:44CT — **rewake**) | Through `/legal` |
| **Codex** | 9226 | Reverse F+R | `banking.*` (OUTBOX stale 02:52CT — **rewake**) | Through legal. **No CDP theater** |
| **Cursor** | 9222 | Lead | **No second kick** while `dep-da4s3m3` in flight | Certify U1 when SHA ≠ `0cec933` |

ACK: `SEAT | ACK | URGENT-BLOCKS-NOW 10:34CT | PORT=n | NOW=<url> | GO`

**Forbidden:** wait Jorge · wait Cursor · wait healthz as idle · For-review drain · remake proven Accounting TESTs · QBO write-back · stacked deploys · empty-queue.
