# CODER INSTRUCTIONS NOW · SCOREBOARD MUST MOVE · USMCA LAUNCH

**Only USMCA.** No TRK. No TRANSP. No QBO sync.

**100% = Fully-Wired 1–12 on USMCA.**

Clicked on the live board reads **origin/main OUTBOX via GitHub** (Render ignores docs/** so a docs-only merge never changed the API copy). Prefer `leaf=module:leafId:col`.

```text
<SEAT> | ACK | STANDARD=USMCA-LAUNCH | ENTITY=USMCA | GO
```
