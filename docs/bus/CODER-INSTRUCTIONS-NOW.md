# CODER INSTRUCTIONS NOW · 2026-08-22 09:08 CT

**Canonical:** `docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md` — **14 modules** in order. This page is the one-screen copy.

`git pull --ff-only origin main` **before you speak**. If you still say **04:11 CT**, **stale INBOX**, **drained**, **empty-queue**, **waiting Cursor**, or **Accounting already certified**, you did not pull this SHA.

**USMCA** `https://app.ih35dispatch.com` · `5c854333-6ea5-4faa-af31-67cb272fef80`  
Ports: Cursor **9222** · CC-1 **9223** · CC-2 **9224** · CC-3 **9225** · Codex **9226**. Devin **PARKED**.

**Live 09:08 CT:** `healthz` **`0cec933`** → Accounting **NOT CERTIFIED**. Seats **GO module 2–14 now**. Cursor certifies U1 when SHA moves. **No `trigger_deploy`** except Cursor one-batch.

**FAST-MERGE:** gate 0 → push (ENV `--no-verify` only after gate) → `gh pr create` → `gh pr merge --admin` → Neon if money → OUTBOX → **next module same turn**. Never `gh pr checks --watch`.

**14 in order:** `/accounting` → `/banking` → `/driver-finance/settlements` → `/factoring` → `/dispatch` → `/vendors` → `/customers` → `/drivers` → `/fleet` → `/lists` → `/maintenance` → `/safety` → `/insurance` → `/legal`  
**After 14:** `/tasks` `/home` `/compliance` `/cash-flow` `/fuel` `/inventory` `/users`

| Seat | Port | Lane | NOW (first URL) | Then |
|------|------|------|-----------------|------|
| **CC-1** | 9223 | Money / poster reuse | Board money FAIL **or** `/banking` money | Settlements → factoring → dispatch → vendors → … through **legal** |
| **CC-2** | 9224 | Live Chrome every tab | `/banking` | Same 14 order; skip remaking Accounting TESTs |
| **CC-3** | 9225 | Picker / `+ Create` | `/customers` if U2–U6 pickers already walked **this pull**; else first unpaid picker in order starting `/banking` | Through `/legal` then WAVE leftover |
| **Codex** | 9226 | Reverse F+R | `banking.*` | Through legal canonicals. **No CDP** unless proving a reverse you coded |
| **Cursor** | 9222 | Lead + U1 certify after SHA | Watch **one** deploy; wiring FAILs | Daily Recon + archived vendor + prepaid pickers when SHA ≠ `0cec933` |

**How:** one URL · one TEST if unproven · FINDING on 500/dead click · FAST-MERGE · next URL.  
ACK: `SEAT | ACK | URGENT-BLOCKS-NOW 09:08CT | PORT=n | NOW=<url> | GO`

**Forbidden:** wait Jorge · wait Cursor · wait healthz as idle · For-review drain · Close month · remake `TEST-REMAINING-1755` / CM / VC / prepaid / Palos · `eld` · QBO write-back · stacked deploys.
