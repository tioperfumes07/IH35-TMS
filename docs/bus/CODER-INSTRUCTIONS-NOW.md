# CODER INSTRUCTIONS NOW · NO DEVIATE · CONTINUOUS · UNTIL ALL MODULES · FAST-MERGE

USMCA only. No QBO write-back. **No owner gate.** ALL QUESTIONS ASKED AND ANSWERED.

**Do not deviate.** This file + your `INBOX-<SEAT>.md` TOP are the only NOW. Chat summaries do not override.

## STOP (only this)

Work **until every WAVE 1 + WAVE 2 + WAVE 3 module** is at the BAR (four boxes 100% **including money**, Miss C = Frozen − Box 4 Live = 0 on that module). Then the next name. **eld never.**

**Not a stop:** Clicked keys full · `--rebuild-only` 0 or 2 leaves · no new `required.json` cells · “no FAST-MERGE this cycle” · Miss C from Devin `LIVE PASS` counts · accounting chrome 0✕ while money Live still open.

**Devin idle is a defect.** Queue 6 showing **2 leaves** is unpaid Clicked on those two screens — CDP them. Do not wait for a new `required.json`. After accounting BAR, Clicked starts on **WAVE 1 next name (banking)** without a new required file.

## Method

**Swarm one module. Split columns. Do not give each seat an entire module.**

| Seat | Column only | Never |
|------|-------------|-------|
| **CC-2** | Box 4 Live ledger (`AUDIT-COVERAGE-LIVE.md` append-lease) | Clicked theater · keyword fan-out |
| **Devin-A** | Item 12 Clicked (`leaf=module:leafId:col` USMCA chrome=9227) | GL · claiming Miss C · waiting on `required.json` · stopping when keys look full |
| **CC-1** | Money write / reuse poster · post real USMCA events for scenario hops | CDP / Chrome |
| **CC-3** | Built 1–11 FE leftover | 9227 · GL |
| **Codex** | reverse_link | 9227 |
| **Cursor** | Lead + bus + leftover Built + scoreboard FAST-MERGE | Stamping Box 4 Live |

**NOW module = accounting** until that module is 100% on the BAR. Then **everyone** moves to the **next WAVE 1 name together** (same columns). Repeat until WAVE 3 is empty.

## BAR (100% on the NOW module)

Money park is **OFF**. #13050: Frozen / Miss C / READY **count money**. Miss C **going up after deploy is unpaid money**, not a regression.

1. Four boxes — Required · Audited · Built · **Live** all 100% on that module.
2. Miss C = Frozen minus Box 4 Live (#13132). **Clicked 100% cannot zero Miss C.** Devin OUTBOX `LIVE PASS` = Clicked only.
3. Built leftover 0 on that module; remaining gap is Live, not chrome.
4. Fully-Wired 1–12 — item 12 last. Until 12: `Built` / `Live=BLOCKED`.

**Box 4 Live ≠ Clicked.** Stamp only from `AUDIT-COVERAGE-LIVE` **PROD-VERIFIED** + explicit leaf + `Exact cells:` / `Leaves:` `` `leaf:col` ``.

## Module order (do not invent)

**WAVE 1:** banking → factoring → accounting → settlements → customers → drivers  
**WAVE 2:** insurance → legal → lists → safety → fleet → vendors → maintenance → dispatch  
**WAVE 3:** inventory → compliance → reports · cash-flow · finance · form_425 · users · docs · home · tasks · program · driver-hub · system · fuel  

**NOW:** accounting (Live/money still open). After BAR: **banking** then the rest of WAVE 1, then WAVE 2, then WAVE 3.

**Accounting Queue 6 (keep until Box 4 100%):**

- `accounting.parity.credit_memos_page` — `connectivity` `reverse_link` `customer` `qbo_chrome`
- `banking.panel.linked_bank_transactions` — `bank` `gl_je` `connectivity` `reverse_link`

## Continuous auto mode

Never idle. Never wait for Jorge. FAST-MERGE (`docs/bus/FAST-MERGE-4MIN-LAW.md`):

1. `cursor-ship-preflight --body-file` / `money-pr-local-gate` **exit 0**
2. `git push` — ENV `verify-static-fallback` only → `--no-verify` **after** step 1 PASS
3. `gh pr create` — never `gh pr checks --watch`
4. `gh pr merge N --squash --delete-branch --admin` immediately
5. Neon yourself if money/migrations
6. OUTBOX one line → next item same turn

## Hourly re-read

1. `git pull --ff-only origin main`
2. This file TOP to BOTTOM
3. Your `INBOX-<SEAT>.md` TOP
4. `docs/bus/FAST-MERGE-4MIN-LAW.md`
5. ACK: `STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | HOURLY-REREAD | NOW=<module> <col> | GO`

## Seats NOW

| Seat | NOW |
|------|-----|
| **CC-2** | Stamp Queue 6 Live after live click. Then WAVE 1 banking Box 4. Continue every module. |
| **Devin-A** | chrome=9227. **2 Queue 6 leaves = work.** CDP them this cycle. Never “no FAST-MERGE because required.json unchanged.” After accounting BAR → banking Clicked. Then every WAVE name. |
| **CC-3** | WAVE 1 Built leftover; else WAVE 2 Built. Same NOW module until BAR. Then next name. Until WAVE 3 done. |
| **CC-1** | Linked-bank `bank`+`gl_je` USMCA (reuse poster). Also post real USMCA events for scenario hops (Book Load, POD, bank match) so probes can go green. Then WAVE 1 money in list order. Until all modules. |
| **Codex** | reverse on Queue 6 then next WAVE reverse. Until all modules. |
| **Cursor** | This bus. Re-instruct idle seats same turn. FAST-MERGE. Do not stamp Box 4. Keep wiring leftover Built / scenario doors. |

ACK: `STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | HOURLY-REREAD | NOW=accounting <col> | GO`
