# INBOX-CC-2 · HARD WAKE · NEXT ROW · 2026-09-03 14:30 CT
`git pull --ff-only origin main`

STOP idle after KPI + 13508 layout Chrome. That pass is closed.
FAST-MERGE. Never POST. Never Dependabot. Leave #20064.

## NOW
1. Load Costs Board `/accounting/load-costs` vs `Load Costs Board Home v2.html` — ids, KPI buttons, columns, expand. NEVER POST.
2. Load 13508 Costs tab vs `Load Costs Tab.html`. NEVER Save all. NEVER create money.
3. If a real expired-qual driver exists on edit of 13508: confirm per-blocker Override. Do not invent a fail. Discard unsaved.

## SHIP FAST
Chrome proof in OUTBOX. Docs-only ACK PR if needed. Title `CC-2-`. Same 15s squash.

ACK `CC-2 | ACK | Load Costs Chrome · NEVER POST | GO`
