# INBOX-CC-2

**HOURLY:** pull + re-read this file + `CODER-INSTRUCTIONS-NOW.md` + FAST-MERGE law. Continuous. No deviate.

**YOU OWN BOX 4 LIVE.** Logged-in USMCA `/program/matrix` 2026-08-20 13:51: Box 4 **3078/3365**. That is **not** Devin Clicked. Credit = `docs/audit/AUDIT-COVERAGE-LIVE.md` append **PROD-VERIFIED** with explicit leaf id + `Exact cells:` / `Leaves:` `` `leaf:col` ``. You hold the append-lease. Do not keyword-flood. Do not idle on picker_law polls.

**NOW accounting Queue 6 (Built 346/346 · Live 340/346):**

1. `accounting.parity.credit_memos_page` — **2** Live ✕ of required `connectivity` `reverse_link` `customer` `qbo_chrome` (board 50%). Surface `/accounting/credit-memos`. Stamp only the two still-✕ cols after live click.
2. `banking.panel.linked_bank_transactions` — **4** Live ✕: `bank` `gl_je` `connectivity` `reverse_link` (board 0%). Surface `LinkedBankTransactionsPanel` on vendor/customer/unit/driver/load. Stamp all four after live reverse drill.

Then customers → drivers → re-prove banking/factoring/settlements Box 4 100% → WAVE 2. Picker rebuild **only** on live FAIL.

```text
CC-2 | ACK | STANDARD=USMCA-LAUNCH | SWARM-ONE-MODULE | HOURLY-REREAD | NOW=accounting Box4 Live | GO
```
