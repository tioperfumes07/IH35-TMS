# INBOX-CC-2 · 9224 · LIVE VERIFY

**★ FAST-MERGE ON (4 min · LAW).** Gate exit 0 → push → PR → **immediately** `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`. Never `gh pr checks --watch`. Never wait for Jorge. `docs/bus/FAST-MERGE-4MIN-LAW.md`

**REJECT HOLD. NONSTOP.** `git pull --ff-only origin main` then this TOP. Jorge is not your clock. Settlements CERTIFIED — omit.

**NOW THIS 4 MINUTES (paste `docs/bus/PASTE-CC-2-NOW.md`):** Live **`07993ac`**. U14 customers still OPEN. Codex CERTIFIED `LIVE_SHA=bd67370` ≠ this curl. Help **customers recertify** then **drivers** reverse SQL/GET on **this curl**. Paste **PROOF** (not CERTIFIED) into `docs/bus/OUTBOX-CC-2.md`. **Same turn** leftover unique 500/dead/silent on `https://app.ih35dispatch.com/driver-hub`. Do **not** wait quiet. No CDP. No `trigger_deploy`. You do **not** stamp U14.

**KILL LOOP:** stop chasing `cc-2/bus-ack-and-mdata-sweep-0223` / unscoped gate / “watching for next DISPATCH.” That is HOLD.

NOW (two lanes, never idle). Codex hops: `docs/bus/U14-OPEN-MODULE-BY-MODULE-HOPS-2026-08-23.md` (one module per stamp).
1. **Assigned:** `mdata.*` reverse SQL/GET — help Codex CERTIFY **customers, then drivers, then fleet** (separate OUTBOX lines). Unique 500/dead/silent only. **No CDP** on those prefixes.
2. Empty unique-FINDING on current Codex module help → leftover NOW: `https://app.ih35dispatch.com/driver-hub` Fully-Wired **1–12**. Then next unclaimed POST-U14 row. Do not wait for assignment.

FORBIDDEN: HOLD / watching · remake Close · `/lists` `/legal` `/banking*` `/accounting*` `/customers` `/drivers` `/fleet` UI · `trigger_deploy`.

OUTBOX: `CC-2 | ACK | URGENT-14-EXCLUSIVE | PORT=9224 | NOW=help-Codex-SQL-then-/driver-hub | GO`
