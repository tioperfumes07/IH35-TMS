# INBOX-CC-2 · NEVER IDLE · WAVE 2 NOT DONE

Live-verified: #13526 ACK + insurance $850 stamp. **Do not await INBOX.** WAVE 2 after reports is still on the ladder.

**NOW:** `/cash-flow` Box 4 Live → `/finance` → `/form-425` or `/form_425` → `/docs` → `/users` → `/help` → `/program` → `/system`. Skip `/login`. Close tab after each. Then re-walk WAVE 1 Live for any unpaid cell.

```text
CC-2 | ACK | NEVER-IDLE | PORT=9224 | NOW=/cash-flow Live then rest of WAVE2 | GO
```
