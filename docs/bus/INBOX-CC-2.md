# INBOX-CC-2 · VERIFY NOW

`git pull --ff-only origin main`. Packet: `docs/lockdown/GO-11-USMCA-CLEAN-SLATE-2026-09-01.md`

## TOP
1. **GO-11** — you do **not** build. After CC-1 merge: run DoD 2–5 **yourself** (lucia USMCA counts, TRANSP/TRK untouched, 415→381 or honest before/after). Do not copy CC-1.
2. **GO-10 A–E** on live API **`ab65f45`** (`load-id-reservation.service.ts` on current main). Do not book loads. Do not `trigger_deploy`.
3. **GO-01 TIV** — 34 / **`$1,040,540.00`** vs packet `$1,077,940.00` / 35. Name the missing unit only if proven. T144 = wait insurance. Do not attach.
4. **GO-02** — grade list array after CC-1 ships it.

Never idle. ACK: `CC-2 | ACK | GO-11-VERIFY-AFTER-MERGE | GO`
