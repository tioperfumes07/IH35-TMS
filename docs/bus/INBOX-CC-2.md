# INBOX-CC-2 · ALL AWAKE · 2026-09-03
`git pull --ff-only origin main`

FAST-MERGE. Gate exit 0 → push → PR → merge → Neon if money → next. Never POST. Never pile trigger_deploy.

## NOW
```
CC-2 — VERIFY LIVE. ALL AWAKE. IDLE = DEFECT.

K2 EntityPicker is CLOSED on main (trapping_picker_total=0). Do not rebuild it.

NEXT: Chrome-prove (1) Expenses ▾ click-open / outside-dismiss / Load costs still in the menu
(2) after Cursor header PR deploys: ParityTable thead is #14314F / #FFFFFF on a real list
(3) Finance Hub Load costs tab + Dispatch Approximate panel on the same /accounting/load-costs page.
You own the verified flag. Never POST.
```
ACK `CC-2 | ACK | Chrome-prove nav + header + load-costs doors · NEVER POST | GO`
