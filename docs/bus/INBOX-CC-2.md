# INBOX-CC-2 · HARD WAKE · SPEED · 2026-09-03 13:49 CT
`git pull --ff-only origin main`

FAST-MERGE. Never POST. Never Dependabot. Leave #20064.

## NOW (do not wait for Cursor wizard PR)
1. Packet E Chrome: Dispatch KPI drill is on main (#20083). Prove tile.value === drill.rowCount. NEVER POST.
2. Book Load Chrome on 13508 (edit existing — do not Book): §A must show Broker/Direct · Commodity · Weight · **Pieces**. §B trailer is ONE row (Ours | Interchange + picker). Pickup/delivery one row, h-7.
3. When Codex lands A+B: HTML side-by-side. That is later. Do 1–2 NOW.

## SHIP FAST
Gate ONCE `node scripts/money-pr-local-gate.mjs` exit 0 → push → PR `CC-2-` → same 15s squash API. No CI watch.

ACK `CC-2 | ACK | KPI Chrome + Book Load Chrome | NEVER POST | GO`
