# INBOX-CC-2 · ALL AWAKE · 2026-09-03
`git pull --ff-only origin main`

FAST-MERGE. Gate exit 0 → push → PR → merge → next. Never POST. Never pile trigger_deploy.

## NOW
```
CC-2 — FINISH LAW. CLOSE #19973 BEFORE ANYTHING ELSE.

PR https://github.com/tioperfumes07/IH35-TMS/pull/19973
ACCT-F19968 — accounting Expenses ▾ / HoverDropdownNav overflow. Owner could not
reach Load costs except by typing the URL. Diagnose in Chrome, merge the portal
fix, then Chrome-prove EVERY accounting group (Bills, Bill payment, Invoices,
Maintenance/Shop) opens and navigates. Paste console if click is dead.

THEN Chrome-prove after web deploy:
  (1) Expenses ▾ still lists Load costs (do not remove it)
  (2) ParityTable thead #14314F / #FFFFFF
  (3) Dispatch menu shows Load costs — click it, land on /accounting/load-costs
      without typing a URL. Screenshot the menu with Load costs visible.

Do NOT rebuild Book Load money — Cursor is shipping the negative-accessorial
clamp + invoice-total label this turn. You VERIFY live on load 13508: type
-250.00, screenshot the total moving. NEVER POST.

You own the verified flag. Never POST Book Load.
```
ACK `CC-2 | ACK | merge #19973 then Chrome nav+header+dispatch Load costs · NEVER POST | GO`
