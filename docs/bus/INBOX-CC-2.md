# INBOX-CC-2

## COORD 23:52Z — SEAT COMMS PROTOCOL RESET (owner, supersedes chat pings)

Orders live on **INBOX TOP**. ACK lands on **that seat's OUTBOX first line**, same hour, same NOW module — never chat, never a Cursor/Devin relay.

**JOB (unchanged):** close **accounting** to Fully-Wired 1–12 + Box 4 Live, money counted in Miss C. Then **customers** together. Seats: Codex, CC-1, CC-2, CC-3, Cursor. Devin parked.

**Correction on the owner's 23:5xZ four-OUTBOX read:** CC-2 was flagged "chat stamps never landed on OUTBOX or GitHub." As of this COORD: **they did land** — PR #13293 (rows 43379/43380, `banking.panel.linked_bank_transactions:bank`/`:gl_je` + `accounting.parity.credit_memos_page:customer`/`:reverse_link`, both real MONEY, post-merge-forensic-confirmed on `origin/main`) merged 23:46:34Z, and the `Miss C UNCHANGED blocker=...` OUTBOX line merged via PR #13300 at 23:53:26Z. The read that found them missing landed before the second merge finished — timing, not a gap. Continuing accounting Box4 Live now.

**Required this turn:** prepend ACK as OUTBOX first line: `CC-2 | ACK COORD-2352Z | NOW=accounting Box4 Live | GO`.

---

**DEVIATION:** “Queue 6 CLEAR” / 0✕ chrome is **not** Miss C. Owner still sees the same Miss C. Stamp Box 4 Live only after a real money row (CC-1 matched JE). Do not fabricate reverse_link on 0 memos.

**HOURLY:** pull + `CODER-INSTRUCTIONS-NOW.md`. OUTBOX: `Miss C was N now M` or `Miss C UNCHANGED blocker=waiting matched JE`.

**YOU OWN BOX 4 LIVE.** Devin Clicked ≠ Live. Credit = `AUDIT-COVERAGE-LIVE.md` **PROD-VERIFIED** + explicit leaf id + `Exact cells:` / `Leaves:` `` `leaf:col` ``.

**NOW accounting Queue 6**, then **banking** Box 4, then every WAVE 1 name, then WAVE 2, then WAVE 3. Same columns. Do not jump WAVE 2 while this module’s money Live is still open.

1. `accounting.parity.credit_memos_page` — honest `reverse_link` if 0 memos (do not fabricate).
2. `banking.panel.linked_bank_transactions` — `bank`/`gl_je` wait on CC-1 data; do not stamp empty-tag theater.

```text
CC-2 | ACK | STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | NOW=accounting Box4 then every WAVE | GO
```
