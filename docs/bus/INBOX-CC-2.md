# INBOX-CC-2 · NOW · 2026-08-20T03:40Z · YOU HAVE INSTRUCTIONS · NO ARCHIVE

Read `docs/bus/CODER-INSTRUCTIONS-NOW.md` §0 + §4 **before you claim idle.**

**YOU ARE CC-2. Lists / catalogs / picker law (Fully-Wired item 9).** CDP 9223.

`picker_law` PASS on drivers/customers/vendors **does not close your seat.** That is one column. Remaining Combobox hosts without `+ Add new` as the **first row** (same Lists wizard, R=W, appears+selected+reload) are still YOUR FO.

**NOW:** lists/catalogs that unblock **customers** (nested create, remaining list cards, Combobox inventory gaps).  
**THEN same turn:** vendors catalogs → remaining `lists` module Combobox/ParityDrawer hosts from surface-bar inventory guards.  
**THEN:** any unpaid `picker_law` Required cell on OWNER SEQ that is still not leaf-specific Built.

Do **not** solo-rewrite `apiRequest()` (~1863 sites). #10455 stays a **board class** for coordinated review.

```text
git pull --ff-only origin main
```

```text
CC-2 | ACK | STANDARD=MATRIX-READY | NOW=lists customers +Add new | NEXT=vendors catalogs | GO
CC-2 | WORKING | NOW=lists customers +Add new | file=<path> | GO
```

**FAST-MERGE** every PR same turn. **20-minute idle cadence = FORBIDDEN** while lists Combobox gaps remain.

**FORBIDDEN:** money (CC-1) · reverse FE (Codex) · Clicked (Devin) · CC-3/Cascade · “lane clean”
