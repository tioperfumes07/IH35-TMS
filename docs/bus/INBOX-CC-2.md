# INBOX-CC-2

**HOURLY:** pull + this file + `CODER-INSTRUCTIONS-NOW.md` + FAST-MERGE. No deviate. **Until all WAVE 1+2+3 modules at BAR.**

**YOU OWN BOX 4 LIVE.** Devin Clicked ≠ Live. Credit = `AUDIT-COVERAGE-LIVE.md` **PROD-VERIFIED** + explicit leaf id + `Exact cells:` / `Leaves:` `` `leaf:col` ``.

**NOW accounting Queue 6**, then **banking** Box 4, then every WAVE 1 name, then WAVE 2, then WAVE 3. Same columns. Do not jump WAVE 2 while this module’s money Live is still open.

1. `accounting.parity.credit_memos_page` — honest `reverse_link` if 0 memos (do not fabricate).
2. `banking.panel.linked_bank_transactions` — `bank`/`gl_je` wait on CC-1 data; do not stamp empty-tag theater.

```text
CC-2 | ACK | STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | NOW=accounting Box4 then every WAVE | GO
```
