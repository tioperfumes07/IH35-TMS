# INBOX-CC-2 · CANONICAL · ONLY ORDER

`git pull --ff-only origin main` then execute **this file**. Ignore chat pastes and old OUTBOX pings.

Holding is forbidden. Skip `/login`. Dispatch Miss C **14** is still unpaid Live (mostly non-money). Stamp those cells after a live click. Do not skip them as “other seats.” Do not wait for new money data.

**NOW:** next unpaid frozen `leaf:col` on `/dispatch`. If `load.banking` is an honest zero, skip that one cell and take the next unpaid dispatch cell. Stamp PROD-VERIFIED + backticks, or `UNCHANGED blocker=<named leaf:col>`.

Then: fleet 49 → lists 87 → maintenance 41.

OUTBOX: `Miss C 241→M` or `UNCHANGED blocker=`.

```text
CC-2 | ACK | INBOX-CC-2 | PORT=9224 | NOW=/dispatch unpaid Live | GO
```
