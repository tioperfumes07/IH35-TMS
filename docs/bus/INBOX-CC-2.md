# INBOX-CC-2 · 9224 · CERTIFY URGENT 6 LIVE NOW

You **verify Live and stamp**. Holding forbidden. Skip `/login`. FAST-MERGE 4–5 min. Unique ledger row ids.

**Owner 2026-08-21 16:05 CT: CERTIFY the Urgent 6 modules RIGHT NOW.** Fleet/lists wait.

## PASTE BOX (copy everything in the fence)

```text
===== CC-2 / GUARD LIVE · PORT 9224 · CERTIFY U6 NOW =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CC-2.md
LAW: USMCA · stamp leaf:col PROD-VERIFIED after live click OR UNCHANGED blocker=<leaf:col>
     Never HOLD. Never fabricate. FAST-MERGE 4MIN. Close the tab after the click.
CHROME: 9224 · never leave /program/matrix open

FAST-MERGE 4–5 MIN (ON — do not deviate):
  LAW: docs/bus/FAST-MERGE-4MIN-LAW.md
  1) node scripts/money-pr-local-gate.mjs MUST exit 0
  2) git push; if ONLY ENV verify-static AFTER gate PASS → git push --no-verify
  3) gh pr create — NEVER gh pr checks --watch
  4) gh pr merge N --squash --delete-branch --admin NOW
     if checkout-main fails: gh api -X PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash
  5) OUTBOX one line → next cell SAME TURN

CERTIFY = Fully-Wired 1–12 (item 12 = Live Chrome). Box 3 Built ≠ certify.
Glance matrix once per module then CLOSE.

U6 MODULES IN ORDER (Live leftover first unpaid, VERTICAL by column):
  accounting → banking → settlements → factoring → dispatch → vendors
COLUMNS: picker_law → trailer → reverse_link → customer → vendor → load → gl_je

Dispatch 14/14 ledger stamps ACCEPTED (43411–43417). After API healthz includes those
merges: prove dispatch Miss C = 0 with authenticated matrix OR stamp remaining unpaid.
Do not skip to fleet while any U6 module still has unpaid Live.

THEN rest of urgent: customers → drivers → fleet → lists.

Session flake: OUTBOX UNCHANGED blocker=session, NEXT cell same turn. Never idle on /login.

OUTBOX: CC-2 | FAST-MERGE | U6 | MOD=<module> | COL=<leaf:col> | Miss C M→N | NEXT=<leaf:col> | GO
ACK: CC-2 | ACK | INBOX-CC-2 | PORT=9224 | NOW=CERTIFY U6 Live accounting→…→vendors | GO
===== END CC-2 =====
```
