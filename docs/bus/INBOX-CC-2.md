# INBOX-CC-2 · 9224 · LANE-DONE → BANKING

Stamp Live after a real click. Never fabricate. Never HOLD. Unique ledger row ids.

**Owner 21:18 CT — THE RULE:** If you have **truly finished Accounting** reverse/picker in your lane, **continue to banking**. Do not idle for leftover-dry. `accounting.modal.decide` stays settlements-wait. No `trigger_deploy`.

Accepted on healthz `0cec933`: JE Source/Bank, register bank Ref, audit WAVE3. Honest-null `expense_number` is not a FAIL. Do not fail posting-lineage on `0cec933`.

**NOW (do not idle):**
1. Pull `origin/main`. Remaining unpaid accounting `reverse_link` / `picker_law` except `modal.decide` — stamp or UNCHANGED.
2. **If that set is empty:** `NOW=banking` reverse/picker (Urgent 6 next). Not customers/drivers/fleet/lists. Not factoring (CC-3).
3. When a **new live SHA** lands (Cursor), Live-click leftover accounting labels in the same turn as banking work — do not sit waiting for it.

## PASTE BOX

```text
===== CC-2 · PORT 9224 · LANE-DONE → BANKING =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CC-2.md
FORBIDDEN: trigger_deploy · idle leftover-dry · customers/drivers/fleet/lists · modal.decide · copy CC-3

NOW: remaining unpaid accounting reverse/picker except modal.decide
ELSE: NOW=banking reverse/picker same turn
ACK: CC-2 | ACK | INBOX-CC-2 | PORT=9224 | NOW=ACCOUNTING-or-BANKING by lane-done | GO
===== END CC-2 =====
```
