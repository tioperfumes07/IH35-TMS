# INBOX-CC-2 · FORCE · 2026-09-01 23:05 CT · VERIFY-LIVE ONLY · NO LOADS

`git pull --ff-only origin main` · live API **29072a4**

**HARD (locked):**
- **Verify-live AFTER CC-1 merges only.** You **never build money** · never migrations · never GL math.
- Jorge books loads. Seats **NEVER POST Book Load** · **NEVER create loads** · **NEVER sample fixtures**.
- Unique FINDING only (500 / dead click / silent no-op). **Never `--watch` CI.**

**Paste:** `docs/bus/PASTE-ALL-SEATS-STOP-NO-SEAT-LOADS-2026-09-01.md`

## VOID this shift
- Booking a load to “prove” plain load-ref digits (already **29072a4** on API).
- Re-diagnosing verify-static dead-port `DATABASE_URL` — **#19428 already fixed on main**; grep before reopening.
- Inventing “24 unbuilt guards” or module recertify theater. Zero escrow claims without Neon proof.

## NOW

1. **BLOCKED until CC-1 escrow PR merges** — then verify-live escrow closeout on prod (RLS app path + bypass positive control). **No zero.** File GUARD-WORKORDERS if FAIL.
2. **BLOCKED until CC-1 GO-19-02 merges** — banking count **381 visible / 34 quarantined** on existing rows. Screenshot + Neon count. No new bank rows.
3. **BLOCKED until CC-1 05/06 merges** — F+R bill linkage on **existing** bill rows only (no seat create).
4. **While blocked:** grep `#19428` verify-static sentinel; close any **unique** verify-live FAIL on non-money surfaces; append AUDIT-COVERAGE Status only on your rows.

## Evidence
Tier: PROD-VERIFIED · `[AUDIT — RE-VERIFY LIVE]` · or `UNVERIFIED — <blocker>`. Never guess.

ACK `CC-2 | ACK | FORCE | NOW=verify-live AFTER CC-1 escrow+02+05/06 · grep #19428 dead-port fixed · unique FINDING only · NEVER build money · NEVER POST Book Load · NEVER --watch | GO`
