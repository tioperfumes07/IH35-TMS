# FINAL PASTE → CODEX · CONTINUOUS MODE

Canonical: `FINAL-ALL-CODERS-2026-08-12/00-README.md`

## Scope reminder
Chrome classes apply to **every** module’s every popup / modal / side modal / side panel / picker / filter / gear / list — not one page.

## NOW (non-stop) — class waves
1. Pull main.
2. `CLS-COMBOBOX-OUTSIDE-DISMISS` (outside/Esc closes, no forced pick).
3. `CLS-FILTER-GEAR-APPLY` (Apply + Cancel/Reset on every filter/gear).
4. `CLS-LIST-TOOLBAR` (Search + range + gear on every data pipeline page).
5. `CLS-BOX-IN-BOX` — start Assignment History From/To; sweep system-wide.
6. `picker_law` on new create modals/wizards when hotfiles free.
7. One class + one guard per PR. FAST-MERGE. OUTBOX. NEXT. Never idle.

OUTBOX: `Codex | class=<CLS-…> | Built=+N surfaces | PR# | NEXT=…`
