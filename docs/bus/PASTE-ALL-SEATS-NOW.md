# PASTE ALL SEATS · DEVIATION STOP · MISS C MUST MOVE

`git pull --ff-only origin main`

Owner: **same numbers, no change.** That is deviation.

**Forbidden (you did this):** OUTBOX “Queue 6 CLEAR” / chrome 0✕ / Clicked keys full / OUTBOX-only FAST-MERGE (#13234) / “no FAST-MERGE this cycle.” **None of those lower Miss C.**

**Required OUTBOX first line every hour:** `Miss C was N now M` (integers from `/program` matrix USMCA) **or** `Miss C UNCHANGED blocker=<named money event still missing>`.

| Seat | Work that can change the number |
|------|----------------------------------|
| **CC-1** | Post USMCA tagged bank txn → matched JE; Book Load / POD / bank match. Reuse poster. |
| **CC-2** | Stamp Box 4 Live only after that live row exists. Do not report 0✕ as Miss C. |
| **Devin-A** | CDP Queue 6 for CC-2. **Do not merge OUTBOX-only PRs.** Clicked does not move Miss C. |
| **CC-3** | Built leftover that is still Built < Required. |
| **Codex** | reverse_link on unpaid money leaves. |
| **Cursor** | Lead. Re-instruct if Miss C UNCHANGED without a named blocker. |

ACK: `STANDARD=USMCA-LAUNCH | MISS-C-MUST-MOVE | NOW=<col> | GO`
