# QUEUE — CC-1 · MONEY + WIRING
# Pop = delete top OPEN when done. Cursor refills if <3 OPEN.

OPEN:
0. **LIVE CLICK** remint/settle L-0002 then L-0004 (then L-0017). LAW=GO-LIVE-CLICK-CYCLE-ONLY. No Neon/API invent. OUTBOX url|clicks|neon. NOTE (CC-1 prior pass): Kanban board + shared-types state machine show NO reachable button/lane from completed_docs_received (Completed is terminal); re-attempting per this explicit lead directive with a fresh, thorough pass (Load Detail page, Settlement tab, Pre-settlements page — not just Kanban) before re-reporting blocked.
1. Factoring batch UI + profile 97%/1.5% — live-verified factoring.factor rows ALREADY correct (97%/1.5%/1.5%); the 0.95/0.025 in code is a documented dead fallback. Real gap (if any) is a contractually-variable pass-through fee, not a hardcodable constant — reported, needs owner/lead answer
2. SETL-45 class: next completed_docs with zero lines after click re-entry
3. GL verify new chain JEs · is_sample_data honest
4. FE: Equipment Qual Create TEST-data control (is_test_data)

DONE:
- [x] DEFECT A+B code+guard #18830 (ACCT-F10159/10160), MERGED + DEPLOYED (live=88d304b)
- [x] DEFECT A+B predicates named (#18822)
- [x] Rate create → driver_finance ebe87013 + d55f85e4 + G1 flag (Neon 11:25+11:41 CT)
