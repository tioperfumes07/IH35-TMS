# SEAT OWNERSHIP LOCK · SURFACES · 2026-09-03

Owner: one seat, one surface, finish it. Find it, file it, do not fix it.
Wizard + Dispatch **HELD** until Jorge pastes Claude redesign. Cursor does **not** start Book Load STEP 2–4.

Seed CSV lives at `~/Downloads/lane-mileage-reimport-source.csv` (commit to `db/seeds/lane-mileage-reimport-source.csv` in the CC-1 PR).

Leave **#20064**. Never POST Book Load. No USMCA fixtures.

## CURSOR | LOCK FIRST (only NOW)

Rewrite `.github/CODEOWNERS` (keep existing notify paths). Guard `scripts/verify-seat-surface-ownership.mjs` + EVEN verify-step **CLAIM → MERGE → AUTHOR** (Rule 37). Prove FAIL on the 28 BookLoadModalV4 commits, then wire.

Then **STOP** on wizard until redesign paste.

## CC-1 | LOAD COSTS + MILEAGE (after lock on main)

Full vertical: lane rebuild + board + in-tab costs + money wiring. Do not touch Book Load. File wizard defects to Cursor.

## CC-2 | BANKING (after lock)

Categorization queue. Canonical `banking.*`. No silent JE. Owner clicks; no fixtures. USMCA live is uncategorized/pending, not “0 rows”.

## CC-3 | SAFETY + COMPLIANCE (after lock)

No money. CDL/med intake + duplicate report + RLS aggregate verdict. No Book Load.

## CODEX | MAINTENANCE (after lock)

No money. If OOS columns already on main, do not rebuild — prove then **unit scoping**. Report sample/CODEX rows. Never DELETE units.

## CASCADE | LISTS + REPORTS (after lock)

Build only lists/reports. Dispatch nav: **map + file:line**, do not delete routes.
