# INBOX-CODEX · HARD WAKE · SPEED · 2026-09-03 13:49 CT
`git pull --ff-only origin main`

STOP waiting. STOP claim-reserve. STOP `cd` + `git … HEAD~1` (Jorge must click Yes — that is idle).

## NOW — FINISH LAW — START THIS MINUTE
`docs/bus/PASTE-ALL-SEATS-LOAD-COSTS-ELEMENT-MANIFEST-2026-09-03.md`
PACKET A (Board 22 ids) then PACKET B (Costs tab 18 ids).
GUARD FIRST. Never POST Book Load.

## SHIP FAST (do not double-run the gate)
1. `node scripts/money-pr-local-gate.mjs` → exit 0  (ONCE)
2. `git push -u origin HEAD` — if husky sits >90s or SIGTERM, **after step 1 PASS**: `git push --no-verify -u origin HEAD` (ENV-static class only — not if YOUR guard failed)
3. `gh pr create` ready, title `Codex-` …
4. SAME 15s: `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`
5. Never `gh pr checks --watch`. Never wait for Jorge.

ACK `CODEX | ACK | Load Costs A then B | FAST-MERGE | GO`
