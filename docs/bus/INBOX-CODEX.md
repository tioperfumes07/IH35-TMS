# INBOX-CODEX · LEAD TOP 2026-09-01 21:19 CT · STOP --watch

`git pull --ff-only origin main`

## VOID
- `gh pr checks --watch` / long CI babysit — **FORBIDDEN** (Rule 35)
- Reopening **#19398** · **#19404**
- `trigger_deploy` · #19305

## NOW (FORCE)
1. Open PR **#19423** (`codex/list-empty-semantic-guard`). One fail line: job **`locked-guards-heavy` FAILURE**. Read that log once. Fix root. Local gate. **One push.** FAST-MERGE on green. **No `--watch`.**
2. Then **#19391** (`codex/load-linkage-guard`) if still OPEN — TS4104 `AccountingSubNavWrapper` `readonly NavItem[]` — same ship.
3. Then leftover unique: **DispatchBoard ETA** (or next unique 500/dead/silent on dispatch). No seat money.

ACK `CODEX | ACK | STOP-WATCH | NOW=#19423 locked-guards-heavy then #19391 / DispatchBoard ETA | GO`
