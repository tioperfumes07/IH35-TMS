# INBOX-CODEX · URGENT 6 VERTICAL BY COLUMN

`git pull --ff-only origin main` then execute **this file**. **FORBIDDEN:** another 340-guard loop, vendors/drivers/customers reverse tours, fake PASS on Checking session. #13548 is CI not Live. FAST-MERGE (~4 min). Never idle.

**Vertical (fastest):** drain one column ID on dispatch leftover, then the next column. Do not hop modules.

## YOUR PENDING

**Urgent 6: 0 of 11** — column order:

### COL `reverse_link` (5)

1. `queues.at_risk:reverse_link`
2. `queues.detention:reverse_link`
3. `queues.border:reverse_link`
4. `queues.border_history:reverse_link`
5. `docs.ocr:reverse_link`

### COL `customer` (3)

6. `queues.at_risk:customer`
7. `queues.detention:customer`
8. `docs.ocr:customer`

### COL `vendor` (2)

9. `queues.border:vendor`
10. `queues.border_history:vendor`

### COL `load` (1)

11. `docs.ocr:load`

Honest zero on empty queues/history: OUTBOX `UNCHANGED blocker=<leaf:col>` — still count it closed for U6 exit. Trailer is CC-3.

**STOP leaving U6** until **11/11** stamped or named UNCHANGED.

**Then rest of Urgent 16 same columns:** reverse_link → customer → vendor → load on fleet **48** → lists **87** → maintenance **41** → safety **7** → cash-flow **5**.

OUTBOX: `Codex | U6  n/11 | COL=reverse_link|customer|vendor|load | NEXT=<leaf:col> | GO`

```text
Codex | ACK | INBOX-CODEX | PORT=9226 | PENDING=11/11 U6 | NOW=COL reverse_link queues.at_risk | GO
```
