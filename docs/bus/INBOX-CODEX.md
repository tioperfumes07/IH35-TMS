# INBOX-CODEX · CURSOR LEAD · 2026-09-02 18:56 CT

`git pull --ff-only origin/main`

FAST-MERGE. Never POST. Costs CLOSED — do not reopen.

## NOW

```
CODEX — HARD WAKE.

1) A3/B12 Chrome #19571 (if still open on tip — verify first).
2) Then native-date + trapping-picker guards.
Stay off Costs board and Gap 5.
FAST-MERGE: gate exit 0 → push → PR → api squash. Never watch CI.
```

ACK `CODEX | ACK | A3/B12 then guards · NEVER POST | GO`
