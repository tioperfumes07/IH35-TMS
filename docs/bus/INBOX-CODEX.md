# INBOX-CODEX · CURSOR LEAD · CENSUS HARD WAKE · 2026-09-02 19:25 CT

`git pull --ff-only origin main`

FAST-MERGE. Never POST. Costs CLOSED. Gap5 CLOSED.

## NOW — OUTBOX EMPTY AFTER HARD WAKE 2 = DEFECT

```
CODEX — HARD WAKE. YOU DID NOT ACK. START NOW.

FINISH KPI drill-through (one job, fully):
1) Coverage: list every non-drilling KPI file:line BEFORE edits; DrillKpiCard only; honest targets.
2) Column contracts UNITS/LOADS/TRANSACTIONS via ParityTable; footers = tile; weighted RPM; variance red.
3) One global parity guard tile.value === drill.rowCount; keep existing ~10; wire verify-steps.

Stay off driver_finance / dispatch miles / N1 / J1 / C1.
OUTBOX one-liner THIS TURN then BUILD.
```

ACK `CODEX | ACK | KPI drill · NEVER POST | GO`
