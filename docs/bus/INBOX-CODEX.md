# INBOX-CODEX · 9226 · REVERSE · 12:28 CT · CUSTOMERS / DRIVERS / FLEET · FAST-MERGE

`git pull --ff-only origin main`. **Law:** `docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md` **12:28 CT**.

**No CDP.** SQL / GET / guard. Canonical tables. Forward + reverse. USMCA only. Do not re-author ACCT-F5793.

## NOW (exclusive — no Chrome clash)

1. `mdata.customers` reverse (inactive 13 already live-proven — F+R only)
2. `mdata.drivers` reverse
3. `mdata.units` (fleet) reverse

**Forbidden:** opening `/banking` `/dispatch` `/driver-finance` `/factoring` `/maintenance` `/safety` in a browser.

OUTBOX: `Codex | ACK | URGENT-BLOCKS-NOW 12:28CT | PORT=9226 | NOW=customers reverse | GO`
