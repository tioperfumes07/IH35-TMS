# INBOX-CODEX · 07:32 CT · NEW NOW · NEVER IDLE

**Deadline: 13:46 CT.** Worktree `/private/tmp/IH35-codex-now`. `tmux attach -t codex`. `git pull --ff-only origin main`. PREPEND ACK. Port **9226**. Chrome: one leaf, prove reverse, **close tab**.

**USMCA ONLY.** No Trucking. No Transportation. No QBO sync. No Clicked (Cursor). No Box 4 stamps (CC-2).

**Why this rewrite:** dispatch reverse was the old NOW. You already closed settlements+factoring reverse on main. **Do not wait for a new FO. Do not idle.**

**NOW = `vendors` `reverse_link` + `connectivity`** (URGENT 6 last money-adjacent catalog). Required leaves that still owe reverse: vendor **detail** AP / documents / tasks / W9 (not list chrome — see `vendors.required.json` note). Both-way drill: vendor → bill/expense/WO → **back to that vendor**. McLeod seriousness.

If a vendors reverse cell is already leaf-guarded **and** live-drilled this hour: **same turn** → `customers` reverse remainder → `drivers` → `fleet` → `lists` → safety → maintenance → insurance → legal → WAVE 2 `home`.

FAST-MERGE 4-min. Money FAIL → `OUTBOX-CC-1.md`. Picker FAIL → `OUTBOX-CC-2.md`.

```text
Codex | ACK | NEVER-IDLE | PORT=9226 | DEADLINE=13:46CT | NOW=vendors reverse+connectivity | NEXT=customers | USMCA-ONLY | GO
```
