# INBOX-CODEX · 9226 · VENDOR ARCHIVE REVERSE + MORE REVERSE · FAST-MERGE

`git pull --ff-only origin main`. No CDP. No `trigger_deploy`. Law: `docs/lockdown/CREATE-TEST-THEN-VOID-LAW-2026-08-22.md`.

**Your queue is NOT empty.** Customers/drivers reverse cited is **not** a stop.

## NOW

Code-trace + live endpoint (no Chrome CDP). Unique FINDING + FAST-MERGE.

1. **`VENDORS-SELECT-HIDES-DEACTIVATED` (mechanical half):** `apps/backend/src/mdata/vendors.routes.ts` GET `/api/v1/mdata/vendors/:id` vs prod policy `vendors_select` (`deactivated_at IS NULL`). Vendor `308f6434-0a51-4109-953e-c86ffb1f0999` is deactivated USMCA and still linked from vendor credits. If the fix is **RLS/migration**, write the exact OPEN prescription for CC-1 and ship any **FE** honesty in `VendorDetail.tsx` (404 ≠ generic Failed to load) in **your** lane. Do not invent GL math.
2. Reverse unpaid Accounting More: `/accounting/credit-memos`, `/accounting/period-close`, `/accounting/cash-forecast`, `/accounting/revenue-recognition` — dead reverse / wrong table / 500.
3. Next unpaid reverse on the board. **Forbidden:** hold · empty-queue · re-poll-only.

## PASTE BOX

```text
===== CODEX · PORT 9226 · FAST-MERGE · VENDOR ARCHIVE + MORE REVERSE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CODEX.md
NOW: vendors_select deactivated GET 404 + credit-memos/period-close/cash-forecast reverse
FORBIDDEN: CDP · hold · empty-queue · wait for Jorge
ACK: Codex | ACK | INBOX-CODEX | PORT=9226 | NOW=vendor archive reverse | GO
===== END CODEX =====
```
