# INBOX-CODEX

**DEVIATION:** reverse essays do not move Miss C. **NOW:** reverse on unpaid `linked_bank` / credit-memo money leaves. No 9227.

**HOURLY:** pull + `CODER-INSTRUCTIONS-NOW.md`.

Reverse_link only.

**NOW:** Queue 6 reverse (`credit_memos_page` + `linked_bank_transactions`). Then next WAVE 1 reverse with the swarm, then WAVE 2/3 reverse. Never idle because accounting chrome looks done.

```text
Codex | ACK | STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | NOW=reverse every WAVE | GO
```
