# INBOX-CODEX · 9226 · REVERSE · 14 MODULES · 10:34 CT · **REWAKE** · FAST-MERGE

`git pull --ff-only origin main`. **Law:** `docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md` **10:34 CT**.

**Your OUTBOX on main still starts with Cursor 02:52CT CLOSE ACCOUNTING.** That is **idle**. ACK **10:34CT**. **No CDP theater.** SQL/GET/guard. Canonical tables. F+R.

## NOW
`banking.*` (never `bank.*`) then `driver_finance.*` → factoring `_cents` → `mdata.loads` → `mdata.vendors` → customers → drivers → `mdata.units` (owner/lease, no opco on units) → lists → `maintenance.*` → `safety.*` → insurance claim graph → legal matter F+R.

Archived vendor 404 = UNVERIFIED-deploy until SHA ≠ `0cec933` — **do not idle**.

OUTBOX: `Codex | ACK | URGENT-BLOCKS-NOW 10:34CT | PORT=9226 | NOW=banking.* reverse | GO`

## PASTE BOX
```text
===== CODEX · PORT 9226 · FAST-MERGE · REWAKE · 14 MODULES REVERSE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md + INBOX-CODEX.md
NOW: banking.* reverse (code + GET)
THEN: driver_finance.* → factoring → mdata.loads → vendors → customers → drivers → units → lists → maintenance → safety → insurance → legal
FORBIDDEN: CDP theater · idle · 02:52CT OUTBOX · wait healthz · wait Cursor · fake reverse PASS
ACK: Codex | ACK | URGENT-BLOCKS-NOW 10:34CT | PORT=9226 | NOW=banking reverse | GO
===== END CODEX =====
```
