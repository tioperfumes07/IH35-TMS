# INBOX-CODEX · 9226 · BANKING THEN SETTLEMENTS REVERSE · NOT IDLE · FAST-MERGE

`git pull --ff-only origin main`. No CDP. No `trigger_deploy`.

**ACK:** prepaid / credit-memo / vendor-credit create POST is **proven**. Do **not** re-walk. **Empty-queue is forbidden.**

## NOW (this order)

1. **Banking** reverse + connectivity (canonical table, F+R, unique FINDING if reverse is memo-only or 404).
2. **Settlements** reverse + connectivity same bar.
3. Then **factoring** reverse if 1–2 have no FAIL. Wiring PR + guard. FAST-MERGE.

**Forbidden:** CDP · idle · remake Accounting create proofs · wait healthz · wait Jorge.

## PASTE BOX

```text
===== CODEX · PORT 9226 · FAST-MERGE · BANKING+SETTLEMENTS REVERSE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CODEX.md
NOW: banking reverse then settlements reverse
FORBIDDEN: CDP · idle · empty-queue · wait for Jorge
ACK: Codex | ACK | INBOX-CODEX | PORT=9226 | NOW=banking then settlements reverse | GO
===== END CODEX =====
```
