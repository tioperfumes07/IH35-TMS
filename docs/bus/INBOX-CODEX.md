# INBOX-CODEX

## COORD 23:52Z — SEAT COMMS PROTOCOL RESET (owner, supersedes chat pings)

Orders live on **INBOX TOP**. ACK lands on **that seat's OUTBOX first line**, same hour, same NOW module — never chat, never a Cursor/Devin relay.

**JOB (unchanged):** close **accounting** to Fully-Wired 1–12 + Box 4 Live, money counted in Miss C. Then **customers** together. Seats: Codex, CC-1, CC-2, CC-3, Cursor. Devin parked.

**Owner's 23:5xZ read of your OUTBOX:** last entry was `WORKING customers reverse NEXT=vendors` — wrong module. Fix now: ACK this COORD, NOW=accounting reverse (Queue 6: `credit_memos_page` + `linked_bank_transactions` reverse_link) before customers or vendors.

**Required this turn:** prepend ACK as OUTBOX first line: `Codex | ACK COORD-2352Z | NOW=accounting reverse | GO`.

---

**DEVIATION:** reverse essays do not move Miss C. **NOW:** reverse on unpaid `linked_bank` / credit-memo money leaves. No 9227.

**HOURLY:** pull + `CODER-INSTRUCTIONS-NOW.md`.

Reverse_link only.

**NOW:** Queue 6 reverse (`credit_memos_page` + `linked_bank_transactions`). Then next WAVE 1 reverse with the swarm, then WAVE 2/3 reverse. Never idle because accounting chrome looks done.

```text
Codex | ACK | STANDARD=USMCA-LAUNCH | ALL-MODULES | SWARM-ONE-MODULE | NOW=reverse every WAVE | GO
```
