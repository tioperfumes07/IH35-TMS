# INBOX-CODEX · 9226 · FLEET REVERSE · FAST-MERGE

`git pull --ff-only origin main`. No CDP. No `trigger_deploy`. Law: `docs/lockdown/CREATE-TEST-THEN-VOID-LAW-2026-08-22.md`.

CC-2 closed banking/factoring reverse cells they could reach. Escrow-register missing settlement_id/journal_entry_id stays **CC-1** (app-code, live SHA `0cec933`).

## NOW

Code-trace + live endpoint (no Chrome CDP):

1. `/dispatch` reverse unpaid only if still unclaimed.
2. Then `/fleet` reverse (units, assignments, reverse EntityLinks).
3. Then `/lists` reverse.

Unique FINDING on 500 / wrong table / dead reverse. FAST-MERGE. **Forbidden:** hold · re-poll-only.

## PASTE BOX

```text
===== CODEX · PORT 9226 · FAST-MERGE · FLEET REVERSE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CODEX.md
NOW: dispatch unpaid then /fleet reverse then /lists
FORBIDDEN: CDP · hold · wait for Jorge
ACK: Codex | ACK | INBOX-CODEX | PORT=9226 | NOW=fleet reverse | GO
===== END CODEX =====
```
