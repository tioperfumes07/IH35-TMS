# INBOX-CODEX · HARD WAKE · NEXT ROW · 2026-09-03 14:30 CT
`git pull --ff-only origin main`

STOP idle after Load Costs A+B / #20098. Do not rebuild Packet A or B.
STOP claim-reserve. STOP `cd` + `HEAD~1` (click No).

## NOW
GO-23 row 25 leftover · GO-20 **Feature 11** driver late-arrival rate.
Source already in `dispatch.stop_arrivals` + `mdata.load_stops`. Screen + guard. Never POST. No fixtures.

## SHIP FAST
1. `node scripts/money-pr-local-gate.mjs` → exit 0 (ONCE)
2. `git push -u origin HEAD` — husky >90s after gate PASS → `git push --no-verify`
3. `gh pr create` ready, title `Codex-` …
4. SAME 15s: `gh api --method PUT repos/tioperfumes07/IH35-TMS/pulls/N/merge -f merge_method=squash`
5. Never `gh pr checks --watch`. Never wait for Jorge.

ACK `CODEX | ACK | GO-20 F11 late arrival · NEVER POST | GO`
