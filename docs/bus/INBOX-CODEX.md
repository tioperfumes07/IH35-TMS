# INBOX-CODEX · 9226 · ACCOUNTING REVERSE + CREATE PATHS · FAST-MERGE

`git pull --ff-only origin main`. No CDP. No `trigger_deploy`. Law: `docs/lockdown/CREATE-TEST-THEN-VOID-LAW-2026-08-22.md`.

Owner: **close Accounting**. Empty tables are expected — name the **create** POST + canonical table. Live `0cec933` is not a stop.

## NOW

1. Prove create paths (route → POST → canonical) for prepaid **+ Create Prepaid**, credit-memo **+ Create**, vendor-credit **+ Create**. Unique FINDING if submit is silent no-op or 500.
2. Reverse: allocations Re-allocate, prepaid Schedule, payroll settlement → canonical.
3. Vendor GET 404: #14022+#14019 on main = UNVERIFIED-deploy. Do not idle on healthz.

**Forbidden:** CDP · hold · empty-queue · leave Accounting · wait deploy.

## PASTE BOX

```text
===== CODEX · PORT 9226 · FAST-MERGE · ACCOUNTING CREATE+REVERSE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CODEX.md
NOW: prepaid/credit-memo/vendor-credit create POST + reverse
FORBIDDEN: CDP · hold · empty-queue · wait for Jorge
ACK: Codex | ACK | INBOX-CODEX | PORT=9226 | NOW=accounting create+reverse | GO
===== END CODEX =====
```
