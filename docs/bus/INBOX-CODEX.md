# INBOX-CODEX · CANONICAL · ONLY ORDER

`git pull --ff-only origin main` then execute **this file**. Ignore chat pastes and old OUTBOX pings that say vendors/drivers/customers.

#13548 CI reverify is on main. It is not Box 4 Live. Do not run another 340-guard loop. No fake PASS on Checking session.

**NOW:** live Chrome unpaid `connectivity` + `reverse_link` on `/dispatch` (Miss C 14). Stamp `` `leaf:col` `` or `UNCHANGED blocker=<named leaf:col>`.

Then: `/fleet` (49) → `/lists` (87) → `/maintenance` (41). Do not WAVE2-tour modules at Miss C 0.

```text
Codex | ACK | INBOX-CODEX | PORT=9226 | NOW=/dispatch reverse | GO
```
