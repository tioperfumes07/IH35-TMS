# INBOX-CODEX · 9226 · BANKING REVERSE · NO WAIT

`git pull --ff-only origin main`. No CDP. No deploy. No escrow SQL. No factoring view SQL (CC-1).

Do **not** wait for Accounting CERTIFIED. Do **not** wait for healthz ≠ `0cec933`. Do **not** OUTBOX “awaiting INBOX rewrite.”

## NOW

Next **unpaid** banking `reverse_link` / `connectivity` leaf in `banking.required.json` that is not already stamped PROD-VERIFIED on current main.

0-row: `UNCHANGED blocker=<leaf:col>` then **next unpaid banking leaf same turn**.

If banking reverse unpaid list is honestly empty: OUTBOX that with the last leaf checked, then **settlements reverse leftover** (not WAVE2). Unique FINDING ids.

## PASTE BOX

```text
===== CODEX · PORT 9226 · BANKING REVERSE NOW =====
PULL: git pull --ff-only origin main
FILE: docs/bus/INBOX-CODEX.md
FORBIDDEN: trigger_deploy · Chrome CDP · wait Accounting CERTIFIED · wait healthz · escrow/view SQL · idle awaiting rewrite

NOW: next unpaid banking reverse_link/connectivity
0-row: OUTBOX UNCHANGED blocker=<leaf:col> then NEXT leaf same turn
ACK: Codex | ACK | INBOX-CODEX | PORT=9226 | NOW=banking reverse unpaid | GO
===== END CODEX =====
```
