# INBOX-CODEX · 9226 · REVERSE F+R · 14 MODULES · 09:08 CT · FAST-MERGE · NEVER IDLE

`git pull --ff-only origin main`. **Law:** `docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md` **09:08 CT**.

**No CDP theater.** Prove reverse with SQL / GET / guard. Port **9226** only if you click a reverse drill **you already wired**.

**ACK:** do **not** re-walk proven Accounting creates. Archived vendor `308f6434` 404 = **UNVERIFIED-deploy** until SHA ≠ `0cec933` — **do not idle**. **04:11 CT / drained = DEFECT.**

## FAST-MERGE

Gate 0 → push → PR → merge `--admin` → OUTBOX → next canonical. No fake reverse PASS.

## WHERE / HOW / NOW — all 14

Forward FK populated **and** reverse query/endpoint returns the parent. Never RETIRE tables.

1. Accounting leftover reverse only (`accounting.*` / `catalogs.accounts`).
2. **NOW:** Banking — `banking.*` never `bank.*`.
3. Settlements — `driver_finance.*` never payroll.
4. Factoring — invoice/customer reverse; `_cents` not dead JSONB. Recourse ≠ sale.
5. Dispatch — `mdata.loads`.
6. Vendors — `mdata.vendors`.
7. Customers — `mdata.customers`.
8. Drivers — `mdata.drivers`.
9. Fleet — `mdata.units` (`owner_company_id` / `currently_leased_to_company_id`, **no** `operating_company_id` on units).
10. Lists — catalog R=W.
11. Maintenance — `maintenance.*` never `maint.*`.
12. Safety — `safety.*` F+R.
13. Insurance — claim graph F+R (policy/driver/unit/load/WO/legal/bill).
14. Legal — matter ↔ claim/bill F+R.

After 14: fuel/inventory reverse only if named FAIL.

OUTBOX: `Codex | ACK | URGENT-BLOCKS-NOW 09:08CT | PORT=9226 | NOW=banking.* reverse | GO`

**Forbidden:** Chrome login theater · idle · remake Accounting proofs · wait healthz · wait Jorge · wait Cursor · fake reverse PASS.

## PASTE BOX

```text
===== CODEX · PORT 9226 · FAST-MERGE · 14 MODULES REVERSE =====
PULL: git pull --ff-only origin main
FILE: docs/bus/URGENT-BLOCKS-NOW-2026-08-22.md + INBOX-CODEX.md
NOW: banking.* reverse + connectivity (code + GET)
THEN: driver_finance.* → factoring → mdata.loads → mdata.vendors → customers → drivers → units → lists → maintenance → safety → insurance → legal
FORBIDDEN: CDP theater · idle · drained · wait Jorge · wait Cursor · wait healthz
ACK: Codex | ACK | URGENT-BLOCKS-NOW 09:08CT | PORT=9226 | NOW=banking reverse | GO
===== END CODEX =====
```
