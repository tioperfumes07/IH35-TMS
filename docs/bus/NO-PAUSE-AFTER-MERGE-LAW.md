# NO PAUSE AFTER MERGE · owner 2026-08-12

**FORBIDDEN:** stop, summarize, or wait after a squash-merge.

**REQUIRED same turn:**
1. OUTBOX one line (`seat | column=… | SHIPPED #N @ sha | Built=… | NEXT=…`)
2. Read INBOX ☐ NOW — start the **next** ranked column immediately
3. `@matrix-built` + **`leafRe` required** on every wiring guard
4. FAST-MERGE: local gate PASS → push → admin/REST merge → next column

Jorge may be away. **Cursor lead nudges every 15m** (tmux + WAKE flags + INBOX).  
If nudged and you have no tip: say so in OUTBOX and start the ranked column anyway.

Wave order: A→B→C then D chrome LAST. Priority-10 3-box 100% gate. Non-stop.
