## FINDING

Jorge order: remind all coders of FAST-MERGE ~4–5 min loop + deploy every 5–10 merges. Stale MILES-INVERT bus text still said pay practical+empty; owner law is driver pay = short miles always with OK popup before trusting corrupted catalog short.

## Summary

- Add identical **FAST-MERGE + DEPLOY** top block to all seat INBOXes (+ DEVIN-A retired, FEED/NOW-CURSOR, OUTBOX-CURSOR).
- New `docs/bus/FAST-MERGE-REMINDER-2026-09-02.md` (6-bullet loop).
- Sync MILES-INVERT-01 + CC-1/CC-2/CODEX/CURSOR INBOX: driver pay = short miles; never practical+empty.
- Book Load: inline inversion flag on MilesStrip + OK-only `MilesInvertAckDialog` when short > practical or reverse-lane fill (MILES-INVERT-01 owner UX).

## LIVE PROOF

- Docs: every `docs/bus/INBOX-*.md` opens with FAST-MERGE block citing `FAST-MERGE-4MIN-LAW.md`.
- FE: `BookLoadModalV4` shows `data-testid="miles-invert-ack-dialog"` on lane autofill when short > practical; OK-only dismiss.

## Test plan

- [x] Gate / preflight exit 0
- [ ] CC-2 Chrome-prove Indianapolis→Laredo autofill + flag + popup on deploy batch

Never POST Book Load. USMCA only.
