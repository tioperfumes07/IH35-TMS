# INBOX-DEVIN-A

**HOURLY:** pull + re-read this file + `CODER-INSTRUCTIONS-NOW.md` + FAST-MERGE law. Continuous. No deviate.

**STOP the old loop** (`node scripts/ops/devin-a-live-loop.cjs` that still `reset --soft`). Kill it. Pull this INBOX. Restart **only** after `scripts/ops/devin-a-live-loop.cjs` on origin/main contains `shipClickedOntoMain`.

## HARD — never silently revert CC-1/CC-3 (STALE-BRANCH-MERGE-SILENTLY-REVERTS-CONCURRENT-WORK / DEVIN-A-STALE-BRANCH-REPEATEDLY-DELETES-MERGED-CODE-FIXES)

#10800 (customers MoneyInput) was reverted by stale Clicked squash-merges #10802 / #10834.

**Before every Clicked merge:** `git fetch origin main` then **hard-reset this dedicated worktree to `origin/main`**. Commit **only** `docs/bus/OUTBOX-DEVIN.md`. Push a **new** `devin-a/clicked-<leaf>-<ts>` branch. Tip **must contain** `origin/main`. `git merge-base --is-ancestor origin/main HEAD`.

**Forbidden:** `git reset --soft origin/main` from a stale tip · recycling `devin-a/live-outbox-proofs-32` · merging branches cut before other seats landed · cwd `IH35-TMS-clean` with dirty customer/accounting files.

**cwd:** `/tmp/IH35-devin-a` or `DEVIN_GIT_ROOT`. Restart loop after `git pull` so `scripts/ops/devin-a-live-loop.cjs` from **current main** is what runs.

chrome=9227. Item 12. `leaf=<module>:<leafId>:<col> | USMCA`

**NOW:** WAVE 1 banking Clicked → factoring → accounting → settlements → customers → drivers  
**THEN:** WAVE 2 insurance → legal → lists → safety → fleet → vendors → maintenance → dispatch  
**THEN:** WAVE 3. Never fuel. Never idle.

```text
Devin-A | ACK | STANDARD=USMCA-LAUNCH | WAVE1-THEN-2 | HOURLY-REREAD | chrome=9227 | NOW=banking Clicked | ONTO-MAIN-OUTBOX-ONLY | GO
```
