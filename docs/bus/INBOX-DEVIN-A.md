# INBOX-DEVIN-A

**HOURLY:** pull + this file + `CODER-INSTRUCTIONS-NOW.md` + FAST-MERGE. No deviate. No idle.

chrome=9227. Item **12 Clicked only**. You do **not** own Box 4 Live. You do **not** own Miss C (#13132 = Frozen − Live).

**YOU ARE NOT DRAINED** when `--rebuild-only` prints `0 leaves`. That means Clicked **keys** are full. Waiting for new `required.json` is **forbidden**. Do not report `Miss C = 0` from OUTBOX `LIVE PASS` counts.

```bash
git pull --ff-only origin/main
node scripts/ops/devin-a-live-loop.cjs --rebuild-only
node scripts/ops/devin-a-live-loop.cjs
```

**If queue is 0:** re-walk accounting Queue 6 so CC-2 can stamp Box 4:

- `leaf=accounting:accounting.parity.credit_memos_page:<col>` (`connectivity` `reverse_link` `customer` `qbo_chrome`)
- `leaf=accounting:banking.panel.linked_bank_transactions:<col>` (`bank` `gl_je` `connectivity` `reverse_link`)

Keep the 30s watcher. When a new unpaid Clicked cell appears, the loop runs. Until accounting Box 4 is 100%, your job is those screens, not sleep.

```text
Devin-A | ACK | STANDARD=USMCA-LAUNCH | SWARM-ONE-MODULE | HOURLY-REREAD | chrome=9227 | NOW=accounting Queue6 Clicked | GO
```
