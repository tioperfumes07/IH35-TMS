# INBOX-DEVIN-A · FORCE · 2026-09-01 23:05 CT · CHROME CANCEL ONLY · NO POST

`git pull --ff-only origin main` · live API **29072a4**

**HARD (locked):**
- **NEVER POST Book Load.** **NEVER create a load.** **NEVER sample/demo.** Jorge owns first real typed Load #.
- Chrome = **optional miles check then CANCEL** — abort any plan that saves.

**Paste:** `docs/bus/PASTE-ALL-SEATS-STOP-NO-SEAT-LOADS-2026-09-01.md`

## VOID this shift
- POST /book-load / persist wizard. CREATE-TEST keep-until-launch does **not** authorize seat loads.
- Leaving browser open idle without filing a FINDING.

## NOW (Chrome optional — Cancel mandatory)

1. Hard-reload `https://app.ih35dispatch.com/dispatch` on live **29072a4**.
2. Open **+ Book Load** (Chrome only — do not POST).
3. Origin **Laredo, TX** → Destination **Denton, TX** — expect Practical **456.7** / Short **452.2** if GO-16 seed live (UNVERIFIED until click).
4. Add delivery stop **Chicago, IL** — **Check ZIP boxes stay empty** (GO-16 Rev B).
5. **CANCEL** — close modal without save. **Zero POST.**

## If idle after Cancel pass
- Walk live USMCA pages for **unique** 500 / dead click / silent no-op → **FILE OPEN row** `docs/audit/GUARD-WORKORDERS.md` with URL + repro. Do not create loads to unblock.

ACK `DEVIN-A | ACK | FORCE | NOW=/dispatch Chrome Laredo→Denton miles check · Chicago Check ZIP empty · CANCEL · NEVER POST · idle=unique FINDING to board | GO`
