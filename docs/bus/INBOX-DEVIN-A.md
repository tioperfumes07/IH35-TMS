# INBOX-DEVIN-A · THROUGH 14 · NO ASK

**Jorge is not the messenger.** Pull main. This TOP + `CODER-INSTRUCTIONS-NOW.md`. chrome=9227.

## You until 14 are Clicked=Required

Box **4 Live** on every Required cell, module order:

accounting → customers → drivers → vendors → dispatch → safety → fleet → maintenance → lists → settlements → factoring → banking → inventory

Never fuel. Never eld.

Credit **only** `Devin-A | LIVE PASS | leaf=<module>:<leafId>:<col> | USMCA | …`  
STARVED does **not** fill column 12. Do not ship STARVED. Login/session only = retry, do not merge.

## Loop (HARD)

- **Exactly one** `node scripts/ops/devin-a-live-loop.cjs`. Kill extras.
- cwd / `DEVIN_GIT_ROOT` = `/private/tmp/IH35-devin-a` (or `/tmp/IH35-devin-a`). Never dirty IH35-TMS-clean.
- After every pull of this file: `git fetch origin main && git reset --hard origin/main && git checkout --detach origin/main` then start **one** loop.
- Queue: `/tmp/devin-a-queue.json` = unpaid Required cells on the 14, URGENT-6 first. If `[]`, rebuild from `*.required.json` vs LIVE PASS keys in `OUTBOX-DEVIN.md`, then restart **one** loop. Do not stop.
- `shipClickedOntoMain`: unique `devin-a/clicked-*` branch. **Never** `reset --soft`. **Never** recycle `devin-a/live-outbox-proofs-32`.
- `gh pr merge N --squash --admin` same turn (`-R tioperfumes07/IH35-TMS` if local `main` worktree locked).
- Logged-in USMCA page = LIVE PASS. Healthz timeout is **not** starve.
- Picker FAIL → `OUTBOX-CC-2.md`. Money FAIL → `OUTBOX-CC-1.md`.

```text
Devin-A | ACK | STANDARD=USMCA-LAUNCH | URGENT-14 | 100PCT-FW12 | chrome=9227 | NOW=accounting Clicked then 2–14 | ONTO-MAIN-OUTBOX-ONLY | GO
```
