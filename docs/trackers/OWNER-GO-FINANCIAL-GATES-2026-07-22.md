# Owner GO — financial gates + QBO allowlist (2026-07-22)

Owner chat (2026-07-22 morning):

> everything should be working, it has already been stated, so flip flags on all companies.
> 3-ok go [QBOBulkLinkPage QboCombobox allowlist]
> 4-i need specific detailed examples [DriverDetail / VehicleProfile qbo_vendor_id]
> 5-that is correct.....then do exhaustive audit [createKind driver = CreateDriverModal only]

## 1–2. Financial gates UNGATED (all companies)

| Const | File | Was | Now |
|-------|------|-----|-----|
| `CC_BILL_PAYMENT_GATED` | `apps/frontend/src/pages/accounting/bill-payments/CCPaymentModal.tsx` | `true` | **`false`** |
| `ACCOUNT_CREATE_GATED` | `apps/frontend/src/components/parity/drawers/NewAccountDrawerForm.tsx` | `true` | **`false`** |

Scope: **all operating companies** (UI gate was global const — not per-entity). Backend CC route + CoA catalog create were already mounted; only the FE submit locks were holding.

Guard: `scripts/verify-owner-financial-gates-ungated.mjs` (+ verify-step 1237).

**Note for open PR #3212 (CHROME-13):** its guard currently asserts `CC_BILL_PAYMENT_GATED === true`. After this lands, that assert must change to `false` (or drop the gate lock from the chrome guard). Claude: reconcile on merge.

## 3. QBOBulkLinkPage — ALLOWLIST confirmed

**KEEP** `QboCombobox` on `apps/frontend/src/pages/lists/accounting/QBOBulkLinkPage.tsx` for QBO vendor + QBO class columns.

**Why:** that page writes **QBO entity ids** (`qbo_vendor_id`, `qbo_class_id`) into the bulk-link payload — not `mdata.vendors` / TMS class UUIDs. Forcing `ReferenceSelect` would select the wrong id space.

Also treat as same allowlist class (QBO-id linkage, not TMS catalog pickers):

- `apps/frontend/src/pages/admin/QboVendorLinkagePage.tsx` + `VendorLinkageModal`
- Driver/Unit QBO mapping fields until owner picks model A/B/C (see examples doc)

## 4. qbo_vendor_id — examples only (no build yet)

See `docs/trackers/QBO-VENDOR-ID-PICKER-EXAMPLES-2026-07-22.md`.

**OWNER DECISION: A** (DriverDetail + VehicleProfile same). Allowlist confirmed — no picker rewrite.

## 5. Driver nested +Create — audit first

See `docs/trackers/DRIVER-PICKER-SYSTEM-AUDIT-2026-07-22.md` (generated by `scripts/inventory-driver-pickers.mjs`).

Next build (separate PR after review): `PLUS-DRIVER-MONEY` then dispatch/safety waves — always `CreateDriverModal`, never a second thin form.
