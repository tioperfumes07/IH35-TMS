# SAF-ORPH-05 — Safety tab wizard depth (remaining)

**Status:** HOLD

## Closed (Layer A)
- All **28** `SAFETY_GROUPS` canonical `/safety/*` routes mounted (`verify-saf-orph05-safety-tab-route-mount` step **2204**).
- Authenticated reachability proven earlier (28/28 HTTP 200; Safety home + HOS live smoke TRANSP/USMCA).
- Leave Balances list wired (step **2206**).
- Weekend scenarios exercised Safety event create + load reverse linkage (Cascade FAIL-S1).

## Remaining (future vertical polish — not blocking module wire certify)
Full DoD-B wizard depth + both-way linkage smoke on **every** Safety tab, both entities.

**Future block:** `SAF-ORPH-05-DEPTH`
