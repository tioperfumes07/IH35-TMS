# NEON APPLY PACKET — BANK-DOM-06 `202609150000` (ready when owner says APPLY)

**PR:** https://github.com/tioperfumes07/IH35-TMS/pull/3602  
**Branch:** `hold/bank-dom-06-fuel-expense-overage`  
**Migration:** `db/migrations/202609150000_bank_dom_06_fuel_card_overage_driver_recovery.sql`  
**Prod:** `tiny-field-89581227` / `br-fancy-credit-akjnd07a`  
**Role:** `neondb_owner` **non-pooler** only (pooler/`ih35_app` cannot ALTER)

## Do NOT apply until Jorge says: `APPLY BANK-DOM-06`

## After apply (Cursor checklist)

1. Run migration SQL once as neondb_owner (non-pooler).
2. Ledger-backfill row for `202609150000` (same pattern as prior batch).
3. Flip `.held-migrations.json` → `applied_on_prod: true` for this file (stamp PR or same PR follow-up).
4. Prove: `to_regclass('fuel.fuel_card_overage_policies')` exists; `driver_finance.driver_settlement_deductions.source_fuel_transaction_id` column exists (lucia bypass if needed).
5. **Do not** flip `FUEL_CARD_OVERAGE_RECOVERY_ENABLED` until owner sets policy limit rows + answers A3.
6. Live $0.05 overage→deduction proof = UNVERIFIED until flag ON + policy seeded.

## Policy seed (after schema live — separate from DDL)

Owner designates per company:
- `per_transaction_limit_cents` (required for any recovery)
- `recover_non_fuel_purchases` (default false)

Zero rows = no-op (safe).

## Related

- Fuel expense→GL half already on main (no re-apply).
- BANK-DOM-05 not in this packet.
