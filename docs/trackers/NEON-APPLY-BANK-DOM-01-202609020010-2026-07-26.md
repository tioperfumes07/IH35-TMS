# Neon apply — BANK-DOM-01 / SWEEP-C2 matches (2026-07-26)

**File:** `202609020010_c2_banking_reconciliation_matches_canonical.sql`  
**Already on main:** #3577  
**Applied by:** Cursor · `neondb_owner` non-pooler · `cursor-owner-batch-2026-07-26`

## Live proof (`br-fancy-credit-akjnd07a`)

| Check | Result |
|---|---|
| `banking.reconciliation_matches` | **EXISTS** |
| `bank.reconciliation_matches` | still present (archive-only, Rule 07) |
| Row copy delta | **0** (both empty — expected) |
| FORCE RLS policy | present |
| Backend writers | already on `banking.*` (#3577) |

Ledger: `_system._schema_migrations` + `ih35_migrations.applied_migrations`.

**Claude:** BANK-DOM-01 schema half is done — verify 0 `bank.reconciliation_matches` writes remain; do not recreate table.  
**Devin:** when touching held registry for this file, mark applied on prod — do not re-run SQL.
