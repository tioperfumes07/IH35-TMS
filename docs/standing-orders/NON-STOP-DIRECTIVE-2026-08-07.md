# NON-STOP DIRECTIVE — owner, re-issued 2026-08-07

ALL CODERS (CC-1 money, CC-2 mechanical, CC-3 USMCA verifier): work NON-STOP.
Finish a task -> IMMEDIATELY pull the next OPEN board item / class / create-surface -> keep going.
Never idle, never wait for approval, never stop to ask Jorge. The only stop is a genuine owner
business/accounting/go-live decision (state it plainly in ONE line) or Neon/app unreachable.

## MISSION (this phase)
Get EVERYTHING properly and COMPLETELY CONNECTED, WIRED, LINKED, and FIXED — both-way linkage +
real economics + no orphans + no silent failures. This is a TOTAL DEEP wiring / linkage /
connectivity audit — 2-routed AND single connectivity — module by module, tab by tab, scenario by
scenario. NOT a surface or mid-surface sweep. Walk every scenario and check ALL linkages in each.

## METHOD
Create every possible transaction in every module/tab/sub-tab (USMCA). The instant it is created,
verify it registers: balanced JE in the GL, both-way linkage present, real economics. Not green =
a wiring gap -> fix the ROOT CAUSE once in the shared helper (fix-once-lands-everywhere) + add a
mutation-proven guard + register in docs/law/LAW.json -> recreate -> repeat until green.
Done = live proof on Neon + app. NOT CI-green. NOT merged.

## RULES
- USMCA-only, from zero. USMCA shares the SAME driver/truck/GPS-Samsara data as transportation
  (carried over, NOT rebuilt) but keeps its OWN books.
- NO QuickBooks / NO mapping. Account-resolution IS wiring: create accounts as needed (owner edits
  later), never block on a missing account.
- Never defer, never patch — fix root cause.
- Prod writes need the lucia bypass + a RETURNING/row-count control in the SAME tx, or the write is
  UNVERIFIED. A 0/empty is never a verdict without the completeness discriminator on the same table.

## FLOW
Findings -> this board (docs/audit/GUARD-WORKORDERS.md) -> owning lane (money->CC-1, mechanical->CC-2),
NEVER through Jorge. Cascade audits deep connectivity + merges as non-author. The orchestrator keeps
you fed and merges green PRs + fixes/routes conflicts. Never end a turn "awaiting instructions" while
lane work remains.
