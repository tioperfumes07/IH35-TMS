# IH35 TMS

Primary verification command (local and CI parity):

- `npm run verify:pre-commit`
- `npm run verify:migration-application-consistency`

Verification infrastructure details:

- `docs/verification.md`
