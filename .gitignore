CLAUDE.md
node_modules
dist/
*.tsbuildinfo
.DS_Store
apps/frontend/src/generated/module-completion.ts
apps/frontend/src/generated/
docs/module-completion/*.md
docs/certification/*.cert.json
!docs/certification/.gitkeep

docs/audits/PASS-8-*
.cursor/rules/00-agent-bus-inbox-CASCADE.mdc
tmp-from-load-zero-*/

# GATE-LIVELOCK-01 — derived, regenerated in ~0.3s by ensureFreshGateStepMap() on every scoped
# local pre-push run; committing it would be a constant merge-conflict generator across this
# repo's many concurrent branches for zero benefit (never read by CI).
scripts/.gate-step-map.json
