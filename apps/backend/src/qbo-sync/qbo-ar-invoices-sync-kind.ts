/** Kind written to qbo.sync_runs for Stage-1 inbound QBO Invoice mirror pulls (QBO→TMS). */
export const QBO_AR_INVOICES_MIRROR_SYNC_KIND = "qbo_ar_invoices_inbound_mirror";

/** Kind written to qbo.sync_runs for Stage-2 mirror → accounting.invoices projection (subledger only). */
export const QBO_AR_INVOICES_PROJECTION_SYNC_KIND = "qbo_ar_invoices_projection";
