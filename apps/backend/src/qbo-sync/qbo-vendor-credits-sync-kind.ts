/** Kind written to qbo.sync_runs for Stage-1 inbound QBO VendorCredit mirror pulls (QBO→TMS). */
export const QBO_VENDOR_CREDITS_MIRROR_SYNC_KIND = "qbo_vendor_credits_inbound_mirror";

/** Kind written to qbo.sync_runs for Stage-2 mirror → accounting.vendor_credits projection (subledger only). */
export const QBO_VENDOR_CREDITS_PROJECTION_SYNC_KIND = "qbo_vendor_credits_projection";
