/** Kind written to qbo.sync_runs for Stage-1 inbound QBO Purchase mirror pulls (QBO→TMS). */
export const QBO_PURCHASES_MIRROR_SYNC_KIND = "qbo_purchases_inbound_mirror";

/** Kind written to qbo.sync_runs for Stage-2 mirror → accounting.expenses projection (subledger only). */
export const QBO_PURCHASES_EXPENSES_PROJECTION_SYNC_KIND = "qbo_purchases_expenses_projection";
