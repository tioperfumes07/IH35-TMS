#!/usr/bin/env node
/** Operator helper: verify DS-1..7 readiness (structure check; live API calls require env). */
console.log("DS verify helper — use admin jobs or POST import endpoints for live sync.");
