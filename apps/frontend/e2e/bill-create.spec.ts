import { test } from "@playwright/test";

test.describe("bill create flow", () => {
  test.skip(true, "TODO(P3-T11.18): blocked on auth-ready e2e harness for maintenance module routes.");

  test("creates bill with repair default and tab switches", async () => {
    // TODO(P3-T11.18): add full interaction assertions once authenticated harness is in place.
  });
});
