/** Standard main content width + horizontal padding (responsive sweep P6-T11178). */
export const PAGE_SHELL_CLASS = "mx-auto w-full max-w-screen-2xl px-4 sm:px-6 lg:px-8";

/** Wide modal panel cap (complex forms / breakdown tables). */
export const MODAL_PANEL_WIDE_CLASS = "max-w-[min(1260px,calc(100vw-2rem))]";

/** Default modal cap for simpler dialogs. */
export const MODAL_PANEL_NARROW_CLASS = "max-w-[min(720px,calc(100vw-2rem))]";
