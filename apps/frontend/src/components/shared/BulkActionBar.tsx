/**
 * Shared bulk action bar entry point for list pages (BULK-6).
 * Re-exports the gated bulk toolbar for consistent permission checks.
 */
export {
  BulkActionBar,
  type BulkActionBarProps,
  type BulkActionItem,
} from "../bulk/BulkActionBar";
